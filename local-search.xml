<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>MyOS</title>
    <link href="/2024/05/17/MyOS/"/>
    <url>/2024/05/17/MyOS/</url>
    
    <content type="html"><![CDATA[<h1 id="my操作系统"><a href="#my操作系统" class="headerlink" title="my操作系统"></a>my操作系统</h1><blockquote><p>常见操作系统：</p><p>MacOS，iOS，Windows，Android，Linux，symbian（NOKIA使用）</p></blockquote><h2 id="什么是操作系统"><a href="#什么是操作系统" class="headerlink" title="什么是操作系统"></a>什么是操作系统</h2><ul><li><p>一台电脑的诞生过程</p><ol><li>厂家组装<strong>裸机</strong></li><li>厂家出售前<strong>安装操作系统</strong></li><li>用户安装<strong>应用程序</strong>（eg：QQ）</li><li>使用QQ聊天</li></ol></li><li><p>操作系统（Operating System）是指控制和管理整个计算机系统的<strong>硬件和软件</strong>资源，并合理地组织调度计算机的工作和资源分配，以提供给用户和其他软件方便的接口和环境</p><p>给每个软件分配合适的硬件资源</p></li><li><p>具体管理的内容</p><ul><li><p>文件系统管理</p><p>文件创建、删除、IO，目录管理，文件权限…</p></li><li><p>进程管理</p><p>创建、调度和终止进程，分配和管理进程资源</p></li><li><p>内存管理</p><p>管理物理内存，虚拟内存…</p></li><li><p>IO设备管理</p><p>IO缓冲区管理，异常和中断处理</p></li><li><p>网络管理</p><p>协议栈的管理和实现、连接的创建和维护、数据包的转发和路由</p></li><li><p>GUI呈现与管理</p></li></ul></li><li><p>操作系统向上提供的接口（服务）</p><ul><li><p>面向用户的：</p><ol><li>GUI</li><li>cmd——分为交互式和文件式（.bat批处理文件——包含一些列命令的文本文件，命令按顺序被逐一执行）</li></ol></li><li><p>面向软件开发&#x2F;程序员</p><p>即：系统调用</p><p><code>系统调用（System Call）是操作系统提供给应用程序的一种编程接口，它允许应用程序请求操作系统执行特权操作，例如访问硬件设备、进行文件操作、创建和管理进程等。系统调用是应用程序与操作系统之间的通信桥梁，允许应用程序执行需要操作系统权限的操作，同时保持了操作系统的安全性和稳定性。</code></p></li></ul></li></ul><h3 id="操作系统的特征"><a href="#操作系统的特征" class="headerlink" title="操作系统的特征"></a>操作系统的特征</h3><blockquote><p>并发 | 共享 | 虚拟 | 异步</p></blockquote><ul><li><h4 id="并发："><a href="#并发：" class="headerlink" title="并发："></a>并发：</h4><p>指多个事件再宏观上是同时发生，但微观上是交替发生的</p><p><code>单核CPU内多个程序的交替执行是并发，多核CPU分别执行多个程序是并行</code></p></li><li><h4 id="共享："><a href="#共享：" class="headerlink" title="共享："></a>共享：</h4><p>指系统中的资源可供内存中多个并发执行的进程共同使用</p><p>资源共享方式：</p><ul><li><p>互斥共享——一段时间内只允许一个进程访问</p><p>例如：微信和摄像头互斥共享摄像头</p></li><li><p>同时共享——允许一个时间段内，多个进行“同时”访问</p><p>例如：多个进程进行磁盘IO，微观上其实是交替进行访问磁盘</p></li></ul></li><li><h4 id="虚拟："><a href="#虚拟：" class="headerlink" title="虚拟："></a>虚拟：</h4><p>指把一个物理上的实体变为若干个逻辑上的对应物。物理实体实际存在，而对应物只是用户感受到的。</p><p>实现技术：</p><ol><li><p>空分复用技术（在有限的内存中运行更大的软件—-有点像硬件层面的并发）</p></li><li><p>时分复用技术（在有限时间内同时执行多个事件）</p></li></ol></li><li><h4 id="异步："><a href="#异步：" class="headerlink" title="异步："></a>异步：</h4><p>在多道程序并发执行下，进程的执行并非十分通常&#x2F;可以预知，而是走走停停，在未来的某一时刻才有可能执行</p></li></ul><h2 id="操作系统的运行机制"><a href="#操作系统的运行机制" class="headerlink" title="操作系统的运行机制"></a>操作系统的运行机制</h2><ul><li><p>内核程序 | 应用程序</p><p>应用程序：常规程序</p><p>内核程序：内核程序控制硬件产生反映，一系列内核程序组成操作系统的内核（Kernel）</p><p>应用程序一般不会有权力跨隔离性操作，而内核程序作为管理者有此权力</p><p>例如：内存清空指令就不会暴露在内核以外</p></li><li><p>内核态（管态） | 用户态（目态）</p><p>指CPU运行的两种状态</p><p>内核态时，CPU运行内核程序——此时有权力执行特权指令</p><p>用户态时，CPU运行应用程序——此时只能执行非特权指令</p><ul><li>CPU中，通过程序状态寄存器管理状态切换</li></ul></li></ul><h3 id="中断和异常"><a href="#中断和异常" class="headerlink" title="中断和异常"></a>中断和异常</h3><ul><li><p>中断：</p><p>指CPU从执行应用程序时，停止执行，并切换回内核态</p><p>中断是让操作系统内核夺回CPU使用权的唯一途径</p><p><code>一般程序时分复用交替的流程为：程序A执行--被中断，切换回内核态--内核态处理中断信息，操作系统选取下一个执行的程序--程序B执行</code></p><p>分类：</p><ul><li><p>内中断（内部异常）</p><ul><li><p>故障 fault：指可能被修复的小问题【例如缺页中断】</p></li><li><p>终止 abort：不可恢复的致命问题【例如除0】</p></li><li><p>陷入 trap （指由于应用程序需要主动调用操作系统而引起的中断）</p></li></ul></li><li><p>外中断</p><ul><li><p>时钟中断（程序切换）</p></li><li><p>I&#x2F;O中断请求</p></li></ul></li></ul></li></ul><h3 id="系统调用"><a href="#系统调用" class="headerlink" title="系统调用"></a>系统调用</h3><p>是操作系统提供给应用程序（程序员）使用的接口，是可被调用的一种特殊函数–可以请求到操作系统内核的服务（一般指令似乎只在CPU用户态）。</p><p>凡是和共享资源有关的操作（如存储分配，IO操作，文件管理等）都必须通过系统调用的方式向操作系统内核提出请求服务，由操作系统内核代为完成（可以保证系统的稳定性和安全性）。</p><p>过程：</p><p>当高级语言代码中调用系统调用库函数：</p><ol><li><p>传递系统调用参数</p></li><li><p>执行陷入执行（从用户态 -&gt; 内核态）</p></li><li><p>内核态执行相应的调用</p></li><li><p>返回应用程序</p></li></ol><h4 id="操作系统体系结构"><a href="#操作系统体系结构" class="headerlink" title="操作系统体系结构"></a>操作系统体系结构</h4><ul><li><p>内核</p></li><li><p>非内核功能</p></li></ul><h3 id="操作系统引导（boot）"><a href="#操作系统引导（boot）" class="headerlink" title="操作系统引导（boot）"></a>操作系统引导（boot）</h3><p>指开机后如何让操作系统运行</p><ul><li>磁盘划分</li></ul><h3 id="虚拟机"><a href="#虚拟机" class="headerlink" title="虚拟机"></a>虚拟机</h3><ul><li><p>早期在硬件设备上构造一个操作系统，此操作系统只能让一个进程进行分配</p></li><li><p>后来出现多道批处理系统：通过将任务加载到队列中，允许一台计算机在同一时刻处理多个任务</p></li><li><p>发展出虚拟机技术 - 允许多个操作系统和应用程序在同一台物理计算机上并行运行</p></li></ul><p>虚拟机：</p><p>使用虚拟化技术，将一台物理机器<strong>虚拟化成多台虚拟机器</strong>，每个虚拟机器都可以独立运行一个操作系统。</p><p>需要使用：虚拟机管理程序（Virtual Mechine Monitor &#x2F; HyperVisor）&#x2F;虚拟机监控程序</p><h5 id="虚拟机管理程序"><a href="#虚拟机管理程序" class="headerlink" title="虚拟机管理程序"></a>虚拟机管理程序</h5><p>第一类：</p><p>运行在硬件和各个操作系统之间，负责给操作系统分配各个硬件的隔离使用</p><p>问题1：原本操作系统需要拥有内核态的执行权限，但运行在虚拟机之上的操作系统实则属于用户态，解决办法是用户态的操作系统调用VMM的API</p><p>第二类：</p><p>运行在宿主操作系统之上，调用宿主操作系统的API间接调用硬件</p><p>例如：Windows上安装的VirtualBox，VMware就是VMM，用VirtualBox安装的centOS镜像就是运行在其上的操作系统。</p><p>此时虚拟机管理程序和宿主操作系统是同一层的</p><p>第二种由于在虚拟的操作系统和真实的硬件之间存在多层代理，在代理实现接口功能的逻辑中可能存在曲线救国的行为</p><p>例如：VM1申请10GB的存储空间，请求到达管理程序，管理程序的内部逻辑或许会将宿主操作系统提供的分散的空间拼凑成10GB，向上提供<strong>好似连续的空间</strong>——就是<strong>虚拟</strong>。</p><p>但第一种直接操作硬件的VMM就没必要了。</p><h3 id="进程与线程"><a href="#进程与线程" class="headerlink" title="进程与线程"></a>进程与线程</h3><ul><li><p>PID（Process ID | 进程ID）</p><p>每一次新建进程，都会标识唯一自增的进程ID</p><blockquote><p>事实证明未必自增，并且一个程序可能启动多个进程【多进程编程】</p></blockquote><p>以进程为单位聚合和许多属性：</p><ul><li>进程所属ID（UID）</li><li>进程分配了多少内存</li><li>进程CPU使用情况</li><li>进程的磁盘IO情况</li><li>进程的网络IO情况</li><li>正在使用哪些IO设备</li><li>正在使用哪些文件</li><li>当前运行状态：就绪 &#x2F; 阻塞 &#x2F; 运行</li><li>…</li></ul><p>以上总体分为四类：</p><ol><li><p>进程描述信息</p></li><li><p>进程运行信息 | 资源利用情况 – CPU，磁盘，网络..</p></li><li><p>进程状态信息 | 就绪，阻塞，运行</p></li><li><p>资源分配清单 | 正在使用哪些文件，分配的内存空间，使用的IO设备</p></li><li><p>处理机相关信息 | PSW，PC等寄存器值（用于实现进程切换）</p></li></ol></li><li><p>PCB（Process Controller Block | 进程控制块）</p><p>操作系统对各个进程的管理信息，都被放置在PCB中</p><p><code>PCB是进程存在的唯一标识，当进程被创建时，操作系统为其创建PCB，当进程结束时，会回收PCB</code></p></li><li><p>进程的组成：</p><ul><li>PCB</li><li>程序段（进程具体执行的代码）程序的运行就是CPU读取程序段指令的过程</li><li>数据段（代码中临时变量存放在此处）</li></ul><blockquote><p>进程是动态的，而进程实体是进程的image，反应了某一时刻的状态</p></blockquote></li></ul><h4 id="进程：是进程实体的运行过程，是系统进行资源分配和调度的一个独立单位"><a href="#进程：是进程实体的运行过程，是系统进行资源分配和调度的一个独立单位" class="headerlink" title="进程：是进程实体的运行过程，是系统进行资源分配和调度的一个独立单位"></a>进程：是进程实体的运行过程，是系统进行资源分配和调度的一个独立单位</h4><p>进程特点：</p><ul><li>动态性：动态产生、变化、消亡</li><li>并发性：内存中多个进程实体，并发执行</li><li>独立性：进程独立运行，独立获得资源，接受调度</li><li>异步性：各个进行以独立的、不可预知的速度向前推进，结束时间不确定会有潜在问题，操作系统需要提供“进程同步机制”来解决异步问题</li><li>结构性：每个进程会配置一个PCB，程序段，数据段</li></ul><h4 id="进程的状态切换"><a href="#进程的状态切换" class="headerlink" title="进程的状态切换"></a>进程的状态切换</h4><ul><li><p>创建态：正在被创建「操作系统将程序的可执行文件读入内存，并为之创建PCB，分配数据块和程序块」</p></li><li><p>就绪态：系统完成创建工作后，线程等待CPU资源</p></li><li><p>运行态：进程被CPU调度：单CPU情况下，同一时刻只会有一个进程处于运行态，多喝CPU可能有多个</p></li><li><p>阻塞态：原本处于运行的进程，通过“系统调用”尝试申请资源，但是资源被占用，线程本身pending</p></li><li><p>终止态：由运行态主动结束，或者遇到不可修复的错误</p></li></ul><blockquote><p>java API：</p><p>直接使用线程相关的方法来控制和转换线程的状态</p><p>例如：Java中Thread类下，yield()方法，显式的将线程由执行态转换为就绪态；wait()方法到达阻塞态，stop()进入终止态</p></blockquote><h4 id="进程的创建"><a href="#进程的创建" class="headerlink" title="进程的创建"></a>进程的创建</h4><p>&#x2F;&#x2F; TODOS</p><h4 id="进程的组织"><a href="#进程的组织" class="headerlink" title="进程的组织"></a>进程的组织</h4><ol><li>链表：维护多个链表</li></ol><p>将状态相同的进程统一管理</p><blockquote><p>有点像mySQL缓存淘汰机制</p></blockquote><p>链表：</p><ul><li><p>执行指针</p></li><li><p>就绪队列指针</p></li><li><p>阻塞队列指针</p><p>有的会按照阻塞缘由&#x2F;等待的资源分成多个链表，例如：等待打印机的阻塞队列 &#x2F; 等待磁盘的阻塞队列</p></li></ul><ol start="2"><li><p>索引：</p><p>相较于链表 用空间换时间 尝试增加连续内存空间以减少寻址时间</p></li></ol><h4 id="进程控制"><a href="#进程控制" class="headerlink" title="进程控制"></a>进程控制</h4><blockquote><p>前置：</p><p>原语：特殊的程序，其执行具有原子性，通常用在较底层的，不可发生意外情况的逻辑中「如CPU切换，设备驱动…」</p><ul><li>原语的实现逻辑 原本CPU在执行每行指令后，都需要监听有无外部中断信号，如果有则被中断，原语通过两个更高权力的指令「特权指令」“关中断指令”，“开中断指令”来取消监听</li></ul></blockquote><blockquote><p>前置：</p><p>mac中，进程间的关系是树形结构，由0号进程kernel_task和1号进程launchd创建其他进程，再由这些进程创建其他进程…</p></blockquote><p>相关原语：</p><ul><li><p>进程的创建</p></li><li><p>进程的终止</p></li><li><p>进程的阻塞</p></li><li><p>进程的唤醒</p></li><li><p>进程的切换</p></li></ul><p>这些类原语基本要做：更新PCB中的信息，将PCB插入合适的队列，分配 &#x2F; 回收资源</p><h3 id="进程通信（IPC）"><a href="#进程通信（IPC）" class="headerlink" title="进程通信（IPC）"></a>进程通信（IPC）</h3><ul><li><p>进程之间的通信必须通过操作系统</p><blockquote><p>隔离是必要的，无法通信是隔离的代价</p></blockquote><p>由于每个进程都分配有对应的进程内存空间，而不同进程不允许相互访问（用户空间访问隔离），所以跨进程的通信只能通过操作系统提供的API（内核空间访问自由）</p><p><img src="https://cdn.xiaolincoding.com/gh/xiaolincoder/ImageHost/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/%E8%BF%9B%E7%A8%8B%E9%97%B4%E9%80%9A%E4%BF%A1/4-%E8%BF%9B%E7%A8%8B%E7%A9%BA%E9%97%B4.jpg"></p><p>三种方式：</p></li><li><p>共享存储</p></li><li><p>消息传递</p></li><li><p>管道通信</p></li></ul><h4 id="共享存储："><a href="#共享存储：" class="headerlink" title="共享存储："></a>共享存储：</h4><blockquote><p>用于同一主机中，不同进程间的通信方式</p></blockquote><p>在内存中申请一片共享内存区，并加一把互斥锁「P，V操作」，即可实现通信</p><p>int shm_open(); &#x2F;&#x2F;系统调用，申请一片共享内存区<br>void * mmap(); &#x2F;&#x2F;通过<strong>mmap</strong>系统调用，将共享内存区映射到进程自己的地址空间</p><blockquote><p>mmap()：</p><p>原本的目的是：将文件keep在内核态，不用通过read复制到用户态再修改，而是用户态改用户态的，改了以后映射到内核态page cache上即可</p><p>共享存储中：两个进程映射同一个page cache的区域，本质上还是会向内核态写入数据，然后另一个线程从内核态的cache上读走（只不过，由于是映射，读的过程其实是自动执行）</p></blockquote><p>具体分两类：</p><ol><li><p>基于存储区的共享：操作系统只负责划出共享存储区，而数据的形式、存放位置等都由线程负责</p><p>– 速度较快，高级通信方式</p></li><li><p>基于数据结构等共享：操作系统分配的共享空间是确定类型的，例如：只能存放一个长度为10的数组</p><p>– 速度较慢，限制较多，低级通信方式</p></li></ol><h4 id="消息传递："><a href="#消息传递：" class="headerlink" title="消息传递："></a>消息传递：</h4><p>进程件的数据交换通过操作系统提供的“发送消息 &#x2F; 接收消息”两个原语</p><blockquote><p>发送方将消息发送到消息队列中，只不过这个队列可能在目标的PCB上，也可能在一个公共区域</p></blockquote><p>交换的数据符合格式：消息头「发送进程ID，接收进程ID，消息长度…」；消息体</p><p>具体分两类：</p><ol><li><p>直接通信方式</p><p>消息直接由发送方到达接收方</p><p>过程：进程P试图向进程Q发起消息msg</p><ul><li><p>P.send(Q,msg)，Q.receive(P,msg)</p></li><li><p>过程中异步解耦，P发起消息先到Q的PCB中维护的消息队列中「缓存，可能是链表」，在由Q搜索有无P的消息</p></li></ul></li><li><p>间接通信方式</p><p>通过“信箱”间接通信</p><p>过程：进程P试图向进程Q发起消息msg</p><ul><li>在操作系统内存中申请信箱空间A，P.send(A,msg)，Q.receive(A,msg)；</li></ul></li></ol><blockquote><p>消息队列和管道都会将消息停留在一片公共区域，中间就有数据从用户态到内核态再到用户态的复制过程，比较费时</p></blockquote><h4 id="管道通信："><a href="#管道通信：" class="headerlink" title="管道通信："></a>管道通信：</h4><ul><li><p>由一片共享的内存空间作为循环队列，并且是阻塞队列</p><blockquote><p>channel &#x2F; pipe</p><p>是一种特殊的文件，只存在于内存，不在文件系统中</p></blockquote></li><li><p>管道只能采用半双工通信，某一时间只能实现单向数据传输</p></li><li><p>管道中的数据一旦被读出，就彻底消失，因此在多个进程读取同一管道时可能会错乱</p></li><li><p>运用：Linux中的管道符号——用于在两个任务间建立管道，上一个任务（进程）的结果通过管道传递给下一个任务的输入</p></li><li><p>问题：通信效率比较低</p><blockquote><p>管道通信是同步通信，写方的信息没被读前写方只能等待</p></blockquote></li></ul><blockquote><p>&#x2F;&#x2F;TODO：netty基于Socket建立的全双工channel，和这个有啥关系，尚硅谷为什么安排在这里说netty？</p></blockquote><h3 id="线程："><a href="#线程：" class="headerlink" title="线程："></a>线程：</h3><blockquote><p>还没有线程概念时，进程是程序执行的基本单位。CPU只需要在多个进程间切换，当切换到进程A时，顺序执行进行A轮到的代码</p></blockquote><blockquote><p>开出多线程后，CPU通过某种调度算法将资源分配个具体线程，此时线程就是程序执行的基本单位</p><blockquote><p>将原本多个进程并发执行的任务给到一个进程，同时又要保证并发性 – 线程</p></blockquote><p>但进程是除CPU外，系统资源的分配单元【打印机，网卡等设备只能被分配到进程级别】</p></blockquote><ul><li><p>变化：</p><ol><li><p>传统的进程间并发，需要切换进程的运行环境，系统开销很大</p><ul><li>尤其页表的切换成本很大 – 页表本身就很大，页表中存储的虚拟地址和物理地址的映射关系需要在将所有页加载完后才知道；另外页表通过快表加速访问，切换后缓存全部过期</li><li>线程的上下文切换只需要将线程间隔离的寄存器信息和栈信息保存到TCB即可</li></ul></li><li><p>线程间的并发，当线程属于一个进程则不需要切换环境【上下文执行的是同一任务，环境变量不变】</p><p>线程的创建时间比进程快</p></li><li><p>进程在创建过程中，需要资源管理信息，比如内存信息，文件系统信息，而线程的创建只需要创建轻量级的TCB，之后便可共享进程的资源</p></li></ol></li><li><p>线程的属性 &#x2F; 特点</p><ol><li><p>线程是处理机调度的单位</p></li><li><p><strong>多CPU计算机中，各个线程可占用不同的CPU</strong></p></li><li><p>每个线程都有一个线程ID，线程控制块（TCB）</p></li><li><p>线程也有 就绪、阻塞、运行 三种基本状态</p></li><li><p>线程几乎不拥有系统资源</p></li><li><p><strong>同一进程的不同线程间 共享进程的资源</strong></p></li><li><p>由于共享内存地址空间，同一进程中的线程间通信甚至无需系统干扰</p></li><li><p>同一进程中的线程切换，不会引起进程切换，且系统开销较小</p></li><li><p>不同进程中的线程切换，会引起进程切换，开销较大</p></li></ol></li></ul><h3 id="线程的实现方式："><a href="#线程的实现方式：" class="headerlink" title="线程的实现方式："></a>线程的实现方式：</h3><ol><li><h4 id="用户级线程（User-Level-Thread）"><a href="#用户级线程（User-Level-Thread）" class="headerlink" title="用户级线程（User-Level Thread）"></a>用户级线程（User-Level Thread）</h4><blockquote><p>早期操作系统只支持进程，不支持线程，当时的 “线程“ 概念是由”线程库“逻辑上实现的 – 即本质上是由程序员作为操作系统的用户将应该并发执行的任务写到一个进程中切换执行</p></blockquote><p>例如：</p><p>早期程序员写好多个线程的任务后，线程库将多个线程的代码逻辑穿插到一起，挤到一个进程内交付给操作系统</p><p><code>现在的线程库有更强的功能，例如线程的创建，销毁，调度算法...</code></p><p>特点：</p><ol><li><p>线程的管理工作由开发线程库的开发者完成，并不需要依靠操作系统</p></li><li><p>线程切换不需要CPU调度，甚至不需要用户态和内核态转变</p></li><li><p>操作系统无法意识到用户级线程的存在 – 本质上还是以进程为CPU基本的调度单位</p></li></ol></li></ol><p>优点：</p><ul><li><p>用户级线程的切换在用户空间即可完成，不需要切换到内核态，线程管理的系统开销比较小</p><p>缺点：</p></li><li><p>进程间不隔离，由于最后都会统一到一个进程中依次执行，假如其中一个线程被阻塞，其他线程也会受牵连</p><ol start="2"><li><h4 id="内核级线程（Kernel-Level-Thread）"><a href="#内核级线程（Kernel-Level-Thread）" class="headerlink" title="内核级线程（Kernel-Level Thread）"></a>内核级线程（Kernel-Level Thread）</h4></li></ol></li></ul><blockquote><p>由操作系统支持的线程</p></blockquote><p>  特点：</p><ol><li><p>线程的调度、切换等工作都由内核负责，因此内核级线程的切换需要CPU在内核态才能完成</p></li><li><p>操作系统为每个内核级线程简历相应的TCB，通过TCB对线程进行管理。</p></li></ol><p>优点：</p><ul><li><p>当一个线程被阻塞后，别的线程还可以继续执行，并发能力较强。</p></li><li><p>多个线程可以被调度到多个CPU上并行执行</p></li></ul><p>缺点：</p><ul><li>一个用户进程会占用多个内核级线程，线程切换由操作系统内核完成，需要切换到内核态，所以线程管理的成本较高</li></ul><p>将用户级线程和内核级线程的思路相结合：</p><blockquote><p>存在用户级线程，也存在内核级线程；于是二者需要有 对应&#x2F;转化 关系</p></blockquote><h4 id="多线程模型"><a href="#多线程模型" class="headerlink" title="多线程模型"></a>多线程模型</h4><ol><li><p>一对一模型：</p></li><li><p>一对多模型：</p></li><li><p>多对多模型：</p><p>要求：n个用户线程映射到m个内核线程，其中n&gt;&#x3D;m</p><p>特点：</p><ul><li><p>克服了多对一模型并发度不高的缺点</p></li><li><p>客服了一对一模型占用太多内核级线程的开销大的缺点</p></li></ul></li></ol><blockquote><p>多对多的模式中，可以通过灵活安排映射情况，实现类似一对一的效果【让忙的用户线程独占，让不忙的共享】</p></blockquote><h4 id="线程的状态与转化"><a href="#线程的状态与转化" class="headerlink" title="线程的状态与转化"></a>线程的状态与转化</h4><blockquote><p>和进程类似：</p></blockquote><h4 id="线程的组织与控制"><a href="#线程的组织与控制" class="headerlink" title="线程的组织与控制"></a>线程的组织与控制</h4><blockquote><p>类似进程通过PCB管理，线程通过TCB管理</p></blockquote><p>TCB组成结构：</p><ul><li><p>线程标识符：TID</p></li><li><p>程序计数器PC：用来标识线程目前执行到哪里</p></li><li><p>寄存器：存储运行的中间状态</p></li><li><p>堆栈指针：堆栈中保存函数调用信息、局部变量等，TCB中存储着指向该位置的指针</p></li><li><p>线程运行状态：运行 &#x2F; 就绪 &#x2F; 阻塞</p></li><li><p>优先级：与线程调度，资源分配相关</p></li></ul><p>可以按照不同策略将线程的TCB串联成几张表，例如一个进程中的几个线程，处于就绪态的几个线程…</p><hr><h2 id="处理机调度"><a href="#处理机调度" class="headerlink" title="处理机调度"></a>处理机调度</h2><blockquote><p>调度：当有一堆任务要处理，但由于资源有限，事情没法同时处理，这就需要确定某种规则来决定处理任务的顺序</p></blockquote><ul><li><p>高级调度 – 作业调度：</p><blockquote><p>作业：用户交给操作系统的一项具体任务（例如：启动一个程序，加载一个文件…）</p></blockquote><p>作业调度：指按照一定的原则从外存的作业后备队列中挑选出一个作业调入内存，并创建与之相对的进程</p><p>调度过程中需要确保内存不会泄露，以及其他偏好</p></li><li><p>低级调度 – 进程调度 &#x2F; 处理机调度：</p><p>按照某种策略从就绪队列中选择一个进程，将处理机分配给它</p><p>进程调度的频率：十几毫秒一次</p></li><li><p>中级调度 – 内存调度：</p><p>当内存不够时，可能会将某些进程的数据调出外存，等内存空闲或者进程需要时再重新调入内存【关于内存空间与外存缓冲区间的调度】</p><p>所以按照某种策略决定将哪个进程挂起、将哪个处于挂起状态的进程重新调入内存，即内存调度</p></li></ul><p>进程的七状态模型：</p><h4 id="进程切换（调度）的时机："><a href="#进程切换（调度）的时机：" class="headerlink" title="进程切换（调度）的时机："></a>进程切换（调度）的时机：</h4><p>需要进行进程调度与切换的情况：</p><ol><li><p>当前运行的进程主动放弃CPU</p><ul><li><p>程序正常终止</p></li><li><p>运行中发生异常而终止</p></li><li><p>进程IO阻塞</p></li></ul></li><li><p>当前进程被动放弃CPU</p><ul><li><p>分给此进程的时间片用完</p></li><li><p>有更加紧急的事情需要优先处理（例如IO中断）</p></li><li><p>有更优先级的进程进入就绪队列</p></li></ul></li></ol><p>不能进行进程调度的情况：</p><ul><li><p>处理中断的过程中</p></li><li><p>进程在操作系统内核程序临界区中</p></li><li><p>在原子操作过程中</p></li></ul><h3 id="进程调度的方式："><a href="#进程调度的方式：" class="headerlink" title="进程调度的方式："></a>进程调度的方式：</h3><ul><li><p>非剥夺调度方式（非抢占方式）</p><p>只允许进程主动放弃处理机</p><p>在运行过程中即使有更紧迫的任务到达，当前进程依然会继续使用处理机，知道该进程终止或主动要求进入阻塞态</p></li><li><p>剥夺调度方式（抢占方式）</p><p>当一个进程正在处理机上执行时，如果有一个更加紧迫的进程需要使用处理机，则立即暂停正在执行的进程。</p></li></ul><h3 id="进程的切换过程："><a href="#进程的切换过程：" class="headerlink" title="进程的切换过程："></a>进程的切换过程：</h3><blockquote><ol><li><p>从就绪队列中选中一个要运行的进程</p></li><li><p>让当前进程让出处理机，选出的进程进入处理机</p></li></ol></blockquote><p>具体的 2 过程：</p><ul><li>对原来运行的进程中的数据进行保留（写入PCB），对新的进程的数据进行恢复（从PCB中读取）</li></ul><h4 id="调度器-调度程序（scheduler）"><a href="#调度器-调度程序（scheduler）" class="headerlink" title="调度器 &#x2F; 调度程序（scheduler）"></a>调度器 &#x2F; 调度程序（scheduler）</h4><blockquote><p>进程在就绪、运行、阻塞三种状态间的切换的具体handler</p></blockquote><ul><li><p>当创建新进程时，新进程加入就绪队列，调度器需要观测是否将这个新进程抢占CPU资源</p></li><li><p>当占用CPU的进程主动退出，调度器来运行调度程序（调度算法）</p></li><li><p>运行的进程阻塞，调度新进程进来</p></li><li><p>IO完毕，可能需要欢喜某些阻塞的进程</p></li></ul><p>调度器决定：</p><ul><li><p>让谁运行？</p></li><li><p>运行多长时间？</p></li></ul><h4 id="闲逛进程："><a href="#闲逛进程：" class="headerlink" title="闲逛进程："></a>闲逛进程：</h4><ul><li>调度程序永远的备胎，当没有其他就绪进程时，运行闲逛进程</li></ul><p>特点：</p><ul><li><p>优先级最低</p></li><li><p>能耗低</p></li><li><p>闲逛进程周期性的执行0地址指令，在指令周期末尾例行检查中断（触发调度程序来检查有无就绪进程）</p></li></ul><h3 id="调度算法的评价指标"><a href="#调度算法的评价指标" class="headerlink" title="调度算法的评价指标"></a>调度算法的评价指标</h3><ol><li><p>CPU的利用率</p></li><li><p>系统吞吐量</p><p>单位时间内完成作业的数量</p></li><li><p>周转时间</p><p>指从作业被提交给系统开始，到作业完成为止的时间间隔</p><ul><li><p>时间包括四部分</p><ol><li><p>作业在外存后备队列上等待作业调度</p></li><li><p>进程在就绪队列上等待进程调度</p></li><li><p>进程在CPU执行</p></li><li><p>进程等待IO的阻塞时间</p></li></ol></li></ul></li><li><p>带权周转时间</p><blockquote><p>由于周转时间包括作业实际运行的时间和阻塞 &#x2F; 等待调度的时间，理论上应该让实际运行时间比阻塞&#x2F;等待时间多得多 – 带权周转时间</p></blockquote><p>执行任务需要相同CPU时间的作业，需要等待的时间越少，带权周转时间越底，用户满意度越高</p></li><li><p>等待时间</p><p>指进程 &#x2F; 作业处于等待处理机状态的时间之和</p><p>—– 等待时间过程会导致 进程 &#x2F; 作业 <strong>饥饿</strong></p></li><li><p>响应时间</p><p>指用户从提出请求到首次产生响应所用的时间</p></li></ol><h2 id="调度算法"><a href="#调度算法" class="headerlink" title="调度算法"></a>调度算法</h2><ol><li><h3 id="FCFS（先来先服务）"><a href="#FCFS（先来先服务）" class="headerlink" title="FCFS（先来先服务）"></a>FCFS（先来先服务）</h3><p>算法思想：公平 - 参照原生队列的FIFO</p><p>是否可抢占：用于非抢占式的算法</p><ul><li>最简单的调度模型 - 下一个任务等待上一个任务结束</li></ul><p>优点：公平、实现简单</p><p>缺点：并没有合理的调度 – 排在长作业后的短作业需要等待很长时间，带权周转时间很大，对于短作业的用户体验不好</p></li><li><h3 id="SJF（短作业优先-short-job-first）"><a href="#SJF（短作业优先-short-job-first）" class="headerlink" title="SJF（短作业优先 short job first）"></a>SJF（短作业优先 short job first）</h3><p>算法思想：针对FCFS对短作业并不友好，SJF追求最少的平均等待时间，最少的平均周转时间，最少的平均带权周转时间</p><p>算法规则：最短的作业优先得到服务</p><p>是否可抢占：常规时非抢占，也有抢占式版本 – <em><strong>SRTN（最短剩余时间优先算法）</strong></em></p></li><li><h3 id="SRTN"><a href="#SRTN" class="headerlink" title="SRTN"></a>SRTN</h3><p>算法思想：每当有进程加入就绪队列时就需要尝试调度；如果新到达的进程剩余时间比当前运行的进程剩余时间更短，则有新进程抢占处理机</p><p>要求：每个任务 &#x2F; 进程 需要标志剩余时间 – 怎么<strong>估算剩余时间？</strong></p><ul><li><ol start="2"><li><ol start="3"><li>特点：</li></ol><p>不公平：对短作业有利，对长作业不利</p><p>可能产生 饥饿现象</p></li></ol></li><li><p>另外：作业 &#x2F; 进程的运行时间由用户提供，并不一定真实，所以不一定能做到真正的短作业优先</p></li></ul></li></ol><blockquote><p>综上所属，FCFS优先选择等待时间最长的作业进行服务，但并不考虑作业需要的时间，SJF优先选择执行时间短的作业进行服务，但不考虑作业等待了多长时间。</p></blockquote><ol start="4"><li><h3 id="HRRN（高响应比优先-Highest-Response-Ratio-Next）"><a href="#HRRN（高响应比优先-Highest-Response-Ratio-Next）" class="headerlink" title="HRRN（高响应比优先 Highest Response Ratio Next）"></a>HRRN（高响应比优先 Highest Response Ratio Next）</h3><p>算法思想：综合考虑作业的等待时间和要求服务的时间</p><p>算法规则：每次调度时，先计算各个作业 &#x2F; 进程的响应比，选择比率较高的为其服务</p><p>是否可抢占：非抢占的调度算法（避免在每个新任务进入后计算一遍执行中的任务的响应比）</p><blockquote><p>感觉也可以做成抢占式</p></blockquote></li><li><h3 id="RR（时间片轮转-Round-Robin）"><a href="#RR（时间片轮转-Round-Robin）" class="headerlink" title="RR（时间片轮转 Round-Robin）"></a>RR（时间片轮转 Round-Robin）</h3><p>算法思想：公平的，轮流的为各个进程服务，让每个进程在一定时间内都可以得到响应</p><p>是否可抢占：抢占式算法：由<strong>时钟装置</strong>发出<strong>时钟中断</strong>来通知CPU时间片已到</p><p>问题：</p><ul><li><p>当时间片太大，算法会退化为FCFS调度算法</p><p>当用户的两个操作（任务）并不连续在一起执行【例如调试操作】，两次调试至少要等待一轮时间片才能回到当前线程</p></li><li><p>时间片太小时导致切换进程 &#x2F; 任务的成本太高</p></li></ul></li><li><h3 id="优先级调度算法"><a href="#优先级调度算法" class="headerlink" title="优先级调度算法"></a>优先级调度算法</h3><p>算法思想：许多应用场景需要根据任务的紧急程度来决定处理顺序</p><p>算法规则：每个作业 &#x2F; 进程标志自身优先级，调度时选择优先级高的作业 &#x2F; 进程</p><p>是否抢占式：</p><ul><li><p>非抢占实现：</p></li><li><p>抢占式实现：</p></li></ul></li></ol><blockquote><p>优先级可以分为静态优先级和动态优先级，区分某些进程在创建后执行中优先级可以调整</p></blockquote><p>问题：</p><ol><li><p>如何合理设置进程的优先级：</p><p>普遍原则：</p><ol><li><p>系统进程优先级高于用户进程</p></li><li><p>前台进程优先级高于后台进程</p></li><li><p>操作系统更偏好I&#x2F;O型进程（相比于计算型进程）：为了让IO更早启动，提高IO设备利用率</p></li></ol></li><li><p>如何利用动态优先级：</p><blockquote><p>使用动态优先级，就是为了让静态优先级策略更加灵活，能够在某些场景下追求更加合理的调度方案 – 更加公平 &#x2F; 资源利用率更高</p></blockquote><p>例如：</p><p>如果某个进程在就绪队列中等待了很长时间，则可以适当提升优先级</p><p>如果某个进程占用了处理机很长时间，可以适当降低其优先级</p></li><li><h3 id="多级反馈队列调度算法："><a href="#多级反馈队列调度算法：" class="headerlink" title="多级反馈队列调度算法："></a>多级反馈队列调度算法：</h3><blockquote><p>对其他调度算法的折中权衡</p></blockquote><p>算法规则：</p><ul><li><p>设置多级就绪队列，各级队列的优先级从高到低，时间片从小到大</p></li><li><p>新的进程到达时，先进入第一级队列，按FCFS原则排队等待被分配时间片。假如用完时间片后进程还未结束，则进程进入下一级队列队尾（如果此时已经在最下级队列，则重新放回最下级队列队尾）</p></li><li><p>当第k级队列为空时，会调度 k+1 级队列的队头分配时间片</p></li><li><p>当CPU上有第K级的任务在执行，而突然来了小于K级的任务，则优先级别高的任务处理，被抢占的任务&#x2F;进程放回原队列队尾</p></li></ul></li></ol><blockquote><p>感觉：每个任务都有先执行一点的机会，但是随着任务时间拉长，其优先级降低，被执行的次序往后排</p></blockquote><p>关于灵活调整优先级，此算法也可以实现：只需将跑过时间片后的任务不降级，重新放回原队列末尾即可</p><ol start="8"><li><h3 id="多级固定队列调度算法："><a href="#多级固定队列调度算法：" class="headerlink" title="多级固定队列调度算法："></a>多级固定队列调度算法：</h3><p>系统中，按进程类型设置多个队列，进程创建后加入某个队列</p><p>多个队列是按照优先级分类，例如：系统进程是第一优先级，交互式进程是第二优先级，批处理进程是第三优先级…</p><p>确认了进程优先级即明确了基本执行顺序，后续为了更加公平、避免饥饿、响应更快可以参考上述算法思想进行优化</p><p>例如：</p><ul><li><p>系统进程队列采用优先级调度</p></li><li><p>交互式队列采用时间片轮询</p></li><li><p>批处理队列采用FCFS</p></li></ul></li></ol><h3 id="进程同步"><a href="#进程同步" class="headerlink" title="进程同步"></a>进程同步</h3><blockquote><p>由于进程具有异步执行的特点：每个进程独立执行，相互不确定彼此是否完毕。但是当多个线程协同工作时，出于业务需求又要按照某种顺序执行。</p></blockquote><p>同步 – 直接制约关系，指为完成某种任务而建立的两个或多个进程需要在某些位置上协调其工作次序而产生的制约关系。</p><p>同步机制准则：</p><ul><li><p>空闲让进</p></li><li><p>忙则等待</p></li><li><p>有限等待</p></li><li><p>让权等待</p></li></ul><h3 id="进程互斥"><a href="#进程互斥" class="headerlink" title="进程互斥"></a>进程互斥</h3><blockquote><p>当进程需要“共享”地访问某个资源时，可能无法实时共享</p></blockquote><p>两种资源共享方式：</p><ul><li><p>互斥共享：系统中的某些资源一个时间内只允许一个进程访问使用，例如打印机，摄像头</p></li><li><p>同时共享：系统中某些资源允许多个进程同时访问</p></li></ul><p>把一个时间段内只允许一个进程使用的资源称为：临界资源</p><p>对临界资源的互斥访问过程：</p><ol><li><p>进入区：负责检查进程是否可入（完成锁的管理）</p></li><li><p>临界区：临界资源相关代码</p></li><li><p>退出区：锁释放</p></li><li><p>剩余区：其他处理</p></li></ol><h4 id="进程互斥的实现方法（软件实现）"><a href="#进程互斥的实现方法（软件实现）" class="headerlink" title="进程互斥的实现方法（软件实现）"></a>进程互斥的实现方法（软件实现）</h4><ol><li><h4 id="单标志法"><a href="#单标志法" class="headerlink" title="单标志法"></a>单标志法</h4><p>算法思想：通过声明一个变量记录当前哪个进程拥有权限访问</p><p>具体逻辑：</p><ul><li><p>进程在进入区通过while循环判断是否轮到自身</p></li><li><p>一旦进入while，在临界区执行自身逻辑</p></li><li><p>退出区更改变量的值</p></li></ul></li></ol><p>问题：</p><ul><li><p>没有合适的超时机制，容易让权限被动的停在某个进程但不释放</p><ol start="2"><li><h4 id="双标志先检查法-双标志后检查法"><a href="#双标志先检查法-双标志后检查法" class="headerlink" title="双标志先检查法 &#x2F; 双标志后检查法"></a>双标志先检查法 &#x2F; 双标志后检查法</h4></li></ol></li></ul><blockquote><p>差不多，不太行</p></blockquote><ol start="3"><li><h4 id="Peterson算法"><a href="#Peterson算法" class="headerlink" title="Peterson算法"></a>Peterson算法</h4><blockquote><p>基于两现场的并发互斥算法 – 本质时一种谦让模式</p></blockquote><blockquote><p>有一组Boolean变量 <strong>flag</strong> 表示各个进程对使用资源的意愿，也有一个变量 <strong>turn</strong> 表示此时哪个进程拥有锁</p></blockquote><p>每个进程尝试访问临界区的过程：</p><ol><li><p>设置自身flag意愿为true</p></li><li><p>让出turn的权力，设置为对方</p></li><li><p>阻塞判断：其他进程如果有想进的，并且turn被占用；则while循环阻塞</p><p>如果其他进程都不想进，turn自然没人占用，顺利进入临界区，或者当其他进程想进，但还没占用turn，进入的权力仍然属于自身线程</p></li></ol></li></ol><p><code>Peterson本质还是和单标志法一样用一个变量turn标识锁的归属，但是能并不像单标志法只能交替进行，而是自身可以决定是否继续占用资源：做决定需要其他线程的状态信息，额外设置了表示意愿的flag数组，通过”谦让“的行为，也能保证进程的顺次执行。</code></p><h4 id="进程互斥的实现方法（硬件）"><a href="#进程互斥的实现方法（硬件）" class="headerlink" title="进程互斥的实现方法（硬件）"></a>进程互斥的实现方法（硬件）</h4><ol><li><h4 id="中断屏蔽方法"><a href="#中断屏蔽方法" class="headerlink" title="中断屏蔽方法"></a>中断屏蔽方法</h4><p>算法思想：利用”开 &#x2F; 关中断指令“，保证过程中的指令不允许被中断，也就不允许发生线程切换，因此不会发生互斥访问</p><p>优点：简单高效</p><p>缺点：</p><ul><li><p>只适用于操作系统内核程序，不适用于用户进程</p></li><li><p>并且不适合多处理机（因为不切换进程只能保证一个CPU上进程不变，但其他CPU上的进程仍然有可能并发）</p></li></ul></li><li><h4 id="TestAndSet-TestAndSetLock-TSL指令"><a href="#TestAndSet-TestAndSetLock-TSL指令" class="headerlink" title="TestAndSet &#x2F; TestAndSetLock TSL指令"></a>TestAndSet &#x2F; TestAndSetLock TSL指令</h4><blockquote><p>由硬件支持的单标志法（但是不局限在两个线程上），在硬件设备中使用TSL执行将checkAndSet原子化成一个指令，于是不怕并发</p></blockquote><p>基本逻辑：</p><p>while循环中检查lock变量是否时true（表示被加锁）</p><p>没加则自身加锁，进入自身代码，其他进程阻塞在外</p></li></ol><p>但是以上方法都存在while（true）循环check，理论上当某个进程无法进入临界区时，应该让出CPU，而不是循环检测</p><h2 id="锁"><a href="#锁" class="headerlink" title="锁"></a>锁</h2><ul><li><p>上述使用变量表示权限归属的方法都是加互斥锁</p><p>互斥锁的缺点是：忙等待</p><p>即：如果某个资源被占用，其他尝试获取资源的进程只能循环查看锁的情况【自旋锁 spin lock】；</p><p>当多个进程共享一个CPU时，由于被阻塞的进程和正在使用资源的进程本质需要顺次执行，于是无法在释放锁第一时刻就获取到锁，而是等到释放锁后下一个自己的时间片才能获取锁。于是互斥锁只有多处理器才能承受。</p></li></ul><h4 id="互斥方法8：原语-信号量"><a href="#互斥方法8：原语-信号量" class="headerlink" title="互斥方法8：原语+信号量"></a>互斥方法8：原语+信号量</h4><blockquote><p>操作系统用一个整数型的变量作为信号量，用来标识系统中某种资源的数量</p><p>本质也是用变量代表锁</p></blockquote><p>操作系统提供一对原语：wait(s),和signal(s)</p><ul><li><p>wait(s)：用来原子化原本轮询S是否空闲的操作</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell">void wait(int S)&#123;<br>    while(S &lt;= 0);<br>    S = S-1;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><p>        避免多个线程同时判断，同时减一的竞态问题</p><ul><li><p>signal(s)释放资源</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs shell">void signal(int S)&#123;<br>    S = S+1;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><h3 id="互斥方法9：原语-记录型信号量"><a href="#互斥方法9：原语-记录型信号量" class="headerlink" title="互斥方法9：原语+记录型信号量"></a>互斥方法9：原语+记录型信号量</h3><blockquote><p>记录型信号量：指不仅记录某个资源的剩余情况，还标记有等待该资源的进程队列</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs shell">typedef struct&#123;<br>    int value;<br>    struct process *L<br>&#125; semaphore;<br></code></pre></td></tr></table></figure><p>原本通过轮询的方式检测信号是否变化，现在反向通知信号量更新</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs shell">void wait(semaphore S)&#123;<br>    S.value--;<br>    if(S.value &lt; 0)&#123;<br>        block(S.L); <br>//表示如果当前资源不足，将本线程挂载到等待队列中<br>    &#125;<br>&#125;void signal(semaphore S)&#123;<br>    s.value++;<br>    if(S.value &lt;= 0)&#123; //有进程在等待<br>        wakeup(S.L);<br>//表示当本进程释放资源后，尝试唤醒 / 通知等待队列的进程    <br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="互斥方法9：AND信号量："><a href="#互斥方法9：AND信号量：" class="headerlink" title="互斥方法9：AND信号量："></a>互斥方法9：AND信号量：</h3><blockquote><p>思想：即使原本的记录型信号量四个同步原则都满足，但任然有可能导致死锁，AND信号量尝试解决死锁问题：</p><p>当一个进程同时想要获取到多个共享资源的情况，可能存在死锁的风险，AND基本思想L将进程在整个允许过程中需要的所有资源，一次性分配给进程，使用完后一起释放</p><p>只要有一个资源未能分配给进程，其他所有可能分配的资源也全都不分配给它</p><p>【容器化？】</p></blockquote><h3 id="管程"><a href="#管程" class="headerlink" title="管程"></a>管程</h3><blockquote><p>背景：虽然信号量机制方便有效管理同步，但每个进程都要独自管理PV操作完成同步，麻烦且容易死锁</p><p>管程：像是一个单例的令牌（拿着它才能执行它规定的操作）</p><blockquote><p>实际上就是将PV操作封装到入口方法中</p></blockquote></blockquote><ul><li><p>管程中定义了共享数据</p></li><li><p>定义了访问这些数据的入口（get &#x2F; set …）</p></li><li><p>管程中有很多“入口”，但每次只能开放其中一个入口，让一个进程或线程进入【由编译器负责实现的互斥】</p></li><li><p>在管程的具体入口方法中，设置条件变量 &amp; 等待&#x2F;唤醒操作解决同步问题</p></li></ul><h3 id="信号量机制实现进程同步"><a href="#信号量机制实现进程同步" class="headerlink" title="信号量机制实现进程同步"></a>信号量机制实现进程同步</h3><p>方法：</p><ol><li><p>明确哪些代码需要同步关系，假设A代码块需要先执行，B代码块需要后执行</p></li><li><p>设置同步信号量S，初始为0</p></li><li><p>在A操作后执行V(S)</p></li><li><p>在B操作前执行P(S)</p></li></ol><p>S信号量代表“某种资源”，本质还是一把锁，通过在操作系统跨进程的全局声明这把锁，让两个进程完成间接的通信，彼此知道同步情况</p><p><code>PV操作本质上挂起等待，撤权唤醒其他进程；最直观想到的用处是进程同步，A挂起直到被B唤醒。也用来实现进程互斥，后到的进程被挂起，直到之前的进程唤醒它...比其他实现互斥关系的策略高级的点是：1. CheckAndSet两步为原子性操作，无竞态关系，2. 由wake反向唤醒而不是while轮询，节约CPU资源；</code></p><p>利用信号量的例子：</p><p>生产者 &#x2F; 消费者 模型</p><p>缓冲区的大小为n，要通过信号量实现阻塞队列模式</p><p><code>信号量的PV方法，能够监控某个资源是否有甚于，有则分配，无则挂起；但只能对某个变量判断其是否大于0，在缓冲队列场景中，既要判断队列中元素是否大于0，还要判断是否小于n，所以申请两个信号量full &amp; empty，full初始化为6，越满越接近0.empty初始化为0，越空越接近0</code></p><p>PV逻辑:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs c">producer()&#123;<br>    <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>)&#123;<br>    <span class="hljs-comment">//生产一个产品</span><br>    P(full);<br>    P(mutex);<br>    V(empty); <span class="hljs-comment">//增加一个元素，empty离0报警差距增大</span><br>    V(mutex);    <br>&#125;<br>&#125;<br>consumer()&#123;<br>    <span class="hljs-keyword">while</span>(<span class="hljs-number">1</span>)&#123;<br>    <span class="hljs-comment">//消费一个产品</span><br>    P(full);<br>    V(empty);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>实现互斥的P操作需要在实现同步的P操作之后，否则会造成死锁</p></blockquote><h3 id="利用信号量解决文件读写锁"><a href="#利用信号量解决文件读写锁" class="headerlink" title="利用信号量解决文件读写锁"></a>利用信号量解决文件读写锁</h3><ul><li><p>声明信号量rw &#x3D; 1，表示一个文件要么在被读，要么在被写</p><p>如此解决读写的互斥行为</p></li><li><p>怎么解决读锁对读锁不互斥？</p><p>在加读锁时，判断当前进程是否是第一个尝试加锁的，是则加</p><p>即：多个读进程只在第一个进程加锁，用来屏蔽写进程。</p></li><li><p>但是在对P(rw)的逻辑处理中出现问题：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span>(count == <span class="hljs-number">0</span>)&#123;<br>    P(rw);<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><p>        将原本原子性的操作外包非原子性的逻辑，可能出现新竞态问题</p><p>        解决方法：将整体再加互斥锁 – 声明范围更大的mutex信号量</p><h2 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h2><p>经典场景：哲学家就餐</p><blockquote><p>五个哲学家进程并发执行，首先都拿起左手边的筷子，接着尝试拿起右手边的筷子，但是右手边的筷子正在被别人拿着，而自己刚刚拿到的左手的筷子就是别人想要的右手边的筷子</p><blockquote><p>A占用B等待的锁，同时等待B占用的锁</p></blockquote><blockquote><p>死锁：在并发环境下，各进程因竞争资源而造成的一种互相等待对方手里的资源，导致各进程都阻塞，都无法向前推进的现象</p></blockquote></blockquote><p>死锁、饥饿、死循环的区别：</p><p>产生死锁的条件：</p><ol><li>互斥条件：资源不能是共享的</li><li>不剥夺条件：后来的进程只能等，不能抢</li><li>保持和请求：进程的状态需要一手保持资源，一手请求资源</li><li>循环等待条件：循环等待的进程构成一种链</li></ol><blockquote><p>对不可剥夺的资源的不合理分配，可能导致死锁</p><p>例如：进程请求和释放资源顺序不当：</p><p>P1，P2进程分别申请占用R1，R2，之后P1又申请R2，P2又申请R1</p><p>例如：信号量使用不当：</p><p>信号量在使用时，使用mutex同步也看作一种互斥资源，也可能产生死锁</p></blockquote><h3 id="死锁的处理策略"><a href="#死锁的处理策略" class="headerlink" title="死锁的处理策略"></a>死锁的处理策略</h3><blockquote><ul><li><p>不允许死锁发生</p><ul><li><p>静态策略（预防死锁）：</p><ol><li><p>破坏互斥条件</p></li><li><p>破坏不剥夺条件</p></li><li><p>破坏请求和保持条件</p></li><li><p>破坏循环等待条件</p></li></ol></li><li><p>动态策略：</p></li></ul></li><li><p>允许死锁发生 - 检测并解除</p></li></ul></blockquote><ol><li><p>破坏互斥：</p><p>例如：</p><h4 id="SPOOLing技术："><a href="#SPOOLing技术：" class="headerlink" title="SPOOLing技术："></a>SPOOLing技术：</h4><blockquote><p>能够尝试将原本独占设备的逻辑改造成共享设备</p><p>例如：原本两个线程互斥使用打印机，SPOOLing使得两个线程的任务都能够被处理（初步地，由另一个任务管理，而非直接给打印机）</p></blockquote><p>但是这种技术只是逻辑上的共享，某些设备根本不能共享一点</p></li><li><p>破坏不剥夺：</p><p>当某个进程需要的资源被其他进行所占有时，如果此进程优先级更高，则可以强行剥夺资源</p><p>缺点：</p><ol><li><p>被强行剥夺资源需要保证之前的内容被保存，而CPU天生支持此操作，其他资源在切换中不一定效率高</p></li><li><p>引入优先级就会带来饥饿问题</p></li></ol></li><li><p>破坏请求和保持条件</p><p>某个进程请求新的资源得不到满足时，它需要立即释放保存的所有资源，以后需要时重新申请。</p><p>or 静态资源分配法：进程在运行前申请完需要的全部资源，在未满足前，不投入运行</p><blockquote><p>资源打包，有点像docker</p></blockquote><p>缺点：</p><ol><li><p>资源和进程耦合绑定，导致资源的灵活程度不高 - 利用率不高</p></li><li><p>隐性的饥饿问题</p><p>由于进程的运行条件苛刻了，不运行的几率就增大了</p><p>当进程A需要qwer四种资源，但qwer始终被其他进程以各种方式占用，A无法get all；</p></li></ol></li><li><p>破坏循环等待：</p><blockquote><p>顺序资源编号法</p><ul><li><p>为了避免循环等待，尝试让拥有了某些资源的进程别去申请某些资源 – （打破环种某处）</p></li><li><p>为系统内的资源编号，规定每个进程必须按照编号递增的顺序请求资源</p></li><li><p>如此一来，假如原本ABCDE五个哲学家，E就不会拥有E而申请A，因为E会停留在申请A上</p></li></ul></blockquote><p>缺点：</p><ol><li><p>设备编号不好实现</p></li><li><p>申请资源的顺序影响业务逻辑 - 资源按ABCD顺序来，但代码应该以DCBA使用资源，就会导致闲置时间长</p></li></ol></li></ol><h4 id="动态策略：避免死锁"><a href="#动态策略：避免死锁" class="headerlink" title="动态策略：避免死锁"></a>动态策略：避免死锁</h4><ul><li><p>安全序列：</p><blockquote><p>指如果系统按照这种序列分配资源，则每个进程都能顺利完成。只要找出一个安全序列，系统就是安全状态。</p><blockquote><p>例如：银行家问题：只有给一家公司打满借款才能被还，否则公司无法偿还 - 银行决定如何将资源顺利分配给各个公司，某些顺序会导致资源锁死在公司，某些顺序可以完整执行</p></blockquote></blockquote></li><li><p>银行家算法：</p><blockquote><p>思想：在某次资源分配之前，判断这次分配是否会导致系统进入不安全状态</p><ul><li><p>操作系统判断基本流程：</p><p>每个进程向操作系统申报需要的资源（用信号量表示的向量，来表示各个资源数目），操作系统判断是否安全 &#x2F; 能否形成安全序列</p></li></ul></blockquote></li></ul><h4 id="死锁的检测和解除"><a href="#死锁的检测和解除" class="headerlink" title="死锁的检测和解除"></a>死锁的检测和解除</h4><ul><li><p>检测：使用化简资源分配图【描述进程和资源的申请与占用的关系】，尝试找到某个进程可以得到申请，释放占用，可以让整张图运作起来</p><p>本质还是找到安全序列</p><p>如果找不到安全序列 - 进入不安全状态 - 即将死锁</p></li><li><p>解除：</p><ol><li><p>资源剥夺法：</p><blockquote><p>简单的将某个线程挂起，就可以让它空出资源</p><blockquote><p>但有些资源也不是说让就让</p></blockquote></blockquote></li><li><p>撤销进程法：</p><blockquote><p>强行撤销部分进程。代价很大，导致进程之前的运行被浪费</p></blockquote></li><li><p>进程回退法：</p><blockquote><p>让某个死锁的进程尝试让出某个资源，只需要先让这个进程回退到还没申请此资源的状态。</p></blockquote></li></ol></li><li><p>如何决定对哪个进程动手？</p><ol><li><p>进程优先级</p></li><li><p>进程执行了多长时间（撤销和回退的成本大）</p></li><li><p>还需要多久能完成（期望大）</p></li><li><p>进程已经占用了多少资源（释放多，期望大）</p></li><li><p>进程是交互式 还是 批处理（交互式则优先级高…）</p></li></ol></li></ul><h1 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h1><blockquote><p>回来吧我的TEG</p></blockquote><ul><li><p>内存的存储单元</p><blockquote><p>内存存储数据的最小单元；从0开始编址。</p><p>按照单元大小分为：</p><ul><li><p>按字节编址：每个存储单元的空间是8bit</p></li><li><p>按字编址：每个存储单元空间是1字（1字通常是16bit，但不同型号有所不同）</p></li></ul></blockquote><p><code>4GB的内存到底多大 -- 内存中可以存放 4*2^30个字节，即一共有 2^32个存储单元 -- 标识单位编号的数需要有32个二进制位。</code></p></li><li><p>CPU与内存的基本执行流程：</p><p>内存种，某个进程占据的内存空间分程序段、数据段、PCB。</p><p>CPU将程序段中的一条条指令读取，并按照指令码完成最基本的操作</p><p>例如：一次加法操作由读取某个地址的数到寄存器，加，将此数放回某位置三步</p></li><li><p>程序经过编译、链接后生成的指令中，指明的是逻辑地址（相对地址 &#x2F; 局部变量内的地址），如何将逻辑地址转化成物理地址呢？</p><p>三种策略 ： 绝对装入 &#x2F; 可重定位装入 &#x2F; 动态运行时装入</p><blockquote><p>引理：程序从写代码到运行的过程</p><ol><li><p>源代码编译（将高级语言翻译成机器语言）</p></li><li><p>将编译后的机器码和所需库函数链接，形成一个完整的装入模块</p></li><li><p>装载（装入）：由装入程序将模块装入内存中</p></li></ol></blockquote><ol><li><p>绝对装入：</p><p>在编译期间尝试修改相对地址到绝对地址</p></li><li><p>可重定位装入：</p><p>在装入过程中尝试修改相对地址到绝对地址</p><p>将一个程序段中相对地址增加统一的偏移量 - 重定向</p></li><li><p>动态重定位装入：</p><p>并不尝试修改地址，而是把地址转换推迟到程序真正要执行时；</p><p>需要一个重定位寄存器，用来记录程序段的偏移量；程序运行时只需要将相对地址+偏移量 &#x3D; 绝对地址</p></li></ol></li></ul><p>链接的三种方法：</p><ol><li><p>静态链接：程序运行之前将各个目标模块和需要的库函数链接成一个完整的可执行文件</p></li><li><p>装入时动态链接：在各个模块装入内存时，边装入边链接</p></li><li><p>运行时动态链接：程序执行中，当需要该模块时，才进行链接</p></li></ol><ul><li><p>内存管理</p><blockquote><p>任务：</p><ol><li><p>内存空间的分配与回收</p></li><li><p>内存空间的扩充（实现虚拟性）</p></li><li><p>地址转换（由逻辑地址到物理地址的转换）</p></li><li><p>存储保护：保证各进程在自身内存空间运行，不会越界访问</p><blockquote><p>两种实现方案：</p><ol><li><p>设置上下限寄存器，当访问的地址越过边界则不允许</p></li><li><p>利用重定位寄存器，界地址寄存器记录边界</p></li></ol></blockquote></li></ol></blockquote></li><li><p>内存空间的扩充</p><ol><li><p>覆盖技术</p><blockquote><p>当程序大小超过物理内存总和时，能否只把核心程序段常驻性加载到内存，而不常用的段在需要时再调用内存</p></blockquote><p>内存中分出“固定区”和若干个“覆盖区”；程序分为若干个段，其中包括常用段和不常用段</p><p>使用场景：</p><blockquote><p>当存在代码段之间互斥调用时效果最为显著</p></blockquote><p>确定：很难实现让操作系统感知哪些程序段是互斥的，只能人为设置互斥代码块</p></li><li><p>交换技术</p><blockquote><p>思想：当内存空间紧张时，系统将内存中某些进程暂时换出外存，必要时将外存中某些已具备运行条件的进程换人内存（进程再内存与磁盘间动态调度）</p></blockquote><p>问题：</p><ol><li><p>在磁盘的什么位置保存进程？</p><p>通常将磁盘空间分为文件去和对换区；文件区用于存放文件，主要追求存储空间的利用率 - 因此对文件区空间的管理采用离散分配方式；</p><p>对换区空间只占磁盘空间的小部分，被换出的进程数据存在对换区；对换区主要关注访问和加载速度 - 因此空间管理采用连续分配方式</p></li><li><p>什么时候交换？</p><p>当内存吃紧，系统负荷增高，许多进程进程发生缺页</p></li><li><p>应该换出哪些进程？</p><p>思考角度：</p><ul><li><p>换出阻塞的进程，反正一时也运行不了</p></li><li><p>换出优先级低的进程（可能导致饥饿）</p></li><li><p>换出驻留时间太长的进程</p></li></ul></li></ol></li><li><p>虚拟内存技术</p></li></ol></li><li><p>内存空间的分配与回收</p><ul><li><p>连续分配方式 - 指为用户进程分配的是一个连续的内存空间</p><ul><li><p>单一连续分配</p><blockquote><p>当只有一个用户程序时，将整个内存中除了系统区外的空间全都给这个进程</p><p>不需要存储保护，但内存利用率不高</p></blockquote></li><li><p>固定分区分配</p><blockquote><p>将整个用户空间划分成若干个固定大小的分区，每个分区运行一道作业</p><p>分区大小可以相同（适合一台计算机控制多个同类任务的场景），也可以不同（适合执行不同任务的计算机，更加灵活）</p></blockquote><p>维护一张分区说明表，来实现各个分区的分配、回收、管理。</p><p>问题：当程序太大 - 小空间放不下 - 需要覆盖技术才能放下</p><p>当程序太小 - 会产生碎片</p></li><li><p>动态分区分配</p><blockquote><p>不预先划分内存分区，而是在进程装入内存时，根据进程的大小动态建立分区，使得分区大小正好适合进程需要</p></blockquote><p>问题：</p><ol><li><p>用什么数据结构记录内存使用情况：</p><ul><li><p>空闲分区表</p></li><li><p>空闲分区链</p></li></ul></li></ol></li></ul></li></ul></li></ul><figure class="highlight nestedtext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><code class="hljs nestedtext">    ![](file://C:\Users\吴松林\AppData\Roaming\marktext\images\2024-03-15-20-33-33-image.png?msec=1715927937190)<br><br>  2. 当很多个空闲分区都满足需求时，应该选择哪个分区进行分配？<br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">动态分区分配算法</span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 首次适应：从头到尾找合适的分区</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 最佳适应：优先使用更小的分区</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 最坏适应：优先使用更大的分区</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 邻近适应：由首次适应演变而来，每次从上次查找结束位置开始查找</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">![](file://C:\Users\吴松林\AppData\Roaming\marktext\images\2024-03-15-21-05-36-image.png?msec=1715927937255)</span><br><br>  3. 如何进行分区的分配与回收<br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">对于“空闲分区表”，通过管理表，实现对内存空间的逻辑管理</span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">表管理的API包括：释放某一进程空间将此空间合并 / 添加到表里；添加某一进程空间更新表中某个空间大小...</span><br><br><br><span class="hljs-comment">#### 内存碎片问题：</span><br><br><span class="hljs-bullet">-</span> <span class="hljs-string">内部碎片：分配给某进程的内存区域中有些部分没用上</span><br><br><span class="hljs-bullet">-</span> <span class="hljs-string">外部碎片：内存中某些空间分区由于太小无法被利用</span><br><br><br>外部碎片解决方案：<br><br><span class="hljs-bullet">-</span> <span class="hljs-string">紧凑（拼凑）技术：将进程占用的空间往前移动</span><br><br>  紧凑技术会改变进程内指令的绝对地址，适合动态重定位装入方式，简单改变PCB中的重定向寄存器即可，而对于前面两种较死板的方案成本较大<br></code></pre></td></tr></table></figure><ul><li><p>非连续分配方式</p><ul><li><p>基本分页存储管理</p><blockquote><p>分页存储：</p><ul><li><p>将内存空间分为一个个大小相等的分区，每个分区就是一个”页框 &#x2F; 页帧 &#x2F; 内存块 &#x2F; 物理块 &#x2F; 物理页面“</p><p>每个页框有一个编号，即“页框号”</p></li><li><p>将进程的逻辑地址空间也分为和页框大小相等的部分，每个部分称为一个“页 &#x2F; 页面”，页面编号称为“页号”</p></li><li><p>操作系统以页框为单位，为各个进程分配内存空间；进程的各个页面与内存的各个页框一一对应</p></li></ul><p>由页表管理每个进程的页面和具体页框的关系</p><ul><li><p>逻辑地址切换成物理地址：</p><p>根据页表确定页面的起始地点，再加上由逻辑地址推算得出的页面内偏移量</p><blockquote><p>系统级优化：由于内存块大小，页面大小都是二的幂次，所以各种运算其实只是位运算，并无实际的乘除</p></blockquote></li></ul></blockquote><p>实际推算指令位置的流程：</p><ol><li><p>由逻辑地址算出页号和页内偏移量</p></li><li><p>查找页表寄存器（记录此进程的页面记录在页表的何处）</p></li><li><p>查找页表，找到对应的页表项标明的内存块号</p></li><li><p>将内存块的起始位置 + 页内偏移量计算出实际物理地址</p></li><li><p>访问</p></li></ol></li></ul></li></ul><figure class="highlight nestedtext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br></pre></td><td class="code"><pre><code class="hljs nestedtext">  <span class="hljs-bullet">-</span> <span class="hljs-string">基于快表的优化查询方案</span><br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">快表：TLB（translation lookside buffer）部署在CPU内的高速缓存，用来存放最近访问的页表项的副本，为了比内存更快的访问</span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">既然是BUffer，类似Redis，也存在缓存命中、缓存过期等问题</span><br><br>    由于局部性原理，块表的命中率可以达到90&amp;%以上<br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">局部性原理：</span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 时间局部性：如果程序执行了某条指令，则不久后这条指令会被再次执行（因为程序中会有大量循环）</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  对于内存中存放指令的空间而言</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- 空间局部性：如果程序访问了某个存储单元，则不久后附件的存储单元也会被访问</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  对于内存中存放变量的空间而言</span><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br><br><br>  单级页表存在的问题：<br><br>  1. 用户进程占用空间可能会很大，导致页面很多，而页表需要是一片连续空间，当页面太多时空间太大<br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">解决方案令人无语：再将大页面分成小页面，由一个管理页表的页表（页目录表 / 一级页表）来维护每个页表块所在内存的具体位置</span><br><br>  2. 由于局部性原理，用户并不需要进程中的所有页面，可以考虑让某些不常访问的页面退出内存<br><br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">虚拟内存技术：通过在页表项增加标识字段，表示页面是否已经调入内存</span><br>    <span class="hljs-punctuation">&gt;</span> <br>    <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">当用户态进程尝试访问在内存中不存在的内存块，会产生缺页中断（内中断），由操作系统将目标页面从外存中调入</span><br><br><span class="hljs-bullet">-</span> <span class="hljs-string">基本分段存储管理</span><br><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">分段存储：进程的地址空间按照自身逻辑关系划分成若干个段，段与段高内聚，每个段有一个段名，便于开发者调用</span><br>  <span class="hljs-punctuation">&gt;</span> <br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">例如：一个进程空间分成三段：</span><br>  <span class="hljs-punctuation">&gt;</span> <br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- MAIN：主函数段</span><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- X：存储某个子函数</span><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">- D：存储全局变量</span><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">  </span><br>  <span class="hljs-punctuation">&gt;</span> <br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">于是逻辑地址由段号 + 段内地址组成</span><br><br>  访问过程：<br><br>  1. 当此进程要参与运行时，进程切换相关的内核程序负责恢复进程的运行环境；其中一步就是从进程的PCB中读取出 段表起始地址和段表长度，之后存储在段表寄存器中<br><br>  2. 根据指令的逻辑地址，推算出逻辑上所在段号和段内地址<br><br>  3. 访问段表寄存器，找到对应段表项对应的物理起始地址<br><br>  4. 将起始地址和段内地址相加，即真实物理地址<br><br><br>  过程中计算位置的操作可以内嵌检查是否越界<br><br>  <span class="hljs-comment">#### 分页与分段的对比：</span><br><br>  1. 分页对用户不可见，分段对用户可见<br><br>  2. 分页的地址空间是一维的，分段的地址空间是二维的（对上层而言，而分段的上层就是开发者）<br><br>  3. 分段更容易实现代码保护（代码内聚程度较高，集权容易，而分页并不是按照逻辑拆分的每个代码块，较难隔离各种权限）<br><br>  4. 分页、分段访问一个逻辑地址都需要两次访问内存（引入二级分页则需要三次），分段存储也可以引入块表机制<br><br><span class="hljs-bullet">-</span> <span class="hljs-string">段页式存储</span><br><br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">对于进程空间，先分段，再分页</span><br>  <span class="hljs-punctuation">&gt;</span> <br>  <span class="hljs-punctuation">&gt;</span> <span class="hljs-string">分段对与用户是可见的，开发者根据段号、段内地址进行开发；而分页对用户不可见，系统根据段内地址自动划分出页号和业内偏移量</span><br><br>  通过段表 + 页表明确对应物理空间的位置<br><br>  段表记录每个段对应的页号；页表记录每个页号对应的内存块号<br></code></pre></td></tr></table></figure><h3 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h3><ul><li><p>传统存储管理方式：</p><ul><li><p>连续分配</p><ol><li><p>单一连续分配</p></li><li><p>固定分区分配</p></li><li><p>动态分区分配</p></li></ol></li><li><p>非连续分配</p><ol><li><p>基于分页存储管理</p></li><li><p>基于分段存储管理</p></li><li><p>段页式存储管理</p></li></ol></li></ul></li></ul><p>传统管理方式的缺点：</p><ol><li><p>任务需要一次性全部装入内存后才能运行；导致当任务很大，空间很小时无法运行</p></li><li><p>同时任务中一些暂时用不到的数据也会长期占用内存，导致内存使用率不高</p></li></ol><blockquote><p>没有调度算法 和 淘汰算法</p></blockquote><h4 id="利用局部性原理可以设计调度算法和淘汰算法"><a href="#利用局部性原理可以设计调度算法和淘汰算法" class="headerlink" title="利用局部性原理可以设计调度算法和淘汰算法"></a>利用局部性原理可以设计调度算法和淘汰算法</h4><p>调度：在程序装入时，将很快就会用到的部分装入内存，暂时用不到的部分留在外存</p><p>另外，当用户访问的信息不在内存时，操作系统负责将信息从外存调入内存</p><p>淘汰：内存不够时，由操作系统将蚕食用不到的信息换出外存</p><blockquote><p>于是在用户看起来内存执行了更多的任务，看起来更大 – 虚拟内存</p></blockquote><p>虚拟内存特征：</p><ol><li><p>多次性：无需在作业执行前一次性全部装入内存，而是运行分成多次调入</p></li><li><p>对换性：存在作业的换入换出</p></li><li><p>虚拟性：…</p></li></ol><ul><li><p>虚拟内存计算要求内存管理使用离散分配</p><blockquote><p>因为动态的调入调出代码块并不适合连续空间</p></blockquote></li></ul><h3 id="请求分页存储管理"><a href="#请求分页存储管理" class="headerlink" title="请求分页存储管理"></a>请求分页存储管理</h3><blockquote><p>由于使用虚拟内存技术主要实现两点：</p><p>需要时调入、空间紧张时调出，所以每个页面需要额外的状态位标识状态，额外的统计指标用于被算法调度，额外的外存地址信息用户写回…</p></blockquote><p>基本过程：</p><ol><li><p>当想访问的页面不在内存中时，产生一个缺页中断；之后由操作系统的缺页中断处理程序处理中断</p><p>在中断过程中，缺页的进程阻塞，放入阻塞队列，调页完成后将其唤醒，放回就绪队列</p></li><li><p>缺页中断处理程序需要考察内存中有无空闲块，如果有，则占用，将外存中目的页面装入，并修改页表</p><p>如果没有，则需要有置换算法尝试淘汰一个页面；假如这个页面在内存期间被修改过，则要将其写回外存，未修改则直接淘汰</p></li></ol><p>问题：</p><ol><li><p>换入换出都是IO操作，都有不小的开销</p></li><li><p>一条指令可能会导致多次缺页中断，因为一条指令操作的数据可能分散在多个页中，【看来需要一次性加载…】</p></li><li><p>由于快表中有慢表的备份，更新慢表时任需更新快表</p></li></ol><h4 id="页面置换算法"><a href="#页面置换算法" class="headerlink" title="页面置换算法"></a>页面置换算法</h4><blockquote><p>由于换入换出成本较大，好的算法应该追求更少的缺页率</p></blockquote><ol><li><p>最佳置换算法（Optimal）</p><blockquote><p>每次选择之后最长时间内不被访问的页面进行淘汰</p></blockquote><p>需要预见未来，使用场景受限</p></li><li><p>先进先出置换算法（FIFO）</p><blockquote><p>淘汰各个占用内存块上，最先进来的页面</p></blockquote></li><li><p>最近最久未使用置换算法（LRU least recently used）</p><blockquote><p>在页表中记录每个页面最近一次范围的时间，淘汰时挑选时间最长的页面</p></blockquote></li><li><p>始终置换算法（CLOCK &#x2F; 最近未用算法 Not Recently Used）</p><blockquote><p>将内存中的页面链接成循环队列，要淘汰时从上次结束位置开选，选出使用次数少（通过在页表中加一个字段 - 访问位），且长时间没考虑的（一周没转到它）</p></blockquote></li><li><p>改进型时钟置换算法</p><blockquote><p>在修改过内容的页面和没修过过内容的页面之中，理论上应该让没修过过的淘汰，因为不用再IO没成本</p><p>所以此算法再添一个因素</p></blockquote><p>用两个标志位（访问位、修改位）标识每个页面</p></li></ol><h4 id="策略优化-局部置换"><a href="#策略优化-局部置换" class="headerlink" title="策略优化 - - 局部置换"></a>策略优化 - - 局部置换</h4><blockquote><p>每个进程最开始会被分配一定数量的物理块，该进程缺页时，只允许从该进程自己的物理块中选出一个进程换出</p></blockquote><p>避免置换算法导致页面管理太乱 - A的页面经常把B的页面顶掉…</p><p>对于经常切换的进程，更合理的策略是：多给几个块</p><h4 id="策略优化-可变分配"><a href="#策略优化-可变分配" class="headerlink" title="策略优化 - - 可变分配"></a>策略优化 - - 可变分配</h4><blockquote><p>系统最开始分配给进程一定量的物理块，之后根据时期情况合理召回或者赋予物理块</p></blockquote><h4 id="策略优化-何时调入页面"><a href="#策略优化-何时调入页面" class="headerlink" title="策略优化 - - 何时调入页面"></a>策略优化 - - 何时调入页面</h4><ol><li><p>预调页策略</p><blockquote><p>按照局部性原理，每次调用都尝试调入相邻的页面</p></blockquote><p>实际测试发现成功率只有50%，所以更多的使用场景是：</p><p>进程首次运行时，开发者配置好要预调的页面</p></li><li><p>请求调页策略</p><p>缺页时才调</p></li></ol><h4 id="从何处调入页面："><a href="#从何处调入页面：" class="headerlink" title="从何处调入页面："></a>从何处调入页面：</h4><blockquote><p>磁盘中的对换区（采用连续分配方式来保证读写更快的区域）</p></blockquote><p>但实际情况不同也有不同策略</p><ol><li><p>对换区够大，万事大吉</p></li><li><p>对换区紧张：对于不会被修改的数据安置在文件区（因为后续不会再有modify的IO），对于可能被修改的部分，安置在对换区</p></li></ol><h4 id="策略优化-抖动-颠簸"><a href="#策略优化-抖动-颠簸" class="headerlink" title="策略优化 - - 抖动 &#x2F; 颠簸"></a>策略优化 - - 抖动 &#x2F; 颠簸</h4><blockquote><p>指：刚刚换出的页面马上又要换入，刚刚换入的页面马上又要换出</p></blockquote><p>出现的主要原因是：给进程分配的物理块（驻留集）太少了</p><p>引理：如何确定进程应该拥有的驻留集数量？</p><blockquote><p>测试，测试方法是：观察某个窗口尺寸下，工作集的大小</p><blockquote><p>指：某个时间段内，实际使用了多少物理块</p></blockquote></blockquote><h3 id="内存映射文件（Memory-Mapped-Files）"><a href="#内存映射文件（Memory-Mapped-Files）" class="headerlink" title="内存映射文件（Memory - Mapped Files）"></a>内存映射文件（Memory - Mapped Files）</h3><blockquote><p>通过在内存中声明一个和文件同等大小的空间，对此空间的修改只在必要时才装入对应文件</p></blockquote><p>特点：</p><ol><li><p>以访问内存的方式访问文件数据</p><p><code>和常规文件访问的主要区别是：没有全部的read操作和write操作，并且对于开发者而言无需关注read，write，就好像所有数据都在内存中一样。</code></p></li><li><p>可以实现文件共享</p><p>将文件映射到内存中后（并不属于任何一个进程的区域），多个进程都可以由页表将自身的虚拟地址空间和此MMF进行联系，以实现共享</p></li></ol><h1 id="文件管理"><a href="#文件管理" class="headerlink" title="文件管理"></a>文件管理</h1><ul><li><p>文件的属性：</p><ol><li>文件名</li><li>标识符：一个系统内各文件标识符唯一，操作系统用于区分各个文件的一种内部名称</li><li>类型</li><li>位置：文件所在路径（逻辑地址），在磁盘中的地址（真实地址）</li><li>大小</li><li>创建时间、修改时间</li><li>文件保护信息：将读、写、执行权分给不同的用户组</li></ol></li><li><p>文件内部数据如何组织：</p><p>文件分类：</p><ol><li>无结构文件：纯粹的二进制流存储</li><li>有结构文件：或许可以尝试用某种逻辑结构存储，空间越小越好</li></ol></li><li><p>从需求出发考虑OS应该向上提供哪些功能</p><ol><li>create 系统调用：表示创建文件</li><li>read</li><li>write</li><li>delete</li><li>open</li><li>close</li></ol></li></ul><h3 id="文件的逻辑结构-物理结构"><a href="#文件的逻辑结构-物理结构" class="headerlink" title="文件的逻辑结构 &amp; 物理结构"></a>文件的逻辑结构 &amp; 物理结构</h3><ul><li>逻辑结构：指在用户看来，文件内部的数据是如何组织的。</li><li>物理结构：指在操作系统看来，文件发数据如何真实存放在磁盘</li></ul><h3 id="有结构文件的逻辑结构"><a href="#有结构文件的逻辑结构" class="headerlink" title="有结构文件的逻辑结构"></a>有结构文件的逻辑结构</h3><blockquote><p>对于无结构文件，没有太多技术压缩空间</p></blockquote><p>有结构文件又称为“记录式文件”。根据各条记录的长度（占用的存储空间）是否相等，分为定长记录 和 可变长记录</p><p>对于可变长记录，真实存储方式就是类似InnoDB的B+树索引，在操作系统中叫做：索引顺序文件（index）</p><h3 id="文件目录："><a href="#文件目录：" class="headerlink" title="文件目录："></a>文件目录：</h3><blockquote><p>产生文件夹效果的手法：</p><p>在一个目录下的文件，首先对应一张目录控制块（一个二维表），刻画了每个子项的具体信息</p><p>对于文件而言，控制块直接指向文件真实地址，对于文件夹而言，控制块指向下一个文件控制块的地址</p></blockquote><p>文件控制块（FCB）：</p><p>包含：</p><ul><li><p>文件基本信息（文件名，物理地址，逻辑结构，物理结构）</p></li><li><p>存取控制信息（是否可读可写，访问用户白名单）</p></li><li><p>使用信息（文件的创建时间，修改时间）</p></li></ul><blockquote><p>问题：对于FCB的访问十分平常，但主要是访问文件名和引用地址两个字段，其余信息避免每次都加载到内存</p><p>方案：将Index文件中只存储文件名和路径信息，其余信息额外存储到索引节点中【冷热分离】</p></blockquote><h5 id="树形目录结构："><a href="#树形目录结构：" class="headerlink" title="树形目录结构："></a>树形目录结构：</h5><p>目录结构：</p><ol><li><p>根目录 – 下包括多个用户目录</p></li><li><p>用户目录 – 下递归包括文件夹目录</p></li><li><p>多级目录结构</p></li></ol><h5 id="无环图目录结构："><a href="#无环图目录结构：" class="headerlink" title="无环图目录结构："></a>无环图目录结构：</h5><blockquote><p>原本树形结构可能存在问题：当不同路径下的文件相同，但也需要分别存储，无法共享</p></blockquote><p>无环图试图将多个文件的引用指向同一个文件</p><h2 id="文件的物理存储"><a href="#文件的物理存储" class="headerlink" title="文件的物理存储"></a>文件的物理存储</h2><blockquote><p>核心完成：由逻辑地址（逻辑块号 + 块内地址）向 真实地址（真实块号 + 块内地址）的转化</p></blockquote><p>按照文件的分配方式划分：</p><ol><li><p>连续分配</p><p>每个文件在磁盘上都占有一组连续的块</p><p>记录对应关系只需要在FCB中记录文件名，起始块号，长度</p></li></ol><blockquote><p>引理：</p><p>OS读取某个磁盘块时，需要移动磁头；当访问两个磁盘块相隔越远，移动磁头所需的时间越长</p></blockquote><ol start="2"><li><p>隐式链接</p><p>文件块之间通过链表形式存储</p><p>FCB中需要存储文件名，头节点块号</p></li><li><p>显示链接：</p><p>将用于链接文件物理块的指针信息显式的存储在另一张表中</p><p>FAT（File Allocation Table）</p><p>好处多多：</p><ol><li><p>只要将FAT预加载到内存，就可以避免链式查找导致的无法随机访问（只要沿着FAT走到对应位置即可）</p></li><li><p>添加，删除的实现更是简单中的简单</p></li></ol></li><li><p>索引分配：</p><blockquote><p>允许文件离散地分配在各个磁盘块中，系统会为每个文件建立一张索引表，索引表中记录文件的各个逻辑块对应的物理块（metadata）</p><p>索引表存储在索引块中</p><blockquote><p>将DFS中tracker内置到storage中一样</p></blockquote></blockquote></li></ol><h4 id="操作系统使用哪种存储方式，取决于选择的策略；策略取决于需求"><a href="#操作系统使用哪种存储方式，取决于选择的策略；策略取决于需求" class="headerlink" title="操作系统使用哪种存储方式，取决于选择的策略；策略取决于需求"></a>操作系统使用哪种存储方式，取决于选择的策略；策略取决于需求</h4><h4 id="文件存储空间管理"><a href="#文件存储空间管理" class="headerlink" title="文件存储空间管理"></a>文件存储空间管理</h4><ul><li><p>磁盘分区：</p><p>windows管理中，将整块磁盘分成C盘，D盘，E盘</p><p>每个盘中分成目录区和文件区</p><p><code>原本某个目录中可能有文件，也可能有目录，二者看似是交叉的；但是目录对子项的联系都是链表</code></p><p>即：逻辑上是树状结构层层分明，物理上也有结构用于管理</p></li><li><p>在某些场景下，多个物理磁盘可以组成一个文件卷</p></li></ul><p>管理策略：</p><ol><li><p>空闲表法：</p><p>维护一张表，记录磁盘中哪些块是空闲的</p><blockquote><p>比较适合 “连续分配方式” – 空位也是连续的便于存储，再将空位添加连续数据也容易</p></blockquote><p>考虑当回收磁盘时需要如何更新meta</p></li><li><p>空闲链表法：</p><p>分两种：空闲盘块链，空闲盘区链</p><p>考虑如何回收：</p><ol><li><p>将回收的盘块一次挂到链尾（可能导致链路太乱，磁头移动较慢）</p></li><li><p>分前后有无空闲盘块</p></li></ol></li><li><p>位示图法：</p><p>通过bit-map标记具体盘块有无占用</p></li><li><p>成组链接法：</p><blockquote><p>每个组的头部标记了下一组中哪些是空白块的信息</p><p>设置一个超级块 标记第一块的空白情况</p></blockquote><p>分配空白块的方式就是沿着头部一个一个查看</p></li></ol><h3 id="文件的基本操作"><a href="#文件的基本操作" class="headerlink" title="文件的基本操作"></a>文件的基本操作</h3><ol><li><p>创建文件：create</p><ul><li><p>传入参数：文件大小（占用外存空间大小）、路径（外存的逻辑地址）、文件名（可能要对应一个uuid）</p></li><li><p>行为：</p><ol><li><p>在外存中找到文件需要的空间【磁盘上确定哪些空间是被占用的，哪些块可用】</p></li><li><p>将实际占用哪些块记录在FAT中，或者其他元数据存储位置</p></li><li><p>将存放的路径尝试记录到目录文件中</p></li></ol></li></ul></li><li><p>打开文件：open</p><ul><li><p>传入参数：路径+文件名</p></li><li><blockquote><p>操作系统的打开和读取不同，打开就像是将元数据预加载，并且将用户对文件的权限先明确；</p><p>即：read前的前置操作</p></blockquote><p>预加载到：<strong>打开文件表</strong></p></li></ul></li><li><p>读文件：read</p><ul><li>传入参数：哪个文件、读取哪个位置的多少数据、读取到什么位置</li></ul></li></ol><h3 id="文件系统的层次结构"><a href="#文件系统的层次结构" class="headerlink" title="文件系统的层次结构"></a>文件系统的层次结构</h3><hr><h3 id="OS-in-xiaolinCoding"><a href="#OS-in-xiaolinCoding" class="headerlink" title="OS in xiaolinCoding"></a>OS in xiaolinCoding</h3><h2 id="内存管理："><a href="#内存管理：" class="headerlink" title="内存管理："></a>内存管理：</h2><ol><li><p>单片机就是简单的单道操作系统</p><ol><li><p>相对于成熟的OS，没有内存地址转换</p></li><li><p>OS中内存地址转换就是将各自任务内的逻辑地址转化成真实的内存地址，<strong>根据所处时代背景分多种方式</strong>。早期类似单片机，逻辑就是物理；后期要执行多道任务，可以在装入程序时将逻辑修改位物理；在后期引入虚拟内存，要考虑页面&#x2F;段&#x2F;进程的调入调出，又有了动态逻辑地址转换</p></li></ol></li><li><p>内存分段：</p><blockquote><p>开发者将程序所在进程划分呈堆段、栈段、代码段、数据段</p></blockquote><ul><li><p>单纯的分段引发的问题：</p><ol><li><p>划分粒度比较大，容易产生外部碎片</p><p>解决碎片问题 - 整理 &#x2F; 调出某个进程</p><p><code>整理就像JVM中的老年代 major GC，为了避免多任务环境下并发的问题，通常会Stop The World，迫使其他任务阻塞</code></p><p><code>调出某个程序：虽然后续的虚拟内存技术就是按照这个思路推进的，但粒度比较小，并且配合有更精妙的任务调出算法</code></p></li></ol></li></ul></li><li><p>内存分页：</p><blockquote><p>把整个虚拟空间 &#x2F; 物理空间 切成一段段固定尺寸的大小</p></blockquote><ul><li><p>问题：</p><p>虽然不会有外部碎片，但还有点点内部碎片，通常在程序最后一个页内</p><blockquote><p>内部碎片指：在一个页内没用到的空间</p></blockquote></li><li><p>分页机制不仅是对内存碎片的优化，同时将分段机制加载的粒度降低，使得调入调出时更加灵活，更加快速，在与置换区调入调出页面能够做到”虚拟内存“，让用户感觉到更大的内存空间。而不是迫于空间不足强制让某个段下线。</p></li><li><p>问题：</p><ol><li><p>分页为了灵活，灵活的成本是管理困难</p><p>一页通常大小是4KB，虚拟地址空间可能有4GB，于是大约需要操作系统管理100万各页。每个页表至少要存储编号和对应物理地址，大概4字节，所以需4MB的内存用来存储页表</p><p>一个进程有4MB，100各进程有400MB</p><ul><li>解决方案：</li></ul></li><li><p>解决太大的问题：多级索引</p></li></ol><blockquote><p>64位操作系统分四级目录</p></blockquote><ol start="2"><li>解决台难管理的问题：</li></ol><ul><li><p>局部性原理</p></li><li><p>快表（优化多级索引带来的额外时间消耗）</p></li></ul></li></ul></li><li><p>发展历程</p><p>早期Intel使用分段内存管理，后期用分页来优化分段（由&#x3D;在分段内存管理上再添一层地址映射关系）</p></li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>MySQL</title>
    <link href="/2024/05/17/MySQL/"/>
    <url>/2024/05/17/MySQL/</url>
    
    <content type="html"><![CDATA[<h1 id="回来吧我的MySQL！"><a href="#回来吧我的MySQL！" class="headerlink" title="回来吧我的MySQL！"></a>回来吧我的MySQL！</h1><ol><li><h2 id="为什么要使用数据库"><a href="#为什么要使用数据库" class="headerlink" title="为什么要使用数据库"></a>为什么要使用数据库</h2></li></ol><ul><li><p>持久化：把数据保存到可掉电式存储设备中，以供之后使用</p><p>不同类型的数据存储到不同介质中</p><p>图片、视频、代码、二进制等存到文件中，复杂类型数据存储到数据库中</p><blockquote><p>为什么数据存在数据库里而不是文件里？</p><p>例如上亿条数据如果存在txt、excel文件里，找寻一条需要O(n)遍历，如果在数据库的一张表里，有很多优化策略提高查询速度</p><p>同时数据库存储类型更丰富</p></blockquote></li></ul><ol start="2"><li><h2 id="数据库与数据库管理系统"><a href="#数据库与数据库管理系统" class="headerlink" title="数据库与数据库管理系统"></a>数据库与数据库管理系统</h2><p>数据库管理系统DBMS管理多个数据库，一般开发人员会针对每一个应用创建一个数据库</p><p>真实的数据库就是一个文件夹下的n多张表（文件）</p><p>MySQL的身份是DBMS，负责打开与操作这些文件</p><p>navicat是一个客户端的可视化工具，代替命令行执行用户和MySQL的交互。</p></li><li><h2 id="RDBMS与非RDBMS"><a href="#RDBMS与非RDBMS" class="headerlink" title="RDBMS与非RDBMS"></a>RDBMS与非RDBMS</h2><p>RDBMS：</p><ul><li><p>DataBase ：以行和列的形式存储数据，形成一张表，多张表构成一个库</p><p>把现实世界的信息抽象成二维数据表示</p></li></ul></li></ol><p>非RDBMS：</p><ul><li><p>非关系型数据库舍弃了关系型数据库中的结构化存储+结构化查询+数据之间的关系；（不需要经过SQL层的解析）获得更高的性能</p></li><li><p>类别：</p><ul><li><p>键值型数据库 redis</p></li><li><p>文档型数据库 MangoDB，通过k-v形式存储XML，JSON格式文档</p></li><li><p>搜索引擎数据库 ElasticSearch 核心原理是：倒排索引</p></li><li><p>列式数据库 HBase 行式是Z，列式是N 可用大量降低系统IO，适合分布式文件系统</p></li><li><p>图数据库 Neo4j</p></li></ul></li></ul><ol start="4"><li><h4 id="MySQL体系结构"><a href="#MySQL体系结构" class="headerlink" title="MySQL体系结构"></a>MySQL体系结构</h4><ul><li><p>连接层：</p><p>对接客户端的连接服务</p><ul><li><p>完成：</p><p>连接处理</p><p>授权认证</p><p>连接池管理</p><p>…</p></li></ul></li><li><p>服务层：</p><p>完成大多数核心服务功能</p><ul><li><p>完成：</p><p>提供SQL接口</p><p>维护缓存</p><p>SQL解析与优化</p><p>内置函数的调用</p><p>完成所有跨存储引擎的操作</p><p>…</p></li></ul></li><li><p>引擎层：</p><p>负责数据的存储和提取</p><ul><li><p>完成：</p><p>对上层提供存储API</p><p>维护索引机制</p><p>可靠存储、回滚、查询、一致性等</p><p>…</p></li></ul></li><li><p>存储层：</p><p>将数据存储在文件系统之上</p><ul><li><p>完成：</p><p>维护数据文件</p><p>维护日志文件</p><p>…</p></li></ul></li></ul></li><li><h3 id="存储引擎"><a href="#存储引擎" class="headerlink" title="存储引擎"></a>存储引擎</h3></li></ol><hr><h2 id="MySQL是怎样运行的"><a href="#MySQL是怎样运行的" class="headerlink" title="MySQL是怎样运行的"></a>MySQL是怎样运行的</h2><ol><li><p>MySQL提供接口的策略：</p><ol><li><p>在linux下成为守护进程（daemon）、在windows下注册为服务（随机自启）</p></li><li><p>提供多层包装的启动模式，连环嵌套：mysqld -&gt; mysqld_safe -&gt; mysql.server</p><p>mysqld是二进制启动程序，后面的都是shell脚本，用来在前者基础上增加功能</p></li></ol></li><li><p>关于网络：</p><ol><li><p>IP地址分类：</p><p>公网IP：指在因特网上直接可达的地址，通过公网IP可以访问别人，也可以被人访问</p><p>分类：</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs markdown">A类：10.0.0.0到127.255.255.255主要分配给大量主机而局域网网络数量较少的大型网络<br><br>B类：128.0.0.0到191.255.255.255一般用于国际性大公司和政府机构<br><br>C类：192.0.0.0到223.255.255.255用于一般小公司校园网研究机构等<br><br>D类：224.0.0.0到239.255.255.255用于特殊用途，又称作广播地址<br><br>E类：240.0.0.0到255.255.255.255暂时保留<br></code></pre></td></tr></table></figure><p>私有地址：</p><p>在上述地址中，预留了一部分不会被公网IP使用的地址，目的是作为私有地址，供家庭、企业、学习等内部组网使用</p><p><code>这些地址只会出现在私网中，通常根据容纳的主机数选择使用哪一段</code></p><p><img src="https://pic1.zhimg.com/80/v2-7f97152e4fa713e0f636320117697ac8_720w.webp" alt="img"></p><p>正常家庭用C类</p><p>学习 &#x2F; 大型企业可能用B &#x2F; A类，比如手机在大型商场上网时获取的是10开头的内网地址</p><h5 id="私网设备连接公网——NAT地址转换"><a href="#私网设备连接公网——NAT地址转换" class="headerlink" title="私网设备连接公网——NAT地址转换"></a>私网设备连接公网——NAT地址转换</h5><blockquote><p>思路：端口映射——将内网众多IP+端口的设备挤到外网一个IP+65535个端口</p></blockquote></li></ol></li><li><p>MySQL服务器过程简述：</p><ol><li><p>关于查询缓存：</p><blockquote><p>mysql 5.7开始不推荐使用缓存，MySQL 8删除缓存</p></blockquote><h4 id="why-缓存？"><a href="#why-缓存？" class="headerlink" title="why 缓存？"></a>why 缓存？</h4><p><code>缓存中维护者MySQL服务器刚刚执行完的结果，维护一个缓存可以避免重复的查询，并且如果缓存是线程共享的，且服务确实存在热点SQL，那么添加缓存直接避免后续的 语法解析、查询优化、存储引擎...</code></p><h4 id="why-not-缓存？"><a href="#why-not-缓存？" class="headerlink" title="why not 缓存？"></a>why not 缓存？</h4><blockquote><p>使用场景有点少，意外情况有点多</p></blockquote><ol><li>配合MySQL事务。较难实现</li><li>SQL中包含系统函数时，无法使用缓存</li><li>为了确保缓存命中，需要对具体的SQL语句处理（而不是直接比较字符串是否出现，这样的话当多个空格，where顺序不同…都会导致无法命中）</li></ol></li><li><p>语法解析：</p><blockquote><p>对客户端发来的文本进行解析，包括：词法解析、语法解析、语义分析… 对应到具体的数据结构上</p><p>本质就是编译此SQL</p></blockquote></li><li><p>查询优化：</p><blockquote><p>语法解析后虽然知道要查的内容，但SQL写法多种多样，传入的并非最快的</p><p>需要经过如：外连接转内连接、表达式简化、子查询转连接…</p><p>最终生成 执行计划</p></blockquote></li><li><p>进入存储引擎</p></li></ol></li><li><h4 id="设置的设置："><a href="#设置的设置：" class="headerlink" title="设置的设置："></a>设置的设置：</h4><ol><li><p>命令行启动时 作为参数设置</p></li><li><p>配置文件设置</p><p>配置文件有默认存在的位置、默认名称、多个文件直接默认优先级…</p><p>配置文件形式：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> my.cnf<br>[client]<br>default-character-set=utf8<br>[mysql]<br>default-character-set=utf8<br>[mysqld]<br>init_connect=<span class="hljs-string">&#x27;SET collation_connection = utf8_unicode_ci&#x27;</span><br>init_connect=<span class="hljs-string">&#x27;SET NAMES utf8&#x27;</span><br>character-set-server=utf8<br>collation-server=utf8_unicode_ci<br>skip-character-set-client-handshake<br>skip-name-resolve<br></code></pre></td></tr></table></figure></li></ol></li><li><p>系统变量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">show variables like <span class="hljs-string">&#x27;default&#x27;</span>;<br></code></pre></td></tr></table></figure><p>系统变量分GLOBAL（全局范围）和SESSION（会话范围）</p><p>系统启动时，将一些GLOBAL的变量设置默认值，新的会话中如果需要更改同样的变量，设置属于自身的SESSION级别的变量</p></li><li><p>编码解码：</p><p>MySQL通过网络连接客户端与服务器，网络就意味着编码，最终都是字节数组</p><ol><li><p>确保客户端与服务器的编码解码字符集相同【一般在客户端的请求中携带了编码方式，由此动态设置SESSION级别的属性】</p></li><li><p>服务器端解码的字符集和用于处理的字符集可能不一致：</p><p>character_set_client决定服务器如何解析客户端的字节数组，正确解析后但可能还无法满足需求——有时想要用另一个字符集进行比较</p><p><code>MySQL很关注字符集之间的比较：比大小、是否相同...SHOW COLLATION</code></p><p>例如：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">select</span> <span class="hljs-string">&#x27;a&#x27;</span> <span class="hljs-operator">=</span><span class="hljs-operator">=</span> <span class="hljs-string">&#x27;A&#x27;</span>;<br></code></pre></td></tr></table></figure><p>语句被服务区正确解析，但输出是true or false取决于用于比较的字符集</p><p>例如：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">select</span> <span class="hljs-keyword">user</span> <span class="hljs-keyword">where</span> name <span class="hljs-operator">=</span> &quot;吴松林&quot;;<br></code></pre></td></tr></table></figure><p>加入原本user表默认使用A字符集，name列中存着的0101就通过A字符将吴松林编码成0101，而此时整句话来自客户端，如果此时整句话不是由A字符集编码的，就无法得到对应的0101，导致匹配失败。</p></li></ol></li></ol><blockquote><p>即：MySQL处理编码解码问题：将整个逻辑分层：下层解决通信中的编码，上层解决业务中的编码（保证语句的字符集和原本表的默认字符集一致，且使用合适的collation），上下层通过字符集转化来切换</p></blockquote><p>另外，服务器按什么字符集返回也需要设置</p><p>三个SESSION级别的变量</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">character_set_client<br>character_set_connection<br>character_set_results<br></code></pre></td></tr></table></figure><ol start="3"><li><p>MySQL字符集设置的偏好：</p><p>优先和操作系统一致——也就是和本地文件系统、命令行工具、其他IO设备协调一致</p><p>但存在特殊场景：当操作系统字符集太低级【ascii】，而MySQL最低是Latin1，要么就是utf8，则此时必须按照MySQL；或者显示的设置了default-character-set</p></li><li><h3 id="InnoDB"><a href="#InnoDB" class="headerlink" title="InnoDB"></a>InnoDB</h3><ol><li><p>InnoDB页：</p><p>完成的功能是：实现对磁盘上数据的读写 –&gt; 一次读写的粒度决定一批操作的效率：InnoDB决定一页设置成16KB，将数据划分为若干页。</p></li><li><p>行格式</p><blockquote><p>真实存储空间如何表示抽象行数据的格式</p></blockquote><ol><li>COMPACT行格式</li></ol><p>内容：</p><ul><li><p>记录的额外信息</p></li><li><p>变长字段长度列表</p><p><code>为了让真实存储的信息可被区分（且可以快速的随机访问），头部记录了类似信息坐标偏移量的信息；针对varchar这种变长属性，需要明确其具体长度</code></p><p>varchar(M)中，M最大值为255，表示最多可以存储M个字符</p><ul><li>理想情况是：存储的字段都不长，最长只有200个字符，且使用ascii编码，那么表示此字段长度只需要0-255 即1字节</li><li>但如果字段比较长，或者使用类似utf-8mb4编码，长度突破255十分常见</li></ul><p><code>方案：提前计算--能用一字节就别用两字节</code></p><p><code>策略：短路计算--能尽早结束就早结束</code></p><p>当字段的最大字符数M × 编码字符集的最大字节数 N &lt; 255 – 一字节</p><p>当 &gt; 255，计算实际的字段占用字节数L，当L大于127时，使用两字节</p><blockquote><p>Question：如何在一堆字节中区分一个字节属于单字节字符 or 双字节字符？</p><blockquote><p>限制：</p><p>当属于单字节字符时，第一个bit是0，双的话第一个bit是1</p><ul><li><p>第一位是0，天生适合ascii，因为本来字符就不多</p></li><li><p>第一位是1，只是对这种字符集对应方案进行了很弱的限制</p><p>【字符集只需要关注如何将字符和某个数对应，并不要求数连续】</p><p>GBK的解决方案是：根据第一个字节将字符分区，再根据第二个字节决定是分区中的哪一个</p></li></ul></blockquote></blockquote></li><li><p>NULL值列表</p><p><code>用于记录一条记录中哪些是null值，由于直接将null标记在真实数据中很占空间，于是将这些null提取出来，统一用0/1表示是否为null，通过优化策略，节约空间</code></p></li><li><p>记录头信息</p><p><code>由固定5bytes组成，用于描述记录的一些属性</code></p><p>例如：</p><ul><li>deleted_flag：标记该记录是否被删除</li><li>min_rec_flag：表示是否是B+树中非叶子节点中最小目录项记录</li><li>n_owned:</li><li>heap_no:当前记录在堆中的相对位置</li><li>next_record:逻辑上下一条记录距离当前的距离</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java">MySQL索引页的管理方式：<br>- 物理存储是连续的，但通过头部信息构建逻辑存储<br>- 通过链表链接每个记录，并且是按照主键ID顺次链接<br>- 头节点和尾节点特殊处理，作为两条特殊的记录<br>- 删除一条记录只是逻辑上标识被删，并由链表绕开此记录，但物理上仍然存在<br>    并且删除后再添加记录时有可能复用此空间 -- 通过一个垃圾链表<br>    当主键就是原本的主键时甚至会回到原本链表的位置<br></code></pre></td></tr></table></figure></li><li><p>记录的真实信息</p></li></ul><blockquote><p>各个属性的值</p></blockquote><p>除了真实值外，<strong>MySQL还会添加额外的真实数据</strong></p><ul><li><p>row_id：</p><p>当整张表没有确认主键、也没用not null 且 UNIQUE的属性被委派为主键，MySQL主动添加一个隐藏列作为主键</p></li><li><p>trx_id:</p><p>事务ID</p></li><li><p>roll_pointer:</p><p>回滚指针</p></li></ul></li></ol></li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs sql">真实数据的记录就是：将每个不为<span class="hljs-keyword">null</span>的属性依次排开<br><br><span class="hljs-operator">-</span> 注意：<br><br>  当编码字符集使用变长的，在 变长字段长度列表 中需要记录定长字段的真实字节数【感觉就是将这个<span class="hljs-type">char</span>退化成<span class="hljs-type">varchar</span>，由于此<span class="hljs-type">char</span>的不确定性也很高】<br><br>  当编码字符集使用定长的，几乎没有不确定性<br><br><br>...<br></code></pre></td></tr></table></figure><ol start="8"><li><p>InnoDB索引页</p><blockquote><p>页是InnoDB管理存储空间的基本单位，InnoDB为了不同目的设计了不同类型的页：</p><p>例如：存放表空间头部信息的页，存放Change Buffer信息的页，存放INODE信息的页，存放undo日志信息的页……以及存放记录的页 – 索引页</p></blockquote><p>一个索引页分为七个部分：</p><ul><li><p>File Header：存储页的通用信息；Page Header：存储页的专有信息</p><blockquote><p>File Header：页号、上一页的地址、下一页的地址、页的类型</p><p>Page Header：槽的数量、free space大小、页中垃圾链表的头节点、当前页在B+树中的层</p></blockquote></li><li><p>User Records：存储真实记录，大小不固定</p></li><li><p>Free Space：尚未使用的部分</p></li><li><p>Page Directory：本质就是稀疏索引（槽），将几个连续的数据分成一组，通过先确定组（二分查找），再确定数据的方式加快查找速度</p><blockquote><p>槽 - 好处是查找的成本降低了，基本上从O(N)降低到O(logN)</p><p>但维护槽同样具有成本，增删记录时要保证每个槽的数据量大致相同，就要求更新槽的信息；</p></blockquote></li></ul></li><li><p>InnoDB的B+树索引</p><blockquote><p>B+树：</p><p>从优化的角度看B+树的发明：</p><ol><li><p>为了在不同索引页中找到目标记录，需要给各个索引页添加目录，由目录构成一级索引</p></li><li><p>这些索引的管理收到数据项crud的影响也不简单，决定复用对数据项的管理方式，即：类似COMPACT行格式，类似槽的页面</p></li><li><p>通过索引可以查到数据项所在页面，但索引本身越来越长，一旦图片16KB，最开始的二分查找就不行了 – 再建第二层索引</p></li></ol></blockquote><p>索引分为两类：</p><ol><li><p>聚簇索引</p><p>特点：</p><ol><li><p>叶子节点存储的是完整的用户记录（包括隐藏列）</p></li><li><p>使用记录的主键值大小进行排序</p></li></ol></li></ol><blockquote><ol><li><p>在叶子节点和内节点中都按照主键排成单向链表</p></li><li><p>叶子节点根据主键值排成双线链表</p></li><li><p>同一层的内节点中，目录项也是根据主键排序的双向链表</p></li></ol></blockquote></li><li><p>二级索引</p><ol><li><p>叶子节点存储的是索引列和主键的匹配信息</p></li><li><p>使用相应的索引列进程排序</p></li></ol></li><li><p>联合索引</p><p>当使用C2，C3两列建立联合索引时，目录页中每项记录的是C2，C3，子页的信息，按照先C2，后C3的顺序顺次排列；叶子节点记录的是C2，C3，主键的信息，按照先C2，后C3的顺序顺次排列；</p></li></ol><blockquote><h4 id="一些结论"><a href="#一些结论" class="headerlink" title="一些结论"></a>一些结论</h4><ol><li><p>InnoDB存储引擎会自动为主键建立聚簇索引（如果没有显式知道主键，并且没有声明不允许为null的UNIQUE键，则会自动添加主键）</p></li><li><p>索引的代价：</p><ol><li><p>空间的代价：</p><p>一个页面是16KB，一颗B+树由多少页组成…</p></li><li><p>时间的代价：</p><ol><li>维护的成本：</li></ol><p>每当对一张表crud时，很容易破坏原本数据项的顺序，而B+树对顺序要求十分苛刻，每一层都是有序的双向链表，所以可能导致一次增删改操作引发页面分裂，页面回收…</p><ol start="2"><li>执行查询语句前会计算使用各个索引的成本，如果索引太多会有很多无用功</li></ol></li></ol></li></ol></blockquote><h4 id="如何确定选择哪个索引"><a href="#如何确定选择哪个索引" class="headerlink" title="如何确定选择哪个索引"></a>如何确定选择哪个索引</h4><blockquote><p>由where限制条件确定是否缩小了<strong>扫描区间</strong>的范围</p><p><code>原本我对限制条件的认识是：条件越多找起来越麻烦，但很多场景下未必；有些限制条件和索引契合度高，可以把扫描区间限制的很小（尤其在联合索引中）；有的限制条件和索引契合度巨低，甚至用or的限制条件可能会破坏好索引带来的简便</code></p><p>根据可能使用的索引，分别判断扫描区间的大小，即可判断该不该用</p></blockquote><p>另外索引也可以用于排序</p><blockquote><p>当有10000条数据，要按照某列排序后返回前十个</p><p>没有索引的场景下需要全部加载到内存里排序</p><p>但有索引时就很简单</p></blockquote><p>另外索引也可以用于分组</p><blockquote><p>当有10000条数据，要按照某列分组后统计</p><p>没有索引的场景下，在扫描过程中不清楚还有没有同组的，需要建临时表来存储统计信息</p><p>但有索引就天然分组</p></blockquote><h4 id="走索引的问题"><a href="#走索引的问题" class="headerlink" title="走索引的问题"></a>走索引的问题</h4><blockquote><p>回表可能不如全表扫描</p><p>假如按照索引列得到很多不连续的主键值，需要一个一个回标查询全部信息，但是对聚簇索引的访问太过随机，就不如连续的访问（因为页都是存储在磁盘的，需要时才IO进内存，太随机容易有颠簸）</p><p>到底是全表还是回表，由查询优化器实现</p><p>但是可以通过限制条件限制回表的代价：limit – 别回表太多次</p></blockquote><h4 id="如何更好的使用索引"><a href="#如何更好的使用索引" class="headerlink" title="如何更好的使用索引"></a>如何更好的使用索引</h4><blockquote><ol><li><p>只为用于搜索、排序、分组的列创建索引 – 减少使用成本</p></li><li><p>考虑索引列中，不重复值的个数：</p><p>当重复值太多，即使是where condition &#x3D; value的条件也会有很多回表查询，回表太多就不如全表扫描</p></li><li><p>索引列的类型尽量小（指能用tinyInt就别用Int）</p><p>因为类型越小，16KB能存下就越多，树的页越少，高度越低</p><blockquote><p>所以主键也能小就小</p></blockquote></li><li><p>覆盖索引</p><p>为了避免回表的成本，可以在查询列表中只包含索引列和主键</p></li><li><p>避免在搜索条件中出现索引列的计算式</p><p>mySQL直接不认识</p></li><li><p>新增记录尽量主键自增，而不是中间插入主键</p><p>因为自增的主键即使分页也只需要把新值插入新页，但中间的主键可能会导致分页+半数数据的复制</p></li></ol></blockquote><ol start="10"><li><p>mysql数据目录</p></li><li><p>InnoDB表空间</p><blockquote><ul><li><p>症结1：</p><p>当一张表的数据量太大，常规的B+树会有巨多的叶子节点和非叶子节点，但无法保证这些逻辑上相关的节点在物理上存储的接近</p><p>因为在磁头移动时，越接近的数据带来的成本越低</p><p>所以尝试给一张表更大的空间（区），相关的空间都先从一个区中申请</p></li><li><p>症结2：</p><p>区的粒度有点大，会产生不少碎片</p><p>所以有碎片区的概念，进而区分出：空闲区、有剩余空间的碎片区、没有剩余空间的碎片区….</p><p>为了管理这些区，出现了经典操作：使用SDES Entry记录每个区的元数据，用各种链表串联各个Entry（很想PCB）</p><ul><li><p>Free链表，在需要申请空闲区时访问</p></li><li><p>Free_Frag链表…</p></li></ul></li></ul></blockquote></li><li><p>单表访问方法</p><blockquote><p>几个量级：</p><ol><li><p>const：指通过主键值在聚簇索引中直接定位，或者通过（唯一）二级索引找到一条后进行回表</p></li><li><p>ref：通过二级索引单点扫描找到挺多条记录，每条都要回表</p></li><li><p>range：在二级索引中就有较大的扫描区间，区间内每条记录分别回表</p></li><li><p>index：遍历二级索引</p></li><li><p>all：全表扫描</p></li></ol><h4 id="索引合并："><a href="#索引合并：" class="headerlink" title="索引合并："></a>索引合并：</h4><p>一般情况下，InnoDB只会为单个索引生成扫描区间，但是某些场景下如果两个索引都能生成区间，并且区间的交集能减少扫描成本，则会有 索引合并</p></blockquote></li><li><p>表连接的原理</p><blockquote><p>连接的本质：将各个表中的记录，一次进行匹配，匹配后的组合返回客户端；如果没有任何过滤条件，则结果就是笛卡尔积</p><p>分为<strong>内连接</strong>和<strong>外连接</strong></p><p>区别在于：外连接会把不符合on自居的记录保留，加入到结果集中</p><p>连接的原理:</p><blockquote><p>嵌套循环连接算法</p></blockquote><p>对于内连接（谁是驱动表无所谓）</p><ol><li><p>选择驱动表，先执行对驱动表的单表查询</p></li><li><p>对步骤1中的查询结果集中的每条记录，都分别到驱动表中查找匹配的记录</p></li></ol></blockquote></li><li><p>基于成本的优化</p><blockquote><p>MySQL在执行一个查询时，可能有不同的执行方案，如何选择成本最低的？</p><p>成本分为IO成本和CPU成本，规定读取页面的成本为1，检测一条记录是否符合搜索条件的成本为0.2</p><p>计算成本步骤：</p><ol><li><p>根据搜索条件，找出所有可能使用的索引</p></li><li><p>计算全表扫描的代价</p></li><li><p>计算使用不同所以执行查询的代价</p></li><li><p>对比各种执行方案，找出最小值</p></li></ol></blockquote></li><li><p>InnoDB统计数据如何收集</p></li><li><p>基于规则的优化</p><blockquote><p>MySQL的设计人员对于任何糟糕的SQL输入，都会将其转化成高级的SQL</p><p>重写操作有：</p><ol><li><p>条件化简：</p><p>表达式的计算</p><p>移除不必要的括号、没用的条件…</p><p>常量替换</p></li><li><p>外连接消除</p><blockquote><p>尝试吧外连接转化成内连接，内连接则可以优化连接顺序降低成本</p></blockquote></li><li><p>子查询优化</p></li><li><p>in的优化</p><ul><li><p>优先转化为半连接</p></li><li><p>无法转化，则尝试用物化表方式</p></li></ul></li></ol></blockquote></li><li><p>explain</p><blockquote><p>Explain用来查看执行计划</p><p>各个列：</p><ol><li><p>table：Explain的每条记录对应一个单表访问，table标识访问哪张表</p></li><li><p>id：查询语句中有几个Select，每个Select会被分配一个唯一ID，尤其在子查询，union中（但子查询有时候会转化成连接查询，导致id唯一）</p><p>但并不是查几张表就有几个ID，比如连接查询就是两个Select，一个id</p></li><li><p>select_type：每个小的select在整个大查询中可能扮演不同的角色</p></li><li><p>type：单表访问方法</p><p>除了const，ref，range，index，all，还有其他方法</p><p>例如：system，fulltext…</p></li><li><p>possible_key和key</p><p>可能用到的索引和真实使用的索引</p></li><li><p>key_len…</p></li></ol><p>explain format&#x3D;json select…用来通过JSON查看执行计划</p></blockquote></li><li><p><strong>optimizer trace</strong></p><blockquote><p>用来查看优化器生成执行计划的整个过程，用来获悉为什么不用别的查询方案</p><p>开启后，在某个数据库中生成相应文件</p></blockquote></li><li><p>InnoDB的Buffer Pool</p><blockquote><p>作用：缓存 - 调节磁盘和CPU的矛盾</p><p>Buffer Pool</p><ul><li><p>为了缓存索引页，MySQL向操作系统申请的一篇连续的内存</p></li><li><p>内部划分为若干个控制块和缓冲页，缓冲页默认和索引页大小一致16KB</p></li><li><p>每个缓冲页对应一个控制块（所属表空间、页号、缓冲页在buffer pool的地址、链表节点信息…）</p></li><li><p>链表们</p><ul><li><p>维护Free链表，用来感知哪些缓冲页可用</p><p>Free链表连接BCB（缓冲控制块）</p><p>想要维护一个Free链表，只需要在上层固定一个基节点（头节点）</p></li><li><p>维护Flush链表，用来感知哪些页是脏页</p></li><li><p>维护LRU链表</p><h4 id="LRU（Least-Recently-Used-最近最少使用）"><a href="#LRU（Least-Recently-Used-最近最少使用）" class="headerlink" title="LRU（Least Recently Used 最近最少使用）"></a>LRU（Least Recently Used 最近最少使用）</h4><blockquote><p>思想：当Buffer Pool空间不足时，尝试将最近最不常使用的页淘汰</p><p>策略：将每个加载进Buffer Pool的页面挂载到LRU链上，新加载到的挂载到头节点后，通过缓存命中一次的更新到头节点上；如此在链表最后的页面就是最近使用最少的</p><p>问题：</p><ul><li>容易被顶（某些不常用的页被大量加载到Buffer Pool，导致原本正常在缓冲区的页面被淘汰）</li></ul><ol><li>在预读场景下更加明显</li></ol><p>    预读：mysql出于局部性原理做的优化   </p><blockquote><p>当对某个区中页面访问多次时，会将整个区的页面全都加载进Buffer Pool，其中又分连续读取56个和随机读取13个</p><p><strong>预读加载页进Buffer Pool时异步的</strong></p></blockquote><ol start="2"><li>在全表扫描场景下也很明显</li></ol><p>    假如是一个很大的表，不得不全表扫描，这就相当于将整个BufferPool换一次血，扫描完后还要重新加载上次没搞完的任务</p><p>解决方案：<strong>冷热分离 &#x2F; 老年代|新生代</strong></p><blockquote><p>MySQL默认老年代占37%</p><blockquote><p>Java老年代与新生代的比例默认2：1，而MySQL恰好反过来，主要原因是：</p><ul><li><p>JVM需要避免Full GC，才将老年代容量加大，并且还限制了对象进入老年代，而MySQL并没有GC的限制，只需要单纯从缓存的角度考虑分区大小即可——防止老年代的数据被顶出去，所以选择加大新生代</p></li><li><p>另外，感觉Java中某些对象生命周期巨长，无论是架构产生的对象还是业务产生的对象，所以老年代需要更大的空间存放它们，但是MySQL在查询时并没有绝对热点的页，所以老年代太大也没必要</p></li></ul></blockquote></blockquote><p>策略：</p><ol><li><p>从磁盘加载进来的页面优先进入新生代，第一次访问时还在新生代，第二次访问时尝试加载到老年代</p><blockquote><p>似乎有点刻意，这样做难道不会导致所有页面都简单过一遍新生代立马到老年代吗？</p><ol><li><p>后续不访问的就只会在老年代消逝（预读）</p></li><li><p>很难确定一个时间点将新生代的升级到老年代</p><p>因为MySQL页和对象有所不同，一页有多条记录，访问一次页面可能是访问了它其中一条，也可能是一百条，于是很难确定是否是热点页面</p></li><li><p>MySQL根据访问时间间距判断是否为热点（排除全表扫描的情况）：在第一次访问处于新生代的页面时标记时间戳，第二次访问时当时间间距较短，表示可能时全表扫描，不升级</p></li></ol></blockquote></li><li><p>对于LRU链表的更新似乎太过频繁 尤其对于经常访问的老年代，每次访问一个页面都要移动到头部，维护成本比较高</p><ul><li><p>提出划分热点数据与更热的数据</p><p>将老年代前四分之一定义为更热，访问此区域不会移动链表…</p></li></ul></li></ol></blockquote></li></ul></li><li><p>刷新脏页到磁盘：</p><p>两种策略：</p><ol><li><p>沿着flush链表刷新</p><blockquote><p>遇到一个刷一个，但可能刷到热点页面，刷进去立马加载回来</p><blockquote><p>感觉这个才是main</p></blockquote></blockquote></li><li><p>沿着LRU新生代刷新</p><blockquote><p>遇到一个先判断是否是更新过的，是则刷进去</p></blockquote></li></ol></li><li><p><strong>Buffer Pool的集群</strong></p><blockquote><p>当Buffer Pool特别大的时候，可能承受不住太高的并发，会将其分成多个小Buffer Pool，彼此独立，类似集群</p><p>而如何分配这些小Pool的大小，为了能够动态分配，MySQL提出了比一整个Pool更小的概念 —— chunk（小号Pool，里面也是控制块和缓冲页）</p><ul><li>之后便以chunk为单位进行增加或删除内存空间</li></ul></blockquote></li></ul><hr><ul><li><p>怎么知道哪个页在Buffer Pool？哈希值+哈希表</p><p>页的哈希值如何刻画？表空间+页号</p></li></ul></blockquote></li><li><p>事务：</p><ol><li>ACID：</li></ol></li></ol><ul><li><p>原子性：</p><blockquote><p>如果在执行操作的过程中发生错误，能够把已经执行的操作恢复到执行前</p><p>错误来源多种多样：主要原因是：一条指令在数据库层面，操作系统层面都是多条指令，任何一个位置都未必保证原子性，出问题都要整个事务一起承担</p></blockquote></li><li><p>隔离性：</p><blockquote><p>任何两个事务操作应该互不影响</p><p>典型场景：事务B读取到事务A未提交的脏数据（多线程竟态问题）</p></blockquote></li><li><p>一致性：</p><blockquote><p>MySQL内部数据需要符合一定规约，比如not null，自增这些强行符合的；同时也有些数据库无法保证的一致性，例如age &gt; 0，转账后收支平衡…这些理论上可以由MySQL的check实现一部分，或者由触发器实现，但更多场景下交由开发者在上层完成校验</p></blockquote></li><li><p>持久性：</p><blockquote><p>数据库承诺的操作一定要落盘</p></blockquote></li></ul><ol start="21"><li><p>redo</p><blockquote><p>redo log重做日志</p><ul><li><p>为了保证事务的持久性，应该在任何一个事务提交后就将其落盘，但是一个事务有多条指令，每条指令可能更改了许多不同的页，不同的页还随机分布在磁盘各个区域，一次刷盘IO带来的成本有点高</p></li><li><p>让步：落盘并非全落，而是只将必要信息持久化，假如MySQL宕机导致事务commit却未落盘，redo必要信息即可复原</p></li><li><p>redo log顺序写入，并且逻辑接近普通指令 - 数据量不大</p></li></ul><p><code>redo 具体粒度如何？ 到底写了什么？</code></p><ol><li><p>假如记录的粒度太小：即每条redo log记录某个页面某个偏移量具体改几个字节，那么一个insert语句会对应巨多log，不光把真实数据写入聚簇索引，还要更新所有二级索引、更新槽信息、更新上下记录的链、更新page header，更新统计信息…</p></li><li><p>假如记录的粒度太大：<strong>直接把insert的SQL记录到redo中</strong>：为什么不可以？</p><blockquote><p>reod记录的是计算结果，直接记录insert会导致重复计算</p></blockquote></li></ol><blockquote><p>总体而言：一条redo的粒度要比纯粹记录修改某个位置要大，也比将整个操作记录成redo要小</p><p>设计者通过抽象出各种操作类型，将几个物理位置的修改聚合到一个抽象的方法中，这个方法的粒度局限在更新一个页的内容（当insert时，包括插入数据，更新链表上下节点、更新槽、更新page header…）</p><blockquote><p>相关概念：mini-transaction</p><p>最大的事务概念中包括多条语句，每条语句可能操作多个页面，每个页面操作可能修改多个位置，其中每一步都可能出错。一种思路是：将大问题分解成小问题，大事务分解成小事务</p><p>mysql提出mini-transaction，用来刻画对页面的一次原子性访问</p><ul><li><p>mini-transaction通过整体写入log buffer中来保证原子性执行</p></li><li><p>多个mini-transaction通过在最后声明结束标识来确保原子性</p></li></ul></blockquote><p>所以在log buffer中，同一事务的不同mini-transaction并不连续存储，而是在并发的进程中顺序存储，但同一mini-transaction中的redo log是连续存储的</p></blockquote><p>redo log 刷盘时机：</p><ol><li><p>空间不足时</p></li><li><p>事务提交时：可以不刷buffer Pool，但必须刷log buffer</p></li><li><p>将某个脏页刷新到磁盘前，要保证redo log刷新到磁盘</p></li><li><p>后台线程以一定频率刷盘</p></li><li><p>正常关闭服务器时</p></li><li><p>checkpoint时</p></li></ol><h4 id="redo管理刷盘方式："><a href="#redo管理刷盘方式：" class="headerlink" title="redo管理刷盘方式："></a>redo管理刷盘方式：</h4><ul><li><p>维护了两个指针</p><ol><li><p>lsn（log sequence number）：从MySQL启动开始，每新增一条redo log，lsn自增log大小的数量</p><p>即：lsn指向log buffer最末尾</p></li><li><p>flushed_lsn：每当将一个block刷入磁盘，flushed_lsn自增对应大小</p><p>即：flushed_lsn指向log buffer最开始</p></li></ol></li></ul><h4 id="redo管理空间覆盖的方式："><a href="#redo管理空间覆盖的方式：" class="headerlink" title="redo管理空间覆盖的方式："></a>redo管理空间覆盖的方式：</h4><ul><li><p>维护了一个指针</p><ol><li>checkpoint_lsn：表示在log buffer中，此指针之前的redo log对应的buffer pool中的内容，都被刷新到磁盘</li></ol><p>每当进行checkpoint操作，就会计算系统中可以被覆盖的log日志有哪些（更新checkpoint_lsn）</p><p>怎么计算？看flush链表头信息</p><p><code>flush链表表示还没刷入磁盘的脏页，而flush尾节点就是上次刷盘的下一个页面，通过此节点即可知上次刷盘刷到哪里</code></p></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">整体流程：<br>- 在Buffer Pool中，一般由异步线程从LRU链表和Flush链表上挑出脏页刷入磁盘<br>- 每个事务的每个mini事务都会先写在redo log buffer中，通过lsn记录增长<br>- 当程序执行任务负荷较重，lsn增长太快；此时在buffer pool异步刷盘不过来<br>- 导致redo log buffer早期的日志无法被覆盖，新的redo log无处可写<br>- 此时只能让用户线程来帮忙刷盘<br></code></pre></td></tr></table></figure></blockquote><blockquote><p>崩溃后页面修复小技巧：</p><p>将不同的redo log通过其表空间和页号计算出哈希值，根据哈希值挂载到不同位置，如此一来挂载同一个桶中的redo便于对同一张表进行修改</p></blockquote></li><li><p>undo</p><blockquote><p>某些场景下，事务只执行了一半就由于不可抗力无法执行（可能由于断电、OS异常，也可能是认为回滚），undo日志的作用就是将和之后没能执行属于同一事务的操作回滚，从而实现原子性</p><blockquote><p>Question：事务不是在提交后才刷盘吗？那在未提交过程中就算中断也不会影响磁盘数据，为什么还需要undo？</p><p>Ans：提交后才刷buffer pool，但是未必提交后才刷log buffer；</p><ul><li>假如在还没提交前刷了很多log buffer，但是宕机了，如果没有undo log，重启后加载redo就相当于之前已经将脏页落盘了</li></ul><blockquote><p>所以新的问题是：根据flush链表落盘时，怎么知道一页上的事务已经提交了呢？</p></blockquote></blockquote></blockquote></li><li><p>MVCC</p><blockquote><p>redo，undo解决MySQL的原子性，而一致性的保障策略很多：</p><ol><li><p>not null，唯一索引，自增主键…</p></li><li><p>开发者自定义限制条件实现一致性</p></li><li><p>防止多个事务并发执行时对同一份数据的并发改操作</p><p><code>并发改破坏一致性：是因为一个线程读后被另一个线程改了，原本线程还按照不存在的数据执行操作，导致数据紊乱</code></p><p>同样的问题有三种类型：</p><ol><li><p>脏写（dirty write）</p><blockquote><p>指：一个事务修改了另一个未提交事务修改过的数据</p><p>异常场景</p><ul><li><p>AB两事务先后修改X的值，B提交后A宕机（尝试回滚），但B已提交要保证持久性，但A没有提交要保证原子性…</p></li><li><p>破环一致性的例子：</p><p>如果原本一致性要求X &#x3D;&#x3D; Y，但A改了X，B改了X和Y，A再改Y就引发数据不一致</p></li></ul></blockquote></li><li><p>脏读（dirty read）</p><blockquote><p>指：一个事务读到了另一个未提交事务修改过的数据，而如果这个数据最后没有提交，则读到的就是不存在的数据</p></blockquote></li><li><p>不可重复读（Non-repeatable Read）</p><blockquote><p>指：一个事务修改了另一个未提交事务读取的数据，导致再读发现数据不同</p></blockquote></li><li><p>幻读（Phantom）</p><blockquote><p>指：一个事务先读取符合条件P的记录，后来另一个事务写入了符合P的数据，之后第一个事务再读就出现两次读取不一致</p></blockquote></li></ol></li><li><h4 id="承受一部分问题，换来一部分性能"><a href="#承受一部分问题，换来一部分性能" class="headerlink" title="承受一部分问题，换来一部分性能"></a>承受一部分问题，换来一部分性能</h4><p>前面四种并发问题的危害性有所不同</p><blockquote><p>脏写 &gt; 脏读 &gt; 不可重复读 &gt; 幻读</p></blockquote><p>设立多种隔离级别，就是对上述问题进行妥协，换来性能提升</p><p>【SQL规范规定的四种隔离级别】</p><ol><li><p>读未提交（READ UNCOMMITTED）</p></li><li><p>读已提交（READ COMMITTED）</p></li><li><p>可重复读（REPEATABLE READ）</p></li><li><p>可串行化（SERIALIZABLE）</p></li></ol></li></ol><blockquote><p>其中，脏写是最严重的问题，所有隔离级别都舍弃</p></blockquote><p>【MySQL支持的四种隔离级别】</p><blockquote><p>MySQL在 可重复读 级别下，很大程度上禁止了幻读的发生</p><p>默认级别就是 可重复读</p></blockquote><hr><h4 id="MVCC"><a href="#MVCC" class="headerlink" title="MVCC"></a>MVCC</h4><blockquote><p>undo 日志不仅在事务回滚中应用，在MVCC中也由undo记录历史版本</p><blockquote><p>undo日志中有两条undo 日志链，insert undo 和 update undo；</p><p>insert undo 指不需要为MVCC服务的undo日志，即其他事务对此操作不需要可见</p><p>update undo 指需要为MVCC服务的undo日志，即其他并发事务由于要访问历史版本，而update undo完全保存了（一般由update，delete操作触发）</p><blockquote><h4 id="为什么需要delete-mark？"><a href="#为什么需要delete-mark？" class="headerlink" title="为什么需要delete mark？"></a>为什么需要delete mark？</h4><p>delete操作，修改主键的update操作，原本都需要将旧记录完全删除掉，但是MVCC需要保证其他并发执行的事务依然能够读到之前的旧记录——能够找到undo版本链，只有通过B+树上旧记录作为入口，否则完全删除的话即使有undo，也找不到</p></blockquote></blockquote><hr><blockquote><p>每对记录进行一次改动，都会记录一条undo log；</p><p>undo log 中，同一记录的多个版本通过 roll_pointer属性串联成链表，在最新版本（聚簇索引）中保存的roll_pointer指向对应undo中的旧版本</p><p>—— 版本链</p><p>每个版本中包括将其修改的事务id，通过保存旧版本即对应事务id来控制并发事务访问相同记录的行为，就是多版本并发控制</p><p>—— MVCC</p></blockquote><hr><p>对于满足 读已提交 的隔离级别，需要保证事务能够识别哪个undo版本是已经提交的；例如：如果某个事务修改了数据但没做提交，另一事务不应该读取到最新版本</p><blockquote><p>MySQL通过对比访问事务自身的事务id和访问目标版本的事务id，以及当前活跃的事务id，来判断是否可见</p><ul><li><p>ReadView</p><blockquote><p>由某个事务生成，用来形容当下事务活跃情况；让创建者明白某个事务id是否已经提交，主要包括以下内容</p><ol><li><p>m_ids：当前系统活跃的读写事务的事务id列表</p></li><li><p>min_trx_id：当前系统活跃的事务id的最小值</p></li><li><p>max_trx_id：当前系统应该分配给下一个事务的事务id</p></li><li><p>creator_trx_id：生成改readView的事务id</p></li></ol></blockquote></li></ul><p>具体流程：</p><blockquote><p>假设被访问的版本的事务id是A</p></blockquote><ol><li><p>如果A &#x3D;&#x3D; 自身id，则标明是自己修改的记录，可见</p></li><li><p>如果A &lt; min_id，则标明A事务已经被提交了，可见</p></li><li><p>如果A &gt; max_id，则标明A事务是在生成read view之后才开启并添加undo，不可见</p></li><li><p>如果A在min和max之间，只能说明曾经执行过这个事务</p><ol><li><p>如果A在活跃事务id列表中，则表示仍未提交，不可见</p></li><li><p>如果A不在，则说明已经提交了，可见</p></li></ol></li></ol><p>注意的点：</p><ol><li><p>只有事务尝试修改内容时（insert，update，delete），才会获取到事务id，否则一直trx_id &#x3D; 0;</p></li><li><p>readView上记录的只是各种事务标记，通过事务标记 匹配 版本链上版本</p></li></ol></blockquote><h4 id="read-commited-repeated-read"><a href="#read-commited-repeated-read" class="headerlink" title="read commited &amp; repeated read"></a>read commited &amp; repeated read</h4><ul><li><p>read committed：</p><blockquote><p>在每次查询时，创建当时的readView</p></blockquote></li><li><p>repeated read：</p><blockquote><p>一个事务多次查询，使用第一次申请的read View</p></blockquote></li></ul></blockquote></blockquote></li><li><p>锁</p><blockquote><p>并发访问情况三种：</p><ol><li><p>读读</p></li><li><p>读写</p></li><li><p>写写</p></li></ol><hr><ul><li><p>写写锁：</p><blockquote><p>所有隔离级别都必须满足，<strong>实现方式就是必须串行</strong></p></blockquote><p>事务在尝试对记录进行修改时，会首先查看记录是否关联一把锁，如果事务关联，表明已被加锁，如果没，则自身尝试加锁</p><ul><li><p>自身加锁：在内存中创建一个锁结构，关键属性有trx_id，is_waiting表示这把锁是否被阻塞</p><p>创建好后和记录关联，有访问权的事务is_waiting&#x3D;false</p></li><li><p>发现有锁：自身再在内存中创建一个锁结构，iswaiting &#x3D; true，也和记录进行关联</p></li></ul></li></ul><p>当第一个事务提交后释放锁，寻找到下一个阻塞的事务，更改iswaiting信息</p><ul><li><p>读写锁：</p><blockquote><p>MySQL控制读写合理进行的方式有：</p><ol><li><p>MVCC</p></li><li><p>加读写锁（读的事务将其他读&#x2F;写的事务全都串行）</p></li></ol><p>使用MVCC可以实现读操作和写操作并发执行互不影响，因为读是针对旧版本的数据（在undo中），写是针对新版本的数据（在B+树中）</p><p>使用读写锁性能较差，但某些场景下是必须的</p><blockquote><p>例如：银行取钱时，读后不允许其他事务写…</p></blockquote><hr><p>读写锁：</p><blockquote><p>读写锁怎么能够实现防止读写、写写、却不妨读读呢？</p></blockquote><p>设计人员设计了两把相关锁：共享锁 &#x2F; 独占锁</p><ul><li><p>共享锁：share锁 S锁</p></li><li><p>独占锁：Exclusive锁 X锁</p></li></ul><p>两个事务都想获取一条记录的S锁，方行；一个事务拥有X锁，另一个事务尝试获取S锁 &#x2F; X锁，阻塞；</p></blockquote></li><li><p>以上读写锁都是行级锁</p><blockquote><p>行级锁粒度小更加灵活，但是维护成本高；表级锁粒度大，简单直接，维护成本也小</p></blockquote><p>以下读写锁都是表级锁</p><blockquote><p>表级锁也区分S锁和X锁，运行逻辑和行级锁类似，不过要兼容行级锁</p><p>兼容：</p><p>当表加了表级S锁，所有行级和表级的X锁都要阻塞；当表加了表记X锁，所有…</p></blockquote><p>问题：</p><blockquote><p>表级锁在加锁前需要判断表上是否有锁，加S和加X都要判断</p><p>但是不光要判断整个表级别有无表级锁，还要判断每行有没有行级锁</p><p>为了不遍历，提出 <strong>意向锁</strong></p></blockquote><p>意向锁：</p><ol><li><p>意向共享锁 IS锁：加在表级别，表示事务想要读</p></li><li><p>意向独占锁 IX锁：加在表级别，表示事务想要写</p></li></ol></li></ul><blockquote><p>一个事务想要读取某条记录时，先尝试在表级别上加IS锁，但如果已经有别的事务加了表级X锁，则阻塞；如果没有，加IS锁表明自身意向后，再对具体行加行级读锁</p></blockquote><blockquote><p>所以当有事务尝试加表级锁X时，需要等待所有的IS锁，IX锁都解开；</p><p>尝试加表级锁S时，需要等待所有IX锁都解开</p></blockquote><h4 id="多种行级锁："><a href="#多种行级锁：" class="headerlink" title="多种行级锁："></a>多种行级锁：</h4><ol><li><p>Record Lock（正经记录锁）</p><p>以上谈论的都是Record Lock，用于在行记录上加锁，分S锁和X锁，用于局部互斥访问</p></li><li><p>Gap Lock（间隙锁）</p><blockquote><p>间隙锁是为了实现在Repeatable Read级别下解决幻读问题</p><p>实际解决方案有两种：MVCC和Gap Lock</p></blockquote><p>幻读场景下，由于没法给不存在的记录加锁，Gap Lock选择给某条记录的周围加锁（实际上是给此记录之前到上一条记录的中间部分不允许插入）</p><blockquote><p>被锁阻塞的事务会加一个人插入意向锁（Insert interntion Lock）挂载到记录上，当gap锁解除时触发插入意向锁相关的事务</p></blockquote></li></ol><hr><p>一行对应一个行级锁，但一万行可能只对应几个行级锁</p><blockquote><p>MySQL为了避免由于访问行数太多，导致维护过多行级锁，提出将有关系的行共同使用同一把行级锁</p></blockquote><p>关系：</p><ol><li><p>操作是在同一个事务中</p></li><li><p>记录是在同一个页面中</p></li><li><p>加锁的类型相同</p></li><li><p>等待状态一致</p></li></ol></blockquote></li></ol><hr><h4 id="SQL注入"><a href="#SQL注入" class="headerlink" title="SQL注入"></a>SQL注入</h4><ul><li><p>手法1：</p><blockquote><p>通过联合查询，使得回显的数据中显示自定义信息 &#x2F; 其他信息</p></blockquote></li><li><p>手法2：</p><blockquote><p>通过order 5 &#x2F; 4 &#x2F; 3 &#x2F; 2 是否正常执行，判断一共有几列</p></blockquote></li><li><p>手法3：</p><blockquote><p>通过联合查询，尝试查到数据库关键统计信息</p><p>在数据库infomation_schema内部存储的系统信息表</p></blockquote><p>核心表：</p><p>SCHEMATA表</p><p>提供了当前mysql实例中所有数据库的信息。是show databases的结果取之此表。<br>TABLES表</p><p>提供了关于数据库中的表的信息（包括视图）。详细表述了某个表属于哪个schema，表类型，表引擎，创建时间等信息。是show tables from schemaname的结果取之此表。<br>COLUMNS表</p><p>提供了表中的列信息。详细表述了某张表的所有列以及每个列的信息。是show columns from schemaname.tablename的结果取之此表。<br>STATISTICS表</p><p>提供了关于表索引的信息。是show index from schemaname.tablename的结果取之此表。<br>USER_PRIVILEGES（用户权限）表</p><p>给出了关于全程权限的信息。该信息源自mysql.user授权表。是非标准表。</p></li><li><p>手法4：报错注入</p></li><li><p>手法5：布尔注入</p></li><li><p>手法6：注释注入：</p><blockquote><p>原本要根据username &#x3D; ‘’ and password &#x3D; ‘’ 判断有无</p><p>如果username传入的值中有# 或者 – 可以将后面的注释掉，结果无论password传入何值，都必正确</p></blockquote></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>JVM</title>
    <link href="/2024/05/17/JVM/"/>
    <url>/2024/05/17/JVM/</url>
    
    <content type="html"><![CDATA[<h1 id="JVM"><a href="#JVM" class="headerlink" title="JVM"></a>JVM</h1><p>Java Virtual Machine | Java程序的运行环境（Java二进制字节码的运行环境）</p><p>优点：</p><ol><li><p>一次编写，到处运行</p><p>JVM的主要目标之一 —— 跨平台能力</p><p>本质上，JVM对不同的操作系统写了不同的代码，而且据说有用多种语言的多种实现。是一种在操作系统之上的<strong>中间层</strong>——JVM为上层屏蔽了不同操作系统的差异（系统调用，内存管理，线程调度…）</p></li><li><p>自动内存管理，垃圾回收机制</p></li><li><p>else</p><ol><li>数组下标越界检查：防止数据覆盖</li><li>多态（面向对象基石），JVM用…实现？</li></ol></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-22-09-25-32-image.png"></p><h3 id="常见的JVM"><a href="#常见的JVM" class="headerlink" title="常见的JVM"></a>常见的JVM</h3><p>JVM本身是一种规范，满足规范的实现都是JVM</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822093117018.png" alt="image-20230822093117018"></p><p>各种实现的底层方法不同，常用JVM是HotSpot</p><h3 id="JVM大致流程"><a href="#JVM大致流程" class="headerlink" title="JVM大致流程"></a>JVM大致流程</h3><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822093730510.png" alt="image-20230822093730510"></p><ol><li>Java文件被编译为字节码文件</li><li>进入类加载器加载到内存中</li><li>方法区存放每个类</li><li>堆中存放每个对象</li><li>通过执行引擎完成对对象的调用 | 通过GC垃圾回收机制处理没用的对象</li><li>程序执行过程中需要使用 虚拟机栈、程序计数器、本地方法栈等。</li></ol><ul><li><p>java代码执行的大致流程：</p><p>Java源代码编译成.class二进制字节码文件（本身人类不可读，但可以通过反编译 成类，或者反编译成通过助记符表示的JVM指令）</p><p>通过解释器变成机器码</p><p>将机器码交给CPU执行</p></li></ul><h2 id="JVM内存结构"><a href="#JVM内存结构" class="headerlink" title="JVM内存结构"></a>JVM内存结构</h2><ol><li><p>程序计数器</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822094459748.png" alt="image-20230822094459748"></p><p>作用：</p><ul><li><p>记住下一条JVM指令的执行地址</p><p>对于JVM指令而言，每条指令匹配一个执行地址</p><p>原因：</p><ol><li><p>想必是因为Java中代码执行顺序不同于面向过程，是通过对象交织起来的，如此一来执行顺序的逻辑需要额外管理</p><p>like: 调用方法后返回到哪里…<strong>似乎Java中一切跳跃的代码都是在调用方法</strong></p></li></ol><hr><p>by the way : </p><p>对比前端代码执行和Java中swing库的执行逻辑</p><ul><li><p>前端：由于构建DOM树的通过每个组件绑定的方法是确定的，所以可以通过事件指定触发script标签中定义的某个具体方法。</p><p>— 也就是代码的执行顺序并非严格按照script从上到下</p></li><li><p>Swing：由于给每个组件绑定的不是方法，而是监听器对象，于是对一个事件会引发哪些方法而言，只能遍历监听器的触发方法</p><p>— 代码执行顺序还是从上到下，不过是由方法调用插入了别的代码</p></li></ul><hr><ol start="2"><li>CPU切换后需要保存某一线程的进度</li></ol><p>解释器执行流程是根据程序计数器中记录的下一条指令所在位置来执行</p></li></ul><p>物理实现：</p></li></ol><ul><li><p>利用CPU中的寄存器</p><p>特点：</p></li></ul><ol><li><p>线程私有</p><p>一条线程拥有一个程序计数器</p></li><li><p>不会存在内存溢出（JVM规范规定）</p></li><li><p>虚拟机栈</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822101155898.png" alt="image-20230822101155898"></p></li></ol><ul><li><p>一个<strong>虚拟机栈</strong>对应一个<strong>线程</strong>，栈表示一段<strong>内存空间</strong></p></li><li><p>栈中存放的元素 —— <strong>栈帧</strong></p><p>每个线程只能有一个活动栈帧，表示当前执行的方法</p></li><li><p>栈帧 —— 表示一次<strong>方法</strong>调用需要的内存空间</p><p>内部可能有的信息：参数、局部变量、返回地址…</p></li></ul><p>注意：</p><ol><li><p>垃圾回收并不涉及栈内存</p><p>由于方法调用完毕后栈内存的信息自动弹出，弹出过程就是简单的毁灭，不必动用GC</p></li><li><p>栈内存分配越大越好吗？</p><p>一个线程需要一个栈内存，内存分配越大，能同时启动的线程就越少；并且一般的栈内存足够方法调用存储信息用。</p></li><li><p>方法内的局部变量是否线程安全？</p><p>变量的线程安全 —— <strong>一个变量对此线程是共享的，还是私有的</strong> —— </p><p><code>对于某个变量的访问和操作不会引发数据不一致、不正确或者异常的问题</code></p><p><code>旨在确保多个线程可以同时访问共享变量而不会导致意外的错误或不确定的结果</code></p></li></ol><blockquote><p>如果方法内<strong>局部变量</strong>没有逃离方法的作用范围 | 外界无法访问到 | 不是参数传入，也不是返回值 | 是纯粹的内部变量，它就是线程安全的</p><p>— 方法内无法定义static，只能引用static，而对static变量的引用也是线程不安全的</p></blockquote><h5 id="StackOverFlow"><a href="#StackOverFlow" class="headerlink" title="StackOverFlow !"></a>StackOverFlow !</h5><p>栈溢出原因：</p><ol><li>栈帧过多 —— 大概率来自方法递归调用</li><li>栈帧过大 —— 情况较少</li></ol><p>可以显示设置栈内存大小，不然有默认值——不同操作系统设置不同，一般为1m，win会根据计算机虚拟内存进行调整</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822161130881.png" alt="image-20230822161130881"></p><p>栈溢出情况举例 —— 对象转JSON导致的循环引用</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822161845253.png" alt="image-20230822161845253"></p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822161853966.png" alt="image-20230822161853966"></p><p>隐式的递归——两方法来回调用</p><h3 id="线程运行诊断"><a href="#线程运行诊断" class="headerlink" title="线程运行诊断"></a>线程运行诊断</h3><p>定位（windows下）：</p><ul><li><p>用<strong>tasklist</strong>定位哪个线程的<strong>内存</strong>占用过高</p></li><li><p>在<strong>资源监视器</strong>中定位哪个线程<strong>CPU</strong>是用来过高</p></li><li><p>jstack 进程id </p><p>可以查看进行下各个线程</p><p>by the way jstack是cmd命令且需要参数，于是不能直接启动jstack.cmd，而是在其目录下打开cmd，再带参数启动</p></li></ul><ol start="3"><li><p>本地方法栈</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822171304540.png" alt="image-20230822171304540"></p><p>本地方法：</p><p>由于Java本身有限制，不能直接和底层操作系统联通，而需要通过C&#x2F;CPP本地方法，与操作系统底层提供的API联通</p><p>本地方法栈：留给调用本地方法的内存空间</p></li><li><p>堆：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822171621020.png" alt="image-20230822171621020"></p><p>堆：通过new关键字，创建的对象都会使用堆内存</p><p>特点：</p><ul><li>是线程共享的，堆中的对象都需要考虑线程安全问题</li><li>堆具有垃圾回收机制</li></ul></li></ol><h5 id="堆内存溢出"><a href="#堆内存溢出" class="headerlink" title="堆内存溢出"></a>堆内存溢出</h5><p>垃圾回收机制无能为力——堆中的对象都是有用的</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">main</span><span class="hljs-params">(String[] args)</span> &#123;<br>        List&lt;String&gt; stringList = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ArrayList</span>&lt;&gt;();<br>        <span class="hljs-type">String</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;hello&quot;</span>;<br>        <span class="hljs-type">Integer</span> <span class="hljs-variable">num</span> <span class="hljs-operator">=</span> <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">while</span> (<span class="hljs-literal">true</span>)&#123;<br>            stringList.add(a);<br>            a = a+a;<br>            num++;<br>            System.out.println(num);<br>        &#125;<br>    &#125;<br></code></pre></td></tr></table></figure><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822172547655.png" alt="image-20230822172547655"></p><ul><li><p>更改默认堆内存大小（将堆内存设置较小易于排查潜在溢出）</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822172858500.png" alt="image-20230822172858500"></p></li></ul><h3 id="堆内存诊断"><a href="#堆内存诊断" class="headerlink" title="堆内存诊断"></a>堆内存诊断</h3><p>工具</p><ol><li><p>jps </p><p>用于查看当前系统中有哪些Java进程</p></li><li><p>jmap</p><p>用于查看堆内存的占用情况 </p><p>jmap -heap 进程id</p></li><li><p><strong>Jconsole</strong></p><p>图形界面，多功能检测工具——可以查看之前的一切，且是实时更新的</p><p>但是有限制——需要提前知道要检测哪个进程——jps+jmap</p></li><li><p><strong>JvisualVM</strong></p><p>终极杀人魔火云邪神</p><p>方法：连接到对应pid的线程–堆转储Heap Dump对某一时刻的堆内存进行切片</p><p>​            即可查看此时堆中任何对象，对象的大小。</p></li></ol><h2 id="方法区"><a href="#方法区" class="headerlink" title="方法区"></a>方法区</h2><p>方法区是所有线程共享的区域，存储和类的结构相关的信息—-such as 属性（field），成员方法和构造方法，运行时常量池。</p><p>方法区在虚拟机启动时创建。</p><p>方法区在逻辑上是堆的组成部分，但不同的JVM实现的方法区的位置不同</p><p>例如：hotspot在JDK1.8之前的<strong>永久代</strong>模式方法区属于堆，但JDK1.8之后的<strong>元空间</strong>模式则不属于。</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822194708668.png" alt="image-20230822194708668"></p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822194729398.png" alt="image-20230822194729398"></p><h3 id="方法区内存溢出"><a href="#方法区内存溢出" class="headerlink" title="方法区内存溢出"></a>方法区内存溢出</h3><ul><li>1.8以前永久代模式可能会内存溢出（当内存中加载了太多类的时候）</li><li>1.8之后元空间和物理内存空间绑定，一般不会溢出</li></ul><p>类加载代码：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230822195427609.png" alt="image-20230822195427609"></p><p>实际开发中，并非手动加载多个类，但框架使用了许多<strong>动态字节码反编译生成类</strong>技术。</p><h4 id="运行时常量池"><a href="#运行时常量池" class="headerlink" title="运行时常量池"></a>运行时常量池</h4><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-09-11-17-image.png"></p><p>引理：类反编译成可读的文本形式（本质还是二进制字节码）</p><ul><li><p>通过javap反编译工具操作.class字节码文件</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-09-21-04-image.png" title="" alt="" width="277"></li><li><p>得知——二进制字节码文件有三个组成部分：</p><ol><li><p>类的基本信息</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-09-23-38-image.png"></p><p>大致内含：</p><p>最后修改时间、签名、类名、类修饰符、Java版本、继承&#x2F;实现关系、字段&#x2F;方法数</p></li><li><p><strong>常量池</strong></p><p>作用：给下面的方法提供一下常量符号</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-09-34-29-image.png"></p><p>类方法用Java指令的写法是：</p><p>做什么   常量符号（参数）</p><p>其中，无论是类，类中某一方法，字符串常量…都在常量池中定义</p><p>常量池是一个k-v表</p><p>在类方法中通过k完成对常量的调用</p></li></ol></li></ul><ol start="3"><li><p>类方法定义</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-09-29-33-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-09-29-46-image.png"></p><p>方法中具体实现就是Java指令（程序计数器的目标）</p></li></ol><ul><li>运行时常量池——在运行时将常量池加载到内存，并将k地址引用变为真实地址</li></ul><h4 id="StringTable"><a href="#StringTable" class="headerlink" title="StringTable"></a>StringTable</h4><ul><li><p>字符串加载流程：</p><ol><li><p>编译到字节码文件时，字符串都进入运行时常量池</p></li><li><p>在Java指令执行到调用某一字符串时：</p><ol><li><p>查看StringTable中有无此字符串对象</p></li><li><p>如果有则引用，如果没有则将字符串创建对象并加入StringTable</p><p>（此处创建对象就是在堆中开辟空间，将对象的引用地址加入哈希表）</p></li></ol><blockquote><p>本质：懒加载——用到再加载</p></blockquote></li></ol></li><li><p>例题：</p><ol><li><p>–</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-04-56-image.png"></p><p>false</p></li><li><p>–</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-08-12-image.png"></p><p>true</p><blockquote><p>归纳：字符串如果是由字面量拼接得到，则在字节码中就是对StringTable的引用，但如果是引用变量拼接，由于变量在之后执行还可变，则只能在运行期间动态生成新字符串对象</p></blockquote></li><li><p>–</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-21-29-image.png"></p><p>通过new String() + new String()得到的结果只是对象，而非加入StringTable中</p><p>intern()方法试图将一个字符串对象加入到串池中，并返回串池中的引用对象</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-24-02-image.png"></p></li><li><p>–</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-27-11-image.png"></p></li></ol></li><li><p>StringTable位置</p><p>Java1.6 版本中，StringTable在方法区（永久代中），但在1.8中转移到堆中</p><p>因为程序对字符串的引用很频繁，而堆的垃圾回收机制比在方法区更灵活&#x2F;频繁，不容易造成内存过满。</p></li><li><p>StringTable的垃圾回收</p><blockquote><p>对于未被引用的字符串对象，也会进行垃圾回收</p></blockquote><p>当内存占用较大，接近运行时常量池最大内存时，会自动执行一次垃圾回收</p></li><li><p>StringTable 性能调优</p><blockquote><p>由于StringTable底层按照哈希表完成，哈希表的调优取决于桶的多少</p><p>——桶过少会加大查找时间，通过多导致元素较分散，内存率使用不高，但查找会快。</p></blockquote></li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-10-48-53-image.png"></p><p>设置-XX:StringTableSize&#x3D;xxx</p><p>默认桶的个数是60013，桶过少导致链表过长，由于经常需要对StringTable查找有无某一元素，所以导致耗时。</p><ul><li><p>性能优化业务场景</p><p>当有大量字符串需要加载到内存，而其中又有大量重复（身份证上所属籍贯…）</p><p>可以将获取到的字符串用intern()入池，返回的便是池中引用的对象</p><p>如果不入池，很可能导致字面量相同的String对象占满堆内存</p><p>入池后，仅仅不同的字符串占用StringTable中的内存</p></li></ul><h2 id="直接内存"><a href="#直接内存" class="headerlink" title="直接内存"></a>直接内存</h2><blockquote><p>不属于JVM堆内存，而是属于系统的可用物理内存</p></blockquote><p>特点：</p><ol><li><p>允许Java程序通过本地方法调用直接分配内存，而无序在Java堆上进行对象分配</p><p>使得在IO操作中性能很高</p></li><li><p>不受JVM垃圾回收机制管控，因此需要程序员来释放内存</p></li><li><p>在NIO中广泛使用</p></li></ol><ul><li><p>老版本读写操作：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-11-15-55-image.png"></p><p>问题在于：数据缓存了两份</p></li><li><p>利用直接内存：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-11-17-14-image.png"></p></li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-11-26-12-image.png"></p><h2 id="垃圾回收"><a href="#垃圾回收" class="headerlink" title="垃圾回收"></a>垃圾回收</h2><ol><li><p>如何判断对象可以回收</p><p>算法：</p><ol><li><p>引用计数法：</p><p>当一个对象被引用时，其计数器+1，当其计数器为0时表示可以被回收</p><p>弊端：</p><p>无法识别循环引用问题：</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-15-11-48-image.png" title="" alt="" width="339"></li><li><p>可达性分析算法：</p><p>首先：明确一系列<strong>根对象</strong>（肯定不能被回收的对象），其次，扫描全堆，如果被根对象引用&#x2F;能够沿着根对象为起点的链引用，则拒绝回收，否则进行回收。</p><p>Question1：什么对象是根对象（GC Root）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-15-30-13-image.png"></p><p>可通过工具查看当下堆中有哪些根对象</p><p>eclipse的MemoryAnalyzer</p><p>是一个Java堆分析器，可以帮助查找内存泄漏并减少内存消耗。</p><p>方法：</p><ol><li><p>通过jmap获取堆转储（Heap Dump：是Java进程在某个时刻的快照，保存了Java对象和类的信息   通常在获取Dump前进行一次GC）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-15-39-37-image.png"></p></li><li><p>MAT打开bin文件开始分析</p></li></ol></li></ol></li><li><p>Java中的四种引用：</p><ol><li><p>强引用（之前所见所有引用）</p></li><li><p>软引用</p></li><li><p>弱引用</p></li><li><p>虚引用</p></li><li><p>终结器引用</p></li></ol><p><code>强引用直接引用对象，其他引用类型（软引用、弱引用、虚引用）都是通过间接方式引用对象，引用本身也是普通的Java对象，持有对其他对象的引用</code></p><p>这些引用类型的只要目的是为了影响对象的生命周期和垃圾回收行为</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-15-55-11-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-15-55-42-image.png"></p><p>引用队列：</p><p>用于管理软弱虚引用对象（当其引用的对象被断开后，普通引用对象即加入引用队列）</p><p>主要作用是：<strong>当被引用的对象被垃圾回收时，通知程序或执行一系列对象被回收后执行的回调函数（钩子）。</strong></p></li></ol><p>虚引用的典型用法：</p><ol><li><p>当操作直接内存时，创建了一个ByteBuffer类对象，并用虚引用对象Cleaner引用ByteBuffer。</p></li><li><p>由于当垃圾回收掉ByteBuffer时并不彻底，并没有释放掉直接内存</p></li><li><p>释放直接内存的步骤就交给虚引用对象Cleaner</p><p>虚引用在断开ByteBuffer后加入到引用队列，引用队列负责执行此虚引用的回调——根据其内存储的直接引用地址，完成内存的释放。</p></li></ol><p>终结器引用：</p><p>是某个对象被回收时的回调（钩子）方法</p><p>在某个对象需要被回收前，其终结器引用会被加入到引用队列中，当引用队列通过这个终结器引用执行了引用对象的finalize()方法后，引用和引用对象双双被清理。</p><p>但据说不推荐finalize()方法</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-16-17-08-image.png" title="" alt="" width="570"><p>软引用：</p><p>业务场景：</p><p>当某些对象虽引用了，但由于其占用内存太大了，将其保存在内存中不如重新读取一遍要好，于是用软引用表示可以在需要被回收的时候可以垃圾回收。</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-16-23-27-image.png" title="" alt="" width="537"><ul><li><p>如果List直接存储byte[]则为强引用</p></li><li><p>上图中，List对SoftReference为强引用，SoftReference对实际对象为软引用</p></li><li><p>软引用好处：</p><p>可以通过触发的垃圾回收机制清理出某些内存，保证程序正确执行</p></li><li><p>软引用坏处：</p><p>被清理的引用就为null了【通过引用队列标记】</p></li></ul><p>软引用+引用队列示例：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-16-34-50-image.png"></p><ol><li><p>给软引用绑定引用队列</p><p>在软引用关联的对象被回收时，自动将软引用加入到queue中</p></li><li><p>可以手写对queue的操作方法完成回调。</p></li></ol><p>弱引用：</p><p>被弱引用关联的对象一定会被回收，即：只能活到下次垃圾回收之前</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Object</span> <span class="hljs-variable">obj</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Object</span>();<br>WeakReference&lt;Object&gt; wf = <span class="hljs-keyword">new</span> <span class="hljs-title class_">WeakReference</span>&lt;Object&gt;(obj);<br>obj = <span class="hljs-literal">null</span>;<br></code></pre></td></tr></table></figure><h3 id="垃圾回收算法"><a href="#垃圾回收算法" class="headerlink" title="垃圾回收算法"></a>垃圾回收算法</h3><blockquote><p>JVM在不同情况下使用不同的回收算法 —— 分代回收机制</p></blockquote><ol><li><p>标记清除算法：</p><p>一阶段：将没有引用的对象标记出来</p><p>二阶段：将垃圾对象占用的空间释放出来（标记为可覆盖）</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-16-46-23-image.png" title="" alt="" width="408"><p>优点：快</p><p>确定：容易产生内存碎片 —— 由于清除操作并没有对内存空间进行整理，而只是标识空白空间可以被重写，容易导致放不下更大的对象</p></li><li><p>标记整理算法：</p><p>和标记清除算法的区别在于：三阶段：整理内存</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-16-49-49-image.png" title="" alt="" width="473"><p>缺点：整理过程较为复杂，导致垃圾回收速度较慢</p></li><li><p>复制算法：</p><p>一阶段：标记可以被回收的垃圾</p><p>二阶段：需要一块大小相同的内存空间（to），将需要保留的对象从老空间（from）复制到新空间中，期间完成整理</p><p>三阶段：对from进行彻底的清理</p><p>四阶段：交换to和from</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-23-16-56-11-image.png" title="" alt="" width="526"><p>缺点：需要一块同等大小的内存</p></li></ol><h3 id="分代回收机制："><a href="#分代回收机制：" class="headerlink" title="分代回收机制："></a>分代回收机制：</h3><p>实现：</p><p>堆内存分为：新生代和老年代</p><p>新生代又分出伊甸园，幸存区From，幸存区To</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-17-11-44-image.png"></p><p>分区的原因——不同区存放不同类型的对象——实行不同的垃圾回收机制</p><p>例：</p><p>老年代存放常用的、更有价值的对象，不经常垃圾回收</p><p>新生代存放朝生夕死的迭代很快的对象，经常垃圾回收</p><p>创建对象的过程：</p><ol><li><p>首先进入伊甸园区</p></li><li><p>当伊甸园占满时，触发一次Minor GC（小垃圾回收） </p><p>将伊甸园和From区的保留的对象复制到To区</p><p>清理伊甸园和From区</p><p>交换From和To</p><blockquote><p>本质上是让伊甸园做存放对象的一阶缓存，From做存放上次没清理掉的对象的二阶缓存，To是一段用于复制的中介空间</p></blockquote><p><strong>ATTENTION:</strong></p><p>minor gc会引发 stop the world ，暂停其他用户的线程，等垃圾回收结束时，用户线程才恢复运行（由于复制过程导致对象的引用地址发生改编）</p></li><li><p>新生代 –&gt; 老年代</p><p>情况一：</p><p>Minor GC没有清理掉的对象（进入From的）寿命+1，当寿命达到某一阈值（最大是15，因为标记此值的表头占4bit），此对象进入（晋升）老年代。</p><p>情况二：</p><p>如果Survivor空间不足以容纳存活的对象，一部分对象会被晋升到老年代。</p></li><li><p>当某一时刻：老年代的空间不足（由于老年代像是新生代的后备空间），于是调用Full GC 对全堆（新生代&#x2F;老年代）进行垃圾回收。</p><p>full GC的Stop the world时间更长，因为要对全堆进行垃圾回收，但如果full GC也空不出空间来，则触发out of memory异常</p></li></ol><p>据说full GC的垃圾回收机制算法</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-17-50-32-image.png"></p><h2 id="垃圾回收过程"><a href="#垃圾回收过程" class="headerlink" title="垃圾回收过程"></a>垃圾回收过程</h2><hr><p>  <img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-17-55-26-image.png"></p><h3 id="垃圾回收器："><a href="#垃圾回收器：" class="headerlink" title="垃圾回收器："></a>垃圾回收器：</h3><ol><li><p>串行的垃圾回收器</p><p>底层：</p><p>单线程的垃圾回收器，回收垃圾时stop the world</p><p>使用场景：</p><p>堆内存较小，适合个人电脑</p></li><li><p>吞吐量优先</p><ul><li>考虑因素：程序运行单位时间内，STW的时间占比最短（考虑程序整体中的垃圾回收时间）</li></ul><p>底层：多线程</p><p>使用场景：</p><p>堆内存较大，适合多核CPU（单核的话相当于多个线程轮流清理同一个内存，效果和串行一致）</p></li><li><p>响应时间优先</p><ul><li>考虑因素：尽可能使单次STW时间更短（不计程序整体执行几次GC，仅最小化单次GC时间）</li></ul><p>底层：多线程</p><p>使用场景：</p><p>堆内存较大，适合多核CPU（same）</p></li></ol><h4 id="串行垃圾回收器"><a href="#串行垃圾回收器" class="headerlink" title="串行垃圾回收器"></a>串行垃圾回收器</h4><p>启动命令：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-21-48-20-image.png"></p><p>执行流程：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-21-48-08-image.png"></p><h4 id="吞吐量优先的垃圾回收器（默认使用）"><a href="#吞吐量优先的垃圾回收器（默认使用）" class="headerlink" title="吞吐量优先的垃圾回收器（默认使用）"></a>吞吐量优先的垃圾回收器（默认使用）</h4><p>启动命令：（默认启动）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-21-51-11-image.png"></p><p>执行流程：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-23-21-51-25-image.png"></p><p>回收垃圾的线程和CPU核数相关</p><p>流程描述：</p><ol><li><p>正常状态下四个CPU同时运行</p></li><li><p>开启垃圾回收</p></li><li><p>所有CPU启动垃圾回收线程，其他线程进入阻塞，共同清理同一块堆内存</p></li></ol><ul><li>手动控制线程数：</li></ul><p>        <img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-25-25-image.png"></p><ul><li><p>开启自动调整大小策略——堆中各个部分大小、晋升的阈值</p><blockquote><p>由于 “响应时间最低”的垃圾回收器减少停顿时间的策略是缩小新生代从而单次垃圾回收时间更短，但是也导致minor GC更加频繁，所以并不合理，通过自动调整各代大小，追求吞吐量最高</p></blockquote><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-26-24-image.png"></p></li><li><p>设置目标——垃圾回收占整个程序的占比</p><p>如果要占比低，往往需要<strong>将堆调大</strong>，总体减少垃圾回收的次数</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-29-02-image.png"></p></li><li><p>设置目标——最大暂停的毫秒数</p><p>如果要减少STW时间，往往需要将堆调小，垃圾回收扫描的空间减少</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-29-31-image.png"></p></li></ul><h4 id="响应时间优先的垃圾回收器"><a href="#响应时间优先的垃圾回收器" class="headerlink" title="响应时间优先的垃圾回收器"></a>响应时间优先的垃圾回收器</h4><p>开启：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-33-22-image.png"></p><p>并发Concurrent 标记清除MarkSweep </p><p>并发：</p><p>    - 可以和用户线程并发执行</p><p>并行：</p><p>    - 其他用户线程必须STW</p><p>工作流程：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-40-46-image.png"></p><ol><li><p>初始标记——简单标标，需要STW</p></li><li><p>并发标记——在初始标记的基础上实现并发标记</p></li><li><p>重新标记——由于在并发标记阶段，其他线程可能对内存中对象有所调整，故而需要整体快速重来一遍</p></li><li><p>并发清理</p></li></ol><blockquote><p>感觉整个工作流程都是为了能够并发标记而前后铺垫，而之所以能够并发清理，估计是已经确定某些对象没有用了，也就线程安全了。</p></blockquote><p>由于前后两个需要STW的阶段时间很短，所以叫响应时间优先的垃圾回收器</p><p>参数：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-46-37-image.png"></p><p>第一个是并行垃圾回收线程，设置为CPU核数，第二个是并发垃圾回收线程，此线程进行并发的垃圾回收，其他线程依旧完成用户工作</p><p>问题：</p><ol><li><p>占用了一个CPU，导致对程序运行效率（吞吐量）有所影响</p></li><li><p>并发清理无法清理并发时产生的垃圾（浮动垃圾），故而需要将其保留至下一次GC</p><p>新问题是：</p><p>既然知道并发清理时会有垃圾产生，就要事先为其预留一定内存，防止内存泄漏</p><p>于是就不能和之前的垃圾回收模式一样在内存不足时进行GC，而是要提前GC</p><p>参数设置：(根据内存占比阈值)</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-09-54-28-image.png"></p></li><li><p>由于并发执行中使用的是<strong>标记清除</strong>算法，不能包含整理（应该是防止影响别的线程），于是可能造成内存碎片不够用——导致降级成串行回收算法（需要先标记整理）。</p><p>如此一来时间较长</p></li></ol><h4 id="G1垃圾回收器"><a href="#G1垃圾回收器" class="headerlink" title="G1垃圾回收器"></a>G1垃圾回收器</h4><p>特点：</p><ul><li><p>同时注重吞吐量和低延迟，是并行算法和并发算法的集成</p></li><li><p>适合超大的堆内存，算法会将堆划分成多个大小相等的Region，每个Region相当于小堆</p></li><li><p>整体上是标记+整理算法，两个区域之间是复制算法</p></li></ul><p>启动：</p><p>JDK9之后默认使用G1，之前版本需要启动</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-07-34-image.png"></p><p>参数设置：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-08-32-image.png"></p><p>流程：</p><ol><li><p>三个阶段：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-20-23-image.png"></p></li><li><p>一阶段Young Collection 新生代垃圾回收</p><ol><li><p>G1将整个堆内存划分成许多Region，分别作为伊甸园区或幸存者区或老年代</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-22-17-image.png"></p><p>一阶段内存对象首先填充伊甸园区</p></li><li><p>当伊甸园区内存满后，通过复制算法做类似Minor GC</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-24-03-image.png"></p></li><li><p>当幸存区内存不够，或者年龄达到阈值，晋升！</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-24-58-image.png"></p></li></ol></li><li><p>二阶段：标记</p><ul><li><p>在Young GC时会进行GC Root的初始标记</p></li><li><p>当老年代占用堆空间的比例达到阈值时，进行<strong>并发标记</strong>（不会STW）</p><p>阈值由参数决定</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-24-10-31-57-image.png" title="" alt="" width="403"><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-32-13-image.png"></p></li></ul></li><li><p>三阶段：混合收集</p><p>对伊甸园区，幸存者区，老年代进行全面的垃圾回收</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-38-52-image.png"></p><ul><li><p>最终标记（由于并发过程其他线程产生了垃圾）</p></li><li><p>拷贝存活</p><ul><li><p>E到S，进行新生代的垃圾回收</p></li><li><p>O到O，通过标记-整理算法（期间涉及区到区的复制），回收老年代的垃圾</p><p>注意：</p><ul><li>并非所有老年代都参与此次的垃圾回收，而是G1会识别最该被回收的垃圾，考虑到有最长时间的限制而不能全部进行回收（复制过程较为耗时）</li></ul></li></ul></li></ul></li></ol><h3 id="Minor-GC-和-Full-GC"><a href="#Minor-GC-和-Full-GC" class="headerlink" title="Minor GC 和 Full GC"></a>Minor GC 和 Full GC</h3><p>对不同的垃圾回收器，两个垃圾回收模式工作于不同区域</p><ol><li><p>串行GC</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-52-35-image.png"></p></li><li><p>并行GC</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-52-50-image.png"></p></li><li><p>CMS（并发GC）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-10-53-10-image.png"></p><p>只有当并发垃圾回收失败时，退化成串行垃圾回收，才会执行Full GC</p><p>并发失败：</p><ol><li><p>并发清理阶段中：如果在并发清理期间，新生成的垃圾增加太快，快过垃圾回收的时间，将导致老年代的空间耗尽，从而促发Full GC。</p></li><li><p>由于缺少内存整理而导致的内存碎片无法放下新对象而导致的full GC</p></li></ol></li><li><p>G1</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-11-03-34-image.png"></p><p>也是只有当并发垃圾回收失败时，退化成串行垃圾回收，才会执行Full GC</p><p>并发失败：</p><ul><li><p>并发清理阶段中：如果在并发清理期间，新生成的垃圾增加太快，快过垃圾回收的时间，将导致老年代的空间耗尽，从而促发Full GC。</p></li><li><p>由于G1的垃圾回收算法是在Region之间复制，复制过程中完成了内存整理，从而不会产生过多的内存碎片。</p></li></ul></li></ol><h4 id="新生代跨代引用"><a href="#新生代跨代引用" class="headerlink" title="新生代跨代引用"></a>新生代跨代引用</h4><p>由于新生代Minor GC时需要判断对象是否被引用，于是需要找出GC root，而GC Root有部分在老年代，每次young GC时都扫描老年代效率很低</p><p>于是采用 Card Table卡表技术，将老年代划分为多个区域存储对象，如果一个区域内的对象引用了新生代的对象，则标记为脏卡</p><p>在新生代中设置Remember Set 记录有哪些脏卡引用了这个新生代，于是只要遍历脏卡，扫描出响应的GC Root即可。</p><h4 id="细说重标记"><a href="#细说重标记" class="headerlink" title="细说重标记"></a>细说重标记</h4><p>原委回顾：</p><ul><li>在并发标记过程中，垃圾回收线程标记过的对象可能在之后又被用户线程调整引用（决定回收还是不回收），于是容易导致错误地将对象回收&#x2F;未回收。</li></ul><p>做法：</p><ul><li><p>设置回调——对于标记线程之后的对象，如果调整其引用关系的话要触发一个回调函数（钩子）——<strong>写屏障技术</strong></p><p>写屏障将被引用的对象加入到队列当中，并将此对象变成灰色（标记为未处理）</p><p><strong>重标记</strong>的过程就是对队列中的对象进行重新判断处理</p></li></ul><h2 id="GC调优"><a href="#GC调优" class="headerlink" title="GC调优"></a>GC调优</h2><p><small>技术要求：VM参数设置，相关工具</small></p><blockquote><p>使用-XX:+PrintFlagFinal打印所有以-XX格式的参数</p></blockquote><h5 id="JVM参数"><a href="#JVM参数" class="headerlink" title="JVM参数"></a>JVM参数</h5><blockquote><p><a href="https://blog.csdn.net/sihai12345/article/details/103485504?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522169286337416800188542270%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D&request_id=169286337416800188542270&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-103485504-null-null.142%5Ev93%5EchatsearchT3_1&utm_term=VM%E5%8F%82%E6%95%B0&spm=1018.2226.3001.4187">深入理解 Java 虚拟机（第二弹） - 常用 vm 参数分析_vm 引数该填什么_hello-java-maker的博客-CSDN博客</a></p></blockquote><p>分类：</p><ol><li><p>标准参数（-）:所有JVM都必须实现的功能</p></li><li><p>非标准参数（-X）：并非保证所有JVM都实现</p></li><li><p>非Stable参数（-XX）：各个JVM实现有所不同</p></li></ol><ul><li><p>标准参数</p><p>通过命令行中 java -help可查看</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-24-16-42-00-image.png" title="" alt="" width="453"><ol><li><p>-client </p></li><li><p>-server</p><p>以client&#x2F;server模式启动JVM，C启动速度快，但运行时性能和内存管理效率不高，S是默认模式，适合生产环境，适用于服务器</p></li><li><p>-classpath</p><p>通知JVM类搜索路径？</p></li><li><p>-DpropertyName&#x3D;value</p><p>定义全局属性值</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-24-16-46-05-image.png" title="" alt="" width="429"></li><li><p>-verbose</p><p>查询GC问题常用命令</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-16-49-35-image.png"></p><p>另外，控制台输出GC信息还可以使用如下命令：</p><p>在JVM的启动参数中加入</p><p>-XX:+PrintGC </p><p>-XX:+PrintGCDetails </p><p>-XX:+PrintGCTimeStamps </p><p>-XX:+PrintGCApplicationStoppedTime</p><p>按照参数的顺序分别输出GC的简要信息，GC的详细信息、GC的时间信息及GC造成的应用暂停的时间.</p></li></ol></li></ul><h6 id="非标准参数"><a href="#非标准参数" class="headerlink" title="非标准参数"></a>非标准参数</h6><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-16-52-50-image.png"></p><ol><li><p>-Xmn</p><p>设置新生代内存大小的最大值</p></li><li><p>-Xms</p><p>设置初始堆大小，也就是堆大小的最小值</p></li><li><p>-Xmx</p><p>设置堆的最大值</p></li><li><p><code>另外，官方默认的配置为**年老代大小:年轻代大小=2:1**左右，使用</code>-XX:NewRatio<code>可以设置年老代和年轻代之比，例如，</code>-XX:NewRatio&#x3D;4<code>，表示年老代:年轻代=4:1</code></p></li><li><p>-Xss</p><p>设置每个线程的栈内存</p></li><li><p>-Xprof</p><p>跟踪正在运行的程序，并将跟踪数据在标准输出输出</p></li><li><p>-Xint</p><p>设置JVM为解释模式（interpret mode）</p><p>解释模式会强制JVM逐行执行所有的字节码，运行速度较慢（没有进行JIT编译）</p></li><li><p>-Xcomp</p><p>设置JVM为编译模式（compile mode）</p><p>JVM在第一次使用时就会把所有字节码编译成本地代码（启动JIT）</p></li><li><p>-Xmixed</p><p>设置JVM为混合模式（默认）</p><p>将解释模式和编译模式进行混合使用（由JVM决定使用哪个）</p></li></ol><h5 id="非-Stable-参数"><a href="#非-Stable-参数" class="headerlink" title="非 Stable 参数"></a>非 Stable 参数</h5><p>分类：</p><ol><li><p>性能参数：</p><p>用于JVM的性能调优和内存分配控制</p></li><li><p>行为参数：</p><p>用于改变JVM的基础行为，比如GC的方式和算法的选择</p></li><li><p>调试参数：</p><p>用于监控、打印、输出等JVM参数，以显示详细信息</p></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-17-09-37-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-17-10-31-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-17-10-56-image.png"></p><h4 id="调优的领域"><a href="#调优的领域" class="headerlink" title="调优的领域"></a>调优的领域</h4><ol><li><p>关于内存的垃圾回收调优</p></li><li><p>关于锁竞争的调优（并发）</p></li><li><p>关于CPU占用的调优</p></li><li><p>关于IO的调优</p></li><li><p>关于数据库调优</p></li><li><p>关于缓存和预取策略的调优</p></li><li><p>关于分布式系统的调优</p></li></ol><h4 id="确定优化的目标"><a href="#确定优化的目标" class="headerlink" title="确定优化的目标"></a>确定优化的目标</h4><ol><li><p>高吞吐量</p><p>表示在特定时间内完成应用程序业务逻辑与垃圾回收操作的比例（即允许单次长时间垃圾回收，但只关注总体上用户线程执行的速度）</p><p>因为可能会导致较长时间的STW，所以适合不关心时延的项目</p><p>比如：科学计算</p><p><strong>适合：ParallelGC</strong></p><hr></li><li><p>低延迟</p><p>关注于降低时延，尽快完成响应（即需要降低单次垃圾回收的最长停顿时间，意味着内存不会太大，吞吐量也就不会太大）</p><p>适合于互联网行业的高可用，低延迟项目</p><p><strong>适合：CMS，G1，ZGC</strong></p></li></ol><h4 id="优化的考虑——最快的GC是不发生GC"><a href="#优化的考虑——最快的GC是不发生GC" class="headerlink" title="优化的考虑——最快的GC是不发生GC"></a>优化的考虑——最快的GC是不发生GC</h4><p>考虑代码的写法能否避免内存频繁地垃圾回收</p><ol><li><p>数据是不是太多了</p><p>例如：resultSet &#x3D; state</p><p>ment.executeQuery(“select * from user”)</p><p>将一张表的全部数据全都加载到了Java内存中，势必导致许多GC</p><p>解决方法是 limit n 避免无效数据进入内存</p></li><li><p>数据表示是否太过臃肿</p><p>例如：</p><ol><li><p>也要求从数据库中获取数据避免查询没用的</p></li><li><p>对Java对象的大小初步认知</p><p>一个最小的对象：内存占16byte</p><p>基本类型int：4byte</p><p>包装类型Integer：24byte</p><p>所以加入将所有的包装类型下降到基本类型，也可以释放一部分内存</p></li></ol></li><li><p>是否存在内存泄漏</p><p>例如：static Map map &#x3D; …</p><p>一直向声明的静态集合中添加数据，但对之前添加的无用数据不做清理</p><p>做法：</p><ol><li><p>软引用，弱引用</p></li><li><p>用第三方缓存实现对需要长期存在的数据的保存</p><p>比如：redis等，不占用JVM内存，同时拥有自己的垃圾过期算法</p></li></ol></li></ol><h4 id="正式从GC考虑调优"><a href="#正式从GC考虑调优" class="headerlink" title="正式从GC考虑调优"></a>正式从GC考虑调优</h4><h6 id="新生代调优"><a href="#新生代调优" class="headerlink" title="新生代调优"></a>新生代调优</h6><p>新生代的特点：</p><ol><li><p>所有的 new 操作的内存分配非常廉价</p><p>TLAB thread-local allocation buffer </p><p>表示伊甸园区为每个线程分配了一块内存用于创建其对象，于是不必考虑多个线程之间冲突的问题</p></li><li><p>死亡对象的回收代价是0</p></li><li><p>大部分对象是用过即死的</p></li><li><p>Minor GC的时间远远低于Full GC</p><p>原因：处理的对象区域更小，执行的操作相对简单（Full GC是全部GC，不光是老年代）</p><p>时间大概相差1-2个数量级</p></li></ol><p><strong>如何调优新生代</strong></p><p>Question-1：新生代的内存越大越好吗？</p><ul><li><p>首先内存较小会导致minor GC过于频繁</p></li><li><p>但新生代内存较大会挤掉老年代的内存——老年代比新生代更怕内存不足——老年代会触发full GC。</p><p>并且新生代内存如果过大，空出许多本不需要的区域，垃圾回收起来也反而更慢。</p></li><li><p>折中：新生代占整个堆的25%–50%</p><p>新生代的内存只要能够容纳所有的【并发量*（请求-响应）】的数据</p></li></ul><p><strong>如何调优幸存区</strong></p><p>幸存区里是两类对象：当前活跃的对象（幸存者，但即将被回收）+ 需要晋升的对象</p><ul><li><p>需要将即将被回收的对象保留在幸存区，又需要将需要晋升的对象速速晋升到老年代</p><p>原因：</p><ul><li><p>如果一个马上不用的对象被粗略的晋升到老年代，会占用老年代的内存空间，导致full GC更加频繁。而只有在下次full GC时才会被清理。</p></li><li><p>如果一个常用对象一直不被晋升，其在新生代中一直minor GC却一直留下，违背了老年代提出热点对象的初衷。</p></li></ul></li><li><p>所以晋升阈值需要配置得当</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-19-48-57-image.png"></p><p>通过查看幸存区当下的对象（大小，年龄）来实时调整</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-19-50-17-image.png"></p></li></ul><h6 id="老年代调优"><a href="#老年代调优" class="headerlink" title="老年代调优"></a>老年代调优</h6><p>以CMS为例</p><ul><li><p>老年代的内存总体而言越大越好</p><p>原因：CMS中，并发清理时如果清理速度不及其他线程垃圾产生速度就会退化到full GC串行回收，于是会预留一定空间给浮动垃圾。预留空间和总空间大小相关</p></li><li><p>关于预留空间的大小设置（占整个老年代比例）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-19-57-59-image.png"></p><p>常见设置：75%–80%</p></li></ul><h2 id="回来吧我的JVM"><a href="#回来吧我的JVM" class="headerlink" title="回来吧我的JVM"></a>回来吧我的JVM</h2><ol><li><p>属于JVM层面的问题</p><ol><li><p>运行着的线上系统突然卡死，系统无法访问，甚至直接OOM</p></li><li><p>线上项目频繁GC，导致系统时延高</p></li><li><p>新项目上线，要设置各种JVM参数</p></li><li><p>怀疑项目出现内存泄漏，但不知道如何找出泄漏的代码</p></li></ol></li></ol><p>学习JVM的目的：</p><p>生产中的性能调优 –&gt; 需要性能监控方法（命令行&#x2F;可视化工具） –&gt; 需要看得懂监控内容和指标-明白垃圾回收算法和垃圾回收器 –&gt; 垃圾回收是基于内存的-明白内存的结构和分配 –&gt;内存中的数据来自class文件-需要了解class文件的加载过程，解释过程</p><h2 id="一：字节码"><a href="#一：字节码" class="headerlink" title="一：字节码"></a>一：字节码</h2><h3 id="Q1：字节码文件时跨平台的吗？"><a href="#Q1：字节码文件时跨平台的吗？" class="headerlink" title="Q1：字节码文件时跨平台的吗？"></a>Q1：字节码文件时跨平台的吗？</h3><p>Java虚拟机不和包括Java在内的任何语言绑定，它只和.class文件这种特定的二进制文件格式所关联</p><p>无论使用任何语言进行软件开发，只要能将源文件编译成class文件，就可以在JVM上执行</p><p>可以说统一而强大的class文件结构是JVM的基石、桥梁</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-16-15-07-23-image.png"></p><h3 id="Q2：-class文件里面是什么？"><a href="#Q2：-class文件里面是什么？" class="headerlink" title="Q2：.class文件里面是什么？"></a>Q2：.class文件里面是什么？</h3><p>源代码经过编译器之后会生成字节码文件，是一种二进制的类文件（不给人读）。内容是JVM指令</p><h3 id="Q3：如何理解Java是半解释半编译型语言？"><a href="#Q3：如何理解Java是半解释半编译型语言？" class="headerlink" title="Q3：如何理解Java是半解释半编译型语言？"></a>Q3：如何理解Java是半解释半编译型语言？</h3><p>Java将所有.java文件通过前端编译器编译成字节码文件后，具体执行是由两种方式</p><blockquote><p>混合执行（Mixed-mode Execution）</p></blockquote><ol><li><p>解释执行（Interpreted Execution）</p><p>源代码逐行被翻译成机器码，并且翻译过程是在运行是逐行进行的 – 程序是逐行进行的</p><ul><li><p>解释执行的速度相对较慢</p></li><li><p>每次运行都需要重新解释</p></li></ul></li><li><p>即时编译（Just-In-Time Compilation）</p><p>JIT编译器在程序运行时将整个或部分字节码一次性编译成机器码，这种编译发生在程序执行之前（会导致启动稍慢）</p><p>一旦代码被编译成机器码，就会保存在内存中，便于之后的执行 – 不需要每次执行都进行翻译</p></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-16-16-47-23-image.png"></p><ul><li>Java结合两方的优势，在程序启动时为了迅速执行代码使用解释器，同时在运行时会将热点代码由JIT做缓存，确保执行效率高（但最初还未记录热点代码时【预热阶段】速度较慢）</li></ul><h3 id="Q4：介绍一下生成class文件的编译器："><a href="#Q4：介绍一下生成class文件的编译器：" class="headerlink" title="Q4：介绍一下生成class文件的编译器："></a>Q4：介绍一下生成class文件的编译器：</h3><p>此编译器的作用就是将.java源代码编译生成.class字节码文件</p><p>常见的组件有javac，ECJ（Eclipse Compiler for Java）| hotspot VM可以切换使用二者</p><ul><li><p>javac：默认使用，但每次编译都会从头开始</p></li><li><p>ECJ：ECJ编译器采取的编译方案是：增量式编译 – 把未编译的部分的源码逐行编译</p><p>因此回避javac快很多</p></li></ul><p>为什么不选ECJ？</p><ul><li><p>似乎因为Javac是Oracle JDK和OpenJDK提供的标准编译器，和其他部分更兼容吧</p><p>或者因为javac就在JDK中，使用也比较简单</p></li></ul><h3 id="Q5：前端编译器将-java编译成-class文件的流程："><a href="#Q5：前端编译器将-java编译成-class文件的流程：" class="headerlink" title="Q5：前端编译器将.java编译成.class文件的流程："></a>Q5：前端编译器将.java编译成.class文件的流程：</h3><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-16-17-09-39-image.png"></p><h3 id="Q6：哪些类型对应有Class对象？"><a href="#Q6：哪些类型对应有Class对象？" class="headerlink" title="Q6：哪些类型对应有Class对象？"></a>Q6：哪些类型对应有Class对象？</h3><p>类加载器将字节码文件转化成在方法区中的类</p><p>方法区中就存储着类的结构信息 - 字段信息&#x2F;方法信息&#x2F;构造方法&#x2F;常量池…</p><p>方法区所说的这些类对象，就是代码中 Class clazz &#x3D; int.class;得到的clazz</p><p>有哪些类有Class对象？</p><ul><li><p>class ：</p><p>外部类、内部类、匿名内部类</p></li><li><p>interface</p></li><li><p>[] 数组</p></li><li><p>enum 枚举</p></li><li><p>annotation 注解</p></li><li><p>primitive type 基本数据类型</p></li><li><p>void</p></li></ul><h3 id="Q7：什么是字节码指令（byte-code）？"><a href="#Q7：什么是字节码指令（byte-code）？" class="headerlink" title="Q7：什么是字节码指令（byte code）？"></a>Q7：什么是字节码指令（byte code）？</h3><p>JVM的指令由一个字节长度的、代表都中特定操作含义的<strong>操作码</strong> 以及跟随其后的0到多个<strong>操作数</strong> 所构成</p><p>是字节码文件中的具体指令  —-  就是.class可视化后的一部分</p><p>是虚拟机能够理解和执行的操作码</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-16-17-24-27-image.png"></p><h3 id="Q8：关于包装类对象的缓存问题："><a href="#Q8：关于包装类对象的缓存问题：" class="headerlink" title="Q8：关于包装类对象的缓存问题："></a>Q8：关于包装类对象的缓存问题：</h3><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-11-16-17-39-33-image.png" title="" alt="" width="352"><p>Integer内部类有一个IntergerCache，内部有一个数组[-128,127]表示热点数的缓存（启动时完成256个Integer的new）</p><p>创建对象时调用valueOf方法，过程中判断是否在池子中</p><blockquote><p>怎么看到这个赋值过程怎么执行的？</p><ul><li>查看字节码文件中的字节码指令</li></ul></blockquote><ul><li><p>Others：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-16-18-00-20-image.png"></p></li></ul><p>为什么Float和Double没有缓存？</p><ul><li>因为没有热点数据 &#x2F; []</li></ul><h2 id="Q9：如何解读字节码文件？"><a href="#Q9：如何解读字节码文件？" class="headerlink" title="Q9：如何解读字节码文件？"></a>Q9：如何解读字节码文件？</h2><ol><li><p>按照16进制打开文件并读取，但人不可读</p></li><li><p>用JClassLib插件读取，可获取到字节码指令</p></li></ol><h3 id="Q10：class文件结构是什么？"><a href="#Q10：class文件结构是什么？" class="headerlink" title="Q10：class文件结构是什么？"></a>Q10：class文件结构是什么？</h3><blockquote><p>由于字节码文件是纯二进制数据，所以部分内容要么定长度，要么前置元数据标明长度</p></blockquote><ul><li><p>魔数</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-17-19-15-27-image.png"></p></li><li><p>Class文件版本</p><p>2 bytes 副版本 2bytes 主版本</p><p>主版本表示JDK版本，数值45&#x2F;46&#x2F;…&#x2F;52&#x2F;53&#x2F;…对应JDK1.8,1.9</p><p>存在在.class文件表示：JVM只能解释运行之前版本（向下兼容），如果版本数过高则拒绝执行</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-17-19-17-28-image.png"></p></li><li><p>常量池计数器</p><p>2 bytes表示常量池容量计数值</p><ul><li><p>计数值从1开始，1表示有0项</p><p>刻意把第0项常量空出来，是为了满足后面某些指向常量池的索引值的数据在特定情况下需要表达：不引用任何一个常量池项目的含义，此时索引值是0；</p></li></ul></li><li><p>常量池</p><p>存放所有常量</p><ul><li><p>常量池是Class文件中的资源仓库，Class文件中其他结构和常量池关联很多</p></li><li><p>常量池也是Class文件中占用空间最大的项目</p></li><li><h3 id="Q10-1：解释一下常量池"><a href="#Q10-1：解释一下常量池" class="headerlink" title="Q10.1：解释一下常量池"></a>Q10.1：解释一下常量池</h3><p>常量池中的数据分为字面量和符号引用</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-17-19-32-10-image.png"></p><h3 id="Q10-1-1：说明一下符号引用和直接引用的关系"><a href="#Q10-1-1：说明一下符号引用和直接引用的关系" class="headerlink" title="Q10.1.1：说明一下符号引用和直接引用的关系"></a>Q10.1.1：说明一下符号引用和直接引用的关系</h3><ul><li><p>符号引用：在还未加载的.class文件中，方法或属性对常量池中常量的引用只是标记了其在常量池内的偏移量；</p><p>即通过常量池找到某个常量的位置 </p></li><li><p>直接引用：指直接指向内存中目标的引用</p></li></ul><p>.class文件中保留着符号引用，在将Class文件加载到内存中时，通过符号引用获取到其真实内存地址，才能实现直接引用</p></li></ul></li><li><p>访问标识（标志）</p><p>2 bytes，用于表示类&#x2F;接口层面的访问信息</p><p>例如 是类还是接口、是public还是什么、是否是abstract，是否被声明为final</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-17-19-48-46-image.png"></p></li><li><p>类索引、父类索引、接口计数器、接口索引集合</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-17-19-49-50-image.png"></p></li><li><p>字段表集合</p><blockquote><p>由于字段叫什么、字段是什么类型、都是无法固定的，所以只能将其放入常量池、再引用常量池中</p><p>所以每个字段在这里的结构都是一样的，可以直接对应到具体内容</p></blockquote><ul><li><p>字段计数器</p></li><li><p>对于每个字段：</p><ul><li><p>标识符</p></li><li><p>访问修饰符</p></li><li><p>是否是static</p></li><li><p>是否是final</p></li><li><p>是否是volatile</p></li><li><p>是否序列化</p></li><li><p>字段数据类型</p></li><li><p>字段名称</p></li></ul></li></ul></li><li><p>方法表集合</p></li><li><p>属性表集合</p></li></ul><h3 id="Q11：回来吧我的垃圾回收"><a href="#Q11：回来吧我的垃圾回收" class="headerlink" title="Q11：回来吧我的垃圾回收"></a>Q11：回来吧我的垃圾回收</h3><blockquote><p>GC（Gabage Collection）：由于内存处理是编程人员最容易出现问题的地方，不当的回收可能会导致程序不稳定甚至崩溃；Java提供的GC功能可以自动检测对象是否超过作用域从而达到自动回收内存的目的；</p><p>Java没有提供显式释放已分配内存的方法，只有System.gc()可以触发GC运行</p><ul><li>当程序员创建对象时，GC就开始监控对象的地址、大小、使用情况</li></ul></blockquote><p><img src="https://pic2.zhimg.com/80/v2-2648bf4a93fbab62c0f71b86de7551fd_720w.webp"></p><blockquote><p>gc要做的三件事情：</p><ol><li><p>明确哪些内存需要回收</p><blockquote><p>判断哪些对象已经死亡</p><ul><li><p>引用计数法</p></li><li><p>GC Root 标记法</p></li></ul></blockquote></li><li><p>什么时候回收</p><blockquote></blockquote></li><li><p>怎么回收</p><blockquote><p>垃圾收集算法（方法论）</p><ul><li><p>标记清除</p></li><li><p>标记复制</p></li><li><p>标记整理</p></li><li><p>分代收集</p></li></ul></blockquote><hr><blockquote><p>垃圾收集器（具体实现）</p><p><img src="https://pic3.zhimg.com/80/v2-f5426caf176fac7d2d7edcf43e127eaa_720w.webp"></p></blockquote></li></ol></blockquote><p>细说分代收集：</p><p><img src="https://pic3.zhimg.com/80/v2-92c31a748102fd71381367a4e3060df6_720w.webp"></p><p>此图表示：大部分的对象都是朝生夕死，经过一次MinorGC后都会被回收；由于对象的存活周期不同，将堆分成新生代和老年代，默认比例为1：2，这样才能根据分区的特点选择合适的垃圾回收算法；新生代基于标记复制算法优化，又区分出Eden区，From Survivor区和To Survivor区，三者比例为8：1：1</p><p>很多设定都是为了避免对象过早进入老年代，尽可能减少触发Full GC。</p><p>两个Survivor以及设置年龄阈值都是为了这个条件</p><ol><li><p>对象在新生代的分配与回收</p><blockquote><p>创建的对象首先分配到Eden区，当Eden区将满时，触发Minor GC</p><blockquote><p>大部分的对象都活不过这次Minor GC</p></blockquote><p>少部分存活下来的被移动到S0（From Survivor）区，同时将存活下来的对象年龄+1；</p><p>移动完后清理Eden区（清空）</p><p>下一次minor GC时，尝试回收Eden和From区的对象，最后将幸存的对象移入To区，年龄 +1；并将From区和To区交换</p><blockquote><p>标记 - 复制算法</p><p>且根据实际情况（大部分都死了）安排了一块小的复制空间，且通过算法，避免… &#x2F;&#x2F;TODO</p></blockquote></blockquote><p>当对象的年龄到达设定阈值，则从S0 &#x2F; S1晋升到老年代</p><p>特殊情况：</p><blockquote><ul><li>大对象 当某个对象分配需要大量的连续内存时，此时对象的创建不会分配在 Eden 区，会直接分配在老年代，因为如果把大对象分配在 Eden 区, Minor GC 后再移动到 S0,S1 会有很大的开销（对象比较大，复制会比较慢，也占空间），也很快会占满 S0,S1 区，所以干脆就直接移到老年代.</li><li>还有一种情况也会让对象晋升到老年代，即在 S0（或S1） 区相同年龄的对象大小之和大于 S0（或S1）空间一半以上时，则年龄大于等于该年龄的对象也会晋升到老年代。</li></ul></blockquote></li></ol><hr><h2 id="回来吧我的JVM-1"><a href="#回来吧我的JVM-1" class="headerlink" title="回来吧我的JVM"></a>回来吧我的JVM</h2><ol><li><h5 id="Java概述"><a href="#Java概述" class="headerlink" title="Java概述"></a>Java概述</h5><blockquote><p>Java技术体系提供了完整的用于软件开发和跨平台部署的支持环境</p><ul><li><p>有一个结构严谨、面向对象的语言</p></li><li><p>能够通过JVM拜托硬件平台的束缚</p></li><li><p>提供相对安全的内存管理和访问机制，避免绝大部分内存泄漏和指针越界</p></li><li><p>热点代码检查和运行时编译，保证随着运行获得更高的性能</p></li><li><p>拥有一套完整的API，各种商业机构&#x2F;开源社区的第三方库</p></li></ul><p>…</p></blockquote></li><li><h5 id="GraalVM"><a href="#GraalVM" class="headerlink" title="GraalVM"></a>GraalVM</h5><blockquote><p>真正意义上 与物理计算机相对应的高级语言虚拟机——至于物理硬件提供不同指令集相关，而与语言无关；能够将任何语言的源代码或者源代码编译后的中间格式【GraalVM 引入了一层 Truffle framework，类似定义了用于编译功能的接口，实现这个接口的语言就可以运行在此虚拟机上】，通过<strong>解释器</strong>转化成能够被GraalVM接受的中间表示</p><blockquote><p>即：设计各种解释器专门针对某种语言，将其转化成中间表示（IR）</p></blockquote><ul><li>和其他语言传统运行的环境相比，GraalVM能提高运行效率：编译自动优化、即时编译热点代码…</li></ul></blockquote><hr><blockquote><p>还提供了Graal 编译器，用来代替C2编译器</p><p>使用更高级 &#x2F; 更复杂 &#x2F; 更激进的优化算法</p></blockquote></li><li><h5 id="JVM的运行时数据区"><a href="#JVM的运行时数据区" class="headerlink" title="JVM的运行时数据区"></a>JVM的运行时数据区</h5><ol><li><p>程序计数器：当前线程所指向的字节码的行号指示器</p><blockquote><p>字节码解释器的工作 就是改变计数器的值，来选取下一条要执行的指令</p></blockquote><p>通过程序计数器实现程序控制流的变化，例如循环、分支、跳转、异常、陷入…</p></li><li><p>java虚拟机栈：描述线程执行方法的情况，每个栈帧包括局部变量表、操作数栈、方法出口，动态链接等.</p><blockquote><p>可能出现的异常情况：</p><ol><li><p>StackOverflow</p></li><li><p>OOM：当允许栈扩展但栈扩展失败时</p></li></ol></blockquote></li><li><p>本地方法栈：作用和虚拟机栈类似，但是为了本地方法调用服务</p><blockquote><p>hotSpot虚拟机将本地方法栈和虚拟机栈合二为一</p></blockquote></li><li><p>Java堆：目前版本中，所有对象实例和数组都分配在堆上</p><blockquote><p>下个版本出现值类型、栈上分配技术、标量替换技术…</p></blockquote><p>所有线程都尝试访问共享的堆，势必为了防止并发问题要有性能上的损失</p><ul><li>解决方案之一：Java在新生代又分出多个线程私有的分配缓冲区，用于提升对象分配的效率</li></ul></li><li><p>方法区：抽象的规范，用于存放类型信息、常量、静态变量、即时编译后代码缓存</p><ul><li><p>早期:hotSpot虚拟机设计中，在内存层面方法区和老年代相邻，并且内部的元素都不易被淘汰，于是叫做永久代</p><p>此时永久代的垃圾回收和老年代绑定，无论谁满都会触发老年代垃圾回收</p></li><li><p>Java8取消永久代，方法区的实现更加零散</p><ul><li><p>字符串常量池转移到Java Heap</p></li><li><p>静态变量转移到java Heap</p></li><li><p>类信息转移到元空间</p><blockquote><p>元空间存在于本地内存中，垃圾回收不受JVM控制，空间不足时也不会触发GC或者OOM，而是由系统实际可用空间来控制</p></blockquote></li></ul></li></ul><hr><p>组成：</p><ol><li><p>运行时常量池：</p><blockquote><p>包括各种class文件的字面量和符号引用（静态加载进来）</p><p>以及运行时动态加载来的（String.intern()）；</p></blockquote></li><li><p>直接内存：</p><blockquote><p>虽然不受JVM管理，但收到本机总内存的限制</p></blockquote></li></ol></li></ol></li><li><h5 id="对象的创建："><a href="#对象的创建：" class="headerlink" title="对象的创建："></a>对象的创建：</h5><blockquote><p>以new关键字为例，其余还有反序列化、复制</p></blockquote><ol><li><p>检查指令的参数能否在常量池中定位到类的符号引用，并检查此类是否已经加载过了，若无则触发类加载</p></li><li><p>虚拟机为新生对象分配内存</p><ul><li><p>问题1：对象大小确定吗：</p><p>对象设计为8字节的整数倍，其中存储对象头信息，字段信息和对齐填充，其中并无要动态扩容的信息</p></li><li><p>如何分配内存空间：</p><p>取决于垃圾回收器决定的内存布局，</p><p>标记整理的垃圾回收器可以依次紧密排列，通过指针指明占用和空闲空间的界限，标记清除的垃圾回收器只能再维护一张空闲列表</p><blockquote><p>维护空闲表的成本也不低，尤其在内存中可能会拖慢响应速度</p></blockquote></li><li><p>解决并发安全问题：</p><blockquote><p>分配空间的操作保证不了原子性</p></blockquote><p>方案：</p><ol><li><p>使用乐观锁CAS机制，配合失败重试方式实现串行</p></li><li><p>TLAB：本地线程分配缓冲，只需要解决空间不足时申请空间时的并发问题【有点像leaf中的双buffer优化】；将原本很多的并发的场景合并，最后只需要解决少量并发</p></li></ol></li></ul></li><li><p>内存层面初始化：将原本可能被垃圾占用的空间覆盖成0值</p></li><li><p>对象初始化：修改对象头的初始化信息，例如分代的年龄信息，属于哪个类的reference信息，hashcode…</p></li><li><p>程序中，对象的初始化：执行构造函数</p></li></ol></li><li><h5 id="垃圾回收："><a href="#垃圾回收：" class="headerlink" title="垃圾回收："></a>垃圾回收：</h5><blockquote><p>要解决的三件事情：</p><ol><li><p>哪些内存需要回收</p></li><li><p>什么时候回收</p></li><li><p>怎么回收</p></li></ol></blockquote><p>垃圾回收算法：</p><ul><li><h6 id="分代收集："><a href="#分代收集：" class="headerlink" title="分代收集："></a>分代收集：</h6><blockquote><p>经验法则：建立在两个假说之上</p><ol><li><p>大多数对象朝生夕灭</p></li><li><p>命越久的对象越难以消亡</p></li></ol><p>于是理应将不同生命周期的对象分类管理，采用不同的回收机制</p><ul><li><p>问题：</p><blockquote><ol><li><p>跨代引用问题</p><p>由于原本全场一起可达性分析，假如某对象的上层消亡了自身也就消亡；但在分代回收中，可达性分析表明新生代某个对象需要回收，但无法回收其引用对象（在老年代），所以新生代的也不能回收</p></li></ol><p>假说：跨代引用相对于同代引用而言很少</p><p>解决：既然少，就可以尝试牺牲额外的空间 &#x2F; 时间解决</p><blockquote><p>在新生代建立一个记忆集，表明哪些对象被老年代引用</p></blockquote></blockquote></li></ul></blockquote></li><li><h6 id="标记-清除"><a href="#标记-清除" class="headerlink" title="标记 - 清除"></a>标记 - 清除</h6><blockquote><p>缺点：内存碎片化，管理困难；连续空间不足容易触发更高级的GC</p></blockquote></li><li><h6 id="标记-复制"><a href="#标记-复制" class="headerlink" title="标记 - 复制"></a>标记 - 复制</h6><blockquote><p>早期方法：拿出一半的复制区，两边做彼此的回收空间</p><p>优化方法：将新生代分出80%伊甸园，10%To Survivor，10%From Survivor</p><ul><li>优化是因为对象的朝生夕死，一次GC能杀掉98%的新对象，并不需要等大的空间</li></ul><blockquote><p>二者的关系并非优化后决定比之前好</p><p>优化后的方法每次能存放90%空间大小的对象，其中最多只能活10%，假如GC存活的对象超过10%，则触发更高级GC</p><ul><li><p>于是10%大小可能比较小，往大只有设置成50%才最保险</p></li><li><p>老年代存活比重较大，不适合M-C</p></li></ul></blockquote></blockquote></li><li><h6 id="标记-整理"><a href="#标记-整理" class="headerlink" title="标记 - 整理"></a>标记 - 整理</h6><blockquote><p>矛盾：GC时是否移动对象：移动则GC时间长，不移动则分配时间长——不同目标的垃圾回收器偏好不同</p><p>偏向吞吐量的垃圾回收器选择标记 - 整理（Parallel Old）</p><p>偏向响应时间的垃圾回收器选择标记 - 清理（CMS）</p></blockquote></li></ul><p>垃圾回收器：</p><p>&#x2F;&#x2F;TODO</p></li><li><h5 id="类文件结构"><a href="#类文件结构" class="headerlink" title="类文件结构"></a>类文件结构</h5><blockquote><p>无关性：</p><p>虽然所有程序最后都要编译成机器码，并且不同的操作系统，不同的硬件支持的指令集不同；</p><p>平台无关性的实现只能在操作系统之上的应用中实现：JVM</p><p>而不同语言最终编译成.class字节码文件，就是无关性的基石</p><p>无关性：既有平台无关性，也有语言无关性：不仅常规运行在JVM上的Scala，Groovy等，未来还有python，R，JS运行在graalVM上</p></blockquote><p>具体结构：</p><blockquote><p>Class文件是单纯的二进制流，其中不想XML，没有分割符号，目的就是最大程度的压缩空间</p><p>所以在无法使用分隔符场景下，每个字节表示的意义，顺序都定死</p><p>为了压缩空间，class文件还进行了很大程度的抽象，体现在字段表，方法表的设计上</p></blockquote></li><li><h5 id="类加载机制"><a href="#类加载机制" class="headerlink" title="类加载机制"></a>类加载机制</h5><blockquote><p>类加载的过程：</p><ol><li><p>加载</p><ol><li>通过类的全限定名获取定义此类的二进制流</li><li>将字节流代表的静态存储结构转化成方法区的运行时数据结构</li><li>在堆内存中生成代表此类的Class对象，作为这个类在方法区的访问入口</li></ol><blockquote><p>从哪里获取class信息相当自由，衍生出许多新技术【不同的类加载器】</p><ol><li>从ZIP包中读取，发展出JAR、WAR包</li><li>从网络中获取，发展出Applet</li><li>运行时计算生成，发展出JDK动态代理</li><li>从数据库读取</li><li>…</li></ol></blockquote></li><li><p>验证</p><blockquote><p>目的是确保Class文件的字节流信息符合规定，不会破坏程序</p><p>验证是由于Class文件并非全部来自编译器，并不能完全相信</p><p>但是验证的过程耗时很长，尤其要验证代码逻辑中有无越界，类转换异常等等需要很多时间</p><p>解决方案之一是将验证过程前移到编译器，通过一个校验码来确保中间没有被修改过</p></blockquote></li><li><p>准备</p><p>将静态变量分配到内存（方法区）中</p></li><li><p>解析</p><blockquote><p>装入</p></blockquote><p>将常量池的符号引用转化成直接引用</p></li><li><p>初始化</p><blockquote><p>执行代码逻辑，例如静态代码块</p></blockquote></li></ol></blockquote></li><li><p>类加载器</p><p>【由一个类的全限定名，获取类的二进制字节流】</p><p>将此功能定义为接口，给不同实现功能很大的技术创新</p><p>应用场景：</p><ol><li>动态类加载</li><li>热部署（通过自定义类加载器实现）</li><li>OSGi：动态模块系统</li><li>代码加密…</li></ol><blockquote><p>每一个类加载器，都拥有一个独立的命名空间 &#x2F; 两个类相等意味着在同一个命名空间下的同一个类 &#x2F; 每个类加载器加载类是相互独立的</p><p>自定义类加载器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs JAVA"><span class="hljs-type">ClassLoader</span> <span class="hljs-variable">loader</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassLoader</span>()&#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class&lt;?&gt; loadClass(String name) <span class="hljs-keyword">throws</span> ClassNotFoundException&#123;<br>        <span class="hljs-comment">//具体实现方案</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>在Java中共有两类类加载器：</p><ol><li>启动类加载器（由C++编写，将基础类库里的class加载进内存）</li><li>其他所有加载器（java原生，都是ClassLoader子类）</li></ol><blockquote><p>三层类加载器模型：</p><ol><li>启动类</li><li>扩展类</li><li>应用程序加载类</li><li>三层之外，用户还可以加入自定义类加载器进行扩展</li></ol></blockquote></blockquote></blockquote><blockquote><blockquote><p>但是大多数的类都是公共的，少部分类需要由特殊的加载器加载，解决此需求于是提出双亲委派模型</p></blockquote><h6 id="双亲委派："><a href="#双亲委派：" class="headerlink" title="双亲委派："></a>双亲委派：</h6><blockquote><p>如果一个类加载器收到类加载的请求，首先不会亲自尝试加载这个类，而是把请求委派给父类，每一层都是如此；因此所有加载请求最终都会传送到最顶层的启动类加载器，只有当父类无法完成这个加载请求时（父类的扫描范围没有这个文件），子加载器尝试自己完成加载</p><blockquote><p>要求：类的等级（优先级）有划分——最重要的类（最常被加载的类）要在启动类扫描范围下</p></blockquote></blockquote><p>&#x2F;&#x2F;TODO：双亲委派</p></blockquote></li><li><h5 id="字节码执行引擎："><a href="#字节码执行引擎：" class="headerlink" title="字节码执行引擎："></a>字节码执行引擎：</h5><blockquote><p>虚拟机执行引擎与物理机执行引擎：</p><p>物理机执行引擎：建立在处理器，缓存，指令集，操作系统之上</p><p>虚拟机执行引擎：由软件提供，所使用的指令集和引擎结构体系在物理层上封装完成，更加灵活</p><h4 id="执行分为解释执行-编译执行"><a href="#执行分为解释执行-编译执行" class="headerlink" title="执行分为解释执行 &amp; 编译执行"></a>执行分为解释执行 &amp; 编译执行</h4><ul><li><p>解释执行：</p><blockquote><p>解释执行过程中，方法是最基本的执行单元，每个方法对应JVM虚拟机栈中的一个栈帧，栈帧中完成方法的逻辑操作</p></blockquote><p>栈帧的内容：</p><ol><li><p>局部变量表：</p><blockquote><p>存放方法的参数和定义的一些局部变量</p><p>这些变量基本上对整个方法都是全局可见的，所以没法在栈中分配</p><blockquote><p>在将Java文件编译成Class文件时，class中就明确了局部变量表的内容；并按照变量的生命周期尝试共用一个变量来减少空间</p><p><code>能够体现：Class文件的设计也是为了支持执行引擎快速工作</code></p></blockquote></blockquote><p>当方法被调用时，虚拟机也是通过局部变量表完成参数的传递</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java">关于局部变量表，有一个优化点：<br>    垃圾回收的可达性分析就是通过局部变量表确定的GC Root<br>    为了让没用的引用对象尽快被回收，应该考虑让没用 / 用完的变量在局部变量表中消失<br>    消失的方案有：<br>    - 离开作用域后被新的变量复用这个变量槽（<br>        方法内限定变量作用域 —— 要么用&#123;&#125;，要么用内部类，都挺少见）<br>    - 结束后手动设置为<span class="hljs-literal">null</span><br></code></pre></td></tr></table></figure></li><li><p>操作数栈：</p><p>就是操作数的栈</p></li><li><p>动态链接：</p><p>每个栈帧包含一个指向运行时常量池中该栈帧所属方法的引用</p><blockquote><p>目的是为了支持动态连接</p></blockquote></li><li><p>方法返回地址</p></li></ol><h3 id="方法调用："><a href="#方法调用：" class="headerlink" title="方法调用："></a>方法调用：</h3><blockquote><p>指如何找到具体调用哪个方法</p></blockquote><p>在Class文件中记录的方法调用，全都只是对常量池内的符号引用，而不是运行时期正在的方法入口，所以需要在某一时刻将符号引用转变成物理地址（直接引用）</p><ul><li><p>静态解析：</p><blockquote><p>在类加载的解析阶段，其实可以确定某些符号引用的具体物理地址</p><p>只要保证叫这个名字的方法只有唯一实现（不存在重载 &#x2F; 重写 &#x2F; 不同实现…）</p><p>符合要求的有：</p><ol><li>静态方法</li><li>私有方法</li><li>final修饰的方法</li></ol></blockquote></li><li><p>分派：</p><blockquote><p>在一些比较类似的方法中确定调用哪个</p></blockquote><ul><li><p>静态分派：</p><blockquote><p>指编译器能做到的选择 &#x2F; 不需要虚拟机运行就能确定</p><blockquote><p>引理：由于多态导致一个类对象可能对外呈现是其父类 &#x2F; 接口</p><p>所以有两个概念：</p><ul><li>外观类型</li><li>实际类型</li></ul><blockquote><p>两种类型都可以相对变化</p><ul><li>外观类型可以通过强制转型，在编译期间转变</li><li>实际类型是谁编译期间无法确定，只能运行时确定</li></ul></blockquote></blockquote><p>静态分派能够解决重载的问题，因为根据传入参数对象的外观类型可以确定一个方法；而外观类型在编译期间即可确定，所以叫做静态</p><blockquote><p>特殊情况：根据传入参数能够匹配多个重载的多个方法；因为参数是多态的，任何一个父类 &#x2F; 接口都符合，所以编译器只能找一个最接近的方法</p></blockquote></blockquote></li><li><p>动态分派：</p><blockquote><p>用来实现重写，以及在重写方法中正确选择</p></blockquote><blockquote><p>基本思想：对于运行时才能确定真实类型的对象，方法调用时是根据其真实类型完成调用；</p><p>真实类型找不到说明继承的父类，再去父类中找</p><p>具体而言就是invokevirtual方法的执行</p></blockquote><p>动态分派的实现方案：</p><blockquote><p>理论OK，实现只是各种优化措施</p><ul><li>优化1：虚方法表：原本遍历方法区的每个方法，找匹配的一个，匹配不了找父类方法区，优化到：维护一个表，记录真实的入口</li><li>优化2：类型继承关系分析</li><li>优化3：守护内联</li></ul></blockquote></li><li><p>单分派 &#x2F; 多分派</p><blockquote><p>方法的接收者和方法参数统称为方法的宗量。单分派是指基于一个宗量进行选择，多分派则指多个</p></blockquote><blockquote><p>Java属于</p><p>静态多分派：重载下要根据调用者和参数共同确定</p><p>动态单分派：重写时只需要根据调用者确定</p></blockquote></li><li><p>语言的动态性：</p><blockquote><p>动态语言：</p><ul><li>类型检查的主体过程在运行期而不是编译期</li><li>变量无类型，而变量的值有类型 &#x2F; 一个变量只有真实类型，没用外观类型——导致：编译期间能检查的很有限，许多只能在运行期间知道</li></ul></blockquote><ol><li><p>编译期间有些情况可以推断外观类型实际的类型是什么，少量情况不行（random）</p><p>于是推出var关键字，用于根据右侧推断左侧类型，但是某些场景下推断不出来，于是编译时无法编译此类调用方法</p></li><li><p>c # 的dynamic类型</p><blockquote><p>var其实是编译的语法糖，和真实执行无关，而dynamic类似一种动态类型——在编译时期完全不关心是什么类型，运行时才进行类型判断</p></blockquote><blockquote><p>感觉也很想JS，python的动态类型</p></blockquote></li><li><h5 id="Java对动态语言的执行："><a href="#Java对动态语言的执行：" class="headerlink" title="Java对动态语言的执行："></a>Java对动态语言的执行：</h5><ol><li><p>方法句柄类：MethodHandler</p><p>一个MethodHandler对象，表示一个类下的一个具体方法的句柄，具体是哪个类哪个方法，编译期不清楚</p><p>如何构造一个MethodHandler？</p><blockquote><p>模拟动态分派的过程：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">MethodType</span> <span class="hljs-variable">mt</span> <span class="hljs-operator">=</span> MethodType.methodType(<span class="hljs-keyword">void</span>.class,<br>                      String.class);<br><span class="hljs-comment">//确定方法描述符</span><br><span class="hljs-keyword">return</span> lookup().findVirtual(reveiver.getClass(),<br>                     <span class="hljs-string">&quot;println&quot;</span>,<br>                     mt).bindTo(receiver);<br><span class="hljs-comment">//在传入对象中动态寻找匹配的方法</span><br></code></pre></td></tr></table></figure><p>即：在运行时模拟invokeVirtual</p></blockquote></li></ol></li></ol></li></ul></li></ul></li><li><p>基于栈的执行引擎 &#x2F; 基于栈的指令集： </p><p>优点：</p><ol><li><p>相比于基于寄存器的指令集需要依靠硬件，基于栈则完全依靠软件逻辑—— 是物理机 和 虚拟机 的区别【可移植性】</p></li><li><p>代码相对紧凑</p></li></ol><p>缺点：</p><ol><li>栈是在内存中维护的，频繁访问内存速度不如访问硬件</li></ol></li></ul></blockquote></li><li><p>前端编译与优化</p><blockquote><p>前端编译 &#x3D;&#x3D; 编译的前半段 &#x3D;&#x3D; .java -&gt; .class文件</p><p>后端编译 &#x3D;&#x3D; 编译的后半段 &#x3D;&#x3D; .class -&gt; 机器码（JIT即时编译 &#x2F; AOT提前编译）</p></blockquote><hr><blockquote><p>前端编译中，有一招插入式注解处理器</p><p>常规注解在运行时生效，插入式注解在编译时可修改代码，例如lombok的@Data</p></blockquote><hr><blockquote><p>前端编译的优化点：语法糖</p><blockquote><p>提高效率，能够让业务的思维连续，不必因为业务无关的技术问题分心</p><p>减少错误</p></blockquote><h4 id="泛型："><a href="#泛型：" class="headerlink" title="泛型："></a>泛型：</h4><blockquote><p>参数化类型——让程序员针对泛化的类型编写相同的代码，提高语言的抽象能力</p></blockquote><hr><ul><li><p>Java与C#的泛型</p><blockquote><ol><li><p>Java：类型擦除式泛型</p><ul><li>泛型只在源码中存在，编译后的字节码文件中，全部翻新被替换成裸类型，弥补方案是使用的时候强制转化</li></ul></li><li><p>C#：具现化式泛型</p><ul><li>无论在源码、编译后的中间语言、还是运行期间的CLR中，泛型表示的类型都真实存在，例如List&lt;int&gt;，List&lt;String&gt;</li></ul></li></ol><p>C#的高级用法：</p><blockquote><p>由于C#中的泛型在运行期间真实存在，就可以参与代码逻辑</p><p>例如：</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs c#"><span class="hljs-comment">//在某个泛型类中，创建泛型类的具体对象</span><br><span class="hljs-keyword">if</span>(item instanceof E)&#123;<br>&#125;<br>E e = <span class="hljs-function">mew <span class="hljs-title">E</span>()</span>;<br>E[] itemArray = <span class="hljs-keyword">new</span> E[<span class="hljs-number">10</span>];<br></code></pre></td></tr></table></figure><p>java中由于.class文件中早已没有泛型了，所以这个正常的逻辑无法执行，只能曲线救国——传入一个表示类型Class对象，让这个对象在运行期间表明原本泛型所表示的类型</p></blockquote><p>Java类型擦除带来的问题：</p><ul><li><p>运行效率较差：</p><p>Java由于擦除过程中给每个泛型类都回归成其Row Type裸类型，但基本数据类型需要转化成包装类，才能符合之前的设计</p><p>装箱拆箱的过程虽然自动，但也有点成本</p><p>其实就是调用Integer.valueof()和Integer.intValue()；</p><p>其中内存中会维护Integer的池子，范围在-128到127，池子之外的Integer。每回装箱就需要new，每回拆箱就可能被回收</p></li><li><p>好处是：class文件原封不动，早期的代码照常运行</p></li></ul><p>Java由于历史原因选择擦除式泛型</p><ol><li><p>第一个岔路：如何兼容之前的代码</p><ul><li><p>C#选择平行开发用泛型技术的新的容器，让开发者平滑迁移</p></li><li><p>Java由于当时就有新集合ArrayList和就集合Vector，再有支持泛型的集合和不支持泛型的集合显得有点冗余</p><p>于是Java选择原地变成泛型</p></li></ul></li></ol><p>原地变成支持泛型的容器也不意味着就要用类型擦除，只是说之前创建的ArrayList要想直接变成ArrayLsit&lt;String&gt;，需要保证裸类型的ArrayList是泛型的ArrayList的父类</p><ol start="2"><li><p>第二个岔路：</p><blockquote><p>如何实现新ArrayList能够顺利替代老ArrayList</p></blockquote><ul><li><p>运行时真实创建ArrayList&lt;String&gt;这个类，并且声明成ArrayList的子类</p></li><li><p>直接把ArrayList<String>还原成ArrayList</p></li></ul></li></ol><p>未来的解决：</p><ul><li><p>值类型（c#中的int，bool…不同于Java的原生数据类型，而是继承自Object的一个类，于是就可以作为容器的原宿避免装箱拆箱）</p><p>介于int和Integer之间，和引用数据类型的区别在于，值类型的分配是在调用栈上，而不是堆里</p></li><li><p>内联类型</p></li></ul></blockquote></li></ul></blockquote></li><li><p>后端编译</p><ol><li><p>即时编译器</p><blockquote><p>早期 Java 程序都是通过解释器解释执行，但当虚拟机发现某个方法或代码块的运行特别频繁，就会认为这块代码是热点代码，为了提高执行效率，会把这些代码编译成本地机器码，并以各种手段进行代码优化</p></blockquote><p>Java虚拟机一般都采用解释器与编译器并存的运行架构</p><ul><li><p>解释器的优点：启动迅速，省去编译的时间，直接执行字节码指令</p></li><li><p>编译器的优点：随着时间推移，越来越多的代码被编译成本地代码，相较于直接执行机器码，解释执行时执行字节码相当于还要在过程中多一步编译操作</p><p>但编译热点代码也是概论问题，无法预知未来哪些代码会被执行；于是可能出现优化比较激进，此时需要解释器充当逃生门</p></li></ul></li><li><p>分层编译：</p><p>未来找到解释执行和编译执行的最佳平衡点，划分了不同的编译层次</p><blockquote><p>不同的编译层次适合不同场景，因为本身许多指标是矛盾的，不同层次追求不同指标【动态调整解释 + 编译的模式】</p></blockquote><ul><li><p>0层：不编译执行，纯解释 – 启动最快</p></li><li><p>1层：使用客户端编译器进行简单的编译，代码优化程度不高</p></li><li><p>2层：使用客户端编译器，解释器负责执行统计工作</p></li><li><p>3层：使用客户端编译器，解释器负责执行全部统计工作</p></li><li><p>4层：使用服务端编译器，耗时更长，优化更好 – 效率最高</p></li></ul></li><li><p>提前编译（AOT）</p><blockquote><p>即时编译的问题：</p><ol><li>占用运行时资源</li></ol><p>于是提出设想：如果能够在运行之前静态编译，那无论多复杂的优化都可以执行</p></blockquote></li></ol></li><li><p>Java内存模型与线程：</p><blockquote><p>起因：不同的操作系统有不同的内存模型，想要实现同一个效果需要不同的代码</p><ul><li>例如：分段内存模型的缓存是根据段来的，分页内存模型的缓存根据页来</li></ul><p>JVM提出的Java内存模型JMM用来屏蔽操作系统的差异，确保Java程序在不同平台表现一致；</p><p>另外，JMM规范本身也是下层实现的接口，对上层而言也有简化操作的意义</p><p>原生操作系统并不提供类似JMM的多线程下并发访问共享变量的规则，如果开发者手写的话，可能要用到线程同步、互斥锁、型号了等等底层技术。</p></blockquote><hr><blockquote><p>JMM规定了主内存和工作内存</p><ul><li><p>主内存：共享变量全都放在此处【线程私有的不参与讨论】</p></li><li><p>工作内存：每个线程与内存交换数据的缓冲 &#x2F; 保存了自身使用的变量的副本；</p></li></ul><p>主内存对应之前所说内存的全部，工作内存对应接近CPU的高速缓存中</p><h5 id="内存间交互的操作："><a href="#内存间交互的操作：" class="headerlink" title="内存间交互的操作："></a>内存间交互的操作：</h5><blockquote><p>规范化变量在主内存和工作内存交互的细节，每个命令都是原子性</p></blockquote><ol><li><p>lock：锁定</p></li><li><p>read：读取</p></li><li><p>load：载入</p></li><li><p>use：使用</p></li><li><p>assign：赋值</p></li><li><p>store：存储</p></li><li><p>write：写入</p></li></ol><p>定义了从主内存与工作内存，工作内存与执行引擎间的操作</p><p>还定义了一些使用规则【用来在原子性层面避免线程不安全场景】：</p><p>比如：不允许read与load间单独出现…不允许一个线程丢弃最佳assign的值…一个变量同一时间只能被一个线程lock…<strong>对一个变量使用lock会先清空工作内存中的缓冲，再用需要load或者assign</strong>…</p><blockquote><p>操作略显繁琐，Java团队提出先行先发原则用于简单判断有无线程不安全</p></blockquote><hr><h3 id="回来吧我的Volatile"><a href="#回来吧我的Volatile" class="headerlink" title="回来吧我的Volatile"></a>回来吧我的Volatile</h3><blockquote><p>关键字的特殊之处在于：Java团队为其开发了特有的访问规则</p><ol><li><p>立即刷新回主内存</p></li><li><p>使用前强制刷新工作内存</p></li><li><p>不会重排</p></li></ol></blockquote></blockquote><blockquote><ul><li><p>volatile变量特性：</p><ol><li><p>全局可见：当一条现场修改了此变量的值，新值对于其他线程而言立即得知；普通变量需要再主动读取主内存后才能修改到新版本</p><blockquote><p>实现方式：大概就是对volatile的操作直接在主内存中（避免了自身工作线程和主内存的不一致）、主内存改好后通知其他线程它们缓冲的volatile的信息无效了（避免下次执行引擎使用前，其他线程和主内存的数据不一致）。</p><blockquote><p>通知是什么操作？</p></blockquote></blockquote><hr><blockquote><p>使用volatile并非一定线程安全，volatile只能保证多个线程间读操作都能读到最新版本；但可能发生<strong>脏写</strong>；</p><p>— A，B线程读到此值，都交付给寄存器 &#x2F; 操作数栈进行逻辑操作了。A将数修改后，B的本地缓存虽然感知到了，但是操作数栈并没有，导致A将B写后的数据覆盖写</p><blockquote><p>本质是：由于java代码层面的指令不是原子性，即使是一条Java代码可能对应多个字节码，一个字节码也可能对应多个机器码…只有强制的原子性操作才能防止竟态 —— 加锁</p></blockquote></blockquote></li></ol></li></ul><ol start="2"><li><p>禁止指令重排</p><blockquote><p>关键操作：在汇编的机器码中多了lock + 空操作的语句；作用是充当内存屏障</p><p>lock还是之前的lock，空操作就意味着单纯依赖lock指令的特性</p><p>原本指令重排并不是随意重排，而是确保前后依赖关系不变的情况下，将可以重排并且重排后可以避免IO、阻塞等问题的指令，以更快的方式执行嘛，lock + 空操作表示要把工作内存的数据和主内存做点交互，这就要求之前的操作全部进行完毕。</p><blockquote><p>volatile同步机制用于写操作时，可能会导致在本地代码上插入许多内存屏障，会有一定开销</p></blockquote></blockquote></li></ol></blockquote></li><li><p>线程安全与锁优化</p><blockquote><ol><li><p>面向对象与面向过程</p><blockquote><p>面向过程是站在计算机的角度抽象问题和解决问题</p><ul><li><p>相对于按照对象 &#x2F; 函数来组织代码，面向过程强调按照程序的执行步骤。</p></li><li><p>强调数据和过程的分离，或者是过程要避免被数据耦合</p></li></ul><p>面向对象则是站在现实世界的思维方式来抽象问题，数据和行为都是对象的一部分。</p><ul><li>但面向对象虽然让开发者以现实世界的思维方式解决问题，但现实和计算机世界还有差距，按照对象行为编写的代码会遇到些问题，这些问题在面向过程中很直观，比如并发问题</li></ul></blockquote></li><li><p>线程安全：</p><p>指：多个线程同时访问一个对象，不需要进行额外的同步操作，在调用方的任何操作都可以得到正确的结果。</p><p>这就要求线程安全的代码本身内部维护了线程同步等保证安全性的手段，目的就是让上层不需要再考虑线程不安全的问题</p></li><li><p>线程安全程度分级：</p><ol><li><p>不可变（只读）：对于一个基本数据类型，只需要声明final即可，但对于一个对象，可能需要对其所有的属性全加final，或者类似String类，外部可见的方法通过某种手段避免修改内部属性（手动维护不可变的属性）</p></li><li><p>绝对线程安全：指一个类无论运行环境如何，调用者都不需要任何额外的同步操作（很难触发异常 &#x2F; 数据不一致）</p><p>常见线程安全集合只能保证相对线程安全，即：多次操作下，某一线程还是会被其他线程影响【在非原子性操作下，仍然线程不安全】</p></li><li><p>相对线程安全：保证对这个对象的但此操作是线程安全的，即：任何线程的一次get，add等操作保证顺利执行【本质过程就是将一次操作封装成原子操作】</p><p>Vector，HashTable，synchronizedMap,SynchronizedList…</p></li><li><p>线程兼容：对象本身并不安全，要求调用段正常使用同步策略</p></li></ol></li><li><p>线程安全的实现方案：</p><ol><li><p>互斥同步：并发多线程下，保证共享数据在同意适合制备一条线程使用</p><ul><li><p>synchronized：</p><p>synchronized回关联一个锁对象，代码块加锁的方式明确指定，实例方法上的synchronized取代码所在对象实例，类方法则使用类对应的Class对象作为锁对象</p><p>在javac编译期间，对应代码前后添加两行：monitorenter &amp; monitorexit，同时传入对象reference参数；</p><p>执行到monitorenter时，尝试获取对象锁，对象锁中标记有锁计数器，以及线程的阻塞队列</p><blockquote><p>特性：</p><ol><li><p>可重入</p></li><li><p>锁在执行中无法释放，线程在阻塞时无法逃离</p></li></ol></blockquote><blockquote><p>问题：</p><p>synchronized是重量级锁，实现的代价较大</p><ol><li><p>线程的阻塞与唤醒：</p><p>由于Java线程关联内核线程，每个线程阻塞操作实质都需要系统调用来阻塞内核线程</p></li></ol></blockquote></li><li><p>ReentrantLock：</p><blockquote><p>JUC下Lock接口下的实现类 &#x2F;&#x2F;TODO</p></blockquote></li></ul></li><li><p>非阻塞同步：</p><p>互斥同步带来的问题是：原本是一个悲观锁的逻辑：维护锁对象的锁技术、线程阻塞和唤醒这些悲观锁操作都有不小的开销，而乐观锁的思路就适合并发不大，无需太多同步开销的场景</p><blockquote><p>思路：无论有无并发线程，先尝试修改，再说提不提交，如果在整个阶段共享数据没有被其他线程修改，则成功提交，否则就意味着这次尝试失败，可能回通过重试 进行补偿</p></blockquote><hr><blockquote><p>技术难点：乐观锁下，冲突检测和设置新值必须是原子性的</p><blockquote><p>类似git的先拉后推，防止多个线程都拉了老版本，都认为可以推新版本，导致的脏写操作。</p></blockquote><p>原子性实现方案：依靠硬件</p><p>Java：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">AtomicInteger</span> <span class="hljs-variable">num</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">AtomicInteger</span>(<span class="hljs-number">0</span>);<br>num.increaseAndGet();<br><br><br><span class="hljs-meta">@IntrinsicCandidate</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">final</span> <span class="hljs-type">int</span> <span class="hljs-title function_">getAndAddInt</span><span class="hljs-params">(Object o, <span class="hljs-type">long</span> offset, <span class="hljs-type">int</span> delta)</span> &#123;<br>        <span class="hljs-type">int</span> v;<br>        <span class="hljs-keyword">do</span> &#123;<br>            v = getIntVolatile(o, offset);<br>        &#125; <span class="hljs-keyword">while</span> (!weakCompareAndSetInt(o, offset, v, v + delta));<br>        <span class="hljs-keyword">return</span> v;<br><span class="hljs-comment">//乐观锁 + 重试的充分体现</span><br><span class="hljs-comment">// while CAS失败，更新最新值再次重试</span><br>&#125;<br></code></pre></td></tr></table></figure></blockquote></li></ol></li></ol></blockquote></li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>GuliMallKnowledge</title>
    <link href="/2024/05/17/GuliMallKnowledge/"/>
    <url>/2024/05/17/GuliMallKnowledge/</url>
    
    <content type="html"><![CDATA[<h2 id="软件开发流程"><a href="#软件开发流程" class="headerlink" title="软件开发流程"></a>软件开发流程</h2><ol><li><p>需求分析——写出需求规格说明书（仅仅说明需求）</p></li><li><p>设计——产品文档，UI界面设计，概要设计，详细设计，数据库设计</p></li><li><p>开发！——写项目代码+单元测试</p></li><li><p>测试——测开人员编写测试用例，写测试报告</p></li><li><p>上线运维——运维人员配置软件运行环境</p></li></ol><h2 id="项目组角色分工"><a href="#项目组角色分工" class="headerlink" title="项目组角色分工"></a>项目组角色分工</h2><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-01-15-12-12-image.png" title="" alt="" width="426"><h2 id="软件环境"><a href="#软件环境" class="headerlink" title="软件环境"></a>软件环境</h2><ol><li><p>本地环境：自己电脑上的运行环境</p></li><li><p>开发环境：开发人员在开发阶段使用的环境，一般外部用户无法访问</p></li><li><p>测试环境：专门给测试人员使用的环境，用于项目测试（另一个服务器上运行其JDK，tomcat，MySQL…）</p></li><li><p>生产环境：即线上环境，提供对外服务的环境</p></li></ol><h2 id="瑞吉外卖"><a href="#瑞吉外卖" class="headerlink" title="瑞吉外卖"></a>瑞吉外卖</h2><ul><li><p>系统管理后台</p><ul><li>功能：分离管理，菜品管理，套餐管理，菜品口味管理，员工登录，员工退出，员工管理，订单管理</li></ul></li><li><p>移动端</p><ul><li>功能：手机号登录，微信登录，地址管理，历史订单，菜品规格，购物车，下单，菜品浏览</li></ul></li><li><p>角色分类</p><ul><li><p>后台系统管理员：用于后台全部操作权限</p></li><li><p>后台系统普通员工：登录后对菜品，套餐，订单进行管理</p></li><li><p>C端用户。</p></li></ul></li><li><p>技术选型：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-15-33-30-image.png"></p><p>说明：</p><ol><li><p>Nginx：</p><p>Nginx是一个用于提供静态资源(HTML,CSS,JS,图像…)的HTTP服务器。</p><p>功能之一是代理前端请求，发送到后端。目的是提供负载均衡，缓存，SSL等功能。</p><ul><li>负载均衡的实现：Nginx在多个后台服务器之间分发请求。</li></ul><p>功能之一是高并发处理（特色）</p></li><li><p>Spring Session：</p><p>Spring Session是Spring Framework的一个<strong>子项目</strong>。用于简化在<strong>分布式系统</strong>中管理用户会话的功能。</p><p>Spring Session的提供了一种抽象的方式来管理用户会话——将会话数据存储在外部存储介质中（如数据库，Redis，Hazelcast等），为了使不同服务器上的应用实例能共享和访问同一用户的会话信息。</p></li><li><p>Swagger：</p><p>Swagger是一个用于描述，设计，构建，管理，测试和文档化RESTful Web服务的工具集。是用来对前后端分离时中间API进行管理的。</p></li><li><p>Redis：</p><p>Remote Dictionary Server 高性能键值存储数据库。</p><p>Redis以键值对的形式存储数据，并支持<strong>多种数据结构</strong>，如字符串，哈希，列表，集合，有序集合…</p><p>Redis主要用途：缓存、会话存储、消息队列、排行榜，实时数据分析，分布式锁，地理位置信息</p></li></ol></li></ul><h2 id="谷粒商城"><a href="#谷粒商城" class="headerlink" title="谷粒商城"></a>谷粒商城</h2><ul><li><p>技术选型</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-01-16-08-12-image.png" title="" alt="" width="450"><ol><li><p>基础篇</p><ul><li><p>后端基础环境</p><p>Spring Boot + Spring Cloud + Docker</p></li><li><p>前端</p><p>三件套+V3+Element-Plus</p></li><li><p>通过<strong>逆向工程</strong>方式构建项目</p></li></ul></li><li><p>架构篇</p><ul><li><p>Elasticsearch  弹性搜索</p></li><li><p>网关</p></li><li><p>远程调用</p></li><li><p>链路追踪</p></li><li><p>缓存</p></li><li><p>Session同步方案</p></li><li><p>全文检索</p></li><li><p>异步编排</p></li><li><p>线程池</p></li><li><p>压力测试与性能优化</p></li><li><p>调优</p></li><li><p>Redis</p></li><li><p>CI&#x2F;CD</p></li></ul></li><li><p>集群篇</p><ul><li><p>k8s集群</p></li><li><p>Devops</p></li><li><p>……</p></li></ul></li></ol></li><li><p>细说各个技术栈</p><ol><li><p>配置linux虚拟环境</p><ol><li><p>virtualBox是虚拟机</p><p>允许在一台计算机上创建和运行虚拟机</p><p><strong>虚拟机</strong>：模拟完整的计算环境，包括操作系统，应用程序等</p></li><li><p>vagrant是自动配置操作系统的代理软件</p><p>是一个用于<strong>构建和维护虚拟化开发环境</strong>的工具</p><p>用它可以加载某个操作系统的镜像，相当于直接配置好操作系统</p><p><strong>镜像</strong>：Image：指一个预先配置好的操作系统或软件环境的副本。镜像包含了操作系统，应用程序，设置，配置，数据等，是一个虚拟机的完整快照。</p></li><li><p>CentOS是基于Linux的操作系统</p></li></ol></li></ol></li></ul><ol start="2"><li><p><strong>Docker</strong> written by golang</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-02-15-23-31-image.png" title="" alt="" width="223"><p>Docker用于将应用程序和其依赖打包成一个独立的、轻量级的<strong>容器</strong>，以便在任何环境中运行。</p><p>如果打包有等级：Maven&lt;Tomcat&lt;Docker</p><p>Maven只打包和管理依赖jar包</p><p>Tomcat打包和管理到各种应用程序Servlet和JSP，但却是专门为Java Web程序设计的——即只能运行javaWeb应用。而且Tomcat的运行依赖本地JVM</p><p>Docker容器目的就是实现应用程序的<strong>可移植性</strong>，<strong>环境隔离性</strong>。</p><p>解决的问题——代码<strong>水土不服</strong>——由于开发&#x2F;测试&#x2F;生产环境不同导致代码运行结果不同。</p><ul><li><p>水土不服——软件跨环境迁移的问题</p></li><li><p>解决方法——打包水土——将程序和环境绑定，用一个容器进行管理——<strong>Docker</strong></p></li></ul><p>另外：Docker容器化也是对内部程序的二次封装，统一原本每个应用程序不同的命令：例如mysql.serve start统一成docker run；mysqld统一成 docker exec…</p></li></ol><h4 id="容器化与云原生："><a href="#容器化与云原生：" class="headerlink" title="容器化与云原生："></a>容器化与云原生：</h4><blockquote><p>伏笔</p></blockquote><h4 id="虚拟化与容器化："><a href="#虚拟化与容器化：" class="headerlink" title="虚拟化与容器化："></a>虚拟化与容器化：</h4><blockquote><p>为什么需要虚拟化 &#x2F; 容器化？</p><ul><li><p>当企业的三个应用程序功耗不大，可以考虑运行在单台物理机上时；如果当三个应用程序的底层依赖的版本有冲突（依赖没有隔离）例如：一个是java 8，一个是Java 17，就会导致物理机底层依赖比较难以管理 ； 更严重的情况是：比Java、MySQL等更加底层的依赖配置起冲突，例如</p><blockquote><p>如果是更底层的运行库&#x2F;库版本冲突，例如某个应用需要glibc，另一个应用需要uclibc，这就无解了</p></blockquote></li><li><p>所以虚拟化技术并不是单纯的开一个新的操作系统，而是说开一个原本操作系统无法提供的环境</p></li></ul></blockquote><hr><ol start="3"><li><p>Spring Cloud</p><p>Spring Cloud是一个用于<strong>构建分布式系统</strong>和<strong>微服务架构</strong>的框架。</p><p>提供了一组工具和组件，用于<strong>简化</strong>分布式系统开发的复杂性和<strong>解决</strong>微服务架构中一些常见问题。</p><p>Spring Cloud下有许多子项目，每个子项目都专注于不同的分布式系统和微服务开发问题。LIsted below</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-16-35-31-image.png"></p><hr></li><li><p>ElasticSearch：</p><p>分布式搜索和分析引擎，能够快速地搜索大量数据，并支持实时查询和分析。用于实现全文搜索，日志分析，数据可视化等功能。</p><hr></li><li><p>Sentinel：</p><p>是一个分布式系统的流量控制和熔断器<strong>库</strong>。旨在提供一种简单，文档，可到的流量控制手段，以保护微服务架构中的应用程序免受流量过载、雪崩效应和故障的影响。</p><hr></li><li><p>Nacos：</p><p>是一个开源系统，支持动态服务发现，配置管理和命名空间管理，服务员数据等功能。总体而言是用于<strong>构建和管理</strong> <strong>微服务架构</strong> 和 <strong>分布式系统</strong> 的平台。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-17-14-43-image.png"></p><hr></li><li><p>zipkin：</p><p>是一个分布式系统的追踪系统。用于收集、存储、查看分布式系统中各个服务之间的调用链路和性能数据。</p><p><code>在微服务架构中，一个业务请求往往会经过多个不同的服务进行处理。每个服务可能又依赖其他的服务，形成一个复杂的调用链路。当系统中出现问题或性能瓶颈时，追踪调用链路和性能数据对于定位问题和优化性能非常重要。</code></p></li></ol><hr><ul><li><p>概念：</p><ol><li><p>微服务：</p><p>是一种架构风格：拒绝从前的大型单体应用，因为在大型应用中一个小项目不可用会影响全局，一个解耦的思想就是在一堆提供相同服务的项目服务中选择能用的即可——这就要求大型项目分离成微服务。微服务架构风格将大型单体应用根据其业务边界进行服务微化拆分，各个服务独立部署运行。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-17-41-45-image.png"></p><p>分层所面临的问题就是<strong>需要传递</strong>——推荐http。</p><hr></li><li><p>集群 &amp; 分布式 &amp; 节点：</p><p>集群：<strong>物理形态</strong>——只要是一堆机器，就可以叫集群，无论他们是不是干同一件事。</p><p>集群往往是解决一台机器容易宕机、数据备份等问题。</p><p>分布式：<strong>工作方式</strong>——将不同的业务分布在不同的地方，其中每个业务根据需要配集群。</p><p>节点：分布式中每个业务的服务器</p><hr></li><li><p>远程调用  RPC</p><p>在分布式系统中，各个服务可能处于不同主机，但服务之间需要相互调用&#x2F;传递信息。称之为远程调用。</p><p>Spring Cloud 使用HTTP + JSON的方式完成远程调用。</p><p>项目中 不同微服务之间通过OpenFeign，主微服务向目的微服务发送http请求，完成远程的方法调用</p><hr></li><li><p>负载均衡</p><p>根据不同的负载均衡算法，将对一个服务的调用平衡到集群的各个服务器上。为了使每一个服务器都不要太忙或者太闲。</p><p>常见算法：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-17-59-56-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-18-00-43-image.png"></p><hr></li><li><p>服务注册&#x2F;发现 &amp; 注册中心</p><p>一个服务内的集群是多台服务器，管理这些服务器哪台上线了，哪台下线了的一个应用就是注册&#x2F;发现中心。对B服务的访问需要经过对B的注册&#x2F;发现中心，找到被发现可用的一个服务器。</p><hr></li><li><p>配置中心</p><p>一个服务内的集群是多台服务器，统一管理这些服务器的配置——配置中心。</p><hr></li><li><p>服务熔断 &amp; 服务降级</p><p>由于微服务之间是用http协议相互调度的，这就不可避免在传输中出现错误。</p><p>如果服务A对B的调度没有及时响应，而对A的调度一直在进行，导致A的服务器很容易压力过大导致宕机</p><ul><li><p>服务熔断：</p><p>如果被调度的服务经常失败（达到某一阈值），开启断路保护机制——后来的请求不再去调度这个服务，而是由本地直接返回默认的数据。</p><p><small>如果只针对超时的请求，该怎么区分是因为传输中不可抗力导致还是被调度端异常导致——最终还是需要识别出多次调度失败的服务</small></p></li><li><p>服务降级：</p><p>在运维期间，当系统处于高峰期时资源紧张。可以让非核心业务<strong>降级运行</strong>——即某些不处理&#x2F;简单处理（抛异常，前端缓存，返回Null，调用Mock数据（模拟数据），调用Fallback处理逻辑（备用方案））</p></li></ul><hr></li><li><p>API网关：API Gateway</p><p>（过滤器&#x2F;拦截器&#x2F;守卫）</p><p>目的是抽象出微服务中都需要的某些功能，某些<strong>需要在客户端请求到达服务前进行</strong>的功能——过滤&#x2F;认证，重定向，负载均衡…</p></li></ol></li></ul><h2 id="Docker"><a href="#Docker" class="headerlink" title="Docker"></a>Docker</h2><ul><li>Docker架构</li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-15-31-35-image.png"></p><ol><li><p>镜像（Image）：相当于一个root文件系统（从Docker创建的内部的系统就源于此文件）</p><p>每个image包含一个应用程序所需的所有代码、库、依赖项和配置。将其打包成一个可运行的、只读的模板。</p></li><li><p>容器（Container）：容器就是镜像的实例化对象。<strong>镜像是静态的定义，容器是镜像运行时的实体</strong>。容器可以被创建、启动、停止、删除、暂停…</p></li><li><p>仓库（Respository）：代码控制中心，用来保存镜像（Docker hub外国中心仓库，private registry私服）</p></li></ol><h3 id="Docker-命令"><a href="#Docker-命令" class="headerlink" title="Docker 命令"></a>Docker 命令</h3><ol><li><p>关于Docker daemon守护线程的</p><ol><li><p>启动dockers服务</p><p>systemctl start docker</p></li><li><p>停止docker服务</p><p>systemctl stop docker</p></li><li><p>重启docker服务</p><p>sysytemctl restart docker</p></li><li><p>查看docker服务状态</p><p>systemctl status docker</p></li><li><p>设置开机自启docker</p><p>systemctl enable docker</p></li></ol></li><li><p>关于Docker镜像的（image文件&#x2F;各个软件）</p><ol><li><p>查看本地所有镜像</p><p>docker images</p><p>docker images -q  #查看所有镜像的id</p></li><li><p>搜索镜像</p><p>docker search 镜像名称</p></li><li><p>拉取镜像：从仓库下来镜像到本地，镜像名称格式：name:版本</p><p>docker pull 镜像名称</p></li><li><p>删除镜像：删除本地镜像</p><p>docker rmi 镜像id  #删除指定本地镜像（remove image）</p><p>docker rmi `docker images -q`  #删除所有本地镜像 （将查看id结果作为参数）</p></li></ol></li><li><p>关于Docker容器的</p><ol><li><p>查看容器</p><p>docker ps  #查看正在运行的容器</p><p>docker ps -a  #查看所有容器</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-16-32-40-image.png"></p></li><li><p>创建并启动容器</p><p>docker run 参数 创建并启动容器</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-16-32-58-image.png"></p><p>参数说明：</p><ul><li><p>-i  保存容器运行。</p></li><li><p>-t  为容器重新分配一个伪输入终端。</p></li><li><p>-d  以守护（后台）模式运行容器。创建一个容器在后台运行</p><ul><li><p>再进入：docker exec -it name &#x2F;bin&#x2F;bash</p></li><li><p>&#x2F;bin&#x2F;bash相当于进入容器的初始化操作（必要的）</p></li></ul></li><li><p>通常：-it：创建交互式容器  -id：创建守护式容器</p><p>用-it就直接进入到容器，用id则还在上一层。</p></li><li><p>–name：为创建的容器命名</p></li></ul></li><li><p>进入容器</p><p>docker exec 参数  #退出容器，容器不会关闭</p></li><li><p>停止容器</p><p>docker stop 容器名称（id）（stop后状态是exited）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-16-46-24-image.png"></p></li><li><p>启动容器</p><p>docker start 容器名称（id）</p></li><li><p>删除容器</p><p>docker rm 容器名称（id）</p></li><li><p>查看容器信息</p><p>docker inspect 容器名称（id）</p></li><li><p>容器自启动（随着虚拟机启动）</p><p>docker自启动已经配置过了，就是随着虚拟机自启动</p><p>一般过程是：虚拟机启动&#x2F;docker启动，用户连接vagrant，用户切换到root</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-21-55-38-image.png"></p></li></ol></li></ol><h3 id="数据卷"><a href="#数据卷" class="headerlink" title="数据卷"></a>数据卷</h3><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-02-16-58-40-image.png" title="" alt="" width="411"><ul><li><p>解决的问题：</p><ol><li><p>Docker 中的Container容器删除后，在容器中的内容也会随之销毁（需要备份）</p></li><li><p>Docker 中的Container容器和外部机器无法直接交换文件（没配虚拟ip时）（需要Docker容器中介）</p></li><li><p>Docker 容器中的Container容器怎么进行数据交互（或许需要中介）</p></li></ol></li><li><p>数据卷</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-02-17-01-57-image.png" title="" alt="" width="397"><ul><li><p>数据卷是宿主机的一个目录或文件</p></li><li><p>当容器目录和数据卷目录绑定后，对方的修改会立刻同步</p></li><li><p>一个数据卷可以被多个容器同时<strong>挂载</strong>，一个容器也可以挂载多个数据卷</p><p>挂载：往往是说容器内的文件挂载到外部文件，从而通过外部读写内部。</p><p>挂载的常见对象：配置、日志、数据文件</p></li></ul></li><li><p>配置数据卷：（文件映射）</p><p>语法：</p><p>在创建容器时，使用 -v 参数 设置数据卷</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-17-13-44-image.png"></p><p>其中，前面是在外层目录中建立的目录</p><p>后面是在Container容器中建立的目录</p><p>Attention：</p><ol><li><p>目录必须是绝对路径</p></li><li><p>如果目录不存在，会自动创建</p></li><li><p>一个容器可以挂载多个数据卷</p></li><li><p>基本Linux语法（文件）：</p><ol><li><p>touch itheima.txt &#x3D;&#x3D;&gt; 创建文件</p></li><li><p>echo i love mazda &gt; itheima.txt &#x3D;&#x3D;&gt; 向此文件中写入数据</p></li><li><p>cat itheima.txt &#x3D;&#x3D;&gt; 读取此文件</p></li></ol></li><li><p>基本Linux文件层次：</p><p><strong>[root@localhost &#x2F;] 斜杠&#x2F;表示根目录</strong>：根目录是整个文件系统的起点，所有目录和文件的根节点。根目录下一级是许多重要的大目录：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-19-17-18-image.png"></p><p>而在home下就拥有每个用户的一个独立的子目录，用于存储用户个人文件和配置信息。</p><p>root似乎没有在home下的文件夹</p><p><strong>[root@localhost ~]波纹~表示用户主目录</strong></p></li></ol></li><li><p>数据卷容器</p><p>主要思想：创建一个容器，这个容器和一个数据卷进行了挂载。然后又创建了许多容器，和这个数据卷容器进行了绑定，实际效果就是后面的每个容器和数据卷进行了绑定。</p><p>有点像定义了一个接口。</p><img title="" src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-02-19-29-28-image.png" alt="" width="404"><p>步骤：</p><ol><li><p>docker run -it –name&#x3D;c3 -v &#x2F;volumn centos:7 &#x2F;bin&#x2F;bash</p><p>创建启动c3数据卷容器，使用-v参数，&#x2F;volumn表示在c3中所挂载到数据卷的文件</p><p>在数据卷中自动创建了一个文件用于挂载，似乎并不关心在数据卷中的文件，所以名字很随意。</p></li><li><p>创建启动c1，c2容器，使用–volumes-from 参数，设置数据卷</p><p>docker run -it –name&#x3D;c1 –volumnes-from c3 centos:7 &#x2F;bin&#x2F;bash</p><p>docker run -it –name&#x3D;c2 –volumnes-from c3 centos:7 &#x2F;bin&#x2F;bash</p></li></ol><p>之后c1,c2中都有volumn文件，看起来是c3主导了。</p></li><li><p>注意的Bug</p><ul><li><p>用vi进入txt写入文件后，先进行ctrl+c结束写入，再:wq保存退出</p></li><li></li></ul></li></ul><h3 id="Docker-应用部署"><a href="#Docker-应用部署" class="headerlink" title="Docker 应用部署"></a>Docker 应用部署</h3><ul><li><p>宿主机和虚拟机</p><p>二者概念相辅相成</p><p>二者都是在虚拟化技术环境下的概念</p><p><strong>宿主机</strong>是承载虚拟机的计算机，<strong>虚拟机</strong>是在宿主机上模拟的独立操作系统环境。</p><p>宿主机要运行虚拟机，就要为其管理和分配物理资源（处理器，内存，存储…）</p><p>虚拟机在宿主机上的虚拟环境运行自己的操作系统和应用程序——感觉像是独立的计算机一样。</p></li><li><p>部署面临第一个问题——外界无法直接访问到虚拟机内的某一容器</p><p>但外界可以通过访问宿主机，宿主机再访问容器</p></li><li><p>不过由宿主机到容器的过程没有特殊逻辑，需要实现配置——<strong>端口映射</strong></p></li></ul><h5 id="端口映射："><a href="#端口映射：" class="headerlink" title="端口映射："></a>端口映射：</h5><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-02-20-00-26-image.png"></p><blockquote><p>docker run -d -p 3306:3306 -v $PWD&#x2F;conf:&#x2F;etc&#x2F;mysql&#x2F;conf.d -v $PWD&#x2F;data:&#x2F;var&#x2F;lib&#x2F;mysql -e MYSQL_ROOT_PASSWORD&#x3D;123456 –name mysql mysql:5.7</p></blockquote><h2 id="bug简述："><a href="#bug简述：" class="headerlink" title="bug简述："></a>bug简述：</h2><ol><li><p>Docker logs -f Container_id 可以查看具体某个容器的日志，某些不显示报错的报错会在日志中报出。</p></li><li><p>关于没有空间</p><ol><li><p>du -sh *</p><p>查看当前目录下各个子目录&#x2F;文件夹大小</p><p>disk usage -summarize human-readable</p></li><li><p>df -h</p><p>显示文件系统的词频空间使用情况。是整个分区的概念，磁盘分区无法进入，它是存储在文件系统的一个设备节点，用于挂载到文件系统树的特定路径上。</p></li><li><p>据说：某个包（vagrant）的存储过大，是因为vagrant挂载了vagrantFile所在文件夹（win下）的所有文件，只要把vagrantFile换到新建文件夹即可</p><p>但我还是直接找到不能再往下走的大文件，将其删除</p></li></ol></li><li><p>关于&#x2F;var&#x2F;lib&#x2F;下没有mysql文件</p><p>本应没有</p><p>重装镜像，装5.7</p></li></ol><h2 id="谷粒商城架构图"><a href="#谷粒商城架构图" class="headerlink" title="谷粒商城架构图"></a>谷粒商城架构图</h2><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-01-19-43-14-image.png"></p><p>大致流程说明：</p><ol><li><p>左侧外网：用户访问到Nginx服务器，服务器访问内网的网关，网关进行路由&#x2F;认证&#x2F;限流等操作后调用业务集群中的各种微服务功能（正中间）。</p></li><li><p>核心微服务的功能实现需要Redis做缓存，MySQL做持久化数据库，RabbitMQ做消息队列，ElasticSearch做全文检索。将需要存储的文件云存储到阿里云OSS（中下方）。</p></li><li><p>对于核心微服务的管理，通过Nacos注册中心&#x2F;配置中心管理。微服务的调用链如果有某处出问题，调用Spring Cloud的业务追踪模块，外加许多其他功能完成错误提示（上方）。</p></li><li><p>开发人员将代码托管到Github，OP运维人员用自动化工具Jenkins从Github拉取代码后，打包成Docker镜像，再用K8s集成服务，以Docker容器来进行（最下方）。</p></li></ol><h2 id=""><a href="#" class="headerlink" title=""></a></h2><h2 id="开发环境搭建"><a href="#开发环境搭建" class="headerlink" title="开发环境搭建"></a>开发环境搭建</h2><ul><li><p>要求：</p><ol><li><p>注意Java版本</p></li><li><p>注意maven版</p></li><li><p>git</p></li></ol></li></ul><h3 id="git"><a href="#git" class="headerlink" title="git"></a>git</h3><p>git是一个分布式<strong>版本控制</strong>系统，内容管理系统，工作管理系统</p><p>用于<strong>跟踪</strong>代码和文件的<strong>变化</strong>，协助<strong>多人</strong>协作开发，并管理项目的不同<strong>版本</strong></p><p>具体作用：</p><ol><li><p>跨区域多人协同开发</p></li><li><p>追踪和记载文件的历史记录</p></li><li><p>组织和保护源代码和文档</p></li><li><p>统计工作量</p></li><li><p>并行开发，提高效率</p></li><li><p>追踪记录整个软件开发过程</p></li><li><p>降低人为错误</p></li></ol><p>常见的版本控制软件&#x2F;系统</p><ul><li><p>Git</p></li><li><p>SVN（subversion）</p></li><li><p>CVS（Concurrent Version System）</p></li><li><p>Visual Studio Online</p></li></ul><p>git将项目代码存储在仓库 repository ，并记录每个文件的历史修改记录</p><p>Git具有<strong>分布式特性</strong>——开发者可以在不同分支上同时工作，然后将分支合并到主要代码库。</p><p>分支：从主分支（master&#x2F;main）分离出来的代码路径，可以并行开发不同功能</p><p>提交：Commit 记录仓库的一个特定状态</p><p>合并：Merge 将一个分支的更改合并到另一分支中</p><p>拉取：Pull 从远程仓库获取更新。</p><p>推送：Push 将本地的更改推送到远程仓库，使其他人看得到修改</p><ul><li>Git客户端负责代码提交、合并…以上功能，Github可以当做Git的可视化+web通信工具。</li></ul><p>版本控制分类</p><ol><li><p>本地版本控制：</p><p>个人在本地计算机记录文件&#x2F;软件的多个版本</p></li><li><p>集中版本控制： 代表：SVN</p><p>企业在中央服务器中存有一版，开发者基于中央服务器的某一历史版本进行开发，开发结果合并回中央服务器中。</p><p>缺点：没有实现热部署——开发虽是并行的，但并不协同。</p></li><li><p>分布式版本控制： 代表：Git</p><p>所有开发人员拥有中央服务器的同一版本所有代码，基于此开发后再提交会中央服务器进行更新</p><p>相较于集中版本控制，分布式中将所有代码进行备份。且备份的代码在所有开发人员中同步更新（热部署）。</p></li></ol><h3 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h3><p>工具：</p><ul><li><p>Git Bash：Unix和Linux风格的命令行</p></li><li><p>Git CMD：windows风格的命令行</p><p>windows的命令行和Linux的命令行语法上略有不同——like cls &#x2F; clear</p></li><li><p>Git GUI：图形界面的Git</p></li></ul><p>常用命令：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-09-10-48-image.png"></p><p>Git理论：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-09-47-53-image.png"></p><p>Git三个工作区域+远程仓库</p><ol><li><p>工作区：working 平时存放代码</p></li><li><p>暂存区：index 用于临时存放改动</p></li><li><p>资源区&#x2F;仓库区&#x2F;本地仓库：head 安全存放数据，里面有所有版本的数据</p><p>又有说法表示head指向最后一次提交的结果</p></li><li><p>远程仓库：托管代码的服务器</p></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-20-34-42-image.png"></p><p>分支：</p><p>用来将<strong>特性开发</strong>绝缘开的。</p><p>创建仓库时，master是默认分支。在其他分支上进行开发，完成后需要合并到主分支上</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-20-40-58-image.png"></p><p>创建一个叫feature_x的分支并切换过去</p><blockquote><p>git checkout -b feature_x</p></blockquote><p>切换回主分支</p><blockquote><p>git checkout master</p></blockquote><p>删除新建的分支</p><blockquote><p>git branch -d feature_x</p></blockquote><p>建立的分支需要被推送到远端仓库，不如该分支就不为他人所见</p><blockquote><p>推：git push origin <branch></p></blockquote><p>Git常用操作：</p><ol><li><p>初始化：</p><ol><li><p>创建项目：</p><p>Git bash中：git init</p><p>空项目结构：</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-04-10-02-25-image.png" title="" alt="" width="367"></li><li><p>克隆&#x2F;复制项目：</p><p>Git bash中：git clone url</p><p>如果是在远端服务器上的仓库：</p><blockquote><p>git clone username@host:&#x2F;path&#x2F;to&#x2F;repository</p></blockquote><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-10-04-31-image.png"></p><p>项目结构</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-10-08-13-image.png"></p></li><li><p>提交操作</p><img title="" src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-04-20-35-15-image.png" alt="" width="636"><blockquote><p>git add &lt;filename&gt;</p><p>git add *</p><p>将代码添加到暂存区</p></blockquote><blockquote><p>git commit -m “提交代码的描述信息”</p><p>将代码提交到HEAD</p></blockquote><blockquote><p>git push origin master </p><p>将在head的改动提交到远端仓库</p><p>master可以替换成要推送的任何分支</p></blockquote></li><li><p>更新与合并</p><p>更新本地仓库，同步最新改动</p><blockquote><p>git pull</p></blockquote><p>在当下的工作目录中获取并合并远端的改动;或要合并其他分支到当前分支</p><blockquote><p>git merge <branch></p></blockquote><blockquote><p>在这两种情况下，git 都会尝试去自动合并改动。遗憾的是，这可能并非每次都成功，并可能出现<em>冲突（conflicts）</em>。 这时候就需要你修改这些文件来手动合并这些<em>冲突（conflicts）</em>。</p></blockquote><p>执行命令标记合并成功</p><blockquote><p>git add <filename></p></blockquote><p>合并改动之前，可以使用如下命令预览差异</p><blockquote><p>git diff <source_branch> <target_branch></p></blockquote></li><li><p>为版本添加标签</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-20-49-39-image.png"></p></li><li></li><li><p>IDEA中集成</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-10-11-45-image.png"></p></li></ol></li><li><p>Git文件操作</p><ul><li><p>文件的四种状态：</p><ol><li>Untracked：未跟踪，表示文件还处于个人编写状态，虽在文件夹中，但没有加入Git库，不参与版本控制。</li></ol><p>通过git add 将状态变为Staged</p><ol start="2"><li><p>Unmodify：文件已经入库，未修改。表示版本库中的文件内容和文件夹里完全一致。这种类型的文件有两种去处——被修改则变为Modified，如果使用git rm移出版本库，则成为untracked文件</p></li><li><p>Modified：文件已修改，但还没有其他操作。这种文件也有两个去处——通过git add 进入暂存区staged，或通过git checkout 丢弃（git checkout表示从库中取出文件，覆盖此文件）</p></li><li><p>Staged：暂存状态，执行git commit后将修改同步到库中，这是库中的文件和本地文件变为一致，文件变为unmodify；执行git reset head filename 取消暂存，文件状态为Modified</p></li></ol></li></ul></li><li><p>gitignore</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-04-10-39-36-image.png"></p></li><li><p>免密登录：</p><p>从本地仓库提交到gitee远程仓库时，避免每次输入密码。</p><p>根据本地的git的信息生成公钥私钥——唯一识别当前git（git内保存有global的账户和密码）——就可以免密登录到gitee本人远程仓库。</p><p>使用SSH协议：用于在不安全的网络上安全地进行远程访问，文件传输和命令执行。</p><p>SSH中使用公钥私钥模式：</p><blockquote><p><strong>公钥和私钥文件：</strong> SSH使用公钥加密来确保通信的安全性。在用户使用SSH连接到远程服务器时，他们需要<strong>一对密钥</strong>：一个私钥保留在用户计算机上，另一个<strong>公钥嵌入在远程服务器的授权文件中</strong>。公钥和私钥通常存储在用户的主目录下的<code>.ssh</code>文件夹中，分别是<code>id_rsa</code>（私钥）和<code>id_rsa.pub</code>（公钥）。</p></blockquote><p>大致：我的本地保存私钥，公钥发到gitee。身份认证时我发送加密后的信息，到达gitee用私钥解密。</p></li><li><p>开源许可证：各种协议限制此项目对外的开源程度</p><p>是否可以随意转载&#x2F;是否能够商业使用…</p></li></ol><h4 id="IDEA集成Git"><a href="#IDEA集成Git" class="headerlink" title="IDEA集成Git"></a>IDEA集成Git</h4><ul><li><p>idea新建项目绑定Git</p><ol><li><p>创建时确定URL</p></li><li><p>新项目中复制来.git文件，即可将新项目和git账户绑定</p><p>因为.git是在git init时产生，并没有保留项目的任何信息，只是指向个人身份的gitee</p></li></ol></li></ul><h2 id="谷粒项目构建"><a href="#谷粒项目构建" class="headerlink" title="谷粒项目构建"></a>谷粒项目构建</h2><blockquote><p> 后台管理系统</p></blockquote><ol><li><p>前端用开源项目——人人开源</p></li><li><p>后端</p><ul><li><p>powerDesign 设计数据库</p></li><li><p>基础crud代码由开源项目生成（从数据库逆向）</p></li></ul></li><li><p>系统项目结构：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-05-19-07-17-image.png"></p></li><li><p>基础crud代码结构</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-05-19-07-58-image.png"></p></li><li><p>项目亮点：</p><ol><li><p>代码生成面临缺失依赖问题：</p><p>由于五个子模块都缺失类似的依赖，所以能抽离出依赖部分最好——于是用一个子模块包含需要的依赖，再导入此子模块作为依赖。</p></li></ol></li></ol><blockquote><p>分布式-微服务</p></blockquote><p><big>老版本选择Spring Cloud</big></p><ul><li><p>注册中心：管理各个微服务</p><p>Spring Cloud Netflix 中的 Eureka</p></li><li><p>配置中心：管理各个微服务的配置信息</p><p>Spring Cloud Config</p></li><li><p>网关：拦截！过滤！</p><p>Spring Cloud Netflix 中的 Zuul</p></li><li><p>短路保护：</p><p>Spring Cloud Netflix 中的 Hystrix</p></li></ul><h4 id="Spring-Cloud-Alibaba"><a href="#Spring-Cloud-Alibaba" class="headerlink" title="Spring Cloud Alibaba"></a>Spring Cloud Alibaba</h4><p>提供微服务开发的一站式解决方案，包含更强的分布式应用的必要组件</p><p>更强：封装到只需要添加一些注解和少量配置。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-05-19-26-55-image.png"></p><p><big>新版本选择Spring Cloud Alibaba</big></p><ul><li><p>注册中心&#x2F;配置中心</p><p>Nacos</p></li><li><p>服务容器（限流、降级、熔断）</p><p>Sentinal</p></li><li><p>分布式事务解决方案</p><p>Seata</p></li></ul><p><small>任用Spring Cloud</small></p><ul><li><p>负载均衡</p><p>Ribbon</p></li><li><p>声明式HTTP客户端（调用远程服务）</p><p>OpenFeign</p></li><li><p>API网关（webflux编程模式）</p><p>Gateway（比Zuul更强）</p></li><li><p>调用链监控</p><p>Sleuth</p></li></ul><hr><h3 id="by-the-way：记录bug"><a href="#by-the-way：记录bug" class="headerlink" title="by the way：记录bug"></a>by the way：记录bug</h3><p>git拉取代码的时候报错：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-05-20-03-43-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-05-20-04-00-image.png"></p><p>解决方法：</p><ol><li><p>解开代理</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs git">git config --global --unset http.proxy<br>git config --global --unset https.proxy<br></code></pre></td></tr></table></figure></li><li><p>清理DNS缓存</p><p>cmd下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ipconfig/flushdns<br></code></pre></td></tr></table></figure></li></ol><hr><h3 id="技术栈"><a href="#技术栈" class="headerlink" title="技术栈"></a>技术栈</h3><ol><li><h1 id="Nacos"><a href="#Nacos" class="headerlink" title="Nacos"></a>Nacos</h1><p><small>用于管理注册中心和配置中心</small></p><ul><li><h3 id="管理中心"><a href="#管理中心" class="headerlink" title="管理中心"></a>管理中心</h3></li></ul><p>步骤：</p><ol><li><p>启动Nacos服务器——可以运行在本地&#x2F;Linux</p></li><li><p>Java代码中：</p><ol><li><p>将Nacos依赖导入微服务的pom文件——将此微服务放入注册中心，并能检测&#x2F;发现其他微服务。（统一放入common）</p></li><li><p>给此微服务写配置信息（目标nacos服务器的位置）</p></li><li><p>必要的注解（前两步都是配置资源可用，这一步是将微服务绑进注册中心）</p></li></ol></li></ol><hr><p>bug记录：<strong>卡最长时间的bug</strong></p><p>原委：nacos服务器启动正常，java的微服务配置没有报错，但jdk，spring-boot，spring-cloud-alibaba都是用的高版本，此时nacos无法发现微服务</p><p>答案：果然是版本匹配的问题</p></li></ol><blockquote><p><a href="https://github.com/alibaba/spring-cloud-alibaba/wiki/%E7%89%88%E6%9C%AC%E8%AF%B4%E6%98%8E">版本说明 · alibaba&#x2F;spring-cloud-alibaba Wiki (github.com)</a></p></blockquote><ul><li><p>原先老哥怀疑是JDK版本过高，却没有怀疑Spring-boot，Spring-cloud，Spring-cloud-alibaba虽都是高版本但能否匹配。（jdk过高可能影响微服务，也可能影响nacos&#x2F;底层用java执行，依赖jdk）</p></li><li><p>最后的答案是：从github官网上<strong>找到了版本匹配说明</strong>（真难找——在wiki中）</p></li></ul><ol start="2"><li><p>关于版本的第二个问题</p><p>原委：想把开源项目renren-fast也加入nacos，但开源项目的版本较落后且不易更改</p><p>答案：按照低版本重新引入相关依赖，而非按照common统一引入</p><hr></li></ol><h3 id="配置中心"><a href="#配置中心" class="headerlink" title="配置中心"></a>配置中心</h3><p>   目的：</p><ul><li><p>当下的问题是：每次更改某些配置，需要改Java微服务的配置文件，并重启微服务。重启就会导致项目阻塞甚至报错，于是想要有类似热部署的功能。</p></li><li><p>nacos配置中心就可以将nacos中的数据实时更新到微服务的运行内存中。并没有绑定某个文件（热部署），而是<strong>覆盖</strong></p><p>步骤：</p></li></ul><ol><li><p>引入依赖 nacos-config</p></li><li><p>原本配置文件中，添加nacos-config配置中心的ip+端口地址</p></li><li><p>原本配置文件中，给项目绑定配置中心的具体某一配置文件</p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yml"><span class="hljs-attr">cloud:</span><br>  <span class="hljs-attr">nacos:</span><br>    <span class="hljs-attr">discovery:</span><br>      <span class="hljs-attr">server-addr:</span> <span class="hljs-number">127.0</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span><span class="hljs-string">:8848</span><br>    <span class="hljs-attr">config:</span><br>      <span class="hljs-attr">server-addr:</span> <span class="hljs-number">127.0</span><span class="hljs-number">.0</span><span class="hljs-number">.1</span><span class="hljs-string">:8848</span><br><span class="hljs-attr">config:</span><br>  <span class="hljs-attr">import:</span><br>    <span class="hljs-bullet">-</span> <span class="hljs-string">nacos:example.properties?refresh=true</span><br></code></pre></td></tr></table></figure></li><li><p>项目中可以动态获取配置</p><p> @value(“${配置项名}”)</p><p> @ConfigurationProperation()</p></li><li><p>在Controller中添加注解 @RefreshScope 表示每次访问时自动更新配置（将配置中心的配置刷新到项目中）</p></li><li><p>据说：如果配置中心和当前应用的配置文件都配置了相同的项，优先用配置文件的。</p><ul><li><h3 id="配置中心细节"><a href="#配置中心细节" class="headerlink" title="配置中心细节"></a>配置中心细节</h3></li></ul><ol><li><strong>命名空间</strong></li></ol><p>应用场景：</p><ol><li><p>环境隔离：对于开发、测试、上线需要不同的配置。而不可能为这些配置中同类的信息取不同的名，于是就将其大分类</p></li><li><p>环境隔离：对于不同微服务，分开不同配置也合理</p></li></ol><p>没有命名空间时，默认都在public保留空间中</p><p>步骤：</p><ol><li><p>配置文件明确命名空间（类似uuid，由nacos创建的空间确定）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-17-31-01-image.png"></p></li><li><p><strong>配置集</strong>：</p></li></ol><p>配置集合</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-17-37-46-image.png"></p><p>服务器预加载时是根据确定配置集ID加载配置的。</p><p>配置集ID：DataID</p><ul><li>nacos服务器可以乱起，后端配置文件可以乱起，但后端需要找得到nacos的文件</li></ul><ol start="3"><li><strong>配置分组</strong></li></ol><p>配置分组&gt;命名空间&gt;配置集 &#x3D;&#x3D;&#x3D;&gt; 配置分组就是将一系列配置组合成一组，可以同时存在多组</p><p>默认：DEFAULT_GROUP</p><p>应用场景：</p><p>淘宝618，双11，平时，可能需要不同的配置，且是更改全局的大配置。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-18-40-50-image.png"></p></li></ol><p>此项目的配置的配置：每个微服务创建自己的命名空间，使用配置分组区分环境：dev开发环境，test测试环境，prod生产环境</p><p>写法：</p><p><small>由于Spring Boot版本不同，还没查到高版本3.0的写法，于是下面是自己测出来的</small></p><ul><li><p>一个微服务必须在外面写清楚某些东西（不能导入）</p><ul><li><p>name</p></li><li><p>nacos服务器的ip地址</p></li><li><p>配置的group</p></li><li><p>配置的命名空间</p></li></ul></li><li><p>在此范围内，其实限制了许多，只能导入同一group同一命名空间的一些配置集。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-19-53-18-image.png"></p><p>参数也可以跟到导入的后面，似乎group也可以，但没搞好</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-19-47-20-image.png"></p></li></ul><ol start="2"><li><h1 id="Feign"><a href="#Feign" class="headerlink" title="Feign"></a>Feign</h1><p><small>用于微服务之间远程调用的声明式API（封装HTTP）</small></p><p>步骤：</p><ol><li><p>项目引入open-feign，starter.balancer依赖</p></li><li><p>当下微服务下编写接口（声明将用Feign访问目标微服务的Mapper）</p><p>具体API：需要辨明要访问的是哪个微服务，哪个Controller的哪个方法</p><p>具体而言：写明微服务的name，http的requestMapping的全路径</p></li><li><p>开启远程调用功能——将以上配置的资源伴随启动加入到内存中。</p><p>在此，Feign对象&#x2F;接口对象，和原本Spring bean对象一致，但不同在于</p><ol><li><p>Feign对象<img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-16-17-33-image.png"></p><p>参数更多，底层实现方法略复杂，但对上层而言还仅仅是个bean</p></li><li><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-16-19-43-image.png"></p><p>和初始化bean对象的模式不同，需要额外指定一个注解+包扫描范围</p></li></ol></li></ol></li><li><h1 id="Gateway"><a href="#Gateway" class="headerlink" title="Gateway"></a>Gateway</h1><p>作用：网关——作为流量的入口，常用功能包括：路由转发、权限校验、限流控制等。</p><p>            之前用zuul，但被Gateway淘汰。</p><p>三大组成部分：</p><ol><li><p>route 路由：</p><p>决定前往哪里</p><p>参数id和uri</p></li><li><p>predicate 断言：</p><p>决定哪些流量需要走此路由（作用类似urlPattern）</p><p>参数繁多，且是多方位的</p><p>局部举例：</p><ul><li><p>后置断言路由</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-09-15-32-29-image.png"></p><p>接收一个参数，datetime类型&#x2F;ZonedDateTime类型，此断言匹配指定日期之后发生的请求。</p></li><li><p>前置断言路由</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-09-15-33-40-image.png"></p></li><li><p>夹逼——用Between表示两个时间之间</p></li><li><p>Cookie断言路由</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-09-15-34-48-image.png" title="" alt="" width="319"><p>接收两个参数，cookie的名称和regexp（Java的正则）</p></li><li><p>header断言路由</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-09-15-35-41-image.png"></p><p>判断请求头是否含有某类信息</p></li><li><p>由主机host，method，path，Query查参数，IP地址</p></li></ul></li><li><p>Filter 过滤器：</p><p>决定流量进入退出时的钩子函数</p><p><code>和vue路由守卫、httpServlet的Filter略有不同，那里对路由到哪里并不关心，因为在进入过滤器前就明确了，于是用next或者doFilter传递，但这里由于环境是分布式集群，于是更关心具体路由到哪个服务器</code></p></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-20-20-31-image.png"></p></li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-08-20-25-03-image.png"></p><h3 id="-1"><a href="#-1" class="headerlink" title=""></a></h3><h3 id="Java8新特性"><a href="#Java8新特性" class="headerlink" title="Java8新特性"></a>Java8新特性</h3><p>lambda、stream、</p><h4 id="lambda"><a href="#lambda" class="headerlink" title="lambda"></a>lambda</h4><ul><li><p>缘由：</p><p>简化定义接口的实现类的代码，但只能简化只有一个抽象方法的接口</p></li><li><p>写法：</p><p>class classname &#x3D; () -&gt; {重写方法的具体代码}</p><p>例如：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-08-52-24-image.png"></p><p>代码中，lambda<strong>只关心最重要的方法重写的逻辑</strong>，而忽略方法定义、接口多态等已知信息。</p></li></ul><p>由lambda函数引入概念</p><h5 id="函数式编程"><a href="#函数式编程" class="headerlink" title="函数式编程"></a>函数式编程</h5><p>在JS中广泛应用</p><ul><li><p>将数据的变化过程进行抽象</p><ul><li><p>函数可以存储在变量中</p></li><li><p>函数可作为参数</p></li><li><p>函数可作为返回值</p></li></ul></li><li><p>函数是第一等公民（之一）</p><p>传统OOP中，变量，参数，返回值都是对象（基本数据类型也可以视为对象），所以对象是第一等公民，但函数式编程中，函数不属于对象也可以传递。</p></li></ul><p><strong>传递</strong>示例：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-09-25-25-image.png"></p><p>方法接收一个Factory类型的参数</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-09-25-56-image.png" title="" alt="" width="273"><p>Factory可用lambda表达式表示为函数</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-09-26-46-image.png" title="" alt="" width="443"><p>调用方法如下：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-09-29-01-image.png"></p><p>第二种更是一次性、简上又简</p><p><strong>返回值</strong>示例：</p><img title="" src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-09-32-02-image.png" alt="" width="500"><p>语法简写</p><ol><li>函数内只有一行时：省略{}和return（不论唯一的一行是否是return）</li></ol><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-09-39-49-image.png"></p><ol start="2"><li><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-09-44-32-image.png"></li></ol><p>思路：能省就省，能推断就推断</p><p>由于有接口做模板限制，实现类的可变位置其实只有参数名和方法逻辑。</p><h4 id="函数式接口"><a href="#函数式接口" class="headerlink" title="函数式接口"></a>函数式接口</h4><ul><li><p>是使用lambda的前提</p></li><li><p>指有且<strong>只有一个抽象方法的接口</strong>，其他静态方法可以有</p></li><li><p>通常用@FunctionalInterface注解声明</p></li></ul><p>常见函数式接口</p><ol><li><p>Predicate 断言</p><p>唯一方法：test(T t)返回bool值，表示验证 t 是否符合test定义的逻辑</p><hr></li></ol><p>        by the way：</p><p>        接口中的default方法：作用就是让实现类可以调用已经写好的逻辑</p><p>        但问题就是：类可以实现多个接口，就容易导致default方法重名，此时需要重写重名方法。</p><ol start="2"><li><p>Function 表示功能</p><p>泛型内两个参数：&lt;返回值类型，参数类型&gt;</p><p>完成符合泛型规范的功能</p></li></ol><h3 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h3><p>当lambda表达式所要完成的业务逻辑已经存在，则直接引用对应的方法</p><p>语法：</p><ol><li>某对象::方法名</li></ol><p>例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> User <span class="hljs-title function_">factoryDemo</span><span class="hljs-params">()</span>&#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">User</span>(<span class="hljs-string">&quot;还是&quot;</span>,<span class="hljs-string">&quot;99&quot;</span>); <br> &#125;<br><br><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> factoryBean(<span class="hljs-keyword">new</span> <span class="hljs-title class_">lambdaDemo1</span>()::factoryDemo, <span class="hljs-string">&quot;德智&quot;</span>);<br></code></pre></td></tr></table></figure><p>具体语法</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-10-37-04-image.png"></p><p>静态方法（类方法）、构造方法，直接通过类名调用，普通方法（成员方法）通过对象调用</p><p>可以被引用的方法是有限制的：（要求被引用的方法格式符合lambda中重写方法的格式）</p><ol><li><p>参数数量相同，对应位置类型兼容</p></li><li><p>返回值类型兼容</p><p>在函数式接口中如果是void，具体实现类返回类型任意</p><p>在函数式接口中如果有限制（返回int、User…），具体实现类只能返回相应的</p></li></ol><p>以上兼容是指函数式接口的抽象方法，和被引用的某个方法之间</p><p>例：</p><ol><li>引用静态方法</li></ol><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-10-45-47-image.png" title="" alt="" width="511"><ol start="2"><li>引用构造方法</li></ol><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-10-51-29-image.png" title="" alt="" width="555"><p>编译器会根据当前位置传入什么参数去自动寻找合适的构造方法</p><ol start="3"><li>引用普通方法&#x2F;成员方法</li></ol><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-10-10-56-19-image.png" title="" alt="" width="563"><p>由于方法引用只明确方法名，所以对重载的方法进行选择是编译器自动完成的，就也是根据参数和返回值找到合适的方法</p><h3 id="Stream流"><a href="#Stream流" class="headerlink" title="Stream流"></a>Stream流</h3><p>是Collection顶级父类接口中的defualt方法，目的就是简化容器操作，提高多处理器的性能</p><p>关注”做什么“，而非”怎么做“</p><p>感觉就是用流替代for遍历</p><p>使用步骤：</p><ul><li><p>获取流 —— 中间操作 —— 终结操作</p><p>对Stream流的操作分为中间操作和终结操作</p><p><strong>中间操作</strong>：返回的任是流对象</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-15-25-47-image.png"></p><p><strong>终结操作</strong>：没有返回值</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-15-25-34-image.png"></p><p><strong>短路操作</strong>：也是终结操作，只是一旦条件命中就停止，应用于对流处理时间有限制的场景。</p></li><li><p>真实执行</p><p><strong>lazy延迟执行</strong></p><p>据说在对流的多次聚合时，中间操作并非按顺序执行，而是先缓存到某个队列中，直到碰到终结操作，才开始执行——给了并行的可能——or say 中间操作先后其实无影响。</p><p><strong>底层并行</strong></p><p>用 Fork&#x2F;Join 分治处理</p><p><strong>本质上是回不去的</strong></p><p>加并行操作并非是在流跑完一般后再跑一遍，而是加入并行队列分治跑。</p><p>而一旦对流对象操作到最后（也就是终结操作完后），流对象就不能再进行任何操作了</p></li><li><p>API之中间操作</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-15-35-19-image.png"></p></li><li><p>API之终结操作</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-15-35-37-image.png"></p></li><li><p>API之收集操作</p><p>语法：stream.collect(收集器)</p><p>收集器明确将流的每个实体归纳成什么形式</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-15-55-41-image.png"></p></li><li><p>by the way</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-10-16-08-34-image.png"></p><p>参数Function类型就是典型用lambda写函数式接口的。</p></li></ul><hr><hr><h3 id="商品服务"><a href="#商品服务" class="headerlink" title="商品服务"></a>商品服务</h3><ol><li><h4 id="三级分类"><a href="#三级分类" class="headerlink" title="三级分类"></a>三级分类</h4><p>业务目标：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-09-16-31-27-image.png"></p><ul><li><p>数据库中要存储每个分类及其所属关系</p></li><li><p>后端需要做出符合层次关系的数据类型</p></li></ul><hr><p><strong>数据库</strong>：</p><p>存储的每行表示某层的一个分类</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-09-16-34-47-image.png"></p><h5 id="构建理论："><a href="#构建理论：" class="headerlink" title="构建理论："></a>构建理论：</h5><p>用cat_id和层级确定此行在分类树的具体位置</p><p>用parent_id确定上下层关系——树型结构</p><p><code>如此一来，在确定了最上层的分类后，表中父id是最上层的就根据父安排在第二层，第三层同理，那感觉层级字段非必要，可能是为了查找性能考虑吧</code></p><h5 id="查找代码逻辑："><a href="#查找代码逻辑：" class="headerlink" title="查找代码逻辑："></a>查找代码逻辑：</h5><p>如此一来，如果想要找某个顶级类的所有子类的分组，就是遍历底层用父id匹配出来。</p><p>代码实现思路：</p><ol><li><p>流处理+过滤器+lambda滤出最上层每个实体，对最上层每个实例设置其下层子实体。</p></li><li><p>具体怎么设置：</p><p>流处理+过滤器+lambda滤出第二层、父id是对应实体的下层实体</p><p>同时由于整个分类有三层，还要考虑用递归再来一遍</p></li><li><p>得到的流中是设置了子类的顶级大类们，再用sorted+lambda按照某顺序排序</p></li><li><p>最后将流转化成列表</p></li></ol><p><small>流处理：完成过滤（Filter）+集合内实体分别操作（map）+排序（sorted）</small></p></li></ol><hr><h3 id="解决前端发8080需要分配到后台各个端口的微服务的问题"><a href="#解决前端发8080需要分配到后台各个端口的微服务的问题" class="headerlink" title="解决前端发8080需要分配到后台各个端口的微服务的问题"></a>解决前端发8080需要分配到后台各个端口的微服务的问题</h3><ul><li><p>由于前后端分离开发，且暂时不考虑完美的API文档。前端的所有xhr请求都默认发送给后台8080端口，但后台的设计是：8080只负责基础框架和crud，其他业务相关的代码按照微服务的方式分配到各个子模块——且各个微服务占有不同端口，统一注册到Nacos注册中心进行管理。</p></li><li><p>解决方法是：后台需要一个分流器将不同需求分流到不同微服务</p><ul><li><p>Gateway可以拦下所有请求</p></li><li><p>Gateway配合Nacos可以将请求送到Nacos注册中心的某个微服务上</p></li><li><p>前端需要将默认发送方式从8080改为到网关</p></li></ul></li><li><p>网关（Gateway）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-08-58-49-image.png"></p><p><strong>uri</strong>：Uniform Resource Identifier 统一资源标识符</p><p>用来表示互联网上资源的字符串序列</p><p>uri包括两种形式：URL  URN</p><p><strong>URL</strong>：统一资源定位符：</p><p>表示一个资源的完整地址，协议包含：http，https，ftp，</p><blockquote><p>例如，<a href="http://www.example.com/index.html">Example Domain</a> 是一个URL。</p></blockquote><p><strong>URN</strong> : 统一资源名称：</p><p>重点在于为资源分配一个唯一的名称，而不关心资源的具体位置。</p><blockquote><p>例如，urn:isbn:0451450523 是一个URN，它标识了一个书籍的国际标准书号（ISBN）。</p></blockquote><hr><p><strong>网关的负载均衡</strong></p><p>网关通过将请求转发到注册中心的同种微服务的不同个体上，实现负载均衡</p><p>大致流程：</p><ul><li><p>网关在后台配置了路由到的微服务</p><blockquote><p>lb:&#x2F;&#x2F;serviceName</p></blockquote></li><li><p>当网关受到请求，首先得到注册中心关于此服务的所有实例信息。</p></li><li><p>根据指定的负载均衡算法（如轮询、随机、权重等），从这些实例中选择一个作为目标。</p></li><li><p>请求转发：</p><p><big>Gateway 将接收到的请求转发到所选的目标实例，这个实例可能是多个同名服务实例中的一个。这个转发过程<strong>是透明的</strong>，客户端无需关心具体的目标实例.</big></p><p>透明：暂不明是通过Feign还是原生http还是其他内部通信协议…</p></li></ul></li></ul><h4 id="记录关于浏览器缓存Ajax的get方法结果"><a href="#记录关于浏览器缓存Ajax的get方法结果" class="headerlink" title="记录关于浏览器缓存Ajax的get方法结果"></a>记录关于浏览器缓存Ajax的get方法结果</h4><ul><li><p>当多次用get+相同的url访问资源</p></li><li><p>服务器返回的响应信息中包含<code>Cache-Control</code>、<code>Expires</code>、<code>Last-Modified</code>等信息，表示能否进行缓存</p></li></ul><p>破解浏览器缓存：</p><ol><li><p>在URL中添加唯一参数</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-17-00-57-image.png"></p><ul><li><p>默认的一个参数 t 获取当前时间戳</p></li><li><p>将 t 和传入参数拼接，一并传入</p></li></ul></li><li><p>设置缓存头部信息</p><p>服务器可以在响应头部设置 <code>Cache-Control: no-cache</code> 和 <code>Expires: 0</code> 等头部信息，告诉浏览器不要缓存响应。</p></li><li><p>用POST据说不会被缓存。</p><p><code>因为它们被认为是非幂等的，即可能对服务器状态产生影响，因此浏览器通常不会缓存 POST 请求的响应。</code></p></li></ol><h4 id="关于拖拽"><a href="#关于拖拽" class="headerlink" title="关于拖拽"></a>关于拖拽</h4><p><small>简记业务逻辑</small></p><ul><li><p>业务逻辑都是基于Element封装好的API：</p><ol><li><p>拖动的视觉动画</p></li><li><p>钩子们&#x2F;由MVVM监听的事件</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-19-50-17-image.png"></p><blockquote><p>allow-drop是在每次按住鼠标拖动时进行判断，这种封装很上层，但问题也就在于太上层了——如果我要在松开鼠标的时候判断，就做不到</p></blockquote></li><li><p>向上提供的API</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-12-08-41-31-image.png" title="" alt="" width="440"></li></ol></li></ul><h3 id="需求分析"><a href="#需求分析" class="headerlink" title="需求分析"></a>需求分析</h3><ul><li><p><strong>拖拽时需要判断能否拖拽</strong></p><p>项目的思路是总层数不能超过三层</p><p>获取正在拖动的标签的最大层数，以及目的地的父节点的层数，相加后是否超过三层</p><p>而如何获取最大层数？</p><p>Element提供的钩子给的参数是当前的节点，于是想要挨个遍历每个子节点，只好<strong>dfs深度遍历</strong>了。</p></li><li><p><strong>拖拽后对后端的影响（对表的影响）</strong></p><ol><li><p>影响 parentId 父节点</p></li><li><p>影响 catLevel 当前节点层级</p></li><li><p>影响 sort 节点们的排序</p></li></ol></li><li><p><strong>拖拽获取信息的具体实现（全靠API提供）</strong></p><p><big>核心在于真正理解Element提供的参数</big></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-20-15-31-image.png"></p><p>三个参数分别是：</p><ol><li><p>正在拖动的节点</p></li><li><p>拖动后较近的节点（目标节点）</p></li><li><p>拖动节点和目标节点的关系</p></li></ol><p><strong>钩子方法：指前端执行到某个阶段自动调用的方法，所以所有参数都是系统传入</strong></p><p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-11-20-19-28-image.png" title="" alt="" width="198">         ————&gt;           <img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-11-20-19-43-image.png" title="" alt="" width="206"></p><p>然后就取决于如何解构Element传递的参数</p></li></ul><h4 id="额外业务："><a href="#额外业务：" class="headerlink" title="额外业务："></a>额外业务：</h4><ul><li><p><strong>关于拖动排序</strong></p><p>对于拖入inner，直接将节点合并到子节点，重排。前端没有必要的顺序，也不必调整库</p><p>对于拖入after，before，后端需要保存拖动后的顺序 —— 在表中只好将拖动后同级节点全部发送给后端，后端将新排好的数据存入表中。</p></li><li><p><strong>关于拖动多次后一并提交到后端</strong></p><p>场景分析：每次拖动都会请求后台，后台再请求数据库，对性能消耗略高</p><p>业务分析：</p><ul><li><p>由于拖拽对后台&#x2F;表的影响无非是更新一张产品表内多行数据，前端缓存中如果有重复修改也可按照最新数据更改。</p></li><li><p>问题在于：缓存的部分没有及时更新到库，导致对接下来的判断没有产生影响。</p><p><code>是缓存的通病————被缓存的数据无法生效</code></p><p>答案：前端存储更多的数据 —— 对于每个参与变化的节点，实时更新其层级。</p></li></ul></li></ul><p><small>简记后端</small></p><p>前端传递的数据是：由于拖动会改变多了Category行，所以前端决定返回所有移动的行以及目的地。</p><p>后端用Category[]接收，SpringMVC即可自动投影成一系列Category对象。</p><p>之后分别update or 调用封装好的 updateBatch 全部更改即可</p><h3 id="關於文件上傳OSS雲服務器"><a href="#關於文件上傳OSS雲服務器" class="headerlink" title="關於文件上傳OSS雲服務器"></a>關於文件上傳OSS雲服務器</h3><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-09-33-47-image.png"></p><ol><li><p>傳統單服務器項目，文件跟隨請求到達服務器，即保存在服務器。</p></li><li><p>但在分佈式項目中，由於瀏覽器每次的請求會被隨機路由到不同的服務器上，導致可能找不到上次傳輸的文件。</p></li><li><p>版本答案是：<strong>提另準備一臺服務器</strong>用來存儲文件。將文件上傳到統一的服務器</p><p>AOP？– 抽象出相同的部分統一操作。？高内聚？</p></li><li><p>專用文件存儲服務器的模式</p><ol><li><p>自建！：</p><blockquote><p>FastDFS（Fast Distributed File System）是一个开源的<strong>分布式文件系统</strong>，旨在提供高性能、高可用性的文件存储解决方案。它是为了解决大规模文件存储和访问的问题而设计的。FastDFS 主要用于存储大量的媒体文件（如图片、音频、视频等）以及其他类型的文件。</p></blockquote><p>思想似乎是有一個管理系統調度&#x2F;記錄分佈式的服務器和文件的對應關係</p><hr><blockquote><p>vsftpd（Very Secure FTP Daemon）是一个开源的<strong>FTP 服务器软件</strong>，旨在提供一个安全、稳定、高性能的<strong>文件传输协议（FTP）服务器</strong>。它是在类 Unix 操作系统上运行的，如 Linux、FreeBSD 等，并且被广泛用于搭建 FTP 服务器，以方便文件的上传、下载和管理。</p></blockquote></li><li><p>雲存儲：</p></li></ol></li></ol><h2 id="分布式文件系统"><a href="#分布式文件系统" class="headerlink" title="分布式文件系统"></a>分布式文件系统</h2><p><small>命运的齿轮从此转动</small></p><blockquote><p>FastDFS是一个开源的分布式文件系统，实现的功能有：文件存储，文件同步，文件访问（上传、下载）</p><p>核心性能：大容量存储和负载均衡问题</p><p>机制：冗余备份，负载均衡，线性扩容…</p></blockquote><h3 id="阿里雲OSS搭建雲存儲"><a href="#阿里雲OSS搭建雲存儲" class="headerlink" title="阿里雲OSS搭建雲存儲"></a>阿里雲OSS搭建雲存儲</h3><h5 id="資源術語："><a href="#資源術語：" class="headerlink" title="資源術語："></a>資源術語：</h5><ol><li><p>存儲空間 Bucket</p><p>劃分出存儲文件的一個基本單元，對此基本單元可以設置相關存儲配置</p></li><li><p>對象&#x2F;文件 Object</p><p>對象是OSS存儲數據的基本單元，對象由<strong>存儲空間内的唯一的key</strong>來標識</p></li><li><p>地域 Region</p><p>表示OSS的數據中心所在物理位置（各個的費用不同）</p></li><li><p>訪問域名 Endpoint</p><p>表示OSS對外服務的訪問域名，OSS以HTTP RESTful的形式對外提供服務。</p><p>據説訪問不同地域所需的域名不同</p></li><li><p>訪問密鑰（yao） AccessKey</p><p>訪問身份驗證中用到的AccessKeyId和AccessKeySecret</p><p>類似Gitee的密鑰</p></li></ol><h5 id="上傳模式"><a href="#上傳模式" class="headerlink" title="上傳模式"></a>上傳模式</h5><ol><li><p>簡單思路</p><p>前端提交multiPart的文件，後端java程序接受，獲取文件流后寫入OSS服務器</p><p>好處：後端是必經之路，可以審核、修改、鑒權…而且後端是唯一有權利上傳的，密鑰只保存在後端即可</p><p>壞處：後端壓力太大</p><p><strong>简述实现</strong>：</p><p>Java后台引入依赖OSS，配置对目标的识别+确认信息：</p><ol><li><p>OSS 服务器的对外访问端口</p></li><li><p>bucket的name</p></li><li><p>密钥id</p></li><li><p>密钥密码</p></li></ol><p>依赖为容器自动注入OssClient对象，调用其流式传输方法即可完成上传。</p></li><li><p>地道思路</p><p><big>服務器簽名后直傳</big></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-10-23-08-image.png"></p><ol><li><p>用戶向服務器獲取加密後的密鑰，添加在文件某處，直接傳輸給OSS</p></li><li><p><code>感覺和Gitee的區別在於，Gitee是自己上傳自己的，無需證明自己的身份，而OSS系統則需要驗證用戶能否上傳，於是還得後端插一脚</code></p></li></ol></li></ol><h4 id="关于文件上传OSS"><a href="#关于文件上传OSS" class="headerlink" title="关于文件上传OSS"></a>关于文件上传OSS</h4><ol><li><p>前端收集到文件&#x2F;表单信息后，对文件&#x2F;表单进行<strong>初步校验。</strong></p><p>校验很不简单——构造rules，写validator… </p></li><li><p>前端在之前需要先获取到后台的<strong>认证标签</strong>，用于在向OSS传输文件时标识身份</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-19-28-52-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-19-29-12-image.png"></p></li></ol><h4 id="关于前端校验"><a href="#关于前端校验" class="headerlink" title="关于前端校验"></a>关于前端校验</h4><ol><li><p>前端数据校验的目的有：减少无效的网络请求、提高用户体验（更快返回校验错误）…</p></li><li><p>但由于前端校验容易被绕过，所以后端校验才是最终的屏障</p></li></ol><ul><li><p>可能的校验技术：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816151006236.png" alt="image-20230816151006236"></p></li></ul><h4 id="关于后台校验"><a href="#关于后台校验" class="headerlink" title="关于后台校验"></a>关于后台校验</h4><ol><li><p>确保数据在进入数据库或进行后续处理之前的合法和安全。校验的对象不止需求手册上来自前端的潜在异常输入，也有绕过前端的其他输入。</p></li><li><p><code>Hibernate Validator 是实现了 Java Bean Validation（JSR 303）规范的一个库。因此，它实际上是 JSR 303 的一个具体实现。</code></p></li></ol><ul><li><p>JSR303：Java规范请求（Java Specification Request）定义了java中对Bean的验证规范。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-17-25-27-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-19-30-52-image.png"></p><p>此包下含标识各种规范的注解</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-12-17-26-46-image.png" title="" alt="" width="214"></li><li><p>后台的校验逻辑：</p><p>当通过RequestBody接收到实体对象，会按照实体类中，属性上声明的校验注解来验证。</p><blockquote><p>需要对需要校验的接收对象标注 @Valid 标签</p></blockquote><ul><li><p>后台验证不过的请求：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-12-19-31-55-image.png"></p><ul><li><p>400：服务器不理解请求</p><ul><li><p>bad request 错误的请求——参数错误没法接受–&gt;校验不过</p></li><li><p>invalid hastname 域名不存在</p></li></ul><p>400还未执行到controller就停了</p></li></ul></li></ul></li><li><p>统一异常处理：</p><p><small>本质是封装原生异常，封装成更符合业务的异常</small></p></li><li><p>简单异常处理：</p><ul><li><p>用@Valid声明的RequestBody，通过就近声明BindingResult可以获取到JSON投影对象的结果。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@RequestMapping(&quot;/Demo&quot;)</span><br>    <span class="hljs-keyword">public</span> R <span class="hljs-title function_">Demo</span><span class="hljs-params">(<span class="hljs-meta">@Valid</span> <span class="hljs-meta">@RequestBody</span> CategoryEntity category, BindingResult result)</span> &#123;<br><br>        <span class="hljs-keyword">if</span> (result.hasErrors()) &#123;<br>            HashMap&lt;String, String&gt; objectObjectHashMap = <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br>            List&lt;FieldError&gt; fieldErrors = result.getFieldErrors();<br>            fieldErrors.stream().forEach((item) -&gt; &#123;<br>                <span class="hljs-type">String</span> <span class="hljs-variable">defaultMessage</span> <span class="hljs-operator">=</span> item.getDefaultMessage();<br>                objectObjectHashMap.put(item.getField(), defaultMessage);<br>            &#125;);<br>            System.out.println(<span class="hljs-string">&quot;YES&quot;</span>);<br>            <span class="hljs-keyword">return</span> R.error(<span class="hljs-number">400</span>, <span class="hljs-string">&quot;测试用例1&quot;</span>).put(<span class="hljs-string">&quot;data&quot;</span>,objectObjectHashMap);<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-keyword">return</span> R.ok(<span class="hljs-string">&quot;我们意念合一！&quot;</span>);<br>        &#125;<br>    &#125;<br></code></pre></td></tr></table></figure></li><li><p>声明了BindingResult后——@Valid接收错误报异常的情况就被BindingResult屏蔽了</p><p><small>声明了一个形参——重写了全部异常处理</small></p></li></ul></li><li><p>validate 自定义规则</p><p>通过写正则</p></li></ul><h5 id="统一异常处理："><a href="#统一异常处理：" class="headerlink" title="统一异常处理："></a>统一异常处理：</h5><ul><li><p>由于假如对每个Controller的方法都用BindingResult异常处理，有点重复有点麻烦</p></li><li><p>答案是：让程序默认报自定义错——不用BR重写，而是全局地统一重写报错操作</p></li></ul><p>要点：</p><ol><li><p>在外面定义的全局异常类需要几个必备参数（配置）</p><ul><li><p>整个异常类——明确包扫描范围</p><p>@ControllerAdvice就是表示异常处理类</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816170221767.png" alt="image-20230816170221767"></p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816170308419.png" alt="image-20230816170308419"></p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816170405581.png" alt="image-20230816170405581"></p></li></ul><p>通过给一个java类加注解，就将所有目的范围内的异常全部引流过来——钩子？AOP？过滤器？…在Spring的思想中，这个注解一定<strong>将类声明成bean对象</strong>加载到容器，在底层报异常时调用此bean的方法。</p><p><big>避免面向对象，而是面向切面</big>——<small>仅仅一个注解，便只需要关心业务逻辑</small></p></li></ol><ul><li><p>每个方法——明确针对的异常</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816171136146.png" alt="image-20230816171136146"></p></li></ul><h5 id="用好错误码："><a href="#用好错误码：" class="headerlink" title="用好错误码："></a>用好错误码：</h5><p>思路：用<strong>枚举类型</strong>将不同错误类型对应不同错误码，前端接收各种各样错误码。</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816172515854.png" alt="image-20230816172515854"></p><p>一种枚举类型相当于一个对象，内部都是静态属性（还是面向对象啦）</p><h4 id="分组校验："><a href="#分组校验：" class="headerlink" title="分组校验："></a>分组校验：</h4><ul><li><p>由于一张表的某一字段在不同业务中的校验模式不同（id在新增行时可为null，在删改时不能为null），于是需要将每张Entity表的每个属性的每个校验注解增加分组。最后在Servlet方法接收参数时声明具体分组</p></li><li><p>Attention：</p><p>每个分组的定义方法是：用一个接口声明，用接口的字节码对象做具体识别符。</p></li></ul><h4 id="自定义校验："><a href="#自定义校验：" class="headerlink" title="自定义校验："></a>自定义校验：</h4><p>步骤：</p><ol><li>编写自定义校验注解</li><li>编写自定义校验器</li><li>关联自定义校验器和自定义注解</li></ol><p>思路：</p><ul><li><p>编写注解：和其他校验注解声明同样的元注解，声明同样的插槽&#x2F;参数</p></li><li><p>编写校验器：</p><ul><li><p>校验器是实现校验器接口的一个类，它的核心作用是：拿到校验注解得到的参数，在校验器中完成逻辑判断</p></li><li><p>怎么拿到？—— 泛！</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816191517333.png" alt="image-20230816191517333"></p><p><small>之前对泛型传参的理解，只有泛型能够传递类型（baseMapper由Entity类似确定上层）</small></p><p>此时泛型传入一个注解类型，由此类型即可得到其被传入的参数</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816192114204.png" alt="image-20230816192114204"></p><p>可见：注解不同于类和类对象，其在内存中似乎是一个唯一的类型。</p></li></ul></li><li><p>关联二者：</p><ul><li><p>谁是在主程序中能够被扫描到的？：注解——于是需要在声明注解时绑定校验器</p><p>感觉双绑有点多余…</p></li></ul></li></ul><h3 id="SPU-and-SKU"><a href="#SPU-and-SKU" class="headerlink" title="SPU and SKU"></a>SPU and SKU</h3><ol><li><p>SPU ( Single Product Unit ) : </p><p>单独产品单元：具有相同基本属性和特征的商品集合</p></li><li><p>SKU（Stock Keeping Unit）：</p><p>库存单位：对具体产品进行编码和表示</p></li><li><p>一个SPU可能包含多个SKU：例如：S20是SPU，黑色128g和绿色256g分别是一个SKU。</p></li></ol><h3 id="基本属性（规格参数）and-销售属性"><a href="#基本属性（规格参数）and-销售属性" class="headerlink" title="基本属性（规格参数）and 销售属性"></a>基本属性（规格参数）and 销售属性</h3><ol><li><p>SPU对应的属性就是基本属性</p></li><li><p>区分不同SKU的属性就是销售属性</p></li></ol><ul><li><p>特征：</p><p>基本属性和销售属性都能被各自的<strong>三级分类</strong>组织起来</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230816195600137.png" alt="image-20230816195600137" style="zoom: 50%;" /></li></ul><hr><hr><h3 id="数据库的逻辑删除"><a href="#数据库的逻辑删除" class="headerlink" title="数据库的逻辑删除"></a>数据库的逻辑删除</h3><ul><li><p>当对某表进行删除时，没有实质上删除数据库中数据，而是通过将某个字段设置为反，或者其他信息，来表示对上不可见。</p></li><li><p>分析：如此一来：关于这个字段的查询需要凌驾于许多SQL之上，比如select，delete，update，insert都需要判断此字段，需要一个对所有SQL都管理的工具</p></li><li><h3 id="myBatis-Plus"><a href="#myBatis-Plus" class="headerlink" title="myBatis-Plus"></a>myBatis-Plus</h3><p>对于自动注入的SQL</p><p>实际上，mp的模式是：<strong>基于接口的映射</strong></p><p>通过<strong>继承通用的Mapper接口</strong>来实现数据库操作，避免手写大量的CRUD代码</p><p>so my question is —— 表和表不一样，继承下来的方法不可能都不同吧？</p><p>答案：<strong>通过泛型传递数据</strong></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-16-35-10-image.png"></p><p>Service层的实现类继承了一个大接口，用泛型传递了这个Service要操作的表。</p><p>于是针对这张表的CRUD代码已经确定</p><p><small>一个问题是：主键是谁在Java的entity对象中体现不来，这在mp的默认规则是：叫id的是主键，或者在entity的主键上加注解@TableId声明主键</small></p></li></ul><p>mp逻辑删除：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-11-16-41-56-image.png"></p><h2 id="分布式高级篇"><a href="#分布式高级篇" class="headerlink" title="分布式高级篇"></a>分布式高级篇</h2><h3 id="ElasticSearch"><a href="#ElasticSearch" class="headerlink" title="ElasticSearch"></a>ElasticSearch</h3><p>是一个分布式搜索引擎</p><p><code>Elasticsearch可以为所有类型的数据提供实时的搜索和分析，包括结构化文本、非结构化文本、数字数据或地理空间数据。Elasticsearch都能够以快速搜索的方式有效地对其进行存储和建立索引（search其实就是对索引的快速查找）。 ElasticSearch不仅可以进行简单的数据检索，还可以聚合信息来发现数据中的趋势和模式。 随着数据和查询量的增长，Elasticsearch的分布式特性可以使部署的集群随之无缝地扩容。</code></p><p>应用场景：</p><ol><li><p>为应用（APP）或网站添加内容搜索框 | 全文检索</p></li><li><p>存储和分析日志、监测值、和安全事件数据 | ELK系统</p></li><li><p>使用机器学习自动实时为数据行为建模</p></li></ol><p>几个ES相关技术关键词：</p><p>全文检索，倒排索引，封装Lucene jar包，ELK：ElasticSearch+Logstach+Kibana</p><hr><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-18-10-37-25-image.png"></p><hr><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-18-10-37-57-image.png"></p><hr><h3 id="项目搭建Nginx服务器"><a href="#项目搭建Nginx服务器" class="headerlink" title="项目搭建Nginx服务器"></a>项目搭建Nginx服务器</h3><p>搭建背景 —— <strong>动静分离</strong> ：</p><p>如果将动态资源和静态资源全都部署在Tomcat服务器上，则对业务的请求和对静态资源的请求全都到达Tomcat服务器，但考虑到<strong>服务器可贵的并发性能</strong>，应该主要交给业务逻辑。于是需要将对静态资源的请求分配到其他服务器。</p><p>动静分离：</p><p>静：图片，JS，CSS等静态资源（以实际文件存在）</p><p>动：服务器需要处理的请求（以代码存在）</p><hr><p>项目的后台管理系统是vue，但将首页等页面放到Tomcat中（各个微服务的resource）</p><p>即：每个微服务不仅有后端逻辑，也内聚了前端的页面</p><p>核心：用 <strong>thymeleaf</strong> 模板引擎</p><blockquote><p>thymeleaf是一款用于渲染XML&#x2F;XHTML&#x2F;<a href="https://so.csdn.net/so/search?q=HTML5&spm=1001.2101.3001.7020">HTML5</a>内容的模板引擎。类似JSP</p></blockquote><h3 id="Thymeleaf"><a href="#Thymeleaf" class="headerlink" title="Thymeleaf"></a>Thymeleaf</h3><p>作用类似 JSP，是一种写html时插入java对象（属性）的语法</p><p>具体而言：</p><p>在Tomcat服务器的resource资源中写有html文件</p><p>文件中通过${}插槽，将内部的内容替换为Controller中插入的同名内容</p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-08-18-16-09-48-image.png" title="" alt="" width="418"><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-18-16-10-03-image.png"></p><p>return 表示要进入某个视图 xxx.html</p><p>某处的配置文件设置有默认前缀后缀 于是可以直接写html文件名</p><ul><li><p>静态资源存放在static文件夹下，即可按照路径直接访问</p></li><li><p>页面放在templates下，直接访问</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-18-16-26-21-image.png"></p><p>springBoot为thymeleaf进行了自动配置 —— 直接访问端口，自动寻找index.html</p></li></ul><h2 id="搭建域名访问环境"><a href="#搭建域名访问环境" class="headerlink" title="搭建域名访问环境"></a>搭建域名访问环境</h2><ul><li><p>正规流程：</p><ol><li><p>购买域名</p></li><li><p>买一台服务器，可以用虚拟专用服务器&#x2F;云服务器，服务器的操作系统是Linux</p></li><li><p>设置域名解析：将域名指向服务器的IP地址</p></li><li><p>服务器上安装web服务器，如Nginx，Tomcat…</p></li><li><p>配置服务器路由：确保将访问服务器的请求路由到web服务器&#x2F;上传的文件</p></li><li><p>设置安全性：配置防火墙、SSL证书等</p></li></ol></li></ul><h4 id="正向代理和反向代理："><a href="#正向代理和反向代理：" class="headerlink" title="正向代理和反向代理："></a>正向代理和反向代理：</h4><p><code>正向代理反向代理作用在同一位置，区别在于封装（代理）的方向</code></p><p>正向代理：服务器代理客户端，去访问原本客户端访问受限的资源</p><p>反向代理：服务器代理后端集群，作为集群的入口&#x2F;门面让客户端访问</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-18-17-13-02-image.png"></p><h2 id="压力测试"><a href="#压力测试" class="headerlink" title="压力测试"></a>压力测试</h2><p><small>对一个系统&#x2F;微服务，在一定的软硬件环境下（linux，4g内存，4核cpu），能够承受的最大负荷量（系统瓶颈）</small></p><p>在压测得出一个系统的瓶颈后，可以通过负载均衡等操作，避免系统压力过大-宕机</p><ul><li><p>压力测试可以发现些单线程场景下不会暴露的<strong>问题</strong></p><ol><li><p><strong>内存泄漏</strong> —— 程序执行中创造了过多对象（占用太多内存）而没有复用</p><p><code>内存泄漏（Memory Leak）是指在计算机程序中，分配的内存空间在不再被程序使用的情况下未能释放，从而导致系统中的可用内存逐渐减少，最终可能导致系统性能下降甚至崩溃。</code></p><p>引发原因：</p><ol><li><p>未释放分配的内存：</p><p>当不再需要某对象&#x2F;某处内存时，没有调用释放内存的函数</p></li><li><p>循环引用：</p><p>对象之间形成了循环引用，即比例引用导致无法被垃圾回收机制正确处理，从而无法释放内存</p></li><li><p>未关闭资源：</p><p>例如文件、网络连接、熟即可连接没有正确关闭，导致资源任然被程序占用，且内存任然被占用</p></li><li><p>缓存未清理：</p><p>由于缓存机制导致大量数据没有被清理而导致占用内存</p></li><li><p>异常退出：</p><p>异常退出导致释放资源和内存的机会被忽略</p></li></ol></li><li><p><strong>并发与同步问题</strong> </p><ul><li>并发：由于多个线程同时执行，导致资源竞争、数据不一致等其他意外行为</li><li>同步：为了解决并发问题，确保多个线程安装顺序执行</li></ul><p>常见的并发和同步问题：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230820170303187.png" alt="image-20230820170303187"></p></li></ol></li><li><p>压测指标</p><ol><li><p>TPS |Transaction per second 每秒完成交易&#x2F;事务数</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230820170857888.png" alt="image-20230820170857888"></p></li></ol></li></ul><h4 id="Jmeter"><a href="#Jmeter" class="headerlink" title="Jmeter"></a>Jmeter</h4><p>压测工具，使用逻辑是：</p><p>底层创建<strong>线程池</strong> —— 线程组中每个线程并非对应本机一个端口，而是通过Jmeter创建的线程池完成通信。线程池或许在必要的情况下利用多个线程吧</p><h3 id="压测："><a href="#压测：" class="headerlink" title="压测："></a>压测：</h3><ul><li><p>影响性能的考虑点：</p><ol><li><p>数据库：</p><ol><li><p>连接池管理</p><ul><li><p>合理的连接数限制，过多的连接数在维持中降低了性能，过低的连接数会导致排队。</p></li><li><p>连接超时设置</p><p><code>合理的连接超时时间可以防止应用程序陷入无响应状态。</code></p></li></ul></li><li><p>关于预编译</p><p><code>使用预编译语句（Prepared Statements）来执行数据库查询。预编译语句可以减少每次查询的解析时间，提高执行效率。</code></p></li><li><p>SQL语句层面的优化</p><ul><li><p>合理使用索引：使用索引缩短查询时间，过多使用导致维护成本过高</p></li><li><p>对于大批量的数据操作，考虑使用批量操作减少交互次数</p></li></ul></li><li><p>使用缓存机制</p><p><code>对于频繁读取但不经常变动的数据，可以考虑使用缓存机制，如内存缓存或分布式缓存。</code></p></li></ol></li><li><p>应用程序</p></li><li><p>中间件</p><ol><li><p>Nginx</p></li><li><p>网关</p></li><li><p>Tomcat</p></li></ol></li><li><p>网络，带宽，操作系统</p></li></ol></li></ul><p><big>性能分析，就是<strong>监控分析</strong>某个程序占用内存、CPU、线程、耗时…需要了解Java程序执行过程 ——<strong>JVM</strong>内存模型</big></p><h4 id="JVisualVM"><a href="#JVisualVM" class="headerlink" title="JVisualVM"></a>JVisualVM</h4><p>动态检测内存工具</p><p>其中，visual GC插件可以检测Minor GC，Full GC等行为</p><h5 id="中间件对性能的影响："><a href="#中间件对性能的影响：" class="headerlink" title="中间件对性能的影响："></a>中间件对性能的影响：</h5><p><code>由于在项目环境中，请求的传发流程是：浏览器发送请求先到达Linux虚拟机（192.168.56.10）中的Nginx（80端口），Nginx完成负载均衡，动静分离，将必要的请求转发到Gateway网关，网关通过配置文件各种断言匹配所来的请求，将其路由到不同微服务，最终执行后台逻辑代码。</code></p><p>ATTENTION：</p><p><strong>为什么要原路返回</strong> ？ 因为要对上层屏蔽细节？</p><ol><li><p>Nginx</p><p>检测方法：</p><ol><li><p>用Jmeter直接向Nginx服务器发送请求（不会路由下去的请求，由于Nginx只能识别对域名的访问，于是只要直接对IP地址访问就不会被路由匹配），测试响应的时间、吞吐量…</p></li><li><p>由于Nginx在docker中，通过命令：docker stats查看容器的性能</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-20-29-01-image.png"></p></li></ol></li><li><p>Gateway</p><p>检测方法：</p><ol><li><p>Jmeter向网关发送请求，检测响应的时间、吞吐量…</p></li><li><p>由于Fateway是Java进程，可以用JvisualVM进行可视化检测</p></li></ol></li></ol><p>结果：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-20-41-53-image.png"></p><p>访问网关大概时间13s，直接访问Java后台时间大概10s，于是访问网关+简单服务大概是二者之和</p><p>由于访问时间大致翻倍了，导致吞吐量大致砍一半了—— 线程数是一定的，每个线程耗时长导致来回次数少。</p><p>结论：</p><ul><li><p>中间件越多，性能损失越大</p></li><li><p>性能大多都损失在性能交互</p></li><li><p>目标优化方向：</p><ol><li><p>增强中间件的吞吐量</p></li><li><p>之间的传输效率提高</p><p>更好的网线，效率更高的传输协议…</p></li></ol></li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-24-20-49-08-image.png"></p><hr><hr><hr><hr><p>myComputer测试结果</p><ol><li><p>Nginx</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-15-39-32-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-15-47-36-image.png"></p><p>测试发现：</p><p>正常：</p></li></ol><ul><li><p>Nginx对CPU的消耗较高，网络IO也较高</p><p>因为Nginx的作用只是接收请求完成匹配进行路由</p><p>关键在于：需要有更多的线程来接收各种请求，所以CPU需要在线程之间切换跑</p><p>不正常：</p></li><li><p>内存占用过高（视频里只有1.5兆）</p><p>盲目分析：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-15-51-27-image.png"></p><ul><li><p>我的吞吐量为13W，视频中只有2k，可能是由于请求量太多导致的</p></li><li><p>当我关闭了Jmeter对Nginx的请求，发现CPU使用立马为0了，但内存依旧1个G，但重启Nginx后内存就清空了</p></li></ul></li></ul><ol start="2"><li><p>Gateway</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-16-52-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-17-09-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-19-52-image.png"></p><p>测试发现：</p><p>正常：</p><ul><li>CPU利用率很高（维护的线程数很多），内存占用不高</li></ul></li><li><p>简单服务</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-27-18-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-27-28-image.png"><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-27-38-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-28-04-image.png"></p><p>测试发现：</p><p>正常：</p><ul><li>由于没用复杂的运算逻辑，只有高高的线程数，导致CPU不高也不低，内存较低</li></ul></li><li><p>Gateway+简单服务、</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-39-13-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-16-39-26-image.png"></p></li><li><p>全链路：</p><ol><li>简单服务（直接返回String）</li></ol></li></ol><p>      <img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-11-17-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-11-31-image.png"></p><p>        2. 涉及数据库的服务</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-19-01-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-19-16-image.png"></p><p>        3. 对首页的直接访问</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-30-27-image.png"></p><p>        4. 对首页完全加载的访问（每张静态图片也去访问Tomcat）</p><p>吞吐量为10</p><p>sumUp</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-43-23-image.png"></p><ul><li><p>所以：主要的性能损耗——</p><ul><li><p>发生在<strong>业务逻辑</strong>上</p></li><li><p>发生在<strong>加载静态资源</strong>上</p></li></ul></li></ul><hr><hr><hr><p> @硅谷测试结果</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-17-53-20-image.png"></p><h2 id="动静分离"><a href="#动静分离" class="headerlink" title="动静分离"></a>动静分离</h2><p>场景描述：</p><ul><li><p>老版本业务部署是：浏览器的一个基础页面请求要过Nginx，Gateway才能到达Tomcat，并且一次页面请求中，html中还包含其他静态资源的请求，当将其部署在Tomcat时，第一次的页面请求就会连带众多对Tomcat的请求。</p><p>不仅导致后台Tomcat占用了许多线程，也必须跑完Nginx-Gateway全套。</p><p>当一个用户进行访问，占用了大量Tomcat线程，导致第二个用户的请求时延增加，往后甚至导致Tomcat宕机。</p></li></ul><p>动静分离：</p><ul><li><p>将不需要过业务逻辑的请求在业务部署的最前端就返回掉，尽量不占用任何不必要的资源 —— 在Nginx中部署静态资源。</p></li><li><p>做法：</p><ol><li><p>将项目的静态资源放在Nginx中</p></li><li><p>定义Nginx能够扫描到的规则：规定&#x2F;static&#x2F;**的所有请求都有Nginx直接返回</p><p><big><span style="color:red"><strong>注意Nginx的规则</strong></span></big></p></li></ol></li></ul><p>动静分离后的压测：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-20-03-17-image.png"></p><h3 id="缓存！"><a href="#缓存！" class="headerlink" title="缓存！"></a>缓存！</h3><p>适合存入缓存的数据</p><ol><li><p>及时性、数据一致性要求不高的</p><p>比如物流信息对及时性要求不高</p><p>数据一致性：在数据库的真实信息和查询得到的信息是否一致</p></li><li><p>访问量大且更新频率不高的数据（读多，写少）</p></li></ol><p><strong>配合缓存的读模式：</strong></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-20-30-30-image.png"></p><h4 id="如何实现缓存："><a href="#如何实现缓存：" class="headerlink" title="如何实现缓存："></a>如何实现缓存：</h4><ol><li><p><strong>本地缓存</strong></p><p>思路：</p><p>对于要调用的方法：在第一次调用后将结果保存为临时变量，下次调用时现场检查变量是否为null即可</p><p>将一堆变量集合到一起就是一个<strong>map</strong>。</p><p>定义：</p><p>本地缓存：和用户线程存储在同一个栈帧中的数据</p><p>问题：</p><p>在单服务器模式下，OK，但是在分布式集群模式下存在弊端</p><ol><li><p>每台服务器都需要本地缓存同样的数据。</p></li><li><p>如果要更改缓存的数据，在单体服务器时，一次同步刷新即可，但在集群模式下，需要全部大改，并且如果是通过请求重新访问数据库来刷新数据，一次刷新只能更改随即一台服务器的缓存，难以实现全部刷新。</p></li></ol></li><li><p><strong>分布式缓存—Redis</strong></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-08-25-20-59-44-image.png"></p></li></ol><p>项目的做法：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-09-04-18-47-56-image.png"></p><p>就是从判断本地有无缓存转化到判断redis有无缓存</p><p>潜在问题：（缓存失效）</p><ul><li><p><strong>缓存穿透</strong>：大并发查询缓存中不存在的信息。每个线程都跑了一次数据库，但却因为查不到信息导致后来同样的请求仍需跑数据库</p><ul><li><p>风险：</p><p>数据库压力瞬时增大，导致崩溃</p></li><li><p>解决：</p><p>将null值缓存，并加入短暂过期时间</p><p>短暂就是为了防止一时的缓存穿透，不能时间过长是因为有可能数据库中真的更新到词条数据，于是需要Redis同步更新。</p></li></ul></li><li><p><strong>缓存雪崩</strong>：当缓存中的key采用了相同的过期时间，缓存在某一时刻同时失效，导致请求全部转发到DB。</p><ul><li><p>风险：</p><p>雪崩——原本程序正常运行，某时刻redis压力突然减少，数据库压力猛增</p></li><li><p>解决：</p><p>失效时间分散</p><p>动态设置热点的失效时间</p><ul><li><p><strong>缓存击穿</strong>：当一个超级热点的key过期了，但过期后的某一时刻被高并发的访问，由于第一个访问数据库的线程是有时延的，还来不及放进redis中，导致高并发的请求全部落到数据库</p><ul><li><p>风险：</p><p>在一个热点数据高并发爆炸访问——击穿</p></li><li><p>解决：</p><p>加锁——高并发导致的问题都可通过加锁来降低性能，提高可用性</p></li></ul></li></ul></li></ul></li></ul><h3 id="缓存击穿之加锁方法"><a href="#缓存击穿之加锁方法" class="headerlink" title="缓存击穿之加锁方法"></a>缓存击穿之加锁方法</h3><h1 id="回来吧谷粒商城！"><a href="#回来吧谷粒商城！" class="headerlink" title="回来吧谷粒商城！"></a>回来吧谷粒商城！</h1><h3 id="缓存的使用："><a href="#缓存的使用：" class="headerlink" title="缓存的使用："></a>缓存的使用：</h3><p>一般数据库只承担数据持久化的工作，而数据读取需要给数据库降压</p><ul><li><p>哪些数据适合存入缓存？</p><ul><li><p>即时性、数据一致性要求不高的（面临 缓存一致性 问题）</p></li><li><p>访问量大且更新频率不高的数据</p></li></ul></li><li><p>缓存部署在项目中的位置：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-11-29-23-40-24-image.png"></p><p>如果所有请求都需要先跑redis，redis的并发程度需要巨高无比</p></li></ul><h3 id="Spring-Cache"><a href="#Spring-Cache" class="headerlink" title="Spring Cache"></a>Spring Cache</h3><blockquote><p>由于手动操作Redis的代码很多都是重复的 &#x2F; 固定的，这就是定义接口 + 实现类的意义所在</p></blockquote>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>SpringBasic</title>
    <link href="/2024/05/17/SpringBasic/"/>
    <url>/2024/05/17/SpringBasic/</url>
    
    <content type="html"><![CDATA[<h1 id="Spring！"><a href="#Spring！" class="headerlink" title="Spring！"></a>Spring！</h1><h3 id="三层和MVC"><a href="#三层和MVC" class="headerlink" title="三层和MVC"></a>三层和MVC</h3><ul><li><p>理解1：两种不同的设计逻辑</p></li><li><p>三层：Controller，service，dao</p></li><li><p>MVC：</p><p>运行流程：</p><p>前端发送给Controller请求，C接收请求将需求交给Model，model完成业务逻辑并访问数据库，将得到的数据返回给C，C要将数据交给View打包成视图，再返回到C，由C返回给客户端。</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230705210004961.png" alt="image-20230705210004961"></p><p>此版本其实是上一版本，在前后端不分离的时期，View视图层使用JSP写的。</p><p>后端将数据封装成JSP视图后，再一起发送到浏览器用于展示（有点后端前移的意思）。</p><p>model中，由javabean实现，其中有实体bean，业务bean，即包括三层中的业务逻辑层和数据访问层</p></li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-05-11-23-33-image.png"></p><ul><li><p>又一理解（来自黑马）：</p><ul><li><p>三层分为表现层，业务层，持久层</p></li><li><p>每层的框架分别是SpringMVC，Spring Framework，Mybatis</p></li><li><p>表现层中的SpringMVC分为C，M，V，其中的面向业务层</p></li></ul></li></ul><p>两种理解似乎是一致的：第一种理解的Model层可以涵盖service层和dao层。第二种理解不过是把三层中Controller层按照业务流程分开三步</p><p>是否要经过view层渲染？在前后端分离开发模式下似乎淘汰了</p><h5 id="最新理解："><a href="#最新理解：" class="headerlink" title="最新理解："></a>最新理解：</h5><p>springMVC就是Controller层的框架</p><p>古早前后端不分离版本：springMVC还要用JSP负责写view层。</p><p>最新版本前后端分离中：springMVC不需要JSP写view，而是要将内容转化成JSON，交由前端解析+渲染</p><h4 id="spring框架与用servlet开发："><a href="#spring框架与用servlet开发：" class="headerlink" title="spring框架与用servlet开发："></a>spring框架与用servlet开发：</h4><p>关系暂不明</p><ul><li><p>入门开发步骤</p><ol><li><p>创建Spring6父工程（项目project），工程下有多个Spring子模块（module）</p><p>父工程下new module：每个module是一个功能或组件。相对独立：有自己的文件解构：配置文件，资源文件。。。可以独立的测试和部署</p><p>微服务？</p></li><li><p>创建类，定义属性和方法</p></li><li><p>按照Spring要求，创建配置文件（xml格式）</p></li><li><p>在Spring配置文件中配置相关信息——声明bean对象</p></li><li><p>测试</p><ul><li><p>加载spring配置文件，用创建对象的方式</p></li><li><p>由此文件对象，获取想要创建的对象（文件里的bean）</p></li><li><p>使用对象调用方法实现逻辑</p></li></ul></li></ol></li><li><p>入门程序分析</p><ol><li><p>程序执行了对象的无参构造方法</p></li><li><p>没有new，怎么创建的对象——反射</p><ul><li><p>在加载配置文件bean.xml时进行解析，目的是获得bean标签的属性值。关键属性值：id和class</p></li><li><p>使用反射，根据class类全路径创建对象（Class字节码文件映射到Class对象）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">Class</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> Class.forName(class属性值)<br></code></pre></td></tr></table></figure><p>由此Class对象获取类对象</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">User</span> <span class="hljs-variable">user</span> <span class="hljs-operator">=</span> (User)c.getDeclaredConstructor().newInstance();<br></code></pre></td></tr></table></figure><p>于是反射的结果任然是创造对象</p><p>正常是由对象实体编译到class文件，反射是由class文件试图回到对象实体：</p><p>这就有一个问题：</p><p>原本的类没有用到全部的属性和方法，属性或许有默认值还有价值，但你没用到的方法是否不会参与编译呢？</p><p>结果是<strong>所有东西都参与编译</strong>——javaC编译时不清楚哪个用了没有。</p></li></ul></li></ol></li></ul><ol start="3"><li>配置的bean对象放在Map集合中，k：String&#x3D;bean对象的唯一标识：id，v：BeanDefinition&#x3D;bean的表述信息类，内含许多属性，包括唯一路径，是否为单例，作用域，name…人为描述——刻画想要怎样的bean对象</li></ol><ul><li><p>Log4j2：Apache下开源的日志记录组件 </p><ul><li><p>日志信息有优先级，不同级别的信息表示此条日志的重要性，级别高的会自动屏蔽级别低的日志</p><ol><li>TRACE</li><li>DEBUG</li><li>INFO</li><li>WARN</li><li>ERROR</li><li>FATAL</li></ol></li><li><p>日志信息输出位置：可选在控制台or文件中</p></li><li><p>日志信息输出格式可调：日期形式…(log4j2只是一个日志框架，其中许多参数赋默认值，却可调整)</p></li><li><p>分为自动写入和手动写入</p><p>手动写入：也是通过调用对象的方法来实现（为什么不设置成静态？）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-type">Logger</span> <span class="hljs-variable">logger</span> <span class="hljs-operator">=</span> LoggerFactory.getLogger(TestUser.class)<br><br>logger.info(<span class="hljs-string">&quot;sth&quot;</span>)<br>logger.warn(<span class="hljs-string">&quot;sth&quot;</span>)。。。<br></code></pre></td></tr></table></figure><p>没想到不分层的逻辑也不new对象，也用了工厂模式+反射</p><p>一旦new就会耦合，就要避免</p></li><li><p>log4j是日志框架的实现，slf4j是日志框架的接口，向下处理实现的操作对上屏蔽。</p><p>在运行时通过反射找到具体的日志实现</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="hljs-tag">&lt;/<span class="hljs-name">groupId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">artifactId</span>&gt;</span>log4j-slf4j2-impl<span class="hljs-tag">&lt;/<span class="hljs-name">artifactId</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">version</span>&gt;</span>2.19.0<span class="hljs-tag">&lt;/<span class="hljs-name">version</span>&gt;</span><br></code></pre></td></tr></table></figure><p>此依赖就是slf4j到log4j2的桥接器坐标</p></li></ul></li></ul><h2 id="IoC"><a href="#IoC" class="headerlink" title="IoC"></a>IoC</h2><p><code>一种设计思想--是用来设计出松耦合的程序</code></p><p>权力由上层交给下层，下层交给IoC容器</p><p>思想：将Java对象的实例化和初始化交给IoC容器，而避免手动new对象，容器内即负责实例化和初始化对象，也管理对象之间的依赖关系（like A需要调用B的方法，只要AB都在IoC中，容器即可完成A对B的调用）</p><p>在IoC容器中的对象成为Spring Bean，这和new出来的对象没有区别</p><p>Ioc流程说明：</p><ol><li><p>xml配置文件</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java">&lt;bean id=<span class="hljs-string">&quot;myBean&quot;</span> class=<span class="hljs-string">&quot;com.example.MyBean&quot;</span>&gt;<br>        &lt;!-- 设置属性值 --&gt;<br>        &lt;property name=<span class="hljs-string">&quot;propertyName&quot;</span> value=<span class="hljs-string">&quot;propertyValue&quot;</span> /&gt;<br>&lt;/bean&gt;<br></code></pre></td></tr></table></figure><p>其中定义了id和BeanDefinition（底层是k-v）</p></li><li><p>通过接口实现类从map中读取需要的bean（接口定义要读取需要的bean，实现类里区分从xml或者注解读取）。</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230707192615033.png" alt="image-20230707192615033"></p></li><li><p>将读取的bean的id和Definition加载到IoC容器，接下来进行<strong>实例化</strong></p></li><li><p>Spring中通过BeanFactory+反射创建对象</p></li><li><p>对象初始化——成为最终对象</p></li><li><pre><code class="java">user user = (user)context.getBean(&quot;id&quot;);<figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm">   <br>   获取真对象<br>- <span class="hljs-keyword">Bean管理：对象的创建过程和Bean属性的注入过程</span><br><span class="hljs-keyword"></span><br>- IoC容器要对读取的<span class="hljs-keyword">beanDefinition实例化，而IoC的两种实现方式有：</span><br><span class="hljs-keyword"></span>  <br>  <span class="hljs-number">1</span>. <span class="hljs-keyword">BeanFactory（程序内部使用的接口，不面向开发人员）</span><br><span class="hljs-keyword"></span>  <br>  <span class="hljs-number">2</span>. ApplicationContext：<span class="hljs-keyword">BeanFactory的子接口，是BeanFactory的上层</span><br><span class="hljs-keyword"></span>     <br>     ![image<span class="hljs-number">-20230707194632359</span>](C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image<span class="hljs-number">-20230707194632359</span>.png)<br>  <br>  <span class="hljs-number">3</span>. ApplicationContext：此接口的实现类们实现了从不同位置，不同方式创建IoC容器<br>     <br>     ![image<span class="hljs-number">-20230707195239359</span>](C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image<span class="hljs-number">-20230707195239359</span>.png)<br>     <br>     ClassPathXml...从类文件下找到配置文件<br>     <br>     FileSystemXml...从文件系统路径下找到配置文件<br>     <br>     ConfigurableApplication...子接口，包含扩展方法，功能更加强大<br>     <br>     web...为web应用准备<br><br>- 从IoC容器获取<span class="hljs-keyword">Bean对象的三种方式：（根据id，根据class，两者同时）</span><br><span class="hljs-keyword"></span>  <br>  ```<span class="hljs-keyword">java</span><br><span class="hljs-keyword"></span>          ApplicationContext <span class="hljs-built_in">context</span> = new ClassPathXmlApplicationContext(<span class="hljs-string">&quot;bean.xml&quot;</span>);<br>          iocDemo1 demo1 = (iocDemo1) <span class="hljs-built_in">context</span>.getBean(<span class="hljs-string">&quot;Demo1&quot;</span>);<br>          System.out.println(demo1);<br>          iocDemo1 demo2 = <span class="hljs-built_in">context</span>.getBean(iocDemo1.class);<br>          System.out.println(demo2);<br>          iocDemo1 demo3 = <span class="hljs-built_in">context</span>.getBean(<span class="hljs-string">&quot;Demo1&quot;</span>,iocDemo1.class);<br>          System.out.println(demo3);<br></code></pre></td></tr></table></figure></code></pre></li></ol><p>  有冲突，会报错——配置文件可以同class，但不能据此找bean</p><ul><li><p>从IoC容器获取Bean对象也可以多态——容器中有实现类的bean，从容器中get的依据是接口的class。</p><p>但由于获取bean对象返回的都是唯一对象，于是如果接口存在多个实现类则Spring无法挑选</p><ul><li><p>而且只能根据接口.class来获取，不能用接口名</p></li><li><p>源代码：<mark>扫描context里bean对象时使用instanceof方法选出和传入class匹配的bean</mark></p><p>而传id自然只能由id扫描到自身，无法扫描到实现类了</p></li></ul></li><li><p>配置文件里可以有id相同的bean，也可以有同一接口的不同实现类的bean，在配置文件中不会报错，但挑选方式（getBean）时要避免相应的错误</p></li></ul><h2 id="DI依赖注入"><a href="#DI依赖注入" class="headerlink" title="DI依赖注入"></a>DI依赖注入</h2><p><code>指Spring创建对象的过程中，将对象依赖的属性通过配置进行注入</code>   配置？</p><p>常见注入方法：set注入和构造注入</p><p>注入引用类型的属性，实际就是维护bean对象之间的关系</p><p>无论是用setter，还是构造方法，在原生代码中都需要new获取对象，于是在Spring中就是需要配置bean</p><ol><li><p>根据Setter：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">iocDemo2</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">Person</span>&#123;<br><br>    String name;<br>    String age;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setName</span><span class="hljs-params">(String name)</span> &#123;<br>        <span class="hljs-built_in">this</span>.name = name;<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setAge</span><span class="hljs-params">(String age)</span> &#123;<br>        <span class="hljs-built_in">this</span>.age = age;<br>    &#125;<br></code></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs xml">    <span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;Demo2&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;org.example02.iocDemo2&quot;</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;age&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;21&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;wsl&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><br>name表示setter方法中后单词小写<br>要求类文件中有set方法（以及无参构造/或者没有构造器，默认无参）<br></code></pre></td></tr></table></figure></li><li><p>根据构造器</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs xml">    <span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;Demo2&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;org.example02.iocDemo2&quot;</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">constructor-arg</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;age&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;22&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">constructor-arg</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">constructor-arg</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;wsl&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">constructor-arg</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><br>要求类文件中有含参构造<br></code></pre></td></tr></table></figure></li></ol><ul><li><p>特殊值处理：</p><ol><li><p>null值：</p><p>由于xml文件里，属性的标准写法是k&#x3D;“v”，于是无论右侧是不是字符串都只能按照字符串写。</p><p>表示null用内嵌标签：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs xml"> <span class="hljs-tag">&lt;<span class="hljs-name">constructor-arg</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;birthday&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">null</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">null</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">constructor-arg</span>&gt;</span><br></code></pre></td></tr></table></figure></li><li><p>标签冲突（&lt;&gt;）：</p><p>转义！</p><p><constructor-arg name="name" value="3 &lt; 4"></constructor-arg></p><p>&amp;lt；表示&lt;,  &amp;gt; 表示&gt;</p><p>或者：CDATA区&#x2F;CDATA节</p><p>​        C表示Character纯文本，不会被识别为其他有含义的符号</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">constructor-arg</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">value</span>&gt;</span>&lt;![CDATA[a&lt;b]]&gt;<span class="hljs-tag">&lt;/<span class="hljs-name">value</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">constructor-arg</span>&gt;</span><br></code></pre></td></tr></table></figure></li></ol></li><li><p>注入的属性是引用数据类型（对象）</p><p>方法：</p><ol><li><p>引用外部bean</p><p>用property标签的ref属性，将别的bean对象的id传进来，就能识别到是哪个对象</p></li><li><p>内部bean</p><p>在bean内部某个property中定义需要的bean对象</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708094017029.png" alt="image-20230708094017029"></p></li><li><p>级联属性配置</p><p>在bean中像设置引用对象的某个属性时使用</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs xml">    <span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;Demo2&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;org.example02.iocDemo2&quot;</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;age&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;21&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;wsl&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;main&quot;</span> <span class="hljs-attr">ref</span>=<span class="hljs-string">&quot;main&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;main.mainName&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;www&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><br>但前提是：Demo2中的main属性需要设置get方法<br></code></pre></td></tr></table></figure></li></ol></li><li><p>注入的属性是引用数据类型（数组）</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708100044822.png" alt="image-20230708100044822" style="zoom: 60%;" /></li><li><p>注入的属性是引用数据类型（List&lt;引用&gt;）</p></li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708100447939.png" alt="image-20230708100447939"></p><ul><li><p>注入的属性是引用数据类型（List&lt;基本&gt;）</p><p>那么list标签内就用value即可</p></li></ul><ul><li><p>注入的属性是引用数据类型（Map&lt;引用&gt;）</p><p>property属性标签—&gt;map集合标签—&gt;entry个例标签—&gt;key标签 and ref标签</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708102644334.png" alt="image-20230708102644334" style="zoom: 50%;" /></li><li><p>注入的属性是引用数据类型（Map&lt;基本&gt;）</p><p>property属性标签—&gt;map集合标签—&gt;entry个例标签—&gt;key标签 and value标签</p></li><li><p>简便办法——用工具标签</p><ul><li><p>前提：在bean.xml配置文件中引入</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:util</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/util&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans</span></span><br><span class="hljs-string"><span class="hljs-tag">       http://www.springframework.org/schema/beans/spring-beans.xsd</span></span><br><span class="hljs-string"><span class="hljs-tag">       http://www.springframework.org/schema/util</span></span><br><span class="hljs-string"><span class="hljs-tag">       http://www.springframework.org/schema/beans/spring-util.xsd&quot;</span></span><br><span class="hljs-tag">&gt;</span><br></code></pre></td></tr></table></figure><ul><li>xmlns（xml nameSpace ）用于指定默认的命名空间——bean标签</li><li><code>xsi</code> 是 XML Schema Instance 的简写。前面指定命名空间，后面在schemaLocation中声明xsi的属性：格式是：namespaceURI   schemaLocation</li></ul><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708105144561.png" alt="image-20230708105144561" style="zoom:67%;" /><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708105209418.png" alt="image-20230708105209418" style="zoom:67%;" /></li><li><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708105241006.png" alt="image-20230708105241006"></p></li></ul></li><li><p>P命名空间——</p><ol><li><p>配置导入P空间文件</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708105934770.png" alt="image-20230708105934770"></p></li><li><p>p注入</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708110139958.png" alt="image-20230708110139958"></p><p>不写property，而是在bean标签内由p:属性注入</p><p>上面的配置就像是给bean标签定义属性p，而p又识别出bean对象的属性作为自己的属性</p><p>在p：后，每个bean对象的属性都有本身和ref两种，用于确定是注入字面量还是引用类型</p></li></ol></li><li><p>引入外部属性文件——</p><p>需求背景是：某些特定的值比较固定，且逻辑上可以和别的区分开，于是写到外部再引入进来</p><p>​                        例如数据库容器配置文件。</p><p>做法：</p><ol><li><p>确保项目里有JDBC对myBatis的依赖</p></li><li><p>编写jdbc.properties,内部写连接数据库的必要信息（id，密码，路径，驱动…）</p></li><li><p>引入命名空间<context>，做法与引入<list><map>一致，在配置文件中用context引入属性文件</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708112024055.png" alt="image-20230708112024055"></p></li><li><p>然后就可以通过配置文件名来调用其里面k-v形式的内容</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708112205072.png" alt="image-20230708112205072"></p></li></ol></li><li><p>bean的作用域——scope属性限制是单例还是多例</p><ol><li><p>scope&#x3D;”singleton”：在IoC容器中，这个bean对象始终为单例</p><p>在IoC容器初始化时创建</p></li><li><p>scope&#x3D;”prototype”：在IoC容器中，这个bean对象可以有多个</p><p>在获取bean时创建</p></li></ol></li><li><p>单例：（默认）</p><ul><li>每次请求获取该对象，都会得到同一个对象实例（context.forName(…）</li><li>常常用在表示程序中共享的某种资源或状态（对程序任何位置都是一致的）</li><li>可以节省资源</li></ul></li><li><p>多例：</p><ul><li>每次请求获取对象实例时都会创建一个新的对象实例（独立的）.</li><li>常常用于表示具有不同状态或不同上下文的对象</li></ul></li></ul><h3 id="bean-生命周期"><a href="#bean-生命周期" class="headerlink" title="bean 生命周期"></a>bean 生命周期</h3><p><small>用property 写法</small></p><ol><li>bean对象创建（调用无参构造）</li><li>给bean对象设置相关属性（通过setter将property注入）</li><li>bean后置处理器（初始化之前）</li><li>bean对象的初始化</li><li>bean后置处理器（初始化之后）</li><li>bean对象创建完成，可以使用</li><li>bean对象销毁（配置指定销毁方法）（需要依赖IoC关闭方法）</li><li>IoC容器关闭</li></ol><ul><li><p>后置处理器</p><p>是一种特殊的Bean，可以对其他Bean进行定制化的处理。</p><p>关键两个方法：</p><ol><li><p>postProcessBeforeInitialization（Object bean，String beanName）{}</p><p>在目标bean初始化方法执行前调用</p><p>可以修改属性值，执行初始化之前的准备工作等</p></li><li><p>postProcessAfterInitialzation(Object bean,String beanName){}</p><p>初始化之后执行…</p></li></ol><p>作用：拦截所有的Bean创建过程（是这个bean所在配置文件的所有bean）</p></li></ul><h3 id="FactoryBean机制"><a href="#FactoryBean机制" class="headerlink" title="FactoryBean机制"></a>FactoryBean机制</h3><ul><li>是一种特殊的bean，获取该bean时，创建的实例是其getObject()方法中返回的别的实例bean。这样做会把复杂组件创建的详细过程和繁琐细节都屏蔽。,并且给bean添加更多的定制和控制操作。</li><li>是Spring整合第三方框架的常用机制</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">myFactoryBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">FactoryBean</span>&lt;Person&gt; &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Person <span class="hljs-title function_">getObject</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Person</span>();<br>    &#125;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Class&lt;?&gt; getObjectType() &#123;<br>        <span class="hljs-keyword">return</span> Person.class;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="基于xml配置文件的自动注入"><a href="#基于xml配置文件的自动注入" class="headerlink" title="基于xml配置文件的自动注入"></a>基于xml配置文件的自动注入</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">bean</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;controller&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;Three.Controller&quot;</span> <span class="hljs-attr">autowire</span>=<span class="hljs-string">&quot;byType&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">property</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;name&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;controller&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">property</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">bean</span>&gt;</span><br></code></pre></td></tr></table></figure><p>某些要手动注入的基本数据类型可以手动写入</p><p>某些在同一个bean文件下的引用数据类型可以自动装配</p><ul><li>byType：根据声明类型识别</li><li>byName：根据声明name识别</li></ul><p>自动注入要求开启setter方法，其实是通过setter方法注入的属性（property）</p><h1 id="基于注解的Bean管理"><a href="#基于注解的Bean管理" class="headerlink" title="基于注解的Bean管理"></a>基于注解的Bean管理</h1><p>注解：</p><p>注解是代码中的一种特殊标记，可以在编译，类加载，运行时被读取并执行相应的处理。通过注解：在不改变原有代码和逻辑的情况下，在源代码中嵌入补充信息。</p><p>作用范围：类上，属性上，方法上</p><h3 id="初步思路："><a href="#初步思路：" class="headerlink" title="初步思路："></a>初步思路：</h3><ol><li>引入依赖</li><li>在bean.xml文件中开启组件扫描（确定IoC容器要扫描那些个包的注解）<ul><li><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708171511383.png" alt="image-20230708171511383"></li><li>也可以用某些语法设置排除某些包，包括某些包</li></ul></li><li>使用注解定义bean</li><li>依赖注入</li></ol><h3 id="依赖注入的注解："><a href="#依赖注入的注解：" class="headerlink" title="依赖注入的注解："></a>依赖注入的注解：</h3><ol><li><p>@Autowired</p><p>默认是根据类型自动装配</p><p>源码：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708172003366.png" alt="image-20230708172003366"></p><p>@Target（）元注解表示限制此注解的作用范围</p><blockquote><p>@Target({ElementType.<em>CONSTRUCTOR</em>, ElementType.<em>METHOD</em>, ElementType.<em>PARAMETER</em>, ElementType.<em>FIELD</em>, ElementType.<em>ANNOTATION_TYPE</em>})</p></blockquote><p>constructor：构造函数</p><p>method：普通方法</p><p>parameter：参数</p><p>field：成员变量</p><p>annotation_type：其他注解</p><p>Type：类，接口，枚举类型上</p></li></ol><ul><li><p>required（）参数表示</p><p>required() 表示被注入的对象是否要求存在（即自动注入的必要性）</p><h4 id="Autowired定义在哪里："><a href="#Autowired定义在哪里：" class="headerlink" title="@Autowired定义在哪里："></a><strong>@Autowired定义在哪里：</strong></h4></li></ul><ol><li><p>定义在类属性上（常规）</p></li><li><p>定义在setter方法上</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708173221479.png" alt="image-20230708173221479" style="zoom: 50%;" /></li><li><p>定义在构造方法上</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708173345132.png" alt="image-20230708173345132" style="zoom:50%;" /></li><li><p>定义在形参上</p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708173449310.png" alt="image-20230708173449310" style="zoom:45%;" /></li><li><p>类中只有一个含参构造时，可以不写@Autowired</p></li><li><p>由于原本自动注入默认是根据类型识别，但如果一个下层接口有多个实现类，获取要再根据name注入。</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708174024755.png" alt="image-20230708174024755"></p><p>value表示要注入的类的名字——默认是类名开头小写。</p></li><li><p>@Resource</p><p>是JDK自带的注解，java版本在8-11即可不用引入依赖</p><p>@R可以和JavaEE，Spring框架一起使用，因为是自带的，@A是Spring框架特定的注解，更丰富更灵活，和Spring更适合</p><ol><li><p>@R默认按照name查找，name找不到再按照类型查找。</p><p>如果@R()参数中没有指定name，根据属性名查找，再找不到根据类型…</p></li><li><p>@R只能标注在字段属性上，和setter方法上，@A更多</p></li><li><p>@R由于直接根据name，不需要@Qualifier从多中选一</p></li></ol></li></ol><h3 id="全注解开发："><a href="#全注解开发：" class="headerlink" title="全注解开发："></a>全注解开发：</h3><p><small>用配置类来代替bean.xml配置文件</small></p><p>配置类：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708191338655.png" alt="image-20230708191338655"></p><p>测试类：</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708191426017.png" alt="image-20230708191426017"></p><h1 id="手写IoC容器"><a href="#手写IoC容器" class="headerlink" title="手写IoC容器"></a>手写IoC容器</h1><h3 id="反射"><a href="#反射" class="headerlink" title="反射"></a>反射</h3><ul><li><p>步骤</p><ol><li><p>获取类的class对象（字节码文件——包含类的解构和元数据信息，可以映射成一个类对象，于是可以调用方法）</p><p>方法：</p><ul><li><p>类名.class</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708192430303.png" alt="image-20230708192430303"></p></li><li><p>对象.getClass()</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708192438909.png" alt="image-20230708192438909"></p></li><li><p>Class.forName(“全路径”)</p><p>全路径指在此模块中找到此类的路径：用.分离</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708192446339.png" alt="image-20230708192446339"></p></li></ul></li><li><p>由class类实例化成对象</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230708192913516.png" alt="image-20230708192913516"></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Class&lt;?&gt; clazz = MyClass.class; <span class="hljs-comment">// MyClass为要实例化的类的字节码文件</span><br>Constructor&lt;?&gt; constructor = clazz.getConstructor(parameterTypes); <span class="hljs-comment">// parameterTypes为构造函数的参数类型数组</span><br><span class="hljs-type">Object</span> <span class="hljs-variable">obj</span> <span class="hljs-operator">=</span> constructor.newInstance(arguments); <span class="hljs-comment">// arguments为构造函数的实际参数数组</span><br></code></pre></td></tr></table></figure><ul><li><p>getConstructor()不会获取原本类中声明为private的构造方法，只会拿到public</p></li><li><p>getDeclaredConstructor()会</p></li><li><p>获取的构造器对象，拥有几个属于构造器独特的方法——</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709093748273.png" alt="image-20230709093748273"></p></li></ul></li><li><p>反射时默认用的无参构造（原本类中也默认有无参构造）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Constructor&lt;reflect.car&gt; constructor1 = carClass.getConstructor();<br>reflect.<span class="hljs-type">car</span> <span class="hljs-variable">car1</span> <span class="hljs-operator">=</span> constructor1.newInstance();<br>System.out.println(car1);<br></code></pre></td></tr></table></figure><p>可以使用有参构造（条件是原本类中也有含参构造）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java">Constructor&lt;car&gt; constructor = carClass.getConstructor(String.class, String.class, String.class);<br><span class="hljs-type">car</span> <span class="hljs-variable">car</span> <span class="hljs-operator">=</span> constructor.newInstance(<span class="hljs-string">&quot;马自达&quot;</span>, <span class="hljs-string">&quot;阿特兹&quot;</span>, <span class="hljs-string">&quot;混动红&quot;</span>);   System.out.println(car);<br></code></pre></td></tr></table></figure><p>注意：如果原本类的构造是<strong>private</strong>的，不仅需要用declaredConstructor，还要设置可行性为true</p></li></ol></li></ul><p>​                <img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709100824508.png" alt="image-20230709100824508" style="zoom: 80%;" /></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs java">Constructor&lt;car&gt; constructor = carClass.getDeclaredConstructor(String.class, String.class, String.class);<br>constructor.setAccessible(<span class="hljs-literal">true</span>);<br><span class="hljs-type">car</span> <span class="hljs-variable">car</span> <span class="hljs-operator">=</span> constructor.newInstance(<span class="hljs-string">&quot;马自达&quot;</span>, <span class="hljs-string">&quot;阿特兹&quot;</span>, <span class="hljs-string">&quot;混动红&quot;</span>);<br></code></pre></td></tr></table></figure><ol start="3"><li><p>由class类获取属性（参数）</p><ol><li><p>常规方法只能获取public的属性</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Field[] fields = carClass.getFields();<br></code></pre></td></tr></table></figure><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709102137763.png" alt="image-20230709102137763"></p></li><li><p>得到所有的属性：（public&#x2F;private）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">Field[] fields = carClass.getDeclaredFields();<br></code></pre></td></tr></table></figure></li><li><p>设置属性值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">Field[] fields = carClass.getDeclaredFields();<br><span class="hljs-keyword">for</span> (Field f:fields) &#123;<br>    <span class="hljs-keyword">if</span>(f.getName().equals(<span class="hljs-string">&quot;subName&quot;</span>))&#123;<br>        f.set(car,<span class="hljs-string">&quot;昂克赛拉&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>注意：如果subName是private的，需要先setAccessible（ture）</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709103111254.png" alt="image-20230709103111254"></p><p>感觉很怪——从属性对象中调用方法设置类的属性，有点倒反天罡</p><p>不过毕竟这个属性来自字节码文件，并不属于哪一个对象</p></li><li><p>得到所有方法，以及执行某个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">for</span> (Method m:methods<br>     ) &#123;<br>    System.out.println(m.getName());<br>    <span class="hljs-keyword">if</span>(m.getName().equals(<span class="hljs-string">&quot;say&quot;</span>))&#123;<br>        m.setAccessible(<span class="hljs-literal">true</span>);<br>        m.invoke(car,<span class="hljs-string">&quot;say it！&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>invoke中第一个参数为执行的对象，后面的可变参数是m的参数</p></li></ol></li></ol><h3 id="细说编译过程："><a href="#细说编译过程：" class="headerlink" title="细说编译过程："></a>细说编译过程：</h3><ul><li><p>Java总览：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-22-57-image.png"></p></li><li><p>Java编译器是唯一能识别.java文件的存在——导致其必然作用</p></li><li><p>Java编译器的作用是：</p><ol><li><p>检查符合规范</p></li><li><p>编译成class文件</p></li></ol></li><li><p>JDK和 JRE</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-27-31-image.png"></p><ul><li><p>Java Development Kit（Java开发工具包）——包含Java从开发到运行的各种工具</p></li><li><p>Java Running Environment（Java运行环境）——只包含运行必要的工具（包括基础类库，JVM）</p></li><li><p>二者的差集就和开发相关：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-31-44-image.png"></p><p>关键模块：</p><ol><li><p>JavaC：用于编译源代码生成class文件</p></li><li><p>JavaP：用于反编译，根据class文件，反解析出其中信息</p></li><li><p>javadoc：生成java文档</p></li></ol></li><li><p>编译阶段：源码通过javac到达.class</p></li><li><p>运行阶段：.class文件在JVM中运行</p></li></ul></li><li><p>Java虚拟机只认.class字节码文件，所以只要能有合适的编译器把某种语言写的源代码编译成字节码，JVM就是通用的</p><p>Kolin，Scala成也JVM，败也JVM</p></li><li><p>Lombok，AspectJ等直接修改字节码文件来修改程序：Lombok将注解生成字节码，AspectJ是在编译时由注解确定织入代码的切点和内容，进行对.class文件的<strong>字节码增强技术</strong></p></li></ul><h5 id="字节码增强技术："><a href="#字节码增强技术：" class="headerlink" title="字节码增强技术："></a>字节码增强技术：</h5><ul><li><p>本质就是指通过修改已编译的字节码文件来实现对程序行为的增强或修改的技术。</p></li><li><p>据说可以实现热部署</p></li></ul><h3 id="编译！"><a href="#编译！" class="headerlink" title="编译！"></a>编译！</h3><ul><li><p>常见编译器：javac（自带），ecj（Eclipse Compiler for Java）</p></li><li><p>流程：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-51-29-image.png"></p><ol><li><p>词法分析&amp;语法分析</p><ul><li><p>词法分析·：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-52-11-image.png"></p></li><li><p>语法分析：（抽象语法树：Abstract Ayntax Tress AST）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-12-19-52-27-image.png"></p><p><small>WC!数据标注的短语结构树！文本挖掘抄袭Javac！</small></p></li><li><p>所以起名有问题逃不过词法分析或语法分析，例如数据类型错会在第一个报错，变量名起冲突会在第二个报错</p></li></ul></li><li><p>填充符号表</p><ul><li><p>由符号地址（位置）和其信息构成的map</p></li><li><p>作用是明确任何符号的作用范围（关联）、重复声明、类型匹配</p></li><li><p>任何符号：类名、方法名、变量名</p></li><li><p>Attention：类的信息包括有无构造器，于是对于没有写构造器的类，会在此添加默认无参构造。</p></li></ul></li></ol></li></ul><h3 id="开写！"><a href="#开写！" class="headerlink" title="开写！"></a>开写！</h3><blockquote><p>@Bean注解：</p></blockquote><ul><li><p>步骤： </p><ol><li><p>创建两个自定义注解</p><p>@Bean：类似@Component，标记此类也要加入IoC容器，成为bean对象</p><p>@Di：类似@Autowired，标记此处需要被注入某个bean</p></li><li><p>创建bean容器接口：手写ApplicationContext，定义方法，返回bean对象</p></li><li><p>实现bean容器接口：**手写AnnotationConfigApplicationContext()**，</p><p>具体要做：</p><ol><li><p><strong>根据规则加载bean</strong></p><p>规则：扫描某个包和其子包，看类上是否有@Bean注解，如果有就把这个类通过反射实例化</p></li><li><p>返回bean对象</p></li></ol></li></ol></li><li><p>细说怎么扫描文件夹——反射构造bean对象并添加到IoC中</p><ol><li><p>参数接收的是com.itheima.Spring类似的路径，需要转成com&#x2F;itheima&#x2F;Spring</p></li><li><p>遍历此文件夹下所有文件，找到class文件</p><ol><li><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709152921153.png" alt="image-20230709152921153"></p><p>urls中是下一层的所有文件&#x2F;文件夹</p></li><li><p>由于url编码会将一些特殊字符（如&#x2F;）转化成%加两个十六进制数，于是需要转回来</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709153145902.png" alt="image-20230709153145902"></p></li><li><p>对于n个可进入的文件&#x2F;文件夹url，为了避免bug，要进行许多判断</p><ul><li><p>是文件夹则再进入</p></li><li><p>文件夹为空则回溯</p></li><li><p>是文件还需判断是不是.class文件</p></li><li><p>关键：对于符合条件的文件，需要转回.分割的路径，回Java逻辑下判断</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709154510127.png" alt="image-20230709154510127"></p></li><li><p>如果该文件是个接口，则跳过</p></li><li><p>在.class中判断上面是否有注解@Bean</p><p>暴力调用方法</p></li><li><p>如果有就实例化，加入map</p><p>还需进行一步：当前class对象是否是实现类，如果是，则加入IoC容器的形式应该是接口类做key，实现类做value</p></li></ul></li></ol><ul><li><p>学到的地方：写方法时分离出另一个方法的思考</p><ul><li><p>原本方法内要迭代，写出去迭代的具体方法便于修改迭代参数</p></li><li><p><strong>将相对独立的方法分离出去</strong></p><p>原本方法接收path，初步操作得到url的枚举类型，但这种不常见的类型尽量在原方法中就屏蔽掉不要传给分离的方法，是变成常见类型后分离出较为抽象的逻辑构成另一个方法。</p></li></ul></li></ul></li></ol></li></ul><blockquote><p>@Di注解</p></blockquote><ul><li><p>思路：</p><p>在ApplicationContext的构造函数中，刚刚完成了填充map，map中存有各个bean对象，但这些对象的属性（引用属性）都没有被注入，于是在构造中要完成遍历+注入。</p><p><mark>ATTENTION:</mark> 不存在只需要被注入而本身不放入容器的类。</p><p>因为IoC容器的bean管理就是管理内部的bean对象（配置方式的bean.xml）之间相互关系。</p><p>即：<strong>需要被注入，自身一定要也在容器中</strong>。</p><p>过程：</p><ul><li><p>遍历map集合，得到其中每个value——bean对象</p></li><li><p>遍历每个bean对象的每个属性，检测是否有@Di注解</p></li><li><p>如果有，依赖注入——实际就是调用field的set方法（此时因为每个对象都被放松成Obj，所以没法调用自身的setter，Spring给出的方法是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java）">field.set(obj,map.get(field.getType()));<br></code></pre></td></tr></table></figure><p><small>感觉底层也应该是调的setter，可能用instanceof暴力匹配？</small></p><p>逻辑：每个要注入的字段field，根据自身的类型在map中找到符合的对象，由此field调用注入方法。</p></li></ul></li><li><p>切记注意私有属性或方法要用declared</p></li></ul><hr><h1 id="AOP"><a href="#AOP" class="headerlink" title="AOP"></a>AOP</h1><h3 id="代理模式："><a href="#代理模式：" class="headerlink" title="代理模式："></a>代理模式：</h3><p>提出背景：为了解决多个功能混合在一个方法中，而并非每个功能都是核心功能。同时避免仅仅用分离出抽象方法但任然在一个类中导致的臃肿。</p><p>代理模式：</p><p>通过在上层调用与下层执行之间加一层代理，来使下层执行的逻辑更加清晰——分离出核心代码。</p><p>据说还可以实现解耦</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709194318447.png" alt="image-20230709194318447"></p><p>当初说创建新线程也用了代理模式：</p><p>是由于实现runnable接口的类本身没有start方法，需要用new Thread()作为代理调用Thread的start方法。</p><p>这里的代理更像是有能力的类包装没能力的类，一起带飞，</p><p>其实也就是外层代理重写内层核心类的方法，包装成需要的结构</p><p>通过调用外层代理类的方法，间接调用核心类方法。</p><h3 id="静态代理："><a href="#静态代理：" class="headerlink" title="静态代理："></a>静态代理：</h3><p>代码写死</p><h3 id="动态代理："><a href="#动态代理：" class="headerlink" title="动态代理："></a>动态代理：</h3><p><small>静态代理是在代码运行之前，代理的模式已经确定（写死）——外层代理能做的事情是有限的，动态代理在代码运行时才创建代理对象（即明确代理能做的事情），相较而言较为动态</small></p><ul><li><p>方法：</p><p><strong>实例化时返回代理对象</strong></p><p>newProxyInstance(ClassLoader loader，Class&lt;?&gt;[] interfaces，InvocationHander h)</p><p>通过代理对象使用原本方法即可自动调用包装后的方法</p><p>invocationHander接口的实现类需要重写invoke()方法——即为包装的逻辑</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709202626943.png" alt="image-20230709202626943"></p><p>参数中：</p><ul><li><p>proxy表示代理的对象</p></li><li><p>method表示代理可能调用的方法（在此重写为Method类型）便于调用</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20230709203400562.png" alt="image-20230709203400562"></p><p><code>反射思想————通过属性/方法改变类（倒反天罡！两极反转！）</code></p><p>target即代理的对象</p></li><li><p>args表示method的参数</p></li></ul></li></ul><h3 id="AOP概念》Aspect-Oriented-Programming"><a href="#AOP概念》Aspect-Oriented-Programming" class="headerlink" title="AOP概念》Aspect Oriented Programming"></a>AOP概念》Aspect Oriented Programming</h3><p>通过预编译和运行期间动态代理方式，在不修改源代码的情况下，给程序动态统一添加额外功能的技术。</p><p>利用AOP可以对业务逻辑的各个部分进行隔离，使得业务逻辑各部分间耦合度降低，提高程序可重用性。</p><ul><li><p>横切关注点：分散在各个模块中，解决同一问题的非核心业务的部分</p><p>如：用户验证、日志管理、事务处理、数据缓存等</p><p>这种思路可以使用多个横切关注点对相关方法进行不同层面的<strong>增强</strong></p></li><li><p>通知（增强）：要添加的功能模块——安全&#x2F;事务&#x2F;日志……</p><p>通知方法：实现此通知的方法</p><p>分类：</p><ol><li><p>前置通知</p></li><li><p>返回通知：代理目标的方法成功结束后执行</p></li><li><p>异常通知：代理目标的方法异常结束执行</p></li><li><p>后置通知：最终结束执行</p></li><li><p>环绕通知：方法之前之后都执行</p></li></ol></li><li><p>切面：</p><p>封装前置通知、后置通知…的类</p></li><li><p>连接点：</p><p>在各个方法中一致的、能够添加增强的位置</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-09-39-25-image.png"></p></li><li><p>切入点：</p><p>得到某个连接点，就是切入点</p></li></ul><h3 id="动态代理分类"><a href="#动态代理分类" class="headerlink" title="动态代理分类"></a>动态代理分类</h3><ul><li><p>当代理目标有接口时——生成的代理类是和它实现了同样接口的好兄弟类</p></li><li><p>当没有接口时——生成的代理类是继承了此类的好大儿</p><p><strong>为什么不能纯粹的另外一个类——通过调用代理目标类的方法前后切入实现增强？</strong></p><p><code>因为生成动态代理类需要一个模板，来确定代理类的结构和行为——比如在JDK实现中，method就间接映射到模板的方法......</code></p></li></ul><ol><li><p>JDK动态代理</p><p>当目标类有接口时用JDK和cglib</p></li><li><p>CGLib动态代理</p><p>目标类没有接口时只能用vglib</p></li></ol><hr><h5 id="AspectJ"><a href="#AspectJ" class="headerlink" title="AspectJ"></a>AspectJ</h5><p>AOP框架</p><p>spring借用其中的注解进行便利开发</p><p>据说AspectJ本质上是静态代理，将代理逻辑“织入”被代理的目标类编译得到的字节码文件——导致效果是动态代理。</p><h5 id="动态代理基本步骤："><a href="#动态代理基本步骤：" class="headerlink" title="动态代理基本步骤："></a>动态代理基本步骤：</h5><ul><li><p>引入aop和aspect相关依赖</p></li><li><p>配置xml文件或配置类实现组件扫描（依赖IoC容器）和aop的aspectJ的自动代理（识别加了@Aspect注解，为其自动生成代理类）</p></li><li><p>创建目标资源</p><ul><li><p>接口</p></li><li><p>实现类</p></li></ul></li><li><p>创建切面类</p><ul><li><p>明确切入点</p></li><li><p>明确通知类型</p><p><strong>通知类型：</strong> 在切入方法上添加，明确切入点</p><ul><li><p>前置：@Before()</p></li><li><p>返回：@AfterReturning()  在return语句后执行</p></li><li><p>异常：@AfterThrowing()</p></li><li><p>后置：@After()   类似try-catch中的finally</p></li><li><p>环绕：@Around() 通过使用try-catch-finally，编写以上各个位置的切入</p></li></ul><p>通过设置参数value&#x3D;“”，编写<strong>切入点表达式</strong></p><img src="file:///C:/Users/吴松林/AppData/Roaming/marktext/images/2023-07-10-10-32-19-image.png" title="" alt="" width="673"><p>其作用就是：明确在哪一个&#x2F;哪些方法附加切入（有点像sql）</p></li><li><p>每个切面类的方法都可以有一个参数——JoinPoint类型，用于获取关于切入点的属性。</p><p><code>------</code></p></li></ul></li><li><p>返回通知AfterReturning——能够得到方法的返回值</p><p>在注解@AfterReturning中添加参数：returning &#x3D; ”sthName“</p><p>之后便可在方法参数中使用Object类型的sthName</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-11-17-41-image.png"></p></li><li><p>异常通知AfterThrowing——能够得到异常类型</p><p>形式和上面类似：有参数 throwing &#x3D; ”sthName“</p></li><li><p>环绕通知Around</p><p>在try-catch-finally中适当位置可切入属于上面的方法</p><ul><li><p>在try中一开始写的是@Before</p></li><li><p>需要模拟一个方法执行</p><p>用更强的ProceedingJoinPoint类型的joinpoint调用proceed方法，获取一个Obj类型的返回值result</p></li><li><p>返回之后可调用@AfterReturning</p></li><li><p>catch中可写异常处理@AfterThrowing</p></li><li><p>finally中写@After</p></li></ul><p><code>------</code></p><hr><p>btw：切面类和代理类不一致：代理类是外包目标类实现拦截和增强，切面类是封装横切关注点的另外的类。切面类使用代理类来实现切面的功能，代理类调用切面类的方法来增强或拦截。</p></li><li><p><strong>重用切入点表达式：</strong></p><p>由于对于同一个方法或同一类方法可能需要写多个切入方法，同时就需要重复写多次切入点表达式，所以可以提取出来，进行复用和在核心代码中简单引用</p><p>定义方法，给方法添加注释@Pointcut(value &#x3D; “表达式”)，然后调用方法即可</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-11-40-58-image.png"></p><p>如果定义的方法在不同切面类中：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-11-41-44-image.png"></p><p>为什么不是抽象成属性直接引用呢？感觉也可以吧</p></li><li><p><strong>切面优先级：</strong></p><p>相同的目标方法上有多个切面时，切面通过优先级控制执行顺序</p><p>外层的优先级较高，先执行</p><p>使用@Order（）注解,  参数越小，越先执行</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-11-45-15-image.png"></p></li></ul><h5 id="基于配置文件的AOP切入"><a href="#基于配置文件的AOP切入" class="headerlink" title="基于配置文件的AOP切入"></a>基于配置文件的AOP切入</h5><p>紧紧用配置文件完成所有事——</p><ul><li><p>找到切面类：</p></li><li><p>配置切入点：</p></li><li><p>配置五种切入方法：</p></li></ul><h5 id="全注解开发"><a href="#全注解开发" class="headerlink" title="全注解开发"></a>全注解开发</h5><p>需要将bean.xml配置文件转化成配置类，用注解完成相应配置</p><ul><li><p>开启全文扫描</p></li><li><p>开启AspectJ自动代理</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-10-16-53-06-image.png"></p><ul><li><p>据说EnableAspectJAutoProxy有个参数proxyTargetClass，是boolean类型</p><p>true——Spring使用基于类的代理（CGlib）</p><p>false——Spring使用基于接口的代理（JDK原生）</p></li></ul></li></ul><h3 id="BTW-配置文件常见前缀配置"><a href="#BTW-配置文件常见前缀配置" class="headerlink" title="BTW:配置文件常见前缀配置"></a>BTW:配置文件常见前缀配置</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">beans</span> <span class="hljs-attr">xmlns</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:xsi</span>=<span class="hljs-string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:p</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/p&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:context</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/context&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xmlns:aop</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/aop&quot;</span></span><br><span class="hljs-tag">       <span class="hljs-attr">xsi:schemaLocation</span>=<span class="hljs-string">&quot;http://www.springframework.org/schema/beans</span></span><br><span class="hljs-string"><span class="hljs-tag">        https://www.springframework.org/schema/beans/spring-beans.xsd</span></span><br><span class="hljs-string"><span class="hljs-tag">        http://www.springframework.org/schema/context</span></span><br><span class="hljs-string"><span class="hljs-tag">        https://www.springframework.org/schema/context/spring-context.xsd</span></span><br><span class="hljs-string"><span class="hljs-tag">        http://www.springframework.org/schema/aop</span></span><br><span class="hljs-string"><span class="hljs-tag">        https://www.springframework.org/schema/aop/spring-aop.xsd&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure><h3 id="JUnit单元测试"><a href="#JUnit单元测试" class="headerlink" title="JUnit单元测试"></a>JUnit单元测试</h3><ul><li><p>Spring整合JUnit，可以省略每次创建容器的过程</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//@SpringJUnitConfig(locations = &quot;classpath:bean.xml&quot;)</span><br><span class="hljs-meta">@SpringJUnitConfig(classes = bean.class)</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Test1</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    Car car;<br><br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">say</span><span class="hljs-params">()</span>&#123;<br>        System.out.println(car.say());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><p>通过注释明确加载哪个bean.xml配置文件，or某个配置类</p><p>JUnit还有巨多关于测试的功能</p><h3 id="BTW：bean-xml注解总结："><a href="#BTW：bean-xml注解总结：" class="headerlink" title="BTW：bean.xml注解总结："></a>BTW：bean.xml注解总结：</h3><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-46-42-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-47-34-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-47-55-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-48-15-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-48-30-image.png"></p><p>另外：</p><p>&lt;context&gt;标签用于<strong>配置</strong>程序上下文信息（上下文可以理解为IoC容器）</p><ul><li><p>&lt;context:component-scan&gt;:开启组件扫描</p></li><li><p>&lt;context:annotation-config&gt;:开启注解驱动功能</p></li><li><p>&lt;context:poperty-placeholder&gt;:用于加载属性文件中的属性值</p></li></ul><h1 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h1><ul><li>Spring框架对JDBC进行封装，封装结果是jdbcTemplate</li></ul><h3 id="JDBCTemplate"><a href="#JDBCTemplate" class="headerlink" title="JDBCTemplate"></a>JDBCTemplate</h3><ul><li><p>引入的依赖：</p><ol><li><p>Spring-jdbc</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-34-08-image.png"></p></li><li><p>mysql</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-34-25-image.png"></p></li><li><p>德鲁伊连接池druid</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-11-19-35-01-image.png"></p><ul><li><p>连接池：</p><p>解决普通JDBC获取连接，关闭连接的耗时繁琐操作——<strong>池化</strong>——将建立连接和关闭连接对用户屏蔽掉，转而对其面向<strong>调用</strong>连接和<strong>释放</strong>连接。</p><p>上层操作简单看：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">package</span> com.czxy;<br>​<br><span class="hljs-keyword">import</span> com.alibaba.druid.pool.DruidDataSource;<br><span class="hljs-keyword">import</span> com.alibaba.druid.pool.DruidPooledConnection;<br><span class="hljs-keyword">import</span> org.junit.Test;<br>​<br><span class="hljs-keyword">import</span> java.sql.SQLException;<br>​<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">TestDruid</span> &#123;<br>    <span class="hljs-meta">@Test</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">testDemo</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> SQLException &#123;<br>        <span class="hljs-comment">//1 获得连接池</span><br>        <span class="hljs-comment">//1.1 核心类</span><br>        <span class="hljs-type">DruidDataSource</span> <span class="hljs-variable">dataSource</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">DruidDataSource</span>();<br>        <span class="hljs-comment">//1.2 基本4项</span><br>        <span class="hljs-comment">//  1) 驱动</span><br>        dataSource.setDriverClassName(<span class="hljs-string">&quot;com.mysql.cj.jdbc.Driver&quot;</span>);<br>        <span class="hljs-comment">//  2) 连接</span><br>        dataSource.setUrl(<span class="hljs-string">&quot;jdbc:mysql://localhost:3306/ssm_db1&quot;</span>);<br>        <span class="hljs-comment">//  3) 用户</span><br>        dataSource.setUsername(<span class="hljs-string">&quot;root&quot;</span>);<br>        <span class="hljs-comment">//  4) 密码</span><br>        dataSource.setPassword(<span class="hljs-string">&quot;1234&quot;</span>);<br>        <span class="hljs-comment">//1.3 特殊项【可选】</span><br>        <span class="hljs-comment">// 1) 初始化大小</span><br>        dataSource.setInitialSize(<span class="hljs-number">5</span>);<br>        <span class="hljs-comment">// 2) 最大值活动数</span><br>        dataSource.setMaxActive(<span class="hljs-number">10</span>);<br>        <span class="hljs-comment">// 3) 最小空闲数</span><br>        dataSource.setMinIdle(<span class="hljs-number">2</span>);<br>​<br>        <span class="hljs-comment">//2 从连接池中获得连接</span><br>        <span class="hljs-comment">//对比两次获取到的连接是否一致</span><br>        <span class="hljs-type">DruidPooledConnection</span> <span class="hljs-variable">conn</span> <span class="hljs-operator">=</span> dataSource.getConnection();<br>        System.out.println(conn);<br>         <span class="hljs-comment">//用完了，返还到连接池中</span><br>        conn.close();<br>​<br>        <span class="hljs-type">DruidPooledConnection</span> <span class="hljs-variable">conn2</span> <span class="hljs-operator">=</span> dataSource.getConnection();<br>        System.out.println(conn2);<br>        conn2.close();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul></li></ol></li><li><p>经典操作：</p><p><code>创建配置文件jdbc.properties,里面配置连接数据库的必要信息——用户，密码，数据库url，驱动</code></p><p><code>在spring的配置文件（bean.xml）中引入此文件引入,引入后就可通过K-V形式访问到数据（properties是k=v，yml格式是k：v）</code></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-09-43-27-image.png"></p></li><li><p>配置JDBCTemplate——将其作为一个bean对象</p><p><strong>说明本身就是一个类</strong></p><p>JDBCTemplate有一个属性——<strong>dataSource</strong>——用于绑定连接池</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-09-44-55-image.png"></p></li><li><p>sql语法复习：</p><p>占位符？——表示写sql时并不确定，需要在真正传入DB时传入参数的位置</p><p>mybatis的#{}，${}转化成？然后用参数注入也是模拟此过程。</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-09-55-31-image.png"></p></li><li><p>JDBCTemplate让程序员面向类的API，于是API设计的很人性化（通过可变参数完成注入）</p></li><li><p>mybatis并没有对象，而是通过配置和注解match到程序和目标sql，此时传参的操作就显得刻意了一点。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">void</span> <span class="hljs-title function_">updateSalary</span><span class="hljs-params">(Integer id, Integer salary, LocalDate fromDate,LocalDate toDate)</span>;<br></code></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">update</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;updateSalary&quot;</span>&gt;</span><br>    update salaries<br>    set salary=#&#123;salary&#125;<br>    where emp_no = #&#123;id&#125;<br>      and from_date = #&#123;fromDate&#125;<br>    and to_date = #&#123;toDate&#125;<br><span class="hljs-tag">&lt;/<span class="hljs-name">update</span>&gt;</span><br></code></pre></td></tr></table></figure></li><li><p>查询的写法：</p><p>写法一：手写中间参数的实现类（用resultSet和rowNum获取想要的结果）</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-10-08-16-image.png"></p><p>思想：查询的参数中有resultSet结果集，此类型内置了一个表，封装了一些API来访问这张结果表：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-10-12-56-image.png"></p><p>写法二：</p><p>使用中间参数的实现类</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-10-13-59-image.png"></p></li></ul><h3 id="事务："><a href="#事务：" class="headerlink" title="事务："></a>事务：</h3><p>事务指一个操作序列，这些操作要么全部执行，要么全不执行。</p><ul><li><p>ACID</p><ul><li><p>原子性（Atomicity）：不可细分，绑定整体</p></li><li><p>一致性（Consistency）：事务中的数据正确，在财会中满足会计恒等式，在sql中满足主键不重复等等</p></li><li><p>隔离性（Isolation）：多个事务之间——在提交前相互隔离，一个事务不会访问到其他事务的中间状态。也要应对并发修改。</p></li><li><p>持久性（Durability）：提交的事务必须保存下来，既是系统崩溃，重启后也要有保留。</p></li></ul></li><li><p>编程式事务和声明式事务：</p><p>编程式要求手动开启事务，提交事务，异常回滚…</p><p>声明式仅仅通过配置即可实现——声明式是编程式的封装，将共同部分屏蔽掉。</p></li></ul><ol><li><p>开启事务</p><ol><li><p>添加事务配置</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-10-45-22-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-10-47-01-image.png"></p></li><li><p>在Service层添加注解：@Transactional</p><ul><li><p>加到方法上单独控制方法</p></li><li><p>加到类上控制所有方法</p></li></ul></li></ol></li><li><p>@Transactional的参数</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2023-07-16-11-07-16-image.png"></p></li></ol>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Back!MyBDer</title>
    <link href="/2024/05/17/Back-MyBDer/"/>
    <url>/2024/05/17/Back-MyBDer/</url>
    
    <content type="html"><![CDATA[<h1 id=""><a href="#" class="headerlink" title=""></a></h1><h3 id="问题1：什么是面向对象？"><a href="#问题1：什么是面向对象？" class="headerlink" title="问题1：什么是面向对象？"></a>问题1：什么是面向对象？</h3><p>对比面向过程、面向对象和面向函数三种处理问题的角度</p><p>面向过程更注重事情的步骤，面向对象更注重事情的参与者，面向函数更关注其中的运算</p><p>比如：洗衣机洗衣服</p><ul><li><p>面向过程编程会按照步骤一步步执行：人打开洗衣机，放衣服，放洗衣服，洗衣机清洗，烘干…</p></li><li><p>面向对象编程会分成两个对象，人做什么，洗衣机做什么</p></li><li><p>面向函数编程更在乎行为：最外层是operate(process)，process传入洗衣服wash(clothes,methods)，methods再传入具体的清洗方法</p></li></ul><p>面向对象和面向函数都能够复用代码</p><h4 id="封装、继承、多态"><a href="#封装、继承、多态" class="headerlink" title="封装、继承、多态"></a>封装、继承、多态</h4><ul><li><p>封装：封装的意义：内部对外未黑盒（透明）、外部暴露允许访问的成员函数和数据项</p><p>类的封装：经典操作提供private数据的getset方法，就是将数据的赋值和获取逻辑把握在自身| 可能要求名字共同前缀 or其他限制。</p><p>框架的封装：例如ORM框架mybatis，开发者只需要利用mybatis特定方法即可实现功能，不需要关心&#x2F;不允许修改实现逻辑</p></li><li><p>继承：继承基类的方法，并作出自己的改变或扩展</p></li><li><p>多态：</p><ol><li><p>父类和多个子类：</p><p>基于对象所属类的不同，外部对同一个方法的调用，实际执行的逻辑不桶</p><p>多态的三要素：继承&#x2F;实现、方法重写&#x2F;实现、父类引用指向子类对象</p></li><li><p>抽象类和多个实现类：</p><p>一个抽象类引用某个实现类的方法，本质上还是实现类对象调用其方法</p><p>一个实现类可用实现多个接口，它就是多个抽象类的实现，对外可被看作是多种状态 – 多态</p></li></ol></li></ul><h3 id="问题2：JDK、JRE、JVM"><a href="#问题2：JDK、JRE、JVM" class="headerlink" title="问题2：JDK、JRE、JVM"></a>问题2：JDK、JRE、JVM</h3><ul><li><p>JDK：java Development Kit Java开发者工具</p></li><li><p>JRE：java Runtime Environment Java运行环境</p></li><li><p>JVM：java virtual machine</p></li></ul><p>下载JDK包含JRE文件夹，JRE中有两个关键文件夹：bin（JVM）、lib（类库）</p><p>JDK除JRE外提供的开发工具：javac，java，jconsole，jmap…</p><h3 id="问题3：-和-equals"><a href="#问题3：-和-equals" class="headerlink" title="问题3：&#x3D;&#x3D; 和 equals"></a>问题3：&#x3D;&#x3D; 和 equals</h3><p>equals如果没有重写的话，在Object类中的equals本质就是 &#x3D;&#x3D; 比较</p><p>&#x3D;&#x3D; 比较的内容：对于基本数据类型，由于其数值保存在栈内存中，直接比较数值</p><p>对于引用数据类型，其比较的是对堆内存的对象的引用地址</p><p>equals通常都会重写，在String中的重写是将字符串转化成char数组后挨个比较</p><blockquote><p>但对与字符串特殊的一点在于：字符串常量池</p></blockquote><p>JVM为了提高性能和减少内存开销，在实例化字符串常量时，为字符串开辟了一个缓存区 - 常量池</p><p>内部本质上维护了一个table，是字符串对字符串对象的引用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;yes&quot;</span>;<br></code></pre></td></tr></table></figure><p>此方法会优先去常量池中看看有无此引用，若有直接使用这个引用，若无则创建对象放入常量池中</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">String</span>(<span class="hljs-string">&quot;yes&quot;</span>);<br></code></pre></td></tr></table></figure><p>此方法会创建一个新的对象，在堆内存中常量池外，但也会保证创建的对象在常量池中有副本</p><p>即：若常量池中没有此字符串，则会在常量池和堆内存中各创建一个，这两个对象中byte[]使用的是同一个</p><blockquote><p>对其他基本数据类型的包装类也有特殊的点：都有常量池的思想（除了float和double）</p></blockquote><p>对于int和int，int和Integer之间都只是基本的数值比较，因为int的值都在栈内存里，int和Integer比较的话Java会自动将Integer拆箱成int</p><p>但对于Integer和Integer的比较，Java维护了 -128到127之间的常量池，内部的Integer引用同一个对象；为什么不把这些Integer也自动都拆成int然后比较呢？毕竟拆箱本身没什么成本</p><blockquote><p>尤其是装箱，如果Integer常量池没有的话需要返回new的对象，而且不会把新对象加入池子中拆箱本身没有什么成本，就是返回Integer中的value</p></blockquote><p>Integer提供了重写的equals方法，内部就是判断各自的value是否相同</p><p>感觉可能是Java为了向后兼容 or else；</p><h3 id="问题3：说说字符串常量池"><a href="#问题3：说说字符串常量池" class="headerlink" title="问题3：说说字符串常量池"></a>问题3：说说字符串常量池</h3><p>JVM为了提高性能和减少内存开销，在实例化字符串常量时，为字符串开辟了一个缓存区 - 常量池</p><p>内部本质上维护了一个table，是字符串对字符串对象的引用</p><h4 id="三种字符串操作："><a href="#三种字符串操作：" class="headerlink" title="三种字符串操作："></a>三种字符串操作：</h4><ol><li><p>“+”连接字符串</p><ul><li><p>普通常量连接：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;a&quot;</span>+<span class="hljs-string">&quot;b&quot;</span>+“c”;<br></code></pre></td></tr></table></figure><p>Java编译器会将其自动视为 </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;abc&quot;</span>;<br></code></pre></td></tr></table></figure></li><li><p>字符串变量之间的连接：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">a</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;a&quot;</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">b</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;b&quot;</span>;<br><span class="hljs-type">String</span> <span class="hljs-variable">c</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;c&quot;</span>;<br><br><span class="hljs-type">String</span> <span class="hljs-variable">s</span> <span class="hljs-operator">=</span> a + b + c;<br></code></pre></td></tr></table></figure><p>由于Java中String是不可变类型，所以以上连接会被反编译成</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java">(<span class="hljs-keyword">new</span> <span class="hljs-title class_">StringBuilder</span>()).append(a).append(b).append(c).toString();<br></code></pre></td></tr></table></figure><p>toString中，返回一个new的字符串对象</p></li></ul></li><li><p>intern()方法： ——  作用是对字符串对象规范表示 &#x2F; 标准化 – 统一从池子里拿</p><p>当调用intern方法时，首先判断常量池中是否存在相同值的字符串常量，如果存在则返回常量池中的字符串的引用，如果不存在，则会在常量池中增加一个相同的字符串常量，返回在常量池中的引用；</p></li></ol><h3 id="问题4：你懂final吗？"><a href="#问题4：你懂final吗？" class="headerlink" title="问题4：你懂final吗？"></a>问题4：你懂final吗？</h3><ol><li><p>final的用法：</p><ul><li>修饰类：表示类不可被继承</li><li>修饰方法：表示方法不可被子类覆盖，但是可用重载（改变参数 &#x2F; 返回值的新方法可存在）</li><li>修饰变量：表示变量一旦被赋值，就不可被修改</li></ul></li><li><p>final修饰变量的初始化时机 – 都要求刚刚加载时完成初始化：</p><blockquote><p>Java会给每个成员变量赋默认值初始化，但不会给局部自动变量初始化</p></blockquote><ul><li><p>final修饰成员变量：</p><ol><li>如果是static的，只能在声明时或者静态代码块中初始化</li><li>如果是普通成员变量，只能在声明时或者非静态代码块中（或者构造器中）初始化</li></ol></li><li><p>final修饰局部变量：</p><p>局部变量可以在声明时指定值，也可以不指定，在后续代码中完成对final的赋值。</p><p>因为不加final声明时本身也不会初始化</p></li></ul></li><li><p>final修饰基本数据类型和引用数据类型</p><ul><li>基本：数值在初始化后就不可被修改</li><li>引用：引用在初始化后就不能再让其指向另一个对象，但引用的值是可变的</li></ul></li><li><p>为什么局部内部类和匿名内部类只能访问final变量？</p><blockquote><p>问题描述：局部内部类和匿名内部类如果想要访问调用方法中的局部变量时，只能读，不能赋值 | 如果想要赋值只能转化成数组[0]  —- 隐式的final修饰</p></blockquote><p>内部类理应可以访问外部的局部变量，但有一个问题：外部方法结束后会销毁局部变量的，这就会导致内部类引用了空的内容；</p><p>最常见的情况就是用匿名内部类开新线程，新的线程可能需要长时间引用某个变量，但外面的方法早就执行完了会自动回收变量。</p><p>解决方案是：将局部变量复制了一份作为内部类的成员变量，内部类实际访问的是局部变量的副本。新的问题是：副本是两个不同的数 &#x2F; 对象，修改其中一个没法自动修改另一个；所以Java的妥协就是 - 不能修改，只能读取 - 来保证内部类的成员变量和外部方法的局部变量是一致的。</p></li></ol><h3 id="问题5：String、StringBuffer、StringBuilder"><a href="#问题5：String、StringBuffer、StringBuilder" class="headerlink" title="问题5：String、StringBuffer、StringBuilder"></a>问题5：String、StringBuffer、StringBuilder</h3><ul><li><p>String是final修饰的不可变类型</p></li><li><p>StringBuffer是线程安全的，StringBuilder是线程不安全的</p><p>线程不安全：多线程环境下存在共享变量，需要额外加锁才能避免竞态问题</p><p>StringBuffer中每个方法都写好了synchronized</p></li><li><p>使用场景：</p><p>优先使用StringBuilder（由于没有锁性能较好），但是当变量是在多线程环境下的共享变量，使用StringBuffer</p><p>同时由于以上二者操作是对同一个对象，而不会想String一样每次操作产生新String</p></li></ul><h3 id="问题6：重载和重写的区别"><a href="#问题6：重载和重写的区别" class="headerlink" title="问题6：重载和重写的区别"></a>问题6：重载和重写的区别</h3><ul><li><p>重载：在同一个类中，方法名相同，参数类型不同、个数不同、顺序不同、返回值不同、修饰符不同等等方法间构成重载，但需要让调用者能够区分调用的是谁（只有访问修饰符或者返回值不同不行）</p></li><li><p>重写：发生在父子类中，方法名、参数列表必须相同；访问修饰符需要大于等于父类。但如果父类是private修饰则不能被重写</p><p>重写方法返回值范围小于等于父类（是父类返回值的类型或其子类）；抛出的异常范围小于等于父类（..其类型或其子类） —- 由于在多态时，子类向上返回的值 &#x2F; 抛出的异常可以被父类对象的上下文兼容；</p></li></ul><h3 id="问题7：接口和抽象类的区别"><a href="#问题7：接口和抽象类的区别" class="headerlink" title="问题7：接口和抽象类的区别"></a>问题7：接口和抽象类的区别</h3><ul><li><p>接口的设计目的：是对类的行为进行约束（让类具有某种能力）</p><p>接口是对行为的抽象 - - 符合 like a 的关系</p></li><li><p>抽象类的设计目的是代码复用。当不同的类具有某些相同的行为时，这些行为可以抽离成一个抽象类 - 其中某些代码（方法）是复用的，其他子类们不同的方法定义成抽象方法</p><p>抽象类的本质是对子类们的抽象 - - 符合 is a 的关系；</p><p>但是抽象类的成本比较高，因为一个类只可以继承一个类。当比较关注类的本质时可以用抽象类；接口虽然在功能上弱化了许多，它只实现了对动作的描述，但一个类可以实现多个接口，更灵活点。在关注类的行为时用接口；</p></li></ul><h3 id="问题8：hashCode-and-equals"><a href="#问题8：hashCode-and-equals" class="headerlink" title="问题8：hashCode and equals"></a>问题8：hashCode and equals</h3><p>重写equals是为了准确判断两个对象是否相等，但在Java的集合框架中，当用到hash来判断某个元素是否存在于某个集合中时，使用计算hashcode后在hashTable中比较，来代替O(n)级别的equals方法。所以和重写equals的目的一致，重写hashCode也是为了在通过计算哈希值判断是否是同一个元素的场景下，能够准确判断</p><p>原本计算hashcode是基于内存地址的，不同对象就算在像也不会有一致的hashcode</p><p>hash的具体判断：例如HashSet，HashSet会先计算对象的hashCode来判断对象加入的位置，如果该位置有值，为了防止是哈希冲突嘛，再调用equals判断两个对象是否真的相同。</p><h3 id="问题9：ArrayList-and-LinkedList"><a href="#问题9：ArrayList-and-LinkedList" class="headerlink" title="问题9：ArrayList and LinkedList"></a>问题9：ArrayList and LinkedList</h3><ul><li><p>ArrayList：基于动态数组，底层是连续的内存空间，每个元素由于<strong>泛型限制</strong>占的空间一致，所以只需要开始位置和偏移量即可访问 – 适合下标访问。</p><p>但也因为本身是连续的空间，导致插入删除的成本比较高，往往需要平行移动一部分元素。但有一种场景是这样：并<strong>不关心</strong>List内部的<strong>顺序</strong>，所以用尾插法就可以很快的插入，此时效率很高。</p><p>同时当数据长度越来越长，会逐渐超过内存空间，于是需要再复制到更大的空间。</p><p><strong>扩容太频繁</strong>也会对代码整体性能产生影响，一般在ArrayList初始化时指定长度就号</p></li><li><p>LinkedList：本身基于链表，可以分散的存储在内存中，适合做数据的插入和删除操作；相对的就不适合查询；遍历时避免用for循环根据索引来查找（因为每次查找都需要从头来）；</p><p>并且LinkedList将元素包了一层node，对内存而言也比较有压力。</p></li></ul><h3 id="问题10：哈师卖"><a href="#问题10：哈师卖" class="headerlink" title="问题10：哈师卖"></a>问题10：哈师卖</h3><ol><li><p>hashTable相比hashMap，在每个方法上都加了Synchronized确保线程安全</p><p>hashMap在多线程场景下可能出现竞态问题导致数据前后不一致；</p><p>HashMap运行key和value为null，但hashTable不允许</p></li><li><p>底层！</p><blockquote><p>数组+链表</p></blockquote><blockquote><p>一点思考：</p><ol><li><p>为什么索引都是从0开始的，因为要计算偏移量，往往整个数组的地址就是第一个元素的地址，只有首位的索引是0才能计算得到自身</p></li><li><p>在集合中查找元素的复杂度层次：</p><p>O(n)：遍历</p><p>O(logn)：二分查找 &#x2F; 搜索二叉树 &#x2F; 平衡二叉树</p><p>O(1)：在连续数组中根据偏移量计算出来，直接获取</p><blockquote><p>HashMap也想实现这种不基于比较的查找，直接根据内容计算出来位置</p></blockquote></li></ol></blockquote><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231205174106471.png" alt="image-20231205174106471"></p><p>添加过程：</p><ol><li><p>计算哈希码：结果是一个int值</p></li><li><p>计算在哈希表中的存储位置：通过对数组的长度取模得到桶的索引</p></li><li><p>存入哈希表：</p><p>情况1：原本位置没有元素，一次添加成功</p><p>情况2：原本位置有元素，经过equals比较确认不是同一元素，则跟在这个桶后（链表后）</p><p>情况3：原本位置有元素，且原本桶内找到相同的元素，用新元素的value代替老元素的value</p></li></ol></li></ol><h4 id="JDK-7-HashMap"><a href="#JDK-7-HashMap" class="headerlink" title="JDK 7 HashMap"></a>JDK 7 HashMap</h4><p>底层就是一个数组+链表的存储结构</p><ul><li><p>链表上每个节点是一个<strong>Entry</strong>，字段包括四部分</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231205175423437.png" alt="image-20231205175423437"></p><p>其中hash表示key的哈希码</p></li><li><p>默认的主数组长度为16</p></li><li><p>主数组的长度可以直接指定，但最终会变成大于指定数的2的幂</p></li><li><p>默认扩容因子是0.75（元素个数达到主数组长度的75%时扩容 - 重新创建新哈希表，将元素都重新装填进新的表中）控制数组存放数据的疏密程度；loadFactor越趋近于一，存放的数据越多，太多会导致查找元素效率低，太低又会导致利用率低、扩容频繁</p></li><li><p>每次主数组扩容为原来的2倍</p></li><li><p>当发生哈希冲突时，新元素需要和整个链表的节点进行比较（equals），如果确定不存在，考虑添加新节点：</p><p>JDK 1.7是在头部添加，1.8是在尾部添加</p></li></ul><h4 id="源码中关于put方法的执行："><a href="#源码中关于put方法的执行：" class="headerlink" title="源码中关于put方法的执行："></a>源码中关于put方法的执行：</h4><ol><li><p>如果key是null，则直接存入索引为0的桶中；</p></li><li><p>第一步：计算哈希码 – 其中除了获取hashCode外还要再多次变换hashcode</p></li><li><p>第二步：计算存储位置：使用位运算代替取余</p></li><li><p>第三步：判断key是否存在，判断策略是：比较一个桶内链表上entry的哈希码 –&gt; key是否相同&#x2F;是同一个对象</p><p><img src="C:\Users\吴松林\Desktop\G-O-D\image-20231206093637296.png" alt="image-20231206093637296"></p><p>短路往往是为了提高效率，本身重写过equals的话可以直接调用equals，但理论上比key也有可能通过</p></li><li><p><strong>扩容的条件是：节点数量达到阈值 &amp;&amp; 新元素的位置已经有节点</strong></p><p>尽量减少扩容次数，因为扩容会导致原来的节点要重新散列到新数组的位置</p><blockquote><p>如果能够预估到节点可能散列到新列表的长度，就免去了取余位运算操作 – 1.8</p></blockquote><p>为了减少扩容，需要预估节点的数量，在初始化时指定哈希表中主数组的长度</p></li><li><p>真正的扩容是由transfer()实现的，扩容需要重新创建一个新的哈希表（主数组），原来的entry都需要重新计算存储位置并添加到新的哈希表中；</p></li><li><p>第四步：如果发生冲突 &#x2F; 取余后是同一个数，而且确定新数之前不存在，就将新元素包装成entry，添加到链表的最前面</p></li></ol><h4 id="细节问题："><a href="#细节问题：" class="headerlink" title="细节问题："></a>细节问题：</h4><ol><li><p>为什么要把hash也放到Entry中</p><p>hash本身就是key的哈希值，避免使用时重复计算</p><ul><li>扩容时需要用哈希来取余</li><li>判断key是否存在时需要直接比较哈希</li></ul></li><li><p>为什么主数组的长度必须是2的幂次？</p><p>因为计算存储位置的公式 h&amp;（length - 1）；只有length是100000类型，length-1才能是111111类型，与运算才能达到取余的效果</p></li><li><p>为什么加载因子选择0.75？</p><p>默认负载因子在时间和空间成本之间提供了一个很好的权衡。较高的值（ &#x3D; 2）会减少复制次数，减少空间开销，但会导致链表过长，增加查找成本；较低的值（&#x3D; 0.2）会导致扩容太频繁，空间开销过大；</p></li><li><p>JDK7 的死循环问题：</p><p>JDK1.7的新节点是添加到链表的头部，导致重新散列后，链表的节点顺序会颠倒；如果是单线程的情况下，这不算问题</p><p>但是在多线程环境下扩容时，多个线程同时执行transfer方法，可能扩容后形成循环列表</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231206102346141.png" alt="image-20231206102346141"></p><blockquote><p>原本3指向7，第一个线程取出3后，原本要设置3的next &#x3D; null，但在链表里一般通过某个指针来操作具体节点嘛，这个指针有可能在指向刚才断开连接操作前被覆盖，导致连接没有断开；</p></blockquote><p>循环导致的问题：</p><ul><li>如果下一步要get()一个不存在的key，或者put()一个不存在的key，都要先循环检查整个链表，检查的停止条件是next &#x3D; null，所以导致根本停不下来</li></ul></li><li><p>多线程put的时候为什么可能导致元素丢失</p><p>其实是多个线程可能都持有 new 的entry，但是添加到链表的过程中，是把上面指向头节点的指针指向new entry，再把new entry的指针指向原本头节点，如果两个线程都要往这个位置插入，就会导致之后上层只能找到最后一个线程添加的节点，之前的节点都只保留向下的引用，但却无法被访问到；</p></li></ol><h3 id="JDK-8-HashMap"><a href="#JDK-8-HashMap" class="headerlink" title="JDK 8 HashMap"></a>JDK 8 HashMap</h3><ul><li>结构变化： 由数组 + 链表 变成了数组 + 链表 + 红黑树</li></ul><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231206104418905.png" alt="image-20231206104418905"></p><ul><li><p>链表长度 &gt;&#x3D; 8，链表转化成红黑树；链表长度减少为6，红黑树退化成链表；但只有总的节点数量 &gt;&#x3D; 64时，才会有红黑树，否则直接进行主数组扩容</p></li><li><p>链表节点为node，红黑树节点为treeNode</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231206104840297.png" alt="image-20231206104840297"></p></li><li><p>添加到链表后面：JDK8后，新节点是加到最后，避免死循环（多线程下也不会导致节点顺序变化）</p></li></ul><h4 id="JDK8-的-put"><a href="#JDK8-的-put" class="headerlink" title="JDK8 的 put"></a>JDK8 的 put</h4><h4 id="细节问题：-1"><a href="#细节问题：-1" class="headerlink" title="细节问题："></a>细节问题：</h4><ol><li><p>为什么是当链表长度 &gt;&#x3D; 8 才变成红黑树？</p><p>首先哈希碰撞是个概率问题，这个概率问题符合泊松分布</p><p>也就是说对于很多的node要计算哈希投影到桶里，每个桶中链表的长度起始是符合泊松分布的</p><p>所以就可以算出来，链表长度为 n 时发生的概率大概为多少</p><p>在长度为8时，发生概率巨低，只有0.00000006，即转化成红黑树的概率很低</p><blockquote><p>这个概率还受负载因子影响</p></blockquote></li><li><p>计算哈希码的方法简单了</p><p><img src="C:\Users\吴松林\AppData\Roaming\Typora\typora-user-images\image-20231206110032367.png" alt="image-20231206110032367"></p><p>为什么要把高位也引入比较？</p><blockquote><p>JDK1.7时期</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">static</span> <span class="hljs-type">int</span> <span class="hljs-title function_">hash</span><span class="hljs-params">(<span class="hljs-type">int</span> h)</span> &#123;<br> <span class="hljs-comment">// This function ensures that hashCodes that differ only by</span><br> <span class="hljs-comment">// constant multiples at each bit position have a bounded</span><br> <span class="hljs-comment">// number of collisions (approximately 8 at default load factor).</span><br><br> h ^= (h &gt;&gt;&gt; <span class="hljs-number">20</span>) ^ (h &gt;&gt;&gt; <span class="hljs-number">12</span>);<br> <span class="hljs-keyword">return</span> h ^ (h &gt;&gt;&gt; <span class="hljs-number">7</span>) ^ (h &gt;&gt;&gt; <span class="hljs-number">4</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>因为取余操作：</p><p>n &#x3D; table.length;<br>index &#x3D; （n-1） &amp; hash;</p><p>n从16开始，翻倍扩容，即：只有低4 &#x2F; 5 &#x2F; 6 … 不为0，与操作会使hash的高位无效化</p><ul><li><p>1.7的版本扰动能力更强，在n &#x3D; 16时会把高于4位的很多部分都异或一遍</p></li><li><p>1.8的版本在性能和准确性中做了均衡</p></li></ul></blockquote></li><li><p>过程：</p><p><img src="https://img-blog.csdnimg.cn/20190720115253741.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2dvb3Nzb24=,size_16,color_FFFFFF,t_70" alt="img"></p><ol start="4"><li><p>原来元素新的分布位置</p><p>主数组扩容位原来的二倍，匀速的索引要么是原来的索引，要么是原来的索引+原本数组容量</p><p>在JDK8中的transfer中，不进行存储位置的重新计算，而是判断此元素应该在原位置还是新位置 </p><p>具体而言：就是要将原本挂在0位置的长链表，分成两个链表挂在两个节点上</p><p>hashMap维护了4个指针，分别是两个链表的头和尾，头是为了最后挂载到主数组上，尾是为了添加新节点</p><p>过程中遍历链表上每个node，添加到对应的链表尾巴上，最后再把头节点挂到主数组上。</p></li><li><p>单链表变成红黑树的过程：</p><p>简单来说，先把单链表变成双向链表，再将双向链表转化成红黑树。</p></li></ol><blockquote><p>1.8的hashMap虽然解决了扩容中的死循环和数据丢失，但仍然存在并发不安全的问题</p><blockquote><p>只要存在【先判断、后操作】或者【先read、后write】，就有可能让某个线程在中间被挂起而数据被修改</p></blockquote></blockquote></li><li><p>复杂度分析：</p><blockquote><p>查询的时间复杂度 &#x3D; 遍历链表的次数 </p><p>插入的时间复杂度 &#x3D; 遍历链表的次数 + 扩容成本平均到每次插入的成本</p><ul><li><p>遍历链表 O(k)，取决于链表最大长度</p></li><li><p>扩容平均时间：在transfer不做优化时，扩容到N一共需要logN次，每次重新挂载所有节点需要O(N)，于是所有扩容就需要O(N * logN)，平均到每次插入logN</p><blockquote><p>但是Java中transfer有优化</p><ol><li><p>四指针移动</p></li><li><p>离线扩容（不占用用户线程的扩容）</p></li></ol></blockquote></li></ul></blockquote></li></ol><h3 id="ConcurrentHashMap！"><a href="#ConcurrentHashMap！" class="headerlink" title="ConcurrentHashMap！"></a>ConcurrentHashMap！</h3><ol><li><p>JDK1.7中，架构和HashMap略有不同</p><p>HashMap既然是给整张map加锁，ConcurrentHashMap也顺应这个思路，只不过最外层包裹了多个HashMap（Segment），相当于将多个hashmap的主数组一字排开构成大hashmap，按照一段一段进行加锁；每个小hashmap通过引用绑定在segment上，所以扩容操作互不影响；</p><p><img src="https://oss.javaguide.cn/github/javaguide/java/collection/java7_concurrenthashmap.png" alt="Java 7 ConcurrentHashMap 存储结构"></p><blockquote><p>Segment默认长度为16，即：ConcurrentHashMap默认支持16个线程并发</p><ul><li><p>HashMap喜欢通过位运算计算所有投影操作</p><ol><li><p>计算哈希桶索引（put，get会根据key的hash投影找到桶，常规为了代码可读用 hash % length；而Hashmap </p><blockquote><p>使用位运算代替取模</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">final</span> V <span class="hljs-title function_">putVal</span><span class="hljs-params">(<span class="hljs-type">int</span> hash, K key, V value, <span class="hljs-type">boolean</span> onlyIfAbsent,</span><br><span class="hljs-params">               <span class="hljs-type">boolean</span> evict)</span> &#123;<br>   ...<br>    <span class="hljs-comment">//n = tab.length</span><br>    <span class="hljs-keyword">if</span> ((p = tab[i = (n - <span class="hljs-number">1</span>) &amp; hash]) == <span class="hljs-literal">null</span>)<br>        tab[i] = newNode(hash, key, value, <span class="hljs-literal">null</span>);<br>   ...<br></code></pre></td></tr></table></figure><p><strong><strong>当 length 为 2 的次幂时，<code>num &amp; (length - 1) = num % length</code> 等式成立。</strong></strong></p></li><li><p>hash扰动</p><blockquote><p>见上</p></blockquote><p>为什么选异或？</p><blockquote><p>与 &amp; 操作和或 | 操作的结果更偏向于 0 或者 1，而异或的结果 0 和 1 有均等的机会</p></blockquote></li></ol></li></ul></blockquote><hr><h5 id="哈希函数"><a href="#哈希函数" class="headerlink" title="哈希函数"></a>哈希函数</h5><ul><li><p>哈希模型：</p><ol><li><p>MD5 ： 输出在 0 ~ 2e64 -1| String.length() &#x3D; 16</p></li><li><p>SHa1： 输出在 0 ~ 2e128 -1 | String.length() &#x3D; 32</p></li></ol><blockquote><p>Java的hashcode()会阉割一部分，只返回2e32 -1的范围，即长度为8</p><p>返回的内容是字符串表示的数，字符串每个char时 0到 f十六进制</p></blockquote></li><li><p>哈希函数特征：</p><ol><li><p>输入域无穷，输出域有限</p></li><li><p>相同的输入必然导致相同的输出 &#x2F; 不存在随机的成分</p></li><li><p>不同输入有小概率产生相同输出 &#x2F; 哈希碰撞</p></li><li><p>离散性 &amp; 均匀性 | hash函数不依赖某个规律</p><blockquote><p>离散性：相似的输入，产生离散的输出</p><p>均匀性：无论输入如何，结果都可以均匀分布在整个范围内</p><ul><li>常用算法会将hashcode % m，同样能保证在0 ~ m-1上均匀分布</li></ul></blockquote></li></ol></li></ul></li></ol><h3 id="问题11：用过MySQL和Redis吗？"><a href="#问题11：用过MySQL和Redis吗？" class="headerlink" title="问题11：用过MySQL和Redis吗？"></a>问题11：用过MySQL和Redis吗？</h3><p>使用那种类型数据库取决于数据的性质</p><ul><li><p>redis的应用场景：</p><ul><li><p>缓存</p></li><li><p>计数器</p></li><li><p>分布式会话</p></li><li><p>排行榜</p></li><li><p>最新列表</p></li><li><p>分布式锁</p></li><li><p>消息队列</p></li></ul></li></ul><h3 id="问题12：聊聊单例模式"><a href="#问题12：聊聊单例模式" class="headerlink" title="问题12：聊聊单例模式"></a>问题12：聊聊单例模式</h3><ul><li><p>单例模式，就是指在整个运行时域，一个类只有一个对象</p></li><li><p>为什么需要考虑单例模式呢？因为在整个项目运行中，有些类对象的创建成本是比较大的，如果频繁的创建和销毁对象会导致CPU性能消耗比较严重</p><p>例如：创建数据库连接的类对象只需要创建一次，后来的操作都复用即可</p></li><li><p>Java中如果实现单例模式：</p><blockquote><p>多种写法，多种思维</p></blockquote><ol><li><p>方法1</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Singleton</span>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">Singleton</span><span class="hljs-params">()</span>&#123;&#125;;<br>    <span class="hljs-comment">//禁止在外创建，只能通过提供的API获取对象</span><br>    <span class="hljs-keyword">private</span>  <span class="hljs-keyword">static</span> <span class="hljs-type">Singleton</span> <span class="hljs-variable">instance</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span>&#123;<br>        <span class="hljs-keyword">if</span>(instance == <span class="hljs-literal">null</span>)&#123;<br>            instance = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();<br>        &#125;<br>        <span class="hljs-keyword">return</span> instance;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>思想：【懒加载】 对象只有在第一次创建时才会被加载，这样做可以避免某个对象在程序中根本不会用到却创建了对象</p><blockquote><p>问题在于：线程不安全</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">//使用双检锁加锁</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Singleton</span>&#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">volatile</span> <span class="hljs-type">Singleton</span> <span class="hljs-variable">instance</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>    <span class="hljs-keyword">private</span> <span class="hljs-title function_">Singleton</span><span class="hljs-params">()</span>&#123;&#125;;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span>&#123;<br>        <span class="hljs-keyword">if</span>(instance = <span class="hljs-literal">null</span>)&#123;<br>            <span class="hljs-keyword">synchronized</span>(Singleton.class)&#123;<br>                <span class="hljs-keyword">if</span>(instance == <span class="hljs-literal">null</span>)&#123;<br>                    instance = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();<br>                &#125;<br>            &#125;<br>        &#125;<br>    <span class="hljs-keyword">return</span> instance;<br>    &#125;    <br>&#125;<br></code></pre></td></tr></table></figure></li></ol></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs java">思路：在<span class="hljs-keyword">synchronized</span>加锁前后都进行检查，外面的检查排除绝大部分创建对象的请求，里面的检查排除和第一个检查并发进入的请求<br><br>&gt; 问题在于：会导致指令重排<br>&gt; <br>&gt; instance = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();并不是原子操作，其中包含三步操作<br>&gt; <br>&gt; <span class="hljs-number">1.</span> 分配内存<br>&gt; <br>&gt; <span class="hljs-number">2.</span> 初始化对象<br>&gt; <br>&gt; <span class="hljs-number">3.</span> 对象指向内存地址<br>&gt; <br>&gt; 虚拟机可能为了效率将指令重排，而如果先将instance指向某块区域则instance不为<span class="hljs-literal">null</span>，容易导致还未在首个线程初始化的instance已经在别的线程被调用<br><br>解决方案：<span class="hljs-keyword">volatile</span><br><br>&gt; <span class="hljs-keyword">volatile</span>能够解决JVM层面的指令重排问题<br></code></pre></td></tr></table></figure><ol start="2"><li><p>用静态内部类</p><p>静态内部类只会在被引用到的时候被加载时加载一次，完美契合单例模式懒加载</p></li><li><p>注意：以上写法都可能被反射破坏——即通过反射创建出新的对象来</p><p>反射本身通过构造器newInstance创建的对象就不是单例的</p><blockquote><p>要么人为避免，要么使用枚举类型【枚举类型不存在无参构造函数】</p></blockquote></li></ol><p>具体根据实例化时机分为饿汉式和懒汉式</p><ol><li><p>饿汉：尽早实例化——在类加载的时期就实例化</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-type">Singleton</span> <span class="hljs-variable">instance</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Singleton</span>();<br><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> Singleton <span class="hljs-title function_">getInstance</span><span class="hljs-params">()</span>&#123;<br>    <span class="hljs-keyword">return</span> instance;<br>&#125;<br></code></pre></td></tr></table></figure></li><li><p>懒汉：第一次调用时执行实例化</p><p>但第一次调用时如果是多线程场景下可能发生竞态问题</p></li></ol><h3 id="问题13：说说volatile"><a href="#问题13：说说volatile" class="headerlink" title="问题13：说说volatile"></a>问题13：说说volatile</h3><blockquote><p>暂无</p></blockquote><h3 id="问题14：抓过包吗？"><a href="#问题14：抓过包吗？" class="headerlink" title="问题14：抓过包吗？"></a>问题14：抓过包吗？</h3><p>抓包是利用特定的软件对网络数据包进行拦截，通过对抓获数据包内的内容进行截获、重发、编辑、转存等操作</p><blockquote><p>抓包对于软件的Debug有很大帮助，但更多应用场景在网络安全：比如了解攻击者所用方法，追查攻击者IP…</p></blockquote><ul><li><p>抓包的使用场景</p><ol><li><p>检测web表单的隐藏字段：许多填写表单都有隐藏字段用来收集用户的数据、预防CRSF攻击、防爬虫等待…为了看到在界面上看不到的字段，只能使用抓包工具</p></li><li><p>想要进行压测，但单纯从Jmeter发请求可能达不到用户点击的效果，于是就需要用抓包的方式获取到用户访问接口的数据包，得知其中的协议内容、参数特点，为了更好的模拟用户请求</p></li><li><p>后端人的接口测试：观察接口的响应参数</p></li><li><p>利用抓的数据包检测数据加密情况</p></li></ol></li></ul><h3 id="问题15：UDP怎么实现可靠传输？"><a href="#问题15：UDP怎么实现可靠传输？" class="headerlink" title="问题15：UDP怎么实现可靠传输？"></a>问题15：UDP怎么实现可靠传输？</h3><ul><li><p>改动UDP协议本身不可能，因为协议本身已经有很成熟的实现方案，试图在内部修改不太可能</p><p>只能从应用层面 &#x2F; AOP代理一层中间层</p><blockquote><p>现成产品：QUIC（Quick UDP Internet Connection） —-  HTTP 3.0</p></blockquote><ul><li><p>为什么有了TCP，还需要UDP来确保稳定传输？</p><p>因为TCP无论上层想要传的数据是什么类型，都需要完成三次握手四次挥手来实现对所有数据类型的可靠传输；</p><p>【头阻塞】TCP为了保证数据包的有序，在传输过程中也要严格有序——一个包丢失需要等待直到这个包成功发送才能继续（为了可靠牺牲效率）</p><p>后来HTTP 2.0虽有多路复用，但没解决根本问题</p></li><li><p>QUIC：</p><p>基于UDP可以实现更多能够满足需求且成本比TCP低的方案【更灵活】</p><p>优点：</p><ol><li><p>自定义连接机制</p><p>取消三次握手，在网络层只进行一次握手，可靠保证在应用层解决</p></li><li><p>解决头阻塞</p></li><li><p>自定义流量控制</p></li><li><p>改进的拥塞控制</p></li></ol></li></ul></li></ul><h3 id="问题16：synchronized和Lock锁的区别？"><a href="#问题16：synchronized和Lock锁的区别？" class="headerlink" title="问题16：synchronized和Lock锁的区别？"></a>问题16：synchronized和Lock锁的区别？</h3><ul><li><h3 id="Synchronized"><a href="#Synchronized" class="headerlink" title="Synchronized"></a>Synchronized</h3><blockquote><p>可重入锁，不可中断【阻塞队列中排队是不可中断的】，非公平的锁</p></blockquote><p><code>每个对象的头部蕴含一把锁，每个对象都关联操作系统层面的一个监视器monitor（在mark word中存在指向monitor的指针）；当一个线程想要进入代码块时，想要去看一下加锁对象的头部信息 - 即尝试获取锁，于是需要一个提供查看锁状态的API；当线程拿到锁进入代码块时，需要改变锁的状态，于是需要一个改变状态的API；此时别的线程想要执行代码块，只能阻塞直到锁释放，于是需要一个阻塞队列用来管理阻塞的线程，并且在锁释放时能唤醒阻塞队列，所有线程管理的操作都是围绕一个锁，一个对象的，所以就抽象出一个概念 - 锁的监视器 -- 监视器本身是操作系统层面的逻辑实现，负责管理线程。</code></p><p>Synchronized底层通过对象Monitor实现，在进入 &#x2F; 退出代码块 &#x2F; 方法时会触发钩子【本质就是由monitor来改变对象头中的状态信息】，在字节码层面中体现为 monitorenter和monitorexit</p><ul><li><p>monitorenter时表示尝试占用对象的锁资源，主要作用就是通过进入数来标识当前有无被占用，如果被占用了需要操作此线程让其阻塞，没有被占用则进入，进入数+1；</p></li><li><p>monitorexit：对于已经进入锁的线程而言，退出则把用于标识的进入数-1；</p></li></ul><h5 id="可重入锁："><a href="#可重入锁：" class="headerlink" title="可重入锁："></a>可重入锁：</h5><p>指同一个线程可以多次获得同一个锁，而不会发生死锁</p><p>意味着线程在持有锁的情况下，可以再次请求并成功获取相同的锁，而不会被阻塞</p><blockquote><p>实现方案：获取计数器</p></blockquote><p>计数器维护的数就是当前线程递归加锁的层数，monitorexit一次再减一；</p><ul><li><p>为什么要递归加锁？</p><ol><li><p>防止死锁：方法上的synchronized都是用一个对象加锁，一不注意的调用就会导致递归加锁，如果synchronized是不可重入的那么就死锁</p></li><li><p>正常的代码应该不会写成递归加锁，因为外面的锁已经把其他线程阻塞住了里面的锁只会减慢性能；</p><p>所以使用场景就是避免死锁</p></li></ol></li></ul><h5 id="不可中断锁："><a href="#不可中断锁：" class="headerlink" title="不可中断锁："></a>不可中断锁：</h5><p>当其余线程再等待的情况下，有没有一种方法能让阻塞的线程更加灵活，避免被阻塞就一直被阻塞</p><ul><li><p>思路1：</p><p>不要轻易进入阻塞队列 – 通过探测锁有没有被获取来执行要不要获取</p><p>Lock对象的tryLock()，标识尝试获取锁，获取成功返回true，获取失败返回false</p></li><li><p>思路2：</p><p>线程进入阻塞队列，但可以尝试被中断</p><p>Lock中 lockInterruptibly()方法表示可被中断的加锁，中断方式就是Lock中 interrupt()；</p><p>调用interrupt后，会在阻塞的位置【调用lockInterruptibly的位置】抛异常，所以捕获这个异常的位置就是让这个线程执行回调的地方；</p></li></ul></li><li><h3 id="Lock锁"><a href="#Lock锁" class="headerlink" title="Lock锁"></a>Lock锁</h3><p>Lock的使用场景是声明一个Lock对象，显式声明加锁给释放锁</p><p>特点：</p><ol><li><p>可重入</p></li><li><p>互斥</p></li><li><p>可选是否公平</p><p>公平锁：等待时间较长的线程更容易获取锁</p><p>非公平锁：synchronized是阻塞的线程来争夺锁</p></li><li><p>可中断性</p></li></ol></li></ul><h3 id="问题17：TCP-IP网络模型有哪几层："><a href="#问题17：TCP-IP网络模型有哪几层：" class="headerlink" title="问题17：TCP&#x2F;IP网络模型有哪几层："></a>问题17：TCP&#x2F;IP网络模型有哪几层：</h3><ul><li><p>应用层</p></li><li><p>传输层</p></li><li><p>网络层</p></li><li><p>网络接口层</p></li></ul><blockquote><p>每层所作的行为都是在下一层基础上功能的丰富，可以由网络的简单到复杂串联每一层的工作</p></blockquote><ol><li><p>最简单的网络 – 两个设备通过物理装置链接（物理层）</p></li><li><p>局域网（数据链路层，解决标识主机身份，共用线路…）</p></li><li><p>广域网（网络层，通过IP协议找到目标网络）</p></li><li><p>完成设备之间的通信，需要考虑应用之间的通信（传输层，基于TCP&#x2F;UDP实现应用级别的数据传输）</p></li><li><p>用户接触的通信（应用层）</p></li></ol><h3 id="问题17：键入网址到网页响应，期间发生了什么"><a href="#问题17：键入网址到网页响应，期间发生了什么" class="headerlink" title="问题17：键入网址到网页响应，期间发生了什么"></a>问题17：键入网址到网页响应，期间发生了什么</h3><blockquote><p>在浏览器中查找，第一步应该都是先排队。</p><p>即Timing中的Queueing：由于浏览器线程数有限，只能按照文件顺序依次查询</p><p><img src="https://img-blog.csdnimg.cn/20190625115923543.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L2xoel8zMzM=,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p></blockquote><ol><li><p>浏览器第一步：解析URL</p><p>解析出：协议 &#x2F; 域名 &#x2F; 文件名</p></li><li><p>解析之后，浏览器确认了web服务器和想要的文件名，根据此信息生成http请求【HTTP协议规定的报文】（开发者工具中看到的Request报文）</p></li><li><p>查询对应web服务器的IP地址 - DNS服务器</p><p><img src="https://cdn.xiaolincoding.com/gh/xiaolincoder/ImageHost/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/%E9%94%AE%E5%85%A5%E7%BD%91%E5%9D%80%E8%BF%87%E7%A8%8B/6.jpg" alt="域名解析的工作流程"></p><p>其中每个节点都有缓存，包括浏览器缓存IP，操作系统缓存IP，本地host文件可配置，再有各个DNS服务器的缓存</p></li><li><p>委托操作系统将消息发送给web服务器</p><blockquote><p>电脑本身完成从应用层到链路层的一切工作，其中数据由应用程序通过socket交给操作系统就是由应用层到传输层的委托，同时也是用户态到内核态的切换</p></blockquote><p>操作系统传输层和网络层维护TCP，UDP，IP协议栈；上层调用socket库就是间接调用协议栈工作</p><p>以TCP为例：</p><ol><li><p>三次握手</p><p><img src="https://cdn.xiaolincoding.com/gh/xiaolincoder/ImageHost4/%E7%BD%91%E7%BB%9C/TCP%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B.drawio.png" alt="TCP 三次握手"></p><p>两端都要保证自身 能发，能收</p><h4 id="过程："><a href="#过程：" class="headerlink" title="过程："></a>过程：</h4><ol><li><p>最开始客户端和服务器都处于closed状态</p></li><li><p>服务器启动，监听某一端口，出于listen状态</p></li><li><p>客户端发起SYN（初始化序列号），之后处于SYN-SEND状态</p></li><li><p>服务器收到SYN，返回报文同时设置SYN + ACK，syn表示让对方也知道自己初始化的序列号，ack表示填入应答号（将对方上次的SYN序列号+1），之后处syn-rcvd状态</p></li><li><p>客户端收到syn-ack后，返回ack，之后处于established状态</p></li><li><p>服务器收到ack后，也处于established状态</p></li></ol><h4 id="风险："><a href="#风险：" class="headerlink" title="风险："></a>风险：</h4><blockquote><p>类似拜占庭问题，任何一个单方向的请求其实都无法完全确认对方收到。</p></blockquote><p>考虑为什么要有这一次握手，等价于考虑没有这次握手的风险</p><p>理论上有无数次握手都不够，但实际上，三次握手已经是可靠的工程解</p><blockquote><p>因为在实际中，就算最后一下ACK没被成功接收，客户端也会发数据，服务器收到数据后自然跟新成established状态</p><p>并且还有超时重传机制，解决了就算客户端没数据可发，也不会干耗着</p></blockquote><ul><li><p>同时，三次握手其他能力：</p><ol><li><p>同步序列号</p><blockquote><p>序列号是每个TCP报文报头上携带的关键信息，作用有：</p><ul><li>接收方可以去除重复的数据；</li><li>接收方可以根据数据包的序列号按序接收；</li><li>可以标识发送出去的数据包中， 哪些是已经被对方收到的（通过 ACK 报文中的序列号知道）；</li></ul></blockquote><p>同步序列号的过程就是双方能够让对方接收到自身序列化，并返回一个序列号+1的应答</p><blockquote><p>服务器方的序列号是什么？</p><p>TCP原生是双工，本来就可以由服务器向客户端发报文，就需要确认序列号</p></blockquote></li><li><p>避免历史连接</p><blockquote><p>总体而言就算为了防止旧的重复连接初始化造成混乱</p><blockquote><p>在只有两次握手的场景下，服务器第一次握手后，就进入established状态，但是这样并不稳妥</p><ul><li><p>场景1：历史连接</p><p>如果第一次握手握了两次，前次一由于网络问题阻塞了；所以会为两个请求都分别开启established，资源浪费</p></li></ul></blockquote></blockquote></li><li><p>避免资源浪费</p></li></ol></li></ul></li><li><p>分割数据成数据包，每个数据包都会加上TCP头信息，生成TCP报文</p><blockquote><p>通过头信息，可以参考本协议的功能 &#x2F; 本层负责的任务</p><p>基本内容：</p><ul><li><p>当前端口，目标端口</p></li><li><p>数据包的序号</p></li><li><p>应答号（在双方通信包中相互确认，用于包丢失）</p><p><code>指下一次「期望」收到的数据的序列号，发送端收到这个确认应答以后可以认为在这个序号以前的数据都已经被正常接收。</code></p></li><li><p>状态位（SYN标识发起连接，ACK标识回复，RST表示重新连接，FIN表示结束链接），这是TCP和UDP不同的一点，TCP的包可以各司其职，而UDP面向无连接</p></li><li><p>窗口大小：负责流量控制（通信双方声明自己能处理的吞吐量）</p></li><li><p>拥塞控制：控制自己发送速度</p></li></ul></blockquote></li><li><p>交给IP协议栈发送</p></li></ol><p>在IP协议中中生成IP报文</p><blockquote><p>IP报文：</p><p>源地址IP，目标地址IP</p><p>为了能够被对方解析恢复成上层的样子，报头中就会携带上层类型参数：例如是TCP数据包，使用HTTP协议。</p></blockquote><p>在数据链路层添加MAC头信息，生成MAC报文</p></li></ol><blockquote><p>mac头信息就是发送方和接收方的mac地址</p><p>即所用协议 IP &#x2F; ARP</p></blockquote><ol start="5"><li><p>正式离开主机，从网卡转化成电信号</p></li><li><p>通过交换机离开局域网</p></li><li><p>通过路由器找到目标子网</p></li><li><p>在服务器层层扒皮，每层完成对应的校验工作</p></li><li><p>信息完整传递到服务器，客户端和服务器完成4次挥手</p></li></ol><hr><ol start="5"><li><p>TCP协议是在传输层明确的通信方式，在底层还有更细节的工作</p><blockquote><p>具体而言：</p><ol><li><p>数据从主机出发，携带目标IP地址</p></li><li><p>线判断此IP是否在一个子网内（通过子网掩码的与运算），如果在，则查询交换机的IP-MAC对应表，直接发给对应主机；如果不在则交给路由器（默认网关）</p></li><li><p>路由器收到的IP数据包中有目标地址IP，根据路由算法算出下一个路由器</p><p>直接 &#x2F; 间接到达目标主机所在子网和公网连接的路由器上</p></li><li><p>目标路由器查询自身以太网里交换机的IP-MAC对应表，将目标IP地址转化成目标MAC地址，发给主机</p></li></ol></blockquote></li></ol><h3 id="问题18：HTTP是什么"><a href="#问题18：HTTP是什么" class="headerlink" title="问题18：HTTP是什么"></a>问题18：HTTP是什么</h3><ul><li><p>超文本传输协议</p><blockquote><p>什么是超文本？</p><p>用文本表示，但超越不同文字的文本，最常见的HTML就是用xml表示图片、视频等内容，而HTTP在头部规定Content-Type，设置body的MIME类型；有text&#x2F;html，application&#x2F;json，iamge&#x2F;jpeg，video&#x2F;mp4…</p></blockquote></li><li><p>常见状态码：</p><p><img src="https://cdn.xiaolincoding.com/gh/xiaolincoder/ImageHost/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C/HTTP/6-%E4%BA%94%E5%A4%A7%E7%B1%BBHTTP%E7%8A%B6%E6%80%81%E7%A0%81.png" alt="五大类 HTTP 状态码"></p><ul><li><p>4XX</p><p>400 Bad Request ：指客户端请求的报文有误，囊括很多错误，例如找不到方法，参数对不上，等等，比较笼统</p><p>403 Forbidden：服务器禁止访问某资源</p><p>404 Not Found：表示资源在改服务器上找不到</p></li></ul></li><li><p>特殊header：</p><ol><li><p>Connection：表示手否是长连接</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-03-14-11-02-07-image.png"></p></li><li><p>upgrade：升级协议…</p></li></ol></li></ul><h3 id="问题19：GET和POST是幂等的吗？"><a href="#问题19：GET和POST是幂等的吗？" class="headerlink" title="问题19：GET和POST是幂等的吗？"></a>问题19：GET和POST是幂等的吗？</h3><ol><li><p>GET方法类似 只读，通常只是为了获取服务器上的资源。所以多次get的结果基本一致，并且据此浏览器可以设置get的缓存来加快访问。破除浏览器缓存 - 加一个随机数参数…</p><blockquote><p>这只是RFC规范的定义，实际上GET也有做新增 &#x2F; 删除的</p></blockquote></li><li><p>POST方法通常表示 新增或者提交数据，会修改服务器上的资源，所以不是幂等的，浏览器一般也不会缓存</p></li></ol><h3 id="问题20：HTTP与HTTPS"><a href="#问题20：HTTP与HTTPS" class="headerlink" title="问题20：HTTP与HTTPS"></a>问题20：HTTP与HTTPS</h3><ol><li><p>区别：HTTP是明文传输，存在安全风险。HTTPS在TCP和HTTP层之间加入SSL&#x2F;TLS安全协议，使得报文能够加密传输。</p><p>HTTP只需要三次握手后就能通信，而HTTPS在三次握手后还需要有SSL&#x2F;TLS握手过程，确保能够建立安全的加密通道（过程中：协商加密算法，交换密钥…）</p><p>默认端口不一样：HTTP-80，HTTPS-443</p></li></ol><h3 id="问题21：HTTP-1-1-HTTP-2-HTTP-3的演变"><a href="#问题21：HTTP-1-1-HTTP-2-HTTP-3的演变" class="headerlink" title="问题21：HTTP&#x2F;1.1 HTTP&#x2F;2 HTTP&#x2F;3的演变"></a>问题21：HTTP&#x2F;1.1 HTTP&#x2F;2 HTTP&#x2F;3的演变</h3><ul><li><p>HTTP&#x2F;2对HTTP1.1的优化</p><ol><li><p>全面二进制：</p><p>头信息和数据体都是二进制（按照实际意义编码），计算机收到后不需要再把明文转化成二进制，而是直接解析，增加了传输效率；同时更加节约空间</p><p>原本HTTP&#x2F;1.1 在传输过程中，文本数据由ASCII这些字符集逐个字符编码，转化后的0101失去实际意义，只有解码后才能看到</p></li><li><p>并发传输 Stream</p><p>解决HTTP1.1的对头阻塞问题</p><blockquote><p>只解决了HTTP层面的对头阻塞，但没解决TCP层面的对头阻塞</p></blockquote></li><li><p>服务器推送 </p><blockquote><p>其中一种方案：webSocket</p></blockquote></li><li><p>头部压缩（当多个请求头部相似，协议会消除重复部分 - 技术：在客户端和服务器同时维护一张头信息表，所有字段在表中由索引号存储）</p></li></ol></li><li><p>HTTP3的优化</p><blockquote><p>把HTTP下层的TCP协议改成UDP</p><p>通过QUIC协议实现可靠传输</p></blockquote></li></ul><h3 id="问题22：对头阻塞"><a href="#问题22：对头阻塞" class="headerlink" title="问题22：对头阻塞"></a>问题22：对头阻塞</h3><blockquote><p>简单定义：当单个慢对象组织其他（后续）对象的前进</p><p>Head-of-line blocking</p></blockquote><ul><li><p>在HTTP1.1中，单条TCP连接上：在切换发送新资源之前，必须完整地传输完当前资源并得到响应：具体原因和纯文本传输有关，纯文本之间无法使用分隔符，导致不能将大文件切片后把小文件插入</p><p>所以一个浏览器页面会并行6个TCP连接，将请求分散，减少对头阻塞。</p></li><li><p>在HTTP2中，通过在每部分资源前添加”帧“，区分了每块所属。</p><p>HTTP2在每个块前添加数据帧，包括两个元数据：所属资源的流ID</p><p>以及块的大小</p><p>通过帧标识每个块的归属，允许在一个连接上正确复用多个资源</p><p>每个资源的传输抽象成Stream流，由Stream ID标识</p></li><li><blockquote><p>HTTP2解决了应用层（HTTP）的对头阻塞，但没能解决TCP层的对头阻塞</p></blockquote><p>TCP层的对头阻塞：</p><p>原因出在：TCP传输和HTTP2的传输是独立的。原本在HTTP2中将不同资源按照不同的流Stream传给TCP，TCP接收每个资源块后按流拼成相应的资源；其中如果一个资源块卡死了却不影响别的资源块进行传输；但是TCP传输也并非理想中的顺利</p><p>TCP也需要将资源分成多个块，每块顺次传输，但此时的分块和刚才HTTP的分块就没有关系了，此时的分块是随意的，并不是按照不同资源（js,css,html…）分的，这就导致TCP层面上，有些包由于网络问题没送到，其他后续的包只能等待它重发后才能一并交付给应用</p><blockquote><p>总之，TCP 不知道 HTTP&#x2F;2 的独立流（streams）这一事实意味着 <strong>TCP 层队头阻塞（由于丢失或延迟的数据包）也最终导致 HTTP 队头阻塞！</strong></p></blockquote></li><li><p>尝试解决TCP层的对头阻塞问题</p><blockquote><p>要让TCP层知道某个包属于某个资源，而其他不属于统一资源的包可以直接交付给上层，而不用等</p></blockquote><p>解决方案之一：QUIC</p><blockquote><p>QUIC被看作是TCP 2.0，它包括TCP的所有特性（可靠性，拥塞控制，流量控制，排序）</p></blockquote><p>QUIC将Stream ID的概念下移到传输层（原本在HTTP的应用层）</p></li></ul><h3 id="问题23：HTTP和RPC"><a href="#问题23：HTTP和RPC" class="headerlink" title="问题23：HTTP和RPC"></a>问题23：HTTP和RPC</h3><ol><li><p>二者都是基于TCP，原装的TCP是基于字节流的，也就是说单纯的通过0101二进制数据传输，收这些数据的一方由于不知道边界在哪所以很难处理成为有效数据</p><blockquote><p>粘包</p></blockquote><p>所以TCP纯裸状态下无法直接使用，需要规定更多规则 - 协议</p><p>比如说：在头部确认数据包的长度，编码集…</p><p>衍生出两类协议 HTTP 和 RPC</p></li><li><p>有HTTP，为什么还要RPC？</p><p>HTTP适合交互双方是异构的，共同遵守一套协议，而RPC可以实现的更加简单，定制化程序高（传输效率更高-因为http的header里无效数据还挺多），适合交互双方同构（CS架构）</p></li><li><p>使用的大致区别</p><ol><li>HTTP通过域名服务器找到目标地址，RPC往往工作在微服务模式下，通过注册中心找到目标地址</li></ol></li></ol><h3 id="问题24：HTTP与WebSocket"><a href="#问题24：HTTP与WebSocket" class="headerlink" title="问题24：HTTP与WebSocket"></a>问题24：HTTP与WebSocket</h3><ul><li><p>场景痛点：</p><blockquote><p><strong>怎么样才能在用户不做任何操作的情况下，网页能收到消息并发生变更。</strong></p></blockquote></li><li><p>解决方案：</p><ol><li><p>轮询</p></li><li><p>长轮询</p><blockquote><p>以上两种方案适用于可以接收延迟的场景，比如扫码登陆…但是像打游戏就不行</p></blockquote></li><li><p>webSocket</p><p>使用场景：</p><blockquote><p>适用于 需要服务器和客户端频繁交互的场景</p><p>例如：小程序游戏，网页聊天室，飞书…</p></blockquote></li></ol></li></ul><h3 id="问题25：TCP机制"><a href="#问题25：TCP机制" class="headerlink" title="问题25：TCP机制"></a>问题25：TCP机制</h3><ul><li><p>什么是TCP：</p><p>面向连接，一对一建立连接后才能进行通信</p><p>可靠，无论网络链路中出现怎样的变化，都能保证数据包到达对岸</p><p>基于字节流，二进制数据被操作系统分成多个TCP报文</p></li><li><p>确定一个TCP连接的要素</p><ol><li><p>源地址</p></li><li><p>源端口</p></li><li><p>目标地址</p></li><li><p>目标端口</p></li></ol></li><li><p>确定一个TCP报文则需要更多：</p><ol><li><p>源端口</p></li><li><p>目标端口</p></li><li><p>序号：用于解决乱序问题</p></li><li><p>确认序号：目的是解决丢包问题</p></li><li><p>状态位：由于不同报文共用一个连接，所以有的报文在全局而言作用不同，总体而言是为了维护连接</p><p>SYN：发起连接</p><p>ACK：回复</p><p>RST：重新连接</p><p>FIN：结束连接</p></li></ol></li><li><h3 id="TCP如何实现可靠传输"><a href="#TCP如何实现可靠传输" class="headerlink" title="TCP如何实现可靠传输"></a>TCP如何实现可靠传输</h3><blockquote><p>明确解决的问题：</p><ul><li>数据的破坏，丢包，重复，乱序…</li></ul><p>机制：</p><ul><li>序列号、确认应答、重发控制、连接管理、窗口控制等</li></ul></blockquote></li></ul><h3 id="问题26：TCP与UDP的区别"><a href="#问题26：TCP与UDP的区别" class="headerlink" title="问题26：TCP与UDP的区别"></a>问题26：TCP与UDP的区别</h3><ol><li><p>TCP提供复杂的控制机制，UDP协议则比较简单</p><p>头部格式如下：</p><p><img src="https://cdn.xiaolincoding.com//mysql/other/format,png-20230309230534096.png" alt="TCP 头格式"></p></li></ol><p><img src="https://cdn.xiaolincoding.com//mysql/other/format,png-20230309230439961.png" alt="UDP 头部格式"></p><p>比较而言，TCP为了实现可靠传输多个几个标记：序列号用于排序，应答号用于防止丢包，控制位用来标识每个TCP数据包的作用，窗口大小用于控制流量…而UDP就只有双方地址，校验和，包长度这种必要信息</p><ol><li><p>连接：TCP面向连接（传输前需要先建立连接），UDP无连接，随包发出即可传输数据</p></li><li><p>一对一，一对多：TCP一对一传输，一条连接上只有两个端点；UDP支持一对一，一对多，多对多的场景</p><blockquote><p>一对多？聊天室？</p><p>UDP三种通信模式：</p><ol><li><p>单播</p></li><li><p>广播：将消息分发到整个局域网内所有主机</p></li><li><p>组播（多播）：将网络上的主机进行逻辑分组。将信息传递给一个小组内的设备</p></li></ol></blockquote></li><li><p>可靠性：TCP可靠交付，保证数据无差错、不丢失、不重复，不乱序；UDP无法保证但也尽力而为</p></li><li><p>拥塞控制，流量控制：</p><p>TCP之所以要有拥塞控制流量控制，是为了保证自己发的包别在网络中丢失，可能白发，可能再次同步需要时间；UDP不受负反馈调节的影响，网络拥堵也照样发</p></li><li><p>首部开销</p></li></ol><ul><li><p>使用场景：</p><ol><li><p>TCP</p><blockquote><p>由于可靠性交付</p></blockquote><p>HTTP&#x2F;HTTPS</p><p>FTP文件传输</p><p>SMTP电子邮件</p><p>TELNET远程终端接入</p></li><li><p>UDP （由于协议本身不可靠，尝试可靠的操作只能由上层应用程序来做）</p><blockquote><p>高效</p></blockquote><p>音视频，多媒体</p><p>广播，聊天室</p></li></ol></li></ul><h3 id="问题27：如何基于UDP协议实现可靠重传？"><a href="#问题27：如何基于UDP协议实现可靠重传？" class="headerlink" title="问题27：如何基于UDP协议实现可靠重传？"></a>问题27：如何基于UDP协议实现可靠重传？</h3><blockquote><p>思路，按照TCP的保证方案，在UDP中做到类似效果</p><ul><li>不改变UDP的具体形式（报头），只能在上层完成功能强化【在UDP的保温内部再分出细节】</li></ul></blockquote><ol><li><p>TCP的缺陷：</p><ol><li><p>TCP建立连接过程有延迟</p></li><li><p>TCP存在对头阻塞问题</p></li><li><p>网络迁移需要重新建立TCP连接</p></li></ol></li><li><p>市面上基于UDP实现的可靠传输方案：QUIC（HTTP&#x2F;3）</p></li></ol><h4 id="QUIC："><a href="#QUIC：" class="headerlink" title="QUIC："></a>QUIC：</h4><p><img src="https://cdn.xiaolincoding.com//mysql/other/http3-over-quic-protocol-works.png"></p><blockquote><p>在传统HTTP报文上，添加了许多QUIC报头，最后再用UDP报头封装所以</p></blockquote><p>points：</p><ol><li><p>解决对头阻塞问题：</p><blockquote><p>传统TCP需要按照序列号顺序确认，一个包由于网络时延导致其他包无法确认</p><p>QUIC将确认的序列号和排序的序列号分开 - 于是可以乱序确认；</p><p>排序的序列号Stream ID</p></blockquote></li></ol><h3 id="问题28：Java前沿技术"><a href="#问题28：Java前沿技术" class="headerlink" title="问题28：Java前沿技术"></a>问题28：Java前沿技术</h3><ol><li><p>ZGC &amp; Shenandoah</p><blockquote><p>回来吧我的ZGC</p><blockquote><p>设计目标：适用于大内存、低延迟的服务</p></blockquote><p>传统G1由于在复制阶段需要STW导致响应时延较长，ZGC尝试在全部阶段几乎都并发执行；</p><p>最后目标是：RT &lt; 10ms</p><p>原理：将传统整个转移赋值STW的阶段分成初始转移+并发转移</p><p>【核心在于能把STW的过程分出必要的STW和基于结果可以并发执行的阶段】，<strong>G1无法分离，因为转移过程中对象地址无法准确定位。</strong></p><p>关键技术：</p><blockquote><p>由于在对象复制阶段想要并发执行，用户线程就要准确定位到目标对象的位置（即使对象发生转移）</p><blockquote><p>方案比Shenandoah更加复杂精妙</p></blockquote><p>读屏障：把线程获取到的不对的指针更新到对的地址上</p><p>着色指针：用来判断对象是否移动</p></blockquote><ol><li><p>读屏障</p><blockquote><p>读屏障 Load Barrier：在读的指令前后，插入某个指令来实现具体需求【AOP】</p><p><code>传统G1面临的问题是：当对象转移时，如果线程尝试写对象可能写在旧的位置，ZGC需要解决任何写操作的目标都确保都是新对象</code></p><p>读屏障就是要插入这样一段代码</p><p>在读到对象时，先判断当前对象是不是新的，如果不是新的则将引用重定向到新的位置</p><p>技术难点：</p><ol><li><p>如何判断是新是旧？</p><p>在着色指针内的四个bit位标识当前对象的状态</p></li><li><p>新的位置在哪？</p><p>维护公共的转移映射表，由此表从旧对象找到新对象</p></li></ol></blockquote></li><li><p>着色指针：</p><p>由于原本84位计算机上表示内存地址的8字节有点大，Windows操作系统也只用了44位</p><p>ZGC将中间4位表示四种状态，由此在不占用额外空间的状态下表明这个指针指向的对象的状态</p></li><li><p>动态Region：大小由2MB，32MB以及更大</p></li><li><p>JDK21之后，ZGC设计了分代回收机制</p></li></ol><p>时延短，代价是吞吐量下降【没有纯粹使用线程来垃圾回收，而是并发的使用少量线程做回收，势必吞吐量不足】</p></blockquote><hr><blockquote><p>回来吧我的Shenandoah</p><blockquote><p>不太追求时延，不要求pause &lt; 10ms；换来吞吐量</p></blockquote><p>比G1优化的点：</p><ol><li><p>并发的整理算法</p></li><li><p>默认不使用分代收集</p></li><li><p>摒弃G1中的记忆集，而用一个全局的数据结构-连接矩阵-来记录跨Region引用关系</p></li><li><p>解决并发回收时对象位置的技术</p><ul><li><p>Brooks Pointer【转发指针】</p><blockquote><p>在每个对象头部维护重定向信息</p><blockquote><p>复杂性要求更高——用户线程如果要修改对象，只能在新地址修改，即要求垃圾回收线程提前把对象复制好并且更新转发地址</p><ul><li><p>阻塞用户线程 </p></li><li><p>CAS自旋尝试 &#x2F; 等待复制完成</p></li></ul></blockquote></blockquote></li><li><p>读屏障 –&gt; 引用访问屏障</p><blockquote><p>读屏障：相当于读取对象操作的AOP，用于在具体读的指令之前将对对象的指针指向正确位置（具体操作就是看转发指针是否指向自身，指向其他位置则表示新对象在其他地方，要在接下来的访问目标前准确替换成新对象 —— 读前屏障）</p></blockquote></li></ul></li></ol></blockquote></li><li><p>GraalVM</p><blockquote><p>一款高性能JDK，官方标语：Build faster，smaller，leaner applications</p><p>特点：</p><ol><li><p>更低的CPU、内存使用率</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-15-19-15-01-image.png"></p><p>早期使用JIT预热阶段CPU消耗较高，包括没有热点代码加速的消耗和编译热点代码的消耗</p></li><li><p>更快的启动速度</p><p>GraalVM在启动 + 处理第一个请求的时延更快</p></li><li><p>通过Truffle框架运行JS，Python，Ruby等语言</p></li></ol></blockquote></li><li><p>模块化</p><blockquote><p>Java 9开始，引入模块系统，Java开始微服务化</p></blockquote><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-14-16-34-58-image.png"></p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-14-16-35-14-image.png"></p><blockquote><p>传统jar包下就是编译后的.class文件，模块化后——模块就是在jar之上再抽象一层，用来管理原先复杂的依赖网</p><p>模块 &#x3D; jar + module-info.class</p><p>其中包含：</p><ul><li>模块名称</li><li>依赖哪些模块</li><li>导出模块内的哪些包（允许直接 <code>import</code> 使用）</li><li>开放模块内的哪些包（允许通过 Java 反射访问）</li><li>提供哪些服务</li><li>依赖哪些服务</li></ul></blockquote><p>看似好像什么也没改，实则有四点好处</p><ol><li><p>为依赖管理而制定的规则：</p><blockquote><p>只要module-info.class表示正确，就可以通过此描述文件计算依赖关系，一旦发现循环依赖，启动就可以报错了</p><p>早期解决方案如果没有maven的话，可能需要在编译期间沿着jar包进行DFS才能发现循环</p><p>maven有pom.xml，相当于对管理的jar包的索引，通过此建立分析的依赖树</p></blockquote></li><li><p>精简JRE：</p><blockquote><p>模块化后JDK被分成94个模块，通过jlink随意组合生成自定义的KRE可以缩小JRE大小</p><p>通过 –add-modules可以将依赖的模块添加到运行时镜像中</p></blockquote></li><li><p>更精准的权限管理</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-14-16-59-46-image.png"></p><p>public粒度太大，如果只想实现对部分modules是public就无力了</p><p>将访问权限管理细分，更有助于实现封装逻辑</p></li><li><p>模块化后，各个模块独立开发，效率较高</p></li></ol><blockquote><p>Java为module-info.java设计了专门的语法</p><p>open &#x2F; export &#x2F; requires …</p></blockquote></li><li><p>java var</p><blockquote><p>从Java10开始，可以从变量的初始值推导变量的类型，于是呈现出Java可以为弱类型语言</p></blockquote></li></ol><h3 id="问题29：包装类型的缓存机制"><a href="#问题29：包装类型的缓存机制" class="headerlink" title="问题29：包装类型的缓存机制"></a>问题29：包装类型的缓存机制</h3><blockquote><p>Java基本数据类型的包装类型都用到缓存机制来提升性能</p><p>Integer：-128到127</p><p>当基本数据类型想要包装时，会优先检查在不在缓存范围内，不在则执行new操作</p><blockquote><p>缓存的范围区间是在性能和资源之间的权衡</p></blockquote></blockquote><h3 id="问题30：如何解决精度丢失问题-超过long类型的数据"><a href="#问题30：如何解决精度丢失问题-超过long类型的数据" class="headerlink" title="问题30：如何解决精度丢失问题 &amp; 超过long类型的数据"></a>问题30：如何解决精度丢失问题 &amp; 超过long类型的数据</h3><ul><li><p>使用BigDecimal实现对浮点数的运算</p></li><li><p>使用BigInteger实现对超过64为整型的表示</p></li></ul><h3 id="问题31：浅拷贝-深拷贝"><a href="#问题31：浅拷贝-深拷贝" class="headerlink" title="问题31：浅拷贝 &amp; 深拷贝"></a>问题31：浅拷贝 &amp; 深拷贝</h3><ul><li><p>浅拷贝：只拷贝最外层，内层还是同一个数据</p><blockquote><p>传统clone()就只会创建一个和this对等的对象，而this内其他引用任然保留</p><p>实现Cloneable后重写clone可以手动实现深拷贝</p></blockquote></li><li><p>深拷贝：深浅全部是新的</p></li></ul><h3 id="问题32：String不可变："><a href="#问题32：String不可变：" class="headerlink" title="问题32：String不可变："></a>问题32：String不可变：</h3><ul><li><p>技术上如何实现不可变：</p><ol><li><p>String本体是char数组 &#x2F; byte数组，这个数组被final修饰导致引用不可变</p></li><li><p>整个String类没有提供public的修改数组内容的方法，所以数组无法直接由String对象改变</p></li><li><p>String类本体被final修饰，无法有子类继承数组，也就不会有新方法来修改</p></li></ol></li><li><p>为什么这么设计？</p><ul><li><p>场景1：由String做HashMap的key时，需求保证不可变才能维护哈希表正常运转</p></li><li><p>场景2：由于字符串常量池</p><p>字符串在人眼里的含义更多是其字面量，某些字符串就应该不变，而不是具有变量的属性</p><blockquote><p>常量池内维护许多String对象，每个对象都被许多地方引用；如何一个线程改变了变量的值，其他引用的地方也会悄悄变化</p></blockquote></li></ul></li></ul><h3 id="问题33：说说集合框架"><a href="#问题33：说说集合框架" class="headerlink" title="问题33：说说集合框架"></a>问题33：说说集合框架</h3><ul><li><p>主要有两大接口派生而来</p><ol><li><p>Collection：用来存放单一元素</p><blockquote><p>有三个子接口：List，Set，Queue</p></blockquote><p>Colloction接口定义了单个元素容器的普遍规范，也就是在List，Set，Queue中都应该具有的API</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-16-10-35-17-image.png"></p><ul><li><p>List：</p><blockquote><p>特殊的API有：排序，静态of方法用于构建List…</p><p>实现类：</p><p>ArrayList：用Object[]维护</p><p>Vector：Object[]</p><p>LinkedList：双向链表</p></blockquote></li><li><p>Set:</p><blockquote><p>特殊API：of，构造函数</p><p>实现类：</p><p>HashSet：底层采用HashMap</p><p>LinkedHashSet：底层采用LinkedHashMap</p><p>TreeSet：底层采用红黑树</p></blockquote></li><li><p>Queue：</p><blockquote><p>特殊API：offer，poll，peek…</p><p>实现类：</p><blockquote><p>默认实现类是LinkedList，LinkedList实现Deque接口（双向链表 天生适合 双端队列）</p><p>PriorityQueue：小根堆实现优先队列</p><p>DelayQueue：延迟队列</p><p>ArrayDeque：可扩容动态双端队列</p><h4 id="BlockingQueue：阻塞队列"><a href="#BlockingQueue：阻塞队列" class="headerlink" title="BlockingQueue：阻塞队列"></a>BlockingQueue：阻塞队列</h4><p>实现类：</p><blockquote><p>考虑的点：</p><ul><li><p>Queue长度是否有限</p></li><li><p>生产者和消费者共享一把锁 &#x2F; 锁分离</p></li></ul></blockquote><ul><li><p>ArrayBlockingQueue：使用数组实现的有界阻塞队列</p><blockquote><p>实现方案：</p><ol><li><p>内部维护定长的数组</p></li><li><p>通过使用ReentrantLock对都系欸对象进行同步</p></li><li><p>通过Condition实现线程间的等待和唤醒操作</p><blockquote><p>维护两个Condition对象，分别用来阻塞 &amp; 唤醒 两组线程</p><ul><li>队列满时，生产者线程等待，一旦被消费者消费一个则通过此Condition唤醒一个生产者</li></ul></blockquote></li></ol></blockquote></li><li><p>LinkedBlockingQueue：使用单向链表实现的阻塞队列</p></li><li><p>PriorityBlockingQueue：支持优先级排序的阻塞队列，需要元素实现Comparable或者Queue传入Comparator</p></li><li><p>SynchronousQueue：同步队列</p></li><li><p>DelayQueue：延迟队列</p></li></ul></blockquote></blockquote></li></ul></li><li><p>Map：用来存放键值对</p><blockquote><p>实现类：</p><p>HashMap：</p><p>LinkedHashMap：继承自HashMap，底层仍然维护数组+链表，此外还有一条双线链表维护节点的插入顺序</p><p>Hashtable：</p><p>TreeMap：底层采用红黑树</p></blockquote></li></ol></li></ul><h3 id="问题34：Spring"><a href="#问题34：Spring" class="headerlink" title="问题34：Spring"></a>问题34：Spring</h3><ul><li><p>IOC：</p><blockquote><p>控制反转：原本对对象创建和管理的控制由开发者交给框架</p><blockquote><p>IOC容器类似一个工厂，当需要创建一个对象时，只需要根据配置文件 &#x2F; 注解 按需获取即可注入，而不需要考虑如何创建出来</p><p>IOC容器负责全局对象的管理，当我们注入一个service时，service内部依赖的各种对象全都由容器按需注入；假如没有容器自动管理，则service的构造函数中还要传入各种内部变量的具体值，dao &#x2F; mapper…</p><p>如果写死的话意味着耦合性很高，当想要替换某一个dao下不同daoImpl时需要改源代码，而不写死，开发者只需要将具体Impl交给IoC，即可完成注入</p></blockquote><blockquote><p>本质：由写死在代码的配置关系移动到配置文件 &#x2F; 编码阶段的注解中</p><blockquote><p>由于注解还是和代码耦合在一起的，虽然更简单，使用配置文件解耦程度更高</p></blockquote></blockquote><p>IoC容器实际上就是一个Map，也是一个bean Factory</p><blockquote><p>Bean factory：是所有Bean容器的根接口，定义了容器的基本方法</p></blockquote><p>IoC生命周期：</p><ol><li>创建容器</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">ApplicationContext</span> <span class="hljs-variable">context</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ClassPathXmlApplicationContext</span>(...)<br></code></pre></td></tr></table></figure><p>AllicationContext类就表示IoC容器，不同的实现类为了兼容不同的场景，由xml的，注解的..</p><ol start="2"><li><p>解析配置文件，操作Bean对象</p><p>操作过程【Refresh方法】</p><ol><li><p>解析配置文件Bean定义，将beanDefinition注册到BeanFactory中</p></li><li><p>钩子：PostProcessor：指在所有Bean注册完成后，初始化前后可以进行些认为操作</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> org.springframework.beans.BeansException;<br><span class="hljs-keyword">import</span> org.springframework.beans.factory.config.BeanPostProcessor;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MyBeanPostProcessor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanPostProcessor</span> &#123;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">postProcessBeforeInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;Before Initialization: &quot;</span> + beanName);<br>        <span class="hljs-keyword">return</span> bean;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> Object <span class="hljs-title function_">postProcessAfterInitialization</span><span class="hljs-params">(Object bean, String beanName)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        System.out.println(<span class="hljs-string">&quot;After Initialization: &quot;</span> + beanName);<br>        <span class="hljs-keyword">return</span> bean;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li><li><p>初始化所有singleton bean（lazy-init的除外）</p><p>初始化过程中，也有钩子允许加载</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">import</span> org.springframework.beans.factory.*;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MyBean</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">BeanNameAware</span>, BeanClassLoaderAware, BeanFactoryAware, InitializingBean &#123;<br><br>    <span class="hljs-keyword">private</span> String beanName;<br>    <span class="hljs-keyword">private</span> ClassLoader classLoader;<br>    <span class="hljs-keyword">private</span> BeanFactory beanFactory;<br><br>    <span class="hljs-comment">// 实现 BeanNameAware 接口的方法</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setBeanName</span><span class="hljs-params">(String name)</span> &#123;<br>        <span class="hljs-built_in">this</span>.beanName = name;<br>        System.out.println(<span class="hljs-string">&quot;Bean Name: &quot;</span> + name);<br>    &#125;<br><br>    <span class="hljs-comment">// 实现 BeanClassLoaderAware 接口的方法</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setBeanClassLoader</span><span class="hljs-params">(ClassLoader classLoader)</span> &#123;<br>        <span class="hljs-built_in">this</span>.classLoader = classLoader;<br>        System.out.println(<span class="hljs-string">&quot;Bean ClassLoader: &quot;</span> + classLoader);<br>    &#125;<br><br>    <span class="hljs-comment">// 实现 BeanFactoryAware 接口的方法</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">setBeanFactory</span><span class="hljs-params">(BeanFactory beanFactory)</span> <span class="hljs-keyword">throws</span> BeansException &#123;<br>        <span class="hljs-built_in">this</span>.beanFactory = beanFactory;<br>        System.out.println(<span class="hljs-string">&quot;Bean Factory: &quot;</span> + beanFactory);<br>    &#125;<br><br>    <span class="hljs-comment">// 实现 InitializingBean 接口的方法</span><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">afterPropertiesSet</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        System.out.println(<span class="hljs-string">&quot;InitializingBean&#x27;s afterPropertiesSet() method&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 自定义的初始化方法，对应配置文件中的 init-method 属性</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">customInitMethod</span><span class="hljs-params">()</span> &#123;<br>        System.out.println(<span class="hljs-string">&quot;Custom Init Method specified in configuration file&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure></li><li><p>广播事件：表示Context加载完成</p></li></ol></li><li><p>管理bean对象</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-type">MessageService</span> <span class="hljs-variable">messageService</span> <span class="hljs-operator">=</span> context.getBean(MessageService.class);<br></code></pre></td></tr></table></figure></blockquote></li><li><p>Bean</p><ol><li><p>Bean的作用域：</p><ul><li><p>Singleton：IoC容器中使用单例的bean</p></li><li><p>prototype：每次获取都会创建一个新的bean示例</p></li><li><p>request：每一次HTTP请求都会创建一个新bean</p></li><li><p>session</p></li><li><p>webSocket：每一次webSocket会话产生一个新bean</p></li></ul></li><li><p>Bean是线程安全的吗</p><ul><li><p>prototype作用域下是的，不存在资源竞争问题</p></li><li><p>singleton下未必，如果此对象类似只读（只负责提供方法：如serviceImpl）就是线程安全的，但如果有会被修改的成员变量就需要小心</p><blockquote><ol><li><p>运行弱一致性：可以将可变的成员变量定义成ThreadLocal中的内容</p></li><li><p>强一致性：加锁</p></li></ol></blockquote></li></ul></li><li><p>Bean的生命周期：</p><blockquote><p>处处定义钩子</p></blockquote><ol><li><p>创建bean实例，在xml中找到后通过反射创建</p></li><li><p>属性赋值 &#x2F; 依赖注入</p></li><li><p>bean初始化 执行Aware接口实现类</p></li><li><p>销毁bean，执行destroy实现方法的代码</p></li></ol><p><img src="https://oss.javaguide.cn/github/javaguide/system-design/framework/spring/spring-bean-lifestyle.png"></p></li></ol></li><li><p>AOP</p><blockquote><p>Aspect-Oriented Programming</p><p>将于业务无关的、却为业务模块共同服务的逻辑封装起来，减少代码重复，降低耦合，提高可读…</p><blockquote><p>事务管理、日志系统、权限校验…</p></blockquote><p>实现方式：动态代理【当被代理的对象实现了某个接口 &#x2F; 要AOP的方法都是override的，Spring会使用JDK Proxy，没有接口的对象使用Cglib】</p><p><img src="https://oss.javaguide.cn/github/javaguide/system-design/framework/spring/230ae587a322d6e4d09510161987d346.jpeg" alt="SpringAOPProcess"></p><blockquote><p>基于AOP思想的又一实现：AspectJ</p></blockquote><blockquote><p>一个AOP框架，方便实现在切入点上运行切面代码</p></blockquote><p>区别：传统AOP基于动态代理，AspectJ基于字节码增强——将目标代码的引用（invokeVirtual）直接插入.class文件具体位置</p></blockquote></li></ul><h3 id="问题35：Java注解如何实现的-TODO"><a href="#问题35：Java注解如何实现的-TODO" class="headerlink" title="问题35：Java注解如何实现的 &#x2F;&#x2F;TODO"></a>问题35：Java注解如何实现的 &#x2F;&#x2F;TODO</h3><blockquote><p>注解是什么：有点像注释，只不过是给机器看的注释，当机器注意到此注解时要执行响应逻辑</p></blockquote><ul><li><p>注解分为</p><p>运行时可见的注解</p><p>运行时不可见的注解</p></li><li><p>注解本质是一个接口，在class文件中标明是interface</p></li><li><p>在一个添加了注解的class文件中，如果方法上加了注解，方法后会有一个属性RuntimeVisibleAnnotations，类上如果有注解则class文件最后会有RuntimeVisibleAnnotations</p></li></ul><h2 id="JAF"><a href="#JAF" class="headerlink" title="JAF"></a>JAF</h2><h3 id="Gobrs-Async"><a href="#Gobrs-Async" class="headerlink" title="Gobrs-Async"></a>Gobrs-Async</h3><blockquote><p>Gobrs-Async是一款功能强大、配置灵活、带有全链路异常回调、内存优化、异常状态管理于一身的高性能多线程并发编程和动态编排框架。</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs java">前置知识：CompeletableFeature<br>- 原本Feature类的作用是让主线程根据异步计算线程的句柄访问到执行结果，但是get()方法是阻塞的-消耗线程资源，isDone()方法是轮询的-消耗CPU资源，更合理的方法是尝试回调 -- 让异步计算的线程执行完后触发某个主线程的逻辑；<br><br>- CompeleableFeature<br>    特点<span class="hljs-number">1</span>：绑定回调函数<br>    &gt; thenRun,thenRunAsync<br>      thenAccept,thenAcceptAsync<br>      thenApply,thenApplyAsync<br>      ......<br>    特点<span class="hljs-number">2</span>：多个任务组合处理<br>    &gt; thenCombine,thenCombineAsync，实现当两个任务都完成时执行绑定的回调函数<br>      其余and组合关系，or组合关系，anyOf组合关系，allOf组合关系...<br></code></pre></td></tr></table></figure><p>沟帮烧鸡的执行流程：</p><h5 id="功能："><a href="#功能：" class="headerlink" title="功能："></a>功能：</h5><p>通过配置文件编排好任务的执行顺序，并且可以很复杂（很切合实际场景），例如：当ABC中任何一个任务执行完立即执行任务D，且停止ABC的执行，明显就是登录场景</p><p>而任务的声明也只需要在原先代码的基础上声明注解Tesk即可，还可添加参数，设置超时时间，重试次数等等…</p><blockquote><p>和常规CompeletableFeature的区别：</p><p>更加灵活：</p><ul><li>原本使用Feature类开发时，任务都是按Runnable类型的lambda表达式写的，所有任务链路写在一起，通过thenRun等表达式串联</li><li>使用沟帮烧鸡后，每个任务有自身钩子函数（prepare：任务开始前执行什么操作，onsuccess：任务成功后执行什么操作，nessary，什么条件下执行此操作…）</li></ul></blockquote><h5 id="技术："><a href="#技术：" class="headerlink" title="技术："></a>技术：</h5><p><img src="https://kevin-cloud-dubbo.oss-cn-beijing.aliyuncs.com/gobrs-async/gobrs-jgt3.png" alt="1.0"></p><p>角色：</p><ol><li><p>任务触发器：面向调用者，负责启动任务执行流</p></li><li><p>规则解析引擎：负责解析配置文件，将配置信息加载成bean对象，再组装成任务树</p></li><li><p>任务启动器：启动执行任务树上的方法</p></li><li><p>任务加载器：负责调用任务执行器</p></li><li><p>任务执行器：…</p></li></ol><h3 id="how-to-do-this"><a href="#how-to-do-this" class="headerlink" title="how to do this"></a>how to do this</h3><blockquote><p>主要使用 <code>CountDownLatch</code> 、<code>ReentrantLock</code> 、<code>volatile</code> 等一系列并发技术开发设计。</p></blockquote><h3 id="DynamicTp"><a href="#DynamicTp" class="headerlink" title="DynamicTp"></a>DynamicTp</h3><ol><li><p>使用ThreadPoolExecutor的痛点</p><blockquote><ol><li><p>代码中创建了一个ThreadPoolExecutor，但是不知道那几个核心参数设置多少比较合适</p></li><li><p>凭经验设置参数值，上线后发现需要调整，改代码重启服务，非常麻烦</p></li><li><p>线程池相对开发人员来说是个黑盒，运行情况不能感知到，直到出现问题</p></li></ol></blockquote></li><li><p>原生ThreadPoolExecutor其实提供了set参数的方法，允许在运行时动态设置参数值</p><p>但是触发set方法怎么能够更加灵活——即使是运维人员也可以简单修改——又结合配置中心的思想（微服务体系中将配置信息解耦），最后总结出：结合配置中心来做运行时线程池参数的动态调整</p></li><li><p>主要功能模块：</p><ol><li><p>配置变更监听模块</p><ul><li><p>监听特定配置中心的指定配置文件</p></li><li><p>解析配置文件内容</p></li><li><p>通知线程池管理模块实现刷新</p></li></ul></li><li><p>线程池管理模块</p><ul><li><p>启动时从配置中心拉取配置信息，生成线程池实例，注册到内部线程池注册中心中</p><blockquote><p>不仅可以使自身DtpExector创建时按照配置中的信息，也可以控制web服务器的线程池 —— 通过adaptor模块</p></blockquote></li><li><p>监听模块监听到配置变更时，将变更信息传递给管理模块，实现参数刷新</p></li><li><p>维护对线程池的crud</p></li></ul></li><li><p>监控模块</p></li><li><p>通知告警模块</p><p>告警类型</p><ul><li><p>线程池参数变更</p></li><li><p>阻塞队列容量达到阈值</p></li><li><p>线程池活性达到阈值</p></li><li><p>触发拒绝策略</p></li></ul></li></ol></li></ol><p>adaptor模块的设计思路</p><blockquote><p>想要管理第三方组件的线程池，首先要明确在这些组件中线程池是如何使用的，也就是整个请求的链路中，线程池的行为如何；</p><p>如果其关键作用的线程池提供了public的set方法供二次开发者调用，那最好，<strong>否则需要通过反射来拿</strong></p></blockquote><ul><li><p>Tomcat中的线程池</p><blockquote><p>Tomcat对于原生线程池做了一定优化，主要在什么时候创建新线程方面调整了策略</p><ul><li><p>原生线程池使用场景是CPU密集型：因为原生线程池中，核心线程数就是CPU核数，再多对于CPU而言就没太大压榨空间了，反而多了切换的成本</p></li><li><p>Tomcat线程池使用场景是IO密集型，为了吞吐量增大点切换成本是合理的，所以此线程池并不可以维护核心线程数，而是能增就增，直到最大线程数</p></li></ul><p>同时，提供了获取线程池的public方法，可以用来封装修改参数的逻辑</p></blockquote></li><li><p>Tomcat内部架构：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-03-21-13-43-12-image.png"></p><p>最外层是一个web Server</p><p>内部将请求监听和请求处理解耦运行，Connector连接器负责开启socket并监听客户端请求，返回响应数据，Engine模块负责具体的请求处理</p><p>将一个Engine模块和多个连接器组合，成为一个Service模块</p><p>在Engine内要运行一个web应用</p><p>区分Host【一台Tomcat可以按不同域名来分别执行逻辑】</p><p>Host下的Context模块就表示一个web应用的环境</p><p>Context内包含多个Servlet</p></li></ul><blockquote><p>连接器的功能：</p><p><img src="https://p3-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/cfd7cbf0e97243a69bcc41f73e8f4eeb~tplv-k3u1fbpfcp-zoom-in-crop-mark:1512:0:0:0.awebp" alt="file"></p><p>基本逻辑：</p><ol><li><p>网络请求到达EndPoint，以TCP&#x2F;IP报文的形式</p><p>在EndPoint处理成Socket信息，给到Processor</p></li><li><p>在Processor中转化成HTTP信息</p><p>提取出Tomcat Request后，给到Adaptor</p></li><li><p>Adaptor交给容器Servlet Request</p></li></ol></blockquote><hr><h4 id="Tomcat源码"><a href="#Tomcat源码" class="headerlink" title="Tomcat源码"></a>Tomcat源码</h4><ol><li><p>ThreadPoolExector找不到的原因是：</p><ul><li><p>上层使用的都是接口类型</p></li><li><p>创建多态对象时使用的是工厂设计模式（newInstance而不是new ThreadPoolExecutor）</p></li><li><p>底层封装有API，不直接解除Executor</p></li></ul></li><li><p>ThreadPoolExector具体使用</p><p>在NIO中，只作为read和write成功后的回调</p><p>在BIO中，作为任务异步执行的真身</p></li></ol><h3 id="leaf"><a href="#leaf" class="headerlink" title="leaf"></a>leaf</h3><blockquote><p>分布式全局唯一ID生成工具</p></blockquote><p>问题场景：</p><blockquote><p>分库分表后需要有唯一ID，但使用传统的自增id不行</p><p>分布式场景下多台服务器需要对唯一ID有共识</p></blockquote><p>传统方式：</p><ol><li><p>直接通过DB自增方式生成ID</p></li><li><p>使用redis缓存生成ID</p></li><li><p>直接使用UUID生成ID</p></li><li><p>雪花snowflake</p></li></ol><blockquote><p>问题：</p><ul><li><p>UUID：长度过长（128bit），完全随机（无序）</p><ol><li><p>尤其当作为数据库主键时，占用空间太大。</p></li><li><p>UUID会保留MAC地址信息</p></li><li><p>加入新数据时并非一条条往后，而是完全随机（没有自增的趋势），中间裂页复制的成本很高</p><p>【并且最无语的时，大概率目标页还在磁盘中，还要有一次IO - 约等于要维护整个B+树在内存】</p></li></ol></li><li><p>Redis自增</p><blockquote><p>由于redis支持原子性操作使得一个数自增</p></blockquote><ol><li>redis是一个内存数据库，需要考虑持久化和重启后续上、以及高可用的问题</li></ol></li><li><p>传统自增ID不仅分库分表解决不了，还有些业务问题</p><ol><li><p>容易暴露信息，让人猜到订单数</p></li><li><p>无法区分不同业务（应该让不同业务的id也有所不同，出问题好定位）</p></li></ol></li><li><p>雪花：</p><blockquote><p>一种以划分命名空间来生成不同ID的算法——将64bit位划成多端，分别表示机器，时间…</p></blockquote><p>优点：</p><ol><li><p>能够做到自增趋势：因为时间戳在高位，自增序列在低维。时间戳基本自增，即使一样也有自增序列兜底</p></li><li><p>根据自身业务设计bit位</p></li></ol><p>缺点：</p><ol><li>强依赖机器时钟，如果机器时钟回拨会导致ID紊乱</li></ol></li></ul></blockquote><hr><p>leaf的方案：</p><ol><li><p>Leaf-segment【预分发号段】</p><blockquote><p>在使用原本数据库的方案上，为了避免每获取一次ID就读写一次数据库，提出按照segment获取的方案</p><p>即：每次拿走多个ID，用完在来读写数据库重新获取</p><blockquote><p>虽然是基于数据库，但是即使数据库分库分表，将不同segment分配给不同单位即可</p></blockquote></blockquote><p><img src="https://img-blog.csdnimg.cn/89a432a6f3a94f9594647242719f6534.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzMxOTYwNjIz,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p><p>优点：</p><ol><li><p>在线扩容很方便</p></li><li><p>leaf单独作为一个服务，和DB耦合程度不太高，即使DB宕机，leaf仍然能坚持到自身号段用完</p></li></ol><p>缺点：</p><ol><li><p>ID号码不够随机</p></li><li><p>还是对DB有耦合，一旦宕机会不可以</p></li><li><p>对数据库的更新操作还是会阻塞一部分获取ID的请求</p></li></ol><p>优化缺点3：双Buffer【一个server基本会拥有两个段号，轮流对外提供服务】</p><blockquote><p>即：在消费完之前某一时刻间下一块segment请求回来</p><p>解决3，部分缓解2</p></blockquote><p><img src="https://img-blog.csdnimg.cn/a688103f2f9c4e70926ca7a41d11a63d.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzMxOTYwNjIz,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p><p>动态步长策略</p><blockquote><p>上一次更新周期长，说明使用id频率不高，这次分配数量也不多</p><p>上一次更新周期短，很可能表示流量很大，则一次性分配更多ID</p></blockquote><hr></li><li><p>Leaf-snowflack</p><blockquote><p>原本的segment方案生成的ID呈递增趋势，但是比较连续，可以被对手计算得到可用信息</p><p>于是引用雪花算法</p></blockquote><p>leaf-snowflake沿用雪花算法的bit位设计方案</p><p><img src="https://img-blog.csdnimg.cn/f55478c80e4443a9b076a10b7ae79978.png" alt="在这里插入图片描述"></p><blockquote><p>解决时钟问题：</p><p>重启后校验一下之前的记录和当前时间</p><p>如果当前时间在之前记录之前，说明时钟回拨过大</p></blockquote></li></ol><h4 id="雪花算法"><a href="#雪花算法" class="headerlink" title="雪花算法"></a>雪花算法</h4><h3 id="jetCache"><a href="#jetCache" class="headerlink" title="jetCache"></a>jetCache</h3><blockquote><p>高并发三剑客</p><p>缓存、分库分表、消息队列</p></blockquote><blockquote><blockquote><blockquote><blockquote><blockquote><blockquote><p>在技术领域，没有一种通用方案可以解决所有问题 —— 没有银弹</p></blockquote></blockquote></blockquote></blockquote></blockquote></blockquote><hr><h3 id="缓存："><a href="#缓存：" class="headerlink" title="缓存："></a>缓存：</h3><blockquote><p>空间换时间，减少对CPU的消耗和IO成本</p></blockquote><p>缓存分类</p><ul><li><p>本地缓存</p><ul><li><p>JDK map 在内存中维护一个get的时间复杂度为常数的数据结构</p></li><li><p>本地缓存框架：Spring Cache、Guava Cache</p></li><li><p>池化思想本身也是缓存思想（空间换时间）</p></li></ul><blockquote><p>问题：集群模式下无法共享</p><h4 id="重启服务缓存消失，突然的雪崩"><a href="#重启服务缓存消失，突然的雪崩" class="headerlink" title="重启服务缓存消失，突然的雪崩"></a>重启服务缓存消失，突然的雪崩</h4></blockquote></li><li><p>分布式缓存</p><blockquote><p>指：和应用分离的缓存组件 &#x2F; 服务</p><p>与本地应用隔离，多个应用直接共享缓存</p></blockquote><p>常用技术：Redis、Memcache、tair</p></li></ul><hr><ol><li><p>JDK中的map</p><ul><li><p>HashMap &amp; ConcurrentHashMap</p></li><li><p>LinkedHashMap：有序的Hashmap，保留元素的插入顺序</p><p>常用来实现LRU链表</p></li><li><p>TreeMap：基于红黑树的有序Map，可以按照键的顺序进行遍历</p></li></ul></li><li><p>本地缓存应用框架</p><blockquote><p>由于要对缓存内容进行管理，例如过期失效，淘汰策略…</p></blockquote></li><li><p>分布式缓存</p><blockquote><p>分布式缓存通常由多台机器组成一个集群，目的是将缓存数据分布在多台机器上·，以提高缓存容量和并发能力</p></blockquote><p>优点：</p><ol><li><p>性能可扩展</p></li><li><p>高可用（集群，主备）</p></li></ol><p>缺点：</p><ol><li>网络延迟</li></ol></li><li><p>多级缓存</p><blockquote><p>本地 + 分布式</p><p>既能加快访问（进程内就有本地缓存）</p><p>还能减少风险（分布式服务也有）</p></blockquote><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-03-21-20-27-33-image.png"></p></li></ol><h3 id="细说JDK中四种用于缓存的数据结构"><a href="#细说JDK中四种用于缓存的数据结构" class="headerlink" title="细说JDK中四种用于缓存的数据结构"></a>细说JDK中四种用于缓存的数据结构</h3><ol><li><p>HashMap</p></li><li></li></ol><h3 id="Rabbit-Rocket-Kafka-Active-技术选型"><a href="#Rabbit-Rocket-Kafka-Active-技术选型" class="headerlink" title="Rabbit Rocket Kafka Active 技术选型"></a>Rabbit Rocket Kafka Active 技术选型</h3><h5 id="Rabbit-MQ："><a href="#Rabbit-MQ：" class="headerlink" title="Rabbit MQ："></a>Rabbit MQ：</h5><ol><li><p>理论：</p><blockquote><p>单机吞吐量 万级，比RocketMQ，Kafka低一个级别</p><p>延迟最低，再微秒级，其他两个都是毫秒级</p><p>可用性策略：为了保持轻量级，用主从架构，其他两个都是分布式架构</p><p>消息可靠性：保证消息基本不丢，其他两个通过参数选择、优化配置可以做到0丢失</p><p>提供灵活的消息模型，例如Exchange模块提供的消息路由、动态队列…</p></blockquote></li><li><p>实践：</p><blockquote><p>提供可视化客户端，可不了解相关命令行指令进行管理</p><p>RabbitMQ提供了许多语言的客户端接口，比较友好</p><p>RabbitMQ本质还是维护一些管道，管道长度有限，无法应对消息堆积，只能曲线救国做点优化</p><blockquote><p>曲线：增加消费者消费能力 &#x2F; 将RabbitMQ无法消费的消息快刷到磁盘（使用惰性队列）</p></blockquote></blockquote></li><li><p>市面：</p><blockquote><ol><li><p>但是本身由erlang语言开发，为了并发能力，但国内少有erlang程序员，基于源码的debug和二次开发较为困难，只能依赖于开源社区，但社区无人担责</p></li><li><p>erlang语言和常规语言区别较大</p><blockquote><p>原生支持并发性和分布式计算（COP：面向并发编程）</p><p>提供轻量级进程和消息传递机制，使得并发编程简单直观</p><p>代价：</p><ol><li><p>erlang中变量不可变，一个变量仅仅是最一个值的引用，绑定后无法更改，只能通过新创建一个变量来实现变量值的变换</p><p>目的是面向并发编程，相当于最悲观的锁</p></li></ol></blockquote></li></ol></blockquote></li></ol><h5 id="Rocket-MQ："><a href="#Rocket-MQ：" class="headerlink" title="Rocket MQ："></a>Rocket MQ：</h5><ol><li><p>理论：</p><blockquote><p>单机吞吐量10万级</p><p>可以支持几百 &#x2F; 几千的topic，而kafka再topic到几百时吞吐量大幅度下降，kafka此时只能选择增加机器资源</p><p>基于分布式，有可扩展性和高可用，天生为了电商秒杀，实时数据分析场景使用</p><p>据说时延也是毫秒级</p><p>Java原生，中国人开发，易于阅读理解</p><ul><li><p>缺点：国际生态不很好</p><p>要配合Flink，Spark进行流计算，但它们对RocketMQ的支持并不完备，不如Kafka</p></li></ul></blockquote><hr><blockquote><p>演进目标</p><ol><li><p>消息基础架构的云原生演进【结合云原生浪潮下的生态技术，提高资源的利用率和弹性能力】</p></li><li><p>集成效率的优化【从API、SDK等多方面重构设计，对外提供能易用，更轻量级的方案】</p></li><li><p>事件、流集成场景的拓展【聚焦消息领域的后处理场景，消息的流式计算和轻计算】</p></li></ol></blockquote><p>架构：</p><p><img src="https://rocketmq.apache.org/assets/images/5.0%E9%80%9F%E8%A7%88-2-b3f035c1b5b2088396c2df5817ce487d.jpeg" alt="img"></p><p>早期架构：</p><p><img src="https://img-blog.csdnimg.cn/20210319105349399.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzIxNTYxNTAx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p><p><code>NameServer命名管理 本质是broker集群的注册中心 &amp; 网关</code></p><ul><li><p>注册中心的作用是管理每个Topic的路由</p></li><li><p>网关的作用是将客户端的消息 &#x2F; 请求路由到准确的服务器上</p></li></ul><blockquote><p>架构</p></blockquote><figure class="highlight cs"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cs">生产者：首先和NameServer集群中随机一台建立长连接，<br>       得知当前发送的Topic存在哪个Broker Master上，<br>       然后再和Broker Master建立长连接<br>消费者：和NameServer集群中随机一台建立长连接，<br>       得知当前想要消息的Topic存在那台Broker Master中<br>       然后和Broker Master建立长连接<br>Broker：主从 + 集群<br>       一个Master对应多个Slave，Master负责读写，Slave负责读取<br>        Broker会定期上传心跳包：包括IP、Port、TopicInfo<br>       【通过不同的配置文件选择模式】<br>        - 两主<br>        - 两主两从 异步复制<br>        - 两主两从 同步复制<br>NameServer：关于Topic的路由注册中心，支持Broker的动态注册和发现<br>       保存Topic和Broker的关系<br>            也是Broker的生命注册中心<br></code></pre></td></tr></table></figure></li></ol><blockquote><p>数据流转</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs java">消息 Message：一条消息属于一个主题<br>主题 Topic：每个主题包含若干条消息，一个生产者可以同时发送多种Topic消息，而一个消费者只能对某种特定的Topic感兴趣（只能订阅一种Topic消息）<br>    Broker内，注册管理Topic，一个Broker可以内含多个Topic，每个Topic又由多个Queue组成，一个Broker下的一个Topic的多个Queue组成一个分片<br>标签 Tag：子主题 / 过滤标签，使用标签可以区分相同Topic下应属于不同模块的消息 / 更加灵活 / 用于过滤（只接受属于Tag的消息）<br>索引key列表：消息的索引键，设置不同的key用于区分消息 &amp; 快速查找消息<br>队列 Queue：存储消息的物理实体。一个Topic可以包含多个Queue；Topic A下的Queue <span class="hljs-number">1</span>就是A的一个分区<br></code></pre></td></tr></table></figure><ol><li><p>生产者发消息前，先向NameServer获取目标Topic的路由信息</p></li><li><p>NameServer中维护着Topic的路由表，已经Broker列表</p><blockquote><p>路由表是一个map，key是Topic的名称，value是属于该Topic的Queue们分散在哪些Broker中的列表</p></blockquote><blockquote><p>Broker列表：用于管理一个主从架构下master和slave的注册信息，key是brokerName，value是又一个map，记录brokerId &amp; Broker的地址</p></blockquote><blockquote><p>一个Topic下，Queue的存储为什么要分散在多个Broker中？</p></blockquote></li><li><p>生产者根据某种Queue的选择策略，从Queue列表中选出一个Queue，准备发送</p></li><li><p>发送前对消息做一些压缩处理</p></li><li><p>发送</p></li></ol><hr><p>消息的消费：消费者从Broker获取消息的方式有pull拉 &amp; push 推；消费者消费的模式有集群消费Clustering &amp; 广播消费BroadCasting</p><ol><li><p>拉：</p><blockquote><p>实时性弱</p></blockquote></li><li><p>推：</p><blockquote><p>实时性高，典型的模式是发布订阅：</p><p>实现方式：某个Consumer在Queue上注册监听器（绑定回调函数，当Queue发现新的消息后触发回调，推送消息）</p><blockquote><ol><li>回调需要维护一个Consumer到Broker的长连接吗？</li></ol></blockquote></blockquote></li><li><p>广播：</p><p>相同Consumer Group的每个Consumer都会收到同样的message</p></li><li><p>集群消费：</p><p>相同Consumer Group的每个Consumer中只有一个收到message</p></li></ol><blockquote><p>Key Point</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs JAVA"><span class="hljs-number">1.</span> 消息通过RPC发出<br><span class="hljs-number">2.</span> 将业务合理拆分到多个Topic<br><span class="hljs-number">3.</span> 扩容 与 队列<br>    每个Topic下的Queue分布在不同的Broker中，目的是为了负载均衡，让message均衡打到承载不同Broker的不同服务器上，同时，当要水平扩容时，新Broker中往往新设原先Topic的新Queue，从而解决老集群压力大的问题<br></code></pre></td></tr></table></figure><p>开源的可视化组件：rocketmq-console，用于控制台管理项目</p><p>本质就是一个【java-web项目】</p><p>页面 &#x2F; 作用</p><ol><li><p>集群管理：监控每个集群下主从节点的行为，比如生产的TPS、消费的TPS…</p></li><li><p>主题管理：对于每个主题的状态管理、路由管理…crud</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-25-09-16-57-image.png"></p><p>默认主题有TopicTest，默认配置有4个队列（平行的四个队列用于防止单个的对头阻塞）</p></li><li><p>消息Message内容</p><ol><li><p>绑定的主题</p></li><li><p>tag</p></li><li><p>值</p></li><li><p>消息主体内容</p></li></ol><blockquote><p>RocketMQ提供一种消息检索功能：根据topic、message key、messageID等条件搜索</p></blockquote></li></ol><p>使用【SpringBoot-starter】：</p><p><img src="C:\Users\吴松林\AppData\Roaming\marktext\images\2024-04-25-09-40-49-image.png"></p><h3 id="回来吧我的RocketMQ"><a href="#回来吧我的RocketMQ" class="headerlink" title="回来吧我的RocketMQ"></a>回来吧我的RocketMQ</h3><blockquote><p>特点：专为万亿级超大规模的消息处理而设计，具有高吞吐量、低延迟、海量堆积、顺序收发等特点</p><p>特性：事务消息、SQL过滤、轨迹追踪、定时消息、高可用多活等</p><p>设计原则：拥抱云原生</p></blockquote><blockquote><p>学习一个工具，从<strong>架构</strong> &amp; <strong>功能</strong> 入手</p></blockquote><h4 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h4><p><mark>RabbitMQ中，队列的作用更偏向消费者，队列中元素的进入条件就是考虑了消费者的过滤条件，而RocketMQ中队列的作用更偏向于生产者，之后消费的过程再通过订阅关系中的tag过滤或者SQL过滤实现</mark></p><p><code>RabbitMQ中，消息进入Broker和进入Queue中还有一层exchange，作用就是将消息准确推送进Queue中，也一定程度上实现了过滤；exchange的Topic模式能够兼容点对点和fanout模式；Topic模式下，每个队列绑定一个路由键，后续消息内也有一个路由键，只有当exchange匹配到消息的路由键和队列的路由键相符则进入Queue，队列的路由键使用模糊匹配来提高灵活性，当路由键是#或者*时，表示此队列会接收所有消息，当路由键没任何通配符，表示此队列直接收一种消息</code></p><p><code>而RocketMQ将消息投递到Queue时就很简单，指定Topic后就不管了，而是将消息过滤的能力交给消费者，通过订阅关系的tag过滤和SQL92过滤实现</code></p><h4 id="功能"><a href="#功能" class="headerlink" title="功能"></a>功能</h4><blockquote><p>RocketMQ按照消息的类型实现特殊的需求</p></blockquote><ol><li>普通消息</li><li>顺序消息：可以将一个MessageGroup中的消息保证先后顺序</li><li>定时消息 &#x2F; 延时消息：控制消息在一定时间内不能被消费</li><li>事务消息：RocketMQ支持的分布式事务消息</li></ol><blockquote><p>其他特性：每个都有背景 &#x2F; 都是为了解决某些场景的问题</p></blockquote><ol><li><p>消息重试 &amp; 流控机制</p><blockquote><p>重试：</p><ul><li>当客户端的请求由于网络故障、服务异常导致调用失败，为了保证消息的可靠性，客户端SDK中内置请求重试的逻辑<ul><li>同步发送的重试：一次消息发送如果失败会一直占用线程资源进行重试，直到成功 &#x2F; 超过最大重试次数</li><li>异步发送的重试：发起消息的线程后续还需要检查消息有无成功传递</li></ul></li></ul><p>问题：</p><ol><li><p>重试就意味着一个消息多次发送，如果服务器本身没问题，只是响应事件超过阈值，就需要解决消息的幂等性问题</p></li><li><p>重试机制并不能保证消息一定发送成功，如果重试仍然失败则需要更强力的保障措施</p></li><li><p>重试最大的诟病就是资源占用太多</p></li></ol><p>流控机制</p><ul><li>当系统容量 &#x2F; 水位过高，RocketMQ服务端会通过快速失败返回流控错误，从而避免底层资源承受过高压力</li><li>处理建议：<ul><li>提前预防水位过高，通过可观测性功能监控，调整</li><li>如果当前MQ确实触发了流控，需要告知业务方替换到其他系统应急处理</li></ul></li></ul></blockquote></li><li><p>消费者分类</p><blockquote><p>RocketMQ <strong>面向不同的业务场景</strong>提供了不同的消费者类型</p><p>场景中的因素</p><ol><li>是否需要手动解决并发消费，如何通过并发消费提高消费速度</li><li>是否需要手动实现同步、异步消息处理</li><li>消费者处理消息时如何返回响应结果，正确处理返回正确结果，消息异常触发重试？从而确保消息可靠处理</li></ol><blockquote><p>消费者方面的数据流转：消息获取 –&gt; 消息处理 –&gt; 消费状态提交</p></blockquote><p>类型</p><ol><li><p>PushConsumer</p><blockquote><p>高度封装，通过给Consumer设定监听器，监听器内绑定消费状态的回调函数，PushConsumer的SDK就会自动按照此逻辑完成回调</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs JAVA"><span class="hljs-comment">// 消费示例：使用PushConsumer消费普通消息。</span><br><span class="hljs-type">ClientServiceProvider</span> <span class="hljs-variable">provider</span> <span class="hljs-operator">=</span> ClientServiceProvider.loadService();<br><span class="hljs-type">String</span> <span class="hljs-variable">topic</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;YourTopic&quot;</span>;<br><span class="hljs-type">FilterExpression</span> <span class="hljs-variable">filterExpression</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">FilterExpression</span>(<span class="hljs-string">&quot;YourFilterTag&quot;</span>, FilterExpressionType.TAG);<br><span class="hljs-type">PushConsumer</span> <span class="hljs-variable">pushConsumer</span> <span class="hljs-operator">=</span> provider.newPushConsumerBuilder()<br> <span class="hljs-comment">// 设置消费者分组。</span><br> .setConsumerGroup(<span class="hljs-string">&quot;YourConsumerGroup&quot;</span>)<br> <span class="hljs-comment">// 设置接入点。</span><br> .setClientConfiguration(ClientConfiguration.newBuilder().setEndpoints(<span class="hljs-string">&quot;YourEndpoint&quot;</span>).build())<br> <span class="hljs-comment">// 设置预绑定的订阅关系。</span><br> .setSubscriptionExpressions(Collections.singletonMap(topic, filterExpression))<br> <span class="hljs-comment">// 设置消费监听器。</span><br> .setMessageListener(<span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageListener</span>() &#123;<br>     <span class="hljs-meta">@Override</span><br>     <span class="hljs-keyword">public</span> ConsumeResult <span class="hljs-title function_">consume</span><span class="hljs-params">(MessageView messageView)</span> &#123;<br>         <span class="hljs-comment">// 消费消息并返回处理结果。</span><br>         <span class="hljs-keyword">return</span> ConsumeResult.SUCCESS;<br>     &#125;<br> &#125;)<br> .build();<br></code></pre></td></tr></table></figure><ul><li>返回消费成功：return ConsumeResult.SUCCESS;</li><li>返回消费失败：return ConsumeResult.FAILURE;</li></ul></blockquote></li><li><p>simpleConsumer：封装性不高，可灵活自定义</p></li></ol><blockquote><p>实现绑定监听器的原理：</p><p><code>对于push的消费类型才需要确认消息是否需要重发，因为pull类型的确认都在消费者中完成，只需要保证消息正确到达即可</code></p><ul><li>Consumer中维护一个缓存列表，记录本地消费的结果</li><li>Server维护一个长轮询的线程，通过批处理的方式查看消费结果</li></ul></blockquote></blockquote></li><li><p>消息过滤</p><blockquote><p>MQ作为消息中间件被广泛应用于上下游的业务集成场景中，但实际同一个主题的消息往往会被多个不同的下游业务方处理，各下游的处理逻辑不同，需要消息过滤来保证下游业务方只关注自身逻辑需要的消息子集</p></blockquote><p>两种过滤方式</p><ol><li>Tag标签过滤【基础过滤能力】</li><li>SQL属性过滤【适合更加复杂的场景】</li></ol><blockquote><p>业务消息的拆分原本是基于Topic的，但是Topic中消息又需要被过滤，所以可以按照其中的tag进行更细粒度的拆分</p><p>但Topic这种资源比较重量级</p></blockquote></li><li><p>消费者负载均衡</p><blockquote><p>消费者的消费模式分为广播消费和共享消费</p><p>即：在一个消费者组中是全都获取消息，还是只有一个会获取到消息</p><p>负载均衡：</p><blockquote><p>消费者组内有多个消费者共同承担消息消费任务，那么需要有一种分派逻辑决定谁消费</p></blockquote><p>两种模式</p><ol><li>消息粒度负载均衡</li><li>队列粒度负载均衡</li></ol></blockquote></li><li><p>消费进度管理</p><blockquote><p>消费者刚启动时应该从哪里开始消费？如何感知某些信息已经被消费了？某消息消费后业务出现问题，该消息能否被重新消费？…</p><blockquote><p>RocketMQ为每个consumer记录一个消费位点（Consumer Offset）【ThreadLocal】</p></blockquote></blockquote></li><li><p>消费重试</p><p>&#x2F;&#x2F;TODO</p></li><li><p>消息存储和清理机制</p><p>&#x2F;&#x2F;TODO</p></li></ol><h5 id="kafka："><a href="#kafka：" class="headerlink" title="kafka："></a>kafka：</h5><ol><li><p>理论：</p><blockquote><p>最开始就是处理海量日志，导致特点是：性能最好，但无法保证消息不丢失</p><blockquote><p>卡夫卡设计为一种 流式处理平台 </p><p>具有的功能有：</p><ol><li><p>消息队列</p></li><li><p>有持久化方案来容错【Kafka对持久化做了许多优化】</p></li><li><p>流式处理类库</p></li></ol></blockquote><p>快的原因：</p><blockquote><ol><li><p>异步 + 批量 + 压缩</p></li><li><p>持久化的磁盘IO也有优化，尽量不成为瓶颈</p></li><li><p>其他优化策略：零拷贝，pagecache…</p></li></ol></blockquote><p>一般配合大数据系统进行日志采集，实时数据计算…</p><p>高可用的实现方案：分布式 + 冗余数据</p></blockquote></li><li><p>实践：</p><blockquote><p>卡夫卡的生态圈十分完备，一旦Kafka推出新特性，整个大数据生态圈的技术站点，如Flink，Spark，Hadoop都会跟进</p></blockquote></li><li><p>问题：</p><blockquote><ol><li><p>实现太过复杂</p></li><li><p>【异步 + 批量 + 压缩】完全基于异步，异步带来批处理，一批的数据压缩后当成一个数据传给消费者，但只适合数据量很大的场景，数据量假如很小，异步的特性完全没用【成也批量异步，败还没败】</p></li><li><p>早期Kafka重度依赖ZooKeeper做元数据管理和集群高可用，后期引入基于Raft的KRaft，简化了Kafka的架构【更轻】</p></li></ol></blockquote></li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs js">无论是消息队列，数据库等中间件，瓶颈很多都会是磁盘的<span class="hljs-variable constant_">IO</span><br>一个数据库在某种场景下<span class="hljs-variable constant_">IO</span>很快，主要是因为其存储结构的设计<br>kafka / 消息队列都只需要将日志顺次记录即可，无需像数据库要随机访问存储<br></code></pre></td></tr></table></figure>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2024/05/16/hello-world/"/>
    <url>/2024/05/16/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
